# Operation `query` — answer a question about the wiki

> **Operation module.** Executor: **curator OK** (background) or main flow.
> For the **shared substrate** (D↔N boundary §2, taxonomy §3, log entry §6) see the playbook
> `wiki-playbook.md`; if you archive a page, write it following
> [`../page-craft.md`](../page-craft.md). Only the specific procedure is described here.

1. Answer by citing the relevant pages (`collect` to orient yourself; `index`/RAG if search is needed).
2. If the exploration is valuable and reusable, **archive it** as a new page (queries accumulate into
   knowledge), written following [`../page-craft.md`](../page-craft.md); update the index + log entry
   `query`. Otherwise no writing.
