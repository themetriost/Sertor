# Log 2026-07-09

Log entries for 2026-07-09.

## [2026-07-09] record | A-09 — 8 hook Python portabili (uv run --no-project python), .ps1 ritirati, upgrade migra legacy

Implementazione A-09 / E2-FEAT-010 completata: portabilità POSIX degli 8 hook host-facing (da PowerShell `.ps1` a Python portabile via `uv run --no-project python`). Hook inerti su mac/Linux risolti alla radice (Principio XII). Single-impl + upgrade migration + supersession FEAT-018 + docs utente aggiornate.
- **Cosa:** 8 hook Python portabili (`wiki-session-start.py`, `rag-freshness.py`, `memory-capture.py`, `fail-loudness.py`, `version-check.py`, `installed-check.py`, `lifecycle-hook.py`, `config-init.py`) + `_hooklib.py` helper (stdlib-only).
- **Dove:** branch `095-portable-hooks-09` (3 commit: US1 wiring, US2 parity gate + smoke, US3 single-impl + migration).
- **Wiring:** rimozione `"shell":"powershell"` dai `settings.json`; hook Python invocati via `uv run --no-project python --assistant [claude|copilot]`.
- **Gate:** `test_portable_hooks_parity.py` (contratto per-assistente byte-identico) + `test_portable_hooks_smoke.py` (esecuzione reale su matrice CI ubuntu+windows).
- **Migrazione:** `sertor upgrade` rilevamento/stripping legacy `.ps1` (helper `remove_hook_entries_by_command_substring`).
- **Docs:** `install.md`, `install-claude.md`, `install-copilot.md` aggiornate (rimosso prerequisito PowerShell Core).
- **Supersession:** E10-FEAT-018 («note pwsh») ritirata (guardia `host_env.py` rimossa, gap risolto).
- **Invarianti:** Principio XI (vehicle), fail-safe/breadcrumb mantenuti, zero core, zero dipendenze nuove.
- **Esiti:** SpecKit completo, Constitution 12/12 ✅, sertor 510/kit 149/root 1166 verdi, ruff clean.
- **Prossimo:** merge su master + live dogfood migration (validazione T022 `sertor upgrade`).
- **Pagina:** [[feat-010-hook-portabili]] (experiment).

## [2026-07-09] record | A-10 — smoke E2E come gate su PR (path-filtered, ref=branch) + leg Python 3.11

A-10 (assessment SWOT, debito-tecnico E10) rende lo smoke E2E d'installazione un VERO gate pre-merge e aggiunge un leg Python 3.11. Chiude il buco che ha fatto passare la regressione degli script smoke `.ps1` nel merge di A-09/#161 (il job smoke non girava affatto sui PR).
- **Smoke sui PR (path-filtered):** nuovo job CI `changes` (`dorny/paths-filter`, con permesso `pull-requests: read`) che flagga se un PR tocca la superficie install/smoke (`packages/sertor/**`, `packages/sertor-install-kit/**`, `packages/sertor-flow/**`, `scripts/smoke.*`, `.github/workflows/ci.yml`); il job `smoke` gira sui PR solo quando rilevante (i PR doc/wiki-only lo saltano → minuti-runner risparmiati).
- **Smoke testa il DIFF del PR:** ref d'install parametrico `SERTOR_SMOKE_REF` (default `master`); sui PR la CI lo imposta a `github.head_ref` → lo smoke installa dal branch del PR, non da master. Gli script `smoke.sh`/`smoke.ps1` erano già parametrici (`-Ref`); modificato solo il wrapper `tests/integration/test_host_smoke.py`.
- **Python 3.11:** leg `ubuntu-latest × 3.11` aggiunto alla matrice `test` via `include` (il progetto dichiara `>=3.11`), senza raddoppiare la matrice; step Python parametrizzati.
- **Dove:** merge `c9e5140`/PR #164 (+ fix permessi `6dfde09`). File: `.github/workflows/ci.yml`, `tests/integration/test_host_smoke.py`. CI di `master` verde (test×3 incl. py3.11 + smoke×4). Il PR si è auto-validato (tocca `ci.yml` → path rilevante → smoke sul proprio branch).
- **Nota collaterale (bug tracciato, da vedere domani):** verificando la memoria conversazioni è emerso che la CATTURA AUTOMATICA (hook `memory-capture` SessionEnd) non popolava l'archivio (`memory list` vuoto benché `SERTOR_MEMORY=true`); il motore è sano (archive manuale = 58 sessioni, full-text + semantica ok). Ipotesi: l'hook era `.ps1` (migrato a `.py` con A-09). Tracciato in roadmap E4 (R-1).
- **Verification:** merge `c9e5140`, CI green (test + smoke).
- **Update 2026-07-14 (record inserito in ritardo — PR #165 riallineata a master attuale, scartato il diff `roadmap.md` stantio):** A-10 è ✅ su `master` (audit A-01..A-20 completo). Sul 🐛 memoria: la cattura-auto E4 **resta aperta** (fix ipotizzato `.ps1`→`.py`, da verificare al SessionEnd); **inoltre** un bug memoria **distinto** (`encode_project_path` non gestisce gli spazi → cattura vuota-silenziosa su path con spazi) è stato poi verificato e registrato come **E4-FEAT-011** (handoff Nunzio 2026-07-09, processato il 2026-07-14).
