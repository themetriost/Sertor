## [2026-07-13] record | E2-FEAT-010 — install multi-target + avviso non-Python + guida uv-assente

**Consegnata e mergiata feature di ergonomia dell'installer (feature 010 da PR #179, `eb1f7a3` su `master`).** Tre pezzi meccanici che migliorano la UX di installazione su ospiti multi-assistente, non-Python, e senza `uv` (senza bloccare l'esecuzione — nessun fallback automatico, solo onestà).

- **Multi-target install** — Il comando `sertor install` (wiki/rag) ora accetta **più assistenti in una sola invocazione** tramite `AssistantId.from_csv()`, che parsifica CSV (`claude,copilot-cli`) o la keyword `all`. Handler `_cmd_install_wiki`/`_cmd_install_rag` iterano sui target in **container disgiunti** (`.claude/**` vs `.github/**`), e il **runtime `.sertor/` è bootstrappato una sola volta** (deps, `.env`, `.mcp.json`) con **report aggregato** che preserva `capability` e propaga `notes`. Fail-fast se un target erra. Single-value (default `claude`) invariato per retrocompatibilità. Codice: `packages/sertor/src/sertor_installer/__main__.py`.
- **Avviso non-Python per ospiti** — Nuovo modulo `packages/sertor-install-kit/src/sertor_install_kit/host_env.py` con primitiva deterministica `is_python_host(root)` (stdlib-only, offline, bounded). Il comando `sertor install rag` emette una **nota advisory** in `InstallReport.notes` se l'ospite non ha Python (assenza di `pyproject.toml`/`setup.py`/`.py` files). I sorgenti rimangono intatti (non-distruttivo); l'avisos guidate l'utente a `uv` e alla documentazione.
- **Guida uv-assente** — Messaggio onesto in `_apply_deps`: comunica che si sta cercando di installare `uv` e spiega lo stato delle dipendenze. Il fallback pip REALE è tracciato come FEAT-006 (go-public, dipendenze interne non ancora pubblicate su PyPI).
- **Fuori scope e tracciato** — pip reale (FEAT-006) · multi-target su `sertor-flow` (follow-up) · reviewer di pulizia codice (governance a sé).
- **Verifica** — Suite completa non-cloud: **1180 test passed / 3 skipped**; `ruff check .` clean; `sertor-core` INVARIATO (non-regressione). Smoke CLI reale su host `.sln` (entrambi i container + note presentate, sorgenti intatti). Post-merge: re-lock via `scripts/dev/relock-runtime.ps1` da `9b5b842` a `eb1f7a3`, re-index incrementale corpus `sertor` **1353 doc / 14778 chunk** (+4 doc/~5 chunk). Smoke MCP: `find_symbol("is_python_host")` → `host_env.py:23`, `search_docs "Installing for several assistants"` accende sezione docs/install.md §9.
- **Nota storica e lint** — Il modulo `host_env.py` fu introdotto originariamente da FEAT-018 (`32c3b3b`, hook Python portabili), poi rimosso da A-09 (`97da7bc`, portabilità POSIX → assunto pwsh) per evitare detection specifici di SO. Ora reintrodotto da FEAT-010 con scopo distinto e privo di anti-pattern (detection solo del Python della radice ospite, non di shell): **nessun drift introdotto**, la roadmap FEAT-018 riflette accuratamente.
- **Doc utente aggiornata** — `docs/install.md` §9: «Installing for several assistants at once» + nota advisory non-Python; intro del comando rimaneggiato per descrivere il CSV.
- **Rituale** — **`record` ✅** (pagine toccate: `assistant-targeting.md` via cenno multi-target) **· distill: non serve** (helper minore `host_env.py`; capacità vive in roadmap/epic/requirements, nessuna entità durevole nuova cross-referenziata da più pagine) **· lint ✅** (nessun drift introdotto; reintroduzione di `host_env.py` risolve in supersession, non falsità) **· explainer: non innescato** (feature di tooling installer, non capacità utente-facing da spiegare a non-tecnici).

## [2026-07-13] record | E2-FEAT-016 — spike model-policy speckit su Copilot CLI → WON'T (non-applicabile)

**Spike chiuso NEGATIVO.** Prossima voce del «tail sertor-cli» dopo FEAT-010. La domanda: i 9 prompt-file
`speckit.*` (depositati da `specify init --ai copilot` in `.github/prompts/*.prompt.md`) possono ricevere
un `model:` di default sulla Copilot CLI?

**Metodo:** letta la **doc ufficiale** (regola «leggi il doc ufficiale del tool, non inferire»). Fonti:
[Copilot CLI custom instructions](https://docs.github.com/en/copilot/how-tos/copilot-cli/customize-copilot/add-custom-instructions),
[Issue #1195 `github/copilot-cli`](https://github.com/github/copilot-cli/issues/1195),
[Prompt files docs](https://docs.github.com/en/copilot/tutorials/customization-library/prompt-files).

**Esito:**
- La doc della **Copilot CLI non menziona affatto i prompt-file**: la CLI legge solo
  `copilot-instructions.md`, `.instructions.md` (`applyTo`), `AGENTS.md`/`CLAUDE.md` e istruzioni locali.
  I `.prompt.md` sono un costrutto **VS Code Chat**, **non CLI-invocabile** → conferma le decisioni Sertor
  FEAT-011/012. Sulla CLI **nessuna superficie consuma gli speckit.\***, quindi **nessun meccanismo** cui
  attaccare `model:`.
- Issue #1195: il campo `model` sui **custom-agent** `.agent.md` ha avuto una **regressione** (unsupported
  in v0.0.399) — riguarda i custom-agent (dominio **FEAT-015**, già consegnato), non i prompt-file.
- Sertor è **copilot-cli-only** (FEAT-012): i prompt-file vivono in VS Code, **fuori scope**.

**Decisione:** **FEAT-016 → ❌ Won't / non-applicabile.** Riaprire **solo** se la Copilot CLI aggiunge il
consumo dei prompt-file. Aggiornati `epic.md` (riga FEAT-016) + roadmap (EXEC bullet + E2 row).

**Rituale: record ✅ · distill: non serve** (spike/decisione, nessuna entità durevole nuova) **· lint ✅**
(chiude un'incoerenza potenziale: l'epic dava FEAT-016 «da spike» aperta a tempo indefinito → ora risolta
con evidenza doc-ufficiale) **· explainer: non innescato** (decisione tecnica interna).

## [2026-07-13] record | E2-FEAT-017 — fetta onestà auto-updater: cenno una-tantum su verdetto `unknown`

Terza (e ultima implementabile ora) voce del «tail sertor-cli». **Problema (finding 2026-07-10):**
l'avvisatore d'aggiornamento (E2-FEAT-013) è **strutturalmente muto** in produzione — la GET di `/VERSION`
da `raw.githubusercontent` dà **404 perché il repo è privato** → `latest=""` → `verdict:"unknown"`
permanente; e il SessionStart parlava **solo** su `behind` → l'ospite non sapeva mai che l'updater **non
può verificare**. Viola Principio XII (una capacità che finge in silenzio).

**Implementato (fetta isolabile, la sola non-gated):**
- **SessionStart** (`assets/rag/hooks/version-check-start.py`): oltre al notice `behind`, se `verdict=unknown`
  e `unknown_notified` non è settato → emette **una-tantum** «SERTOR UPDATE CHECK UNAVAILABLE» (non ha potuto
  verificare — offline o repo privato; Sertor funziona; appare una volta) e **persiste** `unknown_notified=true`
  nello stato (no nag).
- **SessionEnd** (`assets/rag/hooks/version-check.py`): **carry-forward** di `unknown_notified` **finché** il
  verdetto resta `unknown`; **reset** (flag omesso) a qualsiasi verdetto risolto → un futuro episodio `unknown`
  notifica di nuovo una volta.
- **Parità Copilot**: aggiornato il prompt statico `_copilot_version_check_start_specs` (in `install_rag.py`)
  perché l'agente surfacci una volta il cenno `unknown` (D↔N più lasco sulla CLI, best-effort).

**Confine:** solo asset installer (hook) + prompt Copilot; `sertor-core` **INVARIATO**. **Resta gated sul
go-public** il pezzo strutturale (repo pubblico → fetch 200; `/VERSION` bumpato A-15) = **FEAT-006**.

**Verifica:** +4 test in `test_portable_hooks_parity.py` (cenno una-tantum + marca notified · silenzio se già
notificato · SessionEnd preserve-while-unknown · SessionEnd reset-on-resolved); suite completa non-cloud
verde; `ruff check .` clean. **Guardia sync dogfood:** l'edit del bundle-source ha reso stale le copie
`.claude/hooks/version-check{,-start}.py` → **`python -m sertor_installer.sync`** riallineato (lezione: si
edita il **lato bundle**, poi si sincronizza; le 2 copie dogfood aggiornate sono nel commit).

**Rituale: record ✅ · distill: non serve** (fix mirato agli hook, nessuna entità durevole nuova; l'updater
vive già in `assistant-targeting`/epic) **· lint ✅** (nessun drift; chiude l'incoerenza «updater che finge»
del finding 2026-07-10) **· explainer: non innescato** (comportamento hook interno, non capacità utente-facing
da spiegare a non-tecnici).

## [2026-07-13] record | 🌐 GO-PUBLIC — repo reso PUBBLICO (audit segreti CLEAN); PyPI+release gated a CI verde

**Decisione di prodotto dell'utente:** «io andrei pubblico». Sequenza scelta (domanda con contesto):
**«repo public ora, PyPI a CI verde»** — il publish PyPI è irreversibile e la CI è ferma per billing fino al
**1° ago 2026**, quindi i flip irreversibili aspettano la suite verde in CI.

**Gate di sicurezza obbligatorio ESEGUITO prima del flip — audit segreti/history → ✅ CLEAN.** Subagent su
**912 commit** + intero working tree: **0 segreti** nella history e nei file tracciati (gli unici match sono
fixture di test sintetiche `sk-mysecret-…`/`AKIA…`); `.env`/`.sertor/.env`/`prototype/.env` **mai tracciati**
(solo template a valore vuoto); `.gitignore` robusto; nessuna PII/credenziale nei doc. **Nessun rewrite della
history né rotazione chiavi necessari.**

**Fatto (2026-07-13):**
- **Repo `themetriost/Sertor` reso PUBBLICO** (`gh repo edit --visibility public`; verificato `visibility:
  PUBLIC`).
- **Auto-updater sbloccato:** `curl` di `https://raw.githubusercontent.com/themetriost/Sertor/master/VERSION`
  → **HTTP 200** `0.1.0` (prima 404 da privato). Il pezzo strutturale di FEAT-017 è vivo: l'updater ora *può*
  verificare (verdetto `up-to-date`, corretto — nessuna release ancora; rileverà `behind` alla prima
  release+bump).
- **Hardening `.gitignore`:** aggiunti `*.pem`/`*.pfx`/`*.p12` accanto a `*.key` (rete pre-pubblicazione;
  nessun file simile esiste oggi).

**Gated al 1° ago (CI verde) — casa durevole [`go-public-pypi/requirements.md`](../../requirements/sertor-cli/go-public-pypi/requirements.md):**
publish PyPI dei 4 pacchetti (**E2-FEAT-006**, riclassificata Won't→Should/planned) · prima release + bump
`/VERSION` (attiva `behind`) · **pip fallback reale** (chiude il residuo E2-FEAT-010, ora possibile con deps
pubblicate). **Sbloccato ORA (repo pubblico, non PyPI-dipendente):** **E13 Fase 2** (marketing).

**Confine:** operazione di prodotto/ops + config (`.gitignore`) + documentazione (roadmap/epic/requirements);
**zero codice** `sertor-core`/CLI. **Nota git:** PR #181 (hash-fix EXEC) resa **ridondante** da questo commit
go-public (che include roadmap+epic aggiornati) → da **chiudere** come superseded.

**Rituale: record ✅ · distill: non serve** (milestone/decisione; nessuna entità durevole tecnica nuova — il
«go-public» vive in roadmap/epic/requirements) **· lint ✅** (riallineate EXEC+epic al nuovo stato: FEAT-006
Won't→planned, FEAT-017 sbloccato, go-public tracciato; nessun claim in deriva) **· explainer: non innescato**
(decisione di prodotto/ops, non capacità utente-facing da spiegare a non-tecnici — il *risultato* utente, cioè
«installabile da PyPI», sarà spiegabile al taglio della release).

## [2026-07-13] record | 🚀 Prima release pubblica `v0.1.0` + validazione end-to-end (auto-updater + install esterno)

Su richiesta utente («taglia la prima release dai»), subito dopo il go-public. **Publish PyPI escluso** (gated
1° ago, CI verde) — questa è la release **GitHub + tag**, reversibile, che rende Sertor installabile a versione
pinnata via `git+url` dal repo ora pubblico.

**Fatto:**
- **GitHub Release `v0.1.0`** (https://github.com/themetriost/Sertor/releases/tag/v0.1.0), tag su `00dcd62`
  (testa master, include FEAT-010/016/017), marcata `--latest` (non prerelease). Note di release value-first
  (differenziatore code+doc + install `uvx` pinnati al tag + highlights + rimando getting-started/CHANGELOG).
  `/VERSION` resta `0.1.0` (è la versione della prima release; nessun bump — il `behind` si accenderà alla
  **prossima** release che bumpa `/VERSION`).

**Validazione end-to-end (dal vivo, isolata):**
- **Auto-updater — 3 scenari** (dir temporanee, dogfood reale intatto): **up-to-date** (GET reale `/VERSION`
  = 200 pubblico → `verdict:up-to-date`, SessionStart muto) · **behind** (stamp finto `0.0.9` vs `0.1.0` reale →
  `verdict:behind` → «SERTOR UPDATE AVAILABLE: installed 0.0.9, latest 0.1.0») · **unknown una-tantum**
  (FEAT-017: GET forzata 404 → `verdict:unknown` → «UPDATE CHECK UNAVAILABLE» al 1° SessionStart + `unknown_notified:true`
  persistito → 2° SessionStart **muto**). Il go-public ha sbloccato il fetch (prima 404 privato = `unknown` permanente).
- **Install esterno** (simulazione utente terzo, dir **fuori dal checkout**):
  `uvx --from "git+https://github.com/themetriost/Sertor@v0.1.0#subdirectory=packages/sertor" sertor --help`
  → tag pubblico risolto a `00dcd62`, **buildati da git** `sertor`+`sertor-install-kit` (deps workspace uv risolte
  via `git+url`), **85 pacchetti installati**, `sertor --help` **exit 0**. La distribuzione interim `git+url` funziona
  **pubblicamente**.

**Confine:** ops/release + doc; **zero codice**. **Residuo go-public** (gated 1° ago, CI verde): publish PyPI
(`pip install sertor`) + eventuale bump/nuova release che accende `behind` in produzione + pip fallback reale
(residuo FEAT-010) — casa [[go-public-pypi]] (`requirements/sertor-cli/go-public-pypi/`).

**Rituale: record ✅ · distill: non serve** (milestone di release; nessuna entità durevole tecnica nuova) **· lint ✅**
(EXEC/epic riflettono release fatta + PyPI ancora gated; nessun claim in deriva — es. `/VERSION` fermo a `0.1.0`
**non è drift**, è «prima release, nessun bump ancora») **· explainer: non innescato** (una release non aggiunge
una capacità nuova da spiegare a non-tecnici; il valore utente-facing è già in `docs/why-sertor.md`/getting-started).
