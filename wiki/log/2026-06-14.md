# Log 2026-06-14

Voci di registro del 2026-06-14.

## [2026-06-14] record | Spec 019 creata: hardening cache + token (REQ-H4/H5) — specify, checklist verde

Avviata la pipeline SpecKit dei due Should del gruppo C (costo dell'indicizzazione) del requirement `requirements/sertor-core/hardening-produzione/requirements.md`. Branch `019-hardening-cache-token` creato dall'hook before_specify; `.specify/feature.json` → 019, locale per policy.

- **Spec:** `specs/019-hardening-cache-token/spec.md` — 2 user story: P1 = **cache embeddings per content-hash** (REQ-H4: il re-index di corpus invariato non ri-embedda i chunk identici; cache dietro manopola Settings, default DISABILITATA per retrocompatibilità; chiave su contenuto+modello/provider; degrada non-fatale; indice equivalente con/senza cache) e P2 = **token nei log** (REQ-H5: l'evento di embedding logga il conteggio token quando il provider lo espone — Azure `usage.total_tokens` — assenza gestita, mai errore; osservabilità distingue cache-hit da embedding effettivi, rende misurabile SC-3 costo). 13 FR (gruppo A cache FR-001..007, gruppo B log FR-008..010, vincoli trasversali FR-011..013), 6 SC misurabili, edge case riconosciuti.
- **Checklist qualità:** `checklists/requirements.md` tutta verde al primo passaggio; zero NEEDS CLARIFICATION (decisioni chiuse come Assumptions: default cache off, nessuna eviction MVP, chiave su contenuto+modello, token best-effort). Tensione vocabolario accettata (come feature 013/015/018): per una libreria di retrieval "cache"/"embedding event" sono dominio, non leak.
- **Fuori ambito (chiariti nei requirements):** i Could H7/H8/H9/H10/H11 e il refresh incrementale FEAT-009 (la cache è il mitigante immediato, agisce sul passo di embedding a valle del re-chunk).
- **Roadmap:** EXEC aggiornato (hardening IN PROGRESS → spec 019 creata; prossimo `/speckit-plan`, clarify saltabile).
- **Prossimo step:** `/speckit-plan`.

## [2026-06-14] record | Feature 019 implementata: cache embeddings + token nei log (SpecKit completo, su branch)

Implementazione completata della feature 019-hardening-cache-token: i due Should del gruppo C (costo dell'indicizzazione) via pipeline SpecKit full (specify→plan→tasks→analyze→implement) in un'unica giornata. Codice su branch `019-hardening-cache-token`, non ancora mergiato su master.

- **Cache embeddings (REQ-H4):** nuovo decoratore `CachingEmbedder` in `adapters/embeddings/cache.py` + store SQLite `<index_dir>/embed_cache.sqlite` (chiave: model + sha256 contenuto); vettori float64 esatti via `array('d')`, equivalenza byte-esatta con/senza cache; degrado non-fatale on store failure (cache-miss loggato, mai errore). Wiring trasparente SOLO al percorso indexing via `build_embedder(cache=...)` da `build_indexer`; manopola `SERTOR_EMBED_CACHE` (default False, retro-compat). Dedup in-call; hit/miss tracciati in evento osservabilità `embeddings_cache`.
- **Token nei log (REQ-H5):** `_embed_batch` azure.py/ollama.py ritorna `(vettori, token|None)` (Azure `usage.total_tokens`, Ollama `prompt_eval_count`); evento `embeddings` accumula e emette (provider, texts, tokens) — field tokens assente quando il provider non lo espone (FR-009). Porta `embed()` invariata.
- **Fix collaterale (necessario H5):** redazione segreti in `observability/logging.py` matchava per sottostringa → mascherava `tokens`. Reso match per parola (split snake_case/camelCase): `token`/`access_token`/`api_key` redatti, `tokens` (metrica costo) esposto. Test regressione in test_logging.py.
- **Qualità:** 28+ test nuovi (test_embedding_cache.py, test_embedding_token_log.py, +settings/logging estesi, +fixture CountingEmbedder); suite intera 395 (root) + 85 (packages) verde, ruff pulito. Constitution Check PASS 10/10 senza deroghe.
- **Pagine evergreen aggiornate (distill):** `[[indexing-and-retrieval]]` (cache per content-hash nella sezione costo rebuild) e `[[ports-adapters]]` (CachingEmbedder come decoratore + token nei log) via flusso principale.
- **Polish:** docs/install.md (sezione embed cache + token), template `.env` (commento `SERTOR_EMBED_CACHE`).
- **Prossimo step:** PR verso master; al merge: re-index dogfood corpus `sertor` + eventuale abilitazione `SERTOR_EMBED_CACHE=true`.

## [2026-06-14] record | Nuova area wiki «explainers» — descrizioni per non tecnici (panoramica + 6 capacità)

Creata la nuova area wiki `wiki/explainers/` dedicata a descrizioni in linguaggio comune (per non tecnici): ogni capacità di prodotto ha una pagina-specchio che spiega cosa fa senza gergo tecnico, con rimando alla pagina tecnica di concetto. Area additiva al wiki, niente codice toccato.

- **Area creata:** `wiki/explainers/` con type `explainer` aggiunto in `[[taxonomy]]` di `wiki/wiki.config.toml` (UNICA fonte di host-specificity; tassonomia dichiarativa, non vincolante al lint).
- **Pagine create (7):** panoramica `sertor-in-parole-semplici.md` (porta d'ingresso) + 6 capacità: `ricerca-codice-e-documenti.md` · `i-modi-di-cercare.md` (i 4 motori basici in parole semplici) · `wiki-che-cresce.md` · `collegamento-con-l-assistente.md` (MCP integrato) · `installare-su-un-progetto.md` (installer CLI) · `robusto-ed-economico.md` (hardening: riprova su errori, sa dire "non lo so", cache 019, token 019, mostra il costo).
- **Stile:** niente gergo; paragoni quotidiani per ogni concetto; ogni pagina rimanda al dettaglio tecnico nella pagina di concetto corrispondente via wikilink.
- **Index aggiornato:** nuova porta d'ingresso «Capire il prodotto senza gergo», riga area `explainers/` nella tabella di organizzazione, sezione «Explainers (per non tecnici)» con i 7 link; frontmatter `updated`.
- **Lint:** `sertor-wiki-tools lint` clean (0 broken_links / 0 orphans / 0 missing_frontmatter / 0 naming).
- **Branch/commit:** pagine create sul branch 019 (incluse in PR #33 assieme a cache+token); re-index del corpus `sertor` differito al merge su master (standing rule).

## [2026-06-14] record | PR #33 mergiata + cache attivata sul dogfood — risparmio misurato (769.981 token → 0)

PR #33 mergiata su master (merge commit 2424f0f): feature 019 (cache embeddings per content-hash + token nei log) + nuova area wiki `explainers/` (descrizioni per non tecnici) in produzione.

- **Rituale post-merge:** roadmap → DONE (riga hardening: Should gruppo C ✅ su master PR #33; nuova voce DONE dettagliata; dettaglio IN PROGRESS aggiornato). Distill già fatto pre-merge (`[[indexing-and-retrieval]]`, `[[ports-adapters]]`).
- **Cache ATTIVATA sul dogfood:** aggiunto `SERTOR_EMBED_CACHE=true` al `.env` del corpus `sertor` (gitignored).
- **Re-index di verifica sul corpus reale `sertor`:** 415 doc / 3890 chunk, Azure text-embedding-3-large, .index-sertor. A FREDDO (1° re-index, cache vuota): `embeddings_cache hits=0 misses=3890`; embedder reale ricevuti texts=3715 (175 duplicati interni deduplicati in-call); `embeddings tokens=769981`; elapsed ~104s. A CALDO (2° re-index, corpus invariato): `embeddings_cache hits=3890 misses=0` (100% hit); NESSUN evento `embeddings` (embedder non chiamato) → 0 token consumati; elapsed ~8s (~13× più veloce). Esito: re-index di corpus invariato a costo Azure nullo (SC-001 dimostrato live), dedup in-call verificata.
- **MCP:** nessun riavvio necessario (cache solo sul percorso d'indicizzazione; server di query legge indice rinfrescato da disco). Key Azure: ruotata dall'utente.
- **Prossimo:** i Could dell'hardening (H7-H11) oppure la distribuzione multi-assistente, a scelta dell'utente.

## [2026-06-14] record | Nuova epica «Osservabilità e pannello di controllo» (requirements, livello epica)

Creata la nuova epica «Osservabilità e pannello di controllo» a livello di requisiti per gestire la persistenza dei log strutturati emessi dal core (index/embeddings/embeddings_cache/low_confidence/…), che oggi sono effimeri su stderr. Nasce dall'esigenza di report storici e misurazioni di hit/miss della cache.

- **Architettura:** due strati — (1) osservabilità persistente nel core via porta+adapter store locale (SQLite, simmetrico alla cache 019) + servizio aggregazione/report; (2) pannello TUI come 4ª superficie thin consumer (MCP-style).
- **Backlog:** 10 feature MoSCoW (7 Must per MVP: FEAT-001..004 strato persistente + aggregazione + TUI live + report; Should: FEAT-005..006 export OTel + metriche aggregate; Could: web mode, trend, metriche graph/wiki).
- **Decisioni fork:** superficie=TUI (web Could fase 2); dati=SQLite locale built-in + export OTel opzionale (Phoenix, Langfuse, Grafana licensable senza vincoli su Sertor).
- **Criteri successo:** 7 misurabili (audit trail immutabile, latenza query <50ms, coesione osservabilità con retrieval events, …).
- **Vincoli costituzionali:** thin consumer I, porta+adapter II/VIII, local-first+host-agnostico II/X, extra isolati [tui]/[otel] III, store gitignored, non-bloccante VII/IX.
- **Domande aperte:** 7 DA-O-a..g (prezzi €, retention, cosa rende "live" il TUI, privacy testo query, confine fleet/second-brain, innesto su log_event, priorità €/OTel).
- **Riferimenti prior art:** Langfuse, Phoenix, TruLens, Toolong, dolphie, harlequin, k9s, OpenTelemetry GenAI conventions.
- **File:** `requirements/osservabilita/epic.md` creato; roadmap aggiornata (idea → epica aperta con link).
- **Prossimo:** risolvere DA-O prioritarie con utente, poi decomporre Must in EARS, poi SpecKit.

## [2026-06-14] record | Decisioni privacy/€ osservabilità + nuova epica «Memoria conversazioni» (pattern Hermes)

Tre decisioni utente sulla coppia di epiche osservabilità/memoria: (1) **PRIVACY = privacy-by-default a strati** (default solo metriche; testo query+conversazioni = opt-in; ricerca SEMANTICA che embedda = opt-in ulteriore; FTS locale di default; scrub segreti nel testo + retention) — principio CONDIVISO tra le due epiche; (2) **STIMA € alzata da Could a SHOULD**, attesa nell'MVP accanto al report costo (prezzi in config aggiornabile); (3) **SEQUENZA = due epiche SEPARATE** (conoscenza ≠ telemetria), osservabilità prima, memoria elaborata in parallelo.

- **Epica osservabilità aggiornata:** REQ-E8 (privacy-by-default) + REQ-E9 (opt-in a strati) trasversali; FEAT-007 stima € → Should; DA-O-d e DA-O-g segnate RISOLTE. File: `requirements/osservabilita/epic.md` aggiornato.
- **NUOVA epica:** `requirements/memoria-conversazioni/epic.md` creata — **terzo livello / episodica**: archivio grezzo di TUTTE le conversazioni, interrogabile nei casi speciali — il tassello mancante sotto il diario ([[diary-vs-graph]]), fonte grezza per la distillazione (oggi lossy). Ispirazione: pattern Hermes (Nous Research) — **mappa Hermes↔Sertor** (session archive SQLite+FTS = il tier mancante; MEMORY.md/skills ≈ wiki/.claude). 8 feature MoSCoW; MVP = FEAT-001 cattura + FEAT-002 ricerca episodica locale. Privacy condivisa (REQ-M-E1/E2).
- **Nodo centrale:** la cattura è host-specifica (transcript Claude Code nell'harness) → adapter dietro config (Principio X), legata alla distribuzione multi-assistente. 6 domande aperte DA-M-a..f (cattura, granularità, tutto-vs-selettivo, relazione coi corpora, second-brain; privacy già fissata).
- **Verificato:** il precedente N7/D-20 ⛔ riguardava il GATE-al-commit, NON l'archiviazione conversazioni (nessuna decisione riaperta).
- **Roadmap:** riga osservabilità aggiornata (privacy/€ risolte) + nuova riga epica memoria (👍 aperta, in parallelo).

## [2026-06-14] record | Decomposta FEAT-001 osservabilità — strato di osservabilità persistente (requisiti EARS)

Decomposta la prima feature Must dell'epica osservabilità: specifica interamente codificata nei requirements EARS, con verifica a terra su codebase reale, domande aperte catalogate, decisioni di design differite.

- **Origine:** dall'esigenza di persistenza dei log strutturati (`log_event`) emessi dal core durante indexing, embedding, retrieval — oggi effimeri su stderr, non interrogabili per metriche storiche.
- **Requisiti EARS:** 13 REQ + 6 NFR in `requirements/osservabilita/strato-osservabilita-persistente/requirements.md`; porta di osservabilità + adapter di persistenza locale (SQLite simmetrico a cache 019).
- **Fondamenta:** FEAT-001 è il layer su cui costruiscono FEAT-002 (aggregazione/report) e FEAT-003 (TUI live). FEAT-004 (export OTel) agganciato ma indipendente.
- **Vincoli critici:** additivo/non-breaking (firma log_event invariata), default OPT-IN (off = comportamento odierno), non-bloccante degrado (per composabilità come cache 019), privacy-by-default metriche-only via redazione, host-agnostico porta+adapter, store gitignored.
- **A terra:** esaminati i log_event reali nel core — `retrieve` non logga testo query, `embeddings` non logga contenuti chunk — default metriche-only realizzabile oggi. Serve classificazione campi (metrica/contenuto) quando si introdurrà testo (DA-001).
- **Architetture considerate:** (a) logging.Handler trasparente vs (b) emissione esplicita — requisiti neutri, design decisions in DA-O-f.
- **Domande aperte:** 7 DA-O-a..g catalogate (retention, schema store, innesto log_event, priorità €/OTel, privacy + confine fleet/second-brain); DA-001 (classificazione campi) e DA-002 (schema SQLite) aperte per design.
- **Status:** FEAT-001 decomposta nel backlog; prossimo = design delle DA-O prioritarie (utente), poi specifica FEAT-002/003/004 in parallelo o SpecKit.

## [2026-06-14] record | Decomposte FEAT-002/003/004 osservabilità — MVP completo a requisiti

Completata la decomposizione dei 4 Must dell'epica osservabilità (F1 già fatta prima): 3 nuove feature decomposte IN PARALLELO da 3 requirement-analyst (Sonnet), il flusso principale ha aggiornato il backlog dell'epica con il status e committato su master.
- **FEAT-002 aggregazione-report:** 26 REQ + 5 NFR in `requirements/osservabilita/aggregazione-report/requirements.md` — servizio nel core che interroga lo store di FEAT-001 e calcola 5 report canonici (cache hit/miss timeline, token cost per provider, corpus stats, operation latency p50/p95, error/retry counts). Consumer dello store, NO UI, deterministico, privacy-by-default metriche-only.
- **FEAT-003 pannello-tui-live:** 20 REQ + 7 NFR in `requirements/osservabilita/pannello-tui-live/requirements.md` — TUI vista LIVE, thin consumer, extra `[tui]` isolato, read-only/non-intrusivo, host-agnostico, testabile senza TTY.
- **FEAT-004 pannello-tui-report:** 17 REQ + 6 NFR in `requirements/osservabilita/pannello-tui-report/requirements.md` — TUI sfogliabile per i report di FEAT-002 (no aggregazione in UI), viste per cache/costo/corpus/freschezza, degradazione onesta (es. € assente → token).
- **Backlog epica aggiornato:** FEAT-002/003/004 marcate «decomposta» con link ai requirements; l'intero MVP osservabilità (F1→F4) è a requisiti EARS. Decisioni di design aperte (DA-O-a..g) catalogate nei requirements, differite a SpecKit.
- **Prossimo:** avviare SpecKit su FEAT-001 (il fondamento), poi a cascata FEAT-002→004.

## [2026-06-14] record | Nuovo passo del rituale: riassunto non tecnico (explainer) — installabile; + explainer del pannello di controllo

Esteso il rituale di step (richiesta utente): nuovo passo 7 «Riassunto non tecnico (explainer)». Quando uno step sviluppa o PIANIFICA una capacità significativa (requisito/feature), produrre/aggiornare una descrizione in linguaggio comune in `wiki/explainers/` (per non tecnici), con rimando al dettaglio tecnico in `concepts/`/`tech/`. È GIUDIZIO → flusso principale (come distill/lint), NON Haiku. Calibrato al valore (opzionale: solo per capacità che vale spiegare, non per step meccanici/tooling). Applicabile sia a ciò che è FATTO sia a ciò che si STA PER SVILUPPARE (pagina marca lo stato).

- **Estensione CLAUDE.md:** passo 7 aggiunto al rituale in `CLAUDE.md`; passo 8 spostato da 7. Fonte unica = CLAUDE.md (cit. dal playbook §5 tavola operazioni).
- **INSTALLABILE:** passo 4 del nuovo rituale (in inglese `explainers as LLM output`) incorporato anche in `packages/sertor/src/sertor_installer/assets/claude-md-block.md` (asset distribuito agli ospiti). Verificato: guardia di lingua `test_assets_english` verde + 6 test di sync (lint wiki clean).
- **Primo esercizio del nuovo passo:** creato `wiki/explainers/il-pannello-di-controllo.md` — riassunto non tecnico dell'epica osservabilità (4 feature in sviluppo: persistenza log, aggregazione/report, TUI live, TUI sfogliabile), con privacy (solo metriche di default) e immagine di mock cruscotto. Marcato «in sviluppo». Linkato da index.md e da `[[sertor-in-parole-semplici]]`.
- **File toccati:** `CLAUDE.md` (passo 7 rituale), `packages/sertor/src/sertor_installer/assets/claude-md-block.md` (asset anglofono), `wiki/explainers/il-pannello-di-controllo.md` (nuovo), `wiki/index.md` (updated), `wiki/explainers/sertor-in-parole-semplici.md` (backlink aggiunto).
- **Natura:** governance/doc + explainer; nessun codice di prodotto toccato. Re-index differibile.

## [2026-06-14] record | Spec 020 creata: strato di osservabilità persistente (F1 epica osservabilità) — specify, checklist verde

Avviata la pipeline SpecKit della prima feature Must dell'epica osservabilità. Branch `020-osservabilita-persistente` (creato dall'hook before_specify; `.specify/feature.json` → 020, locale per policy). Fonte: `requirements/osservabilita/strato-osservabilita-persistente/requirements.md`.

- **Spec creata:** `specs/020-osservabilita-persistente/spec.md` — 3 user story: P1 «i numeri restano e si possono ritrovare» (MVP: archivio locale interrogabile per operation+tempo degli eventi che il core già emette via `log_event`); P2 «chi non la attiva non vede cambiamenti» (opt-in conservativo, default off, nessuna regressione, additivo/non-breaking); P3 «conservare non deve rompere né rallentare» (non-bloccante + degrado non-fatale come la cache 019). 13 FR, 7 SC misurabili, edge case.
- **Checklist verde:** `checklists/requirements.md` al primo passaggio; zero NEEDS CLARIFICATION (default conservativi ereditati dall'epica). Lasciate al plan (DA-O-f) il MECCANISMO di intercettazione e lo SCHEMA dello store.
- **Roadmap:** EXEC aggiornato — osservabilità diventa IN PROGRESS (F1 in SpecKit, spec creata; prossimo `/speckit-plan`); blocco hardening rimosso.
- **Prossimo step:** `/speckit-plan` su F1 (progettare lo store guardando i requisiti di F2).

## [2026-06-14] record | Plan 020 completato: osservabilità persistente — DA-O-f risolta (logging.Handler), Constitution PASS

Fase plan della feature 020-osservabilita-persistente (F1 epica osservabilità) completata sul branch omonimo: 5 artefatti (plan, research, data-model, contracts/observability-store + contracts/event-capture, quickstart). Constitution Check PASS 10/10 senza deroghe.

- **DECISIONE CARDINE DA-O-f risolta (research D1):** la cattura degli eventi avviene con un `logging.Handler` stdlib (`EventPersistenceHandler` in observability/capture.py) attaccato dal composition root al logger `sertor_core` SOLO se abilitato. Legge i campi GIÀ strutturati+redatti dal LogRecord (operation + extra, ts da record.created). Vantaggi: (a) ZERO modifiche a log_event/call-site (additivo, FR-005); (b) NON-fatale GRATIS — il framework logging non propaga le eccezioni di un handler (handleError) → FR-007 by design; (c) default-off = nessun handler/store (FR-004); (d) redazione già applicata in `extra` → FR-009 gratis. Alternativa (emissione esplicita in log_event) respinta (più accoppiata). Verificato a terra: log_event fa `safe=redact(fields)` poi `extra={operation, **safe}`.
- **Store (research D2):** SQLite `<index_dir>/observability.sqlite` (stdlib, gitignored, simmetrico alla cache 019), tabella events(id, ts, operation, fields json) con indici (operation,ts)/(ts). DIMENSIONATO per le aggregazioni di FEAT-002: bucket temporali via funzioni data SQLite, estrazione campi via json_extract, correlazione tra eventi via finestra temporale. La query di F1 è solo per-operation+intervallo (FR-003); le aggregazioni sono di F2.
- **Porta (D4):** 7ª porta `ObservabilityStore` (record_event/query_events) in domain/ports.py = seam tra «dove vivono gli eventi» (F1) e «chi li interroga» (F2) — giustificata da un consumatore reale, non YAGNI. Adapter SqliteObservabilityStore.
- **Manopole (D6):** `Settings.observability_enabled` (SERTOR_OBSERVABILITY, default False); sede derivata da index_dir; gancio retention previsto (politica DA-O-b rinviata). Non-bloccante (D5): insert sincrono a bassa cardinalità (eventi per-operazione), QueueHandler come via di fuga documentata.
- **CLAUDE.md:** marker SPECKIT aggiornato 019→020. Roadmap EXEC: F1 plan completo, prossimo /speckit-tasks.
- **Prossimo step:** `/speckit-tasks`.

## [2026-06-14] record | Feature 020 implementata: strato di osservabilità persistente (F1) — SpecKit completo, su branch

Implementazione completata della feature 020-osservabilita-persistente (F1 epica osservabilità) via pipeline SpecKit full (specify→plan→tasks→analyze→implement) in un'unica giornata. Branch `020-osservabilita-persistente`, NON ancora mergiato. È il FONDAMENTO: FEAT-002 (aggregazione/report) e il pannello TUI (F3/F4) leggeranno questo store.

- **Porta + adapter domain:** 7ª porta `ObservabilityStore` (record_event/query_events) in domain/ports.py; adapter `SqliteObservabilityStore` (observability/store.py — SQLite `<index_dir>/observability.sqlite`, tabella events(id,ts,operation,fields json) + indici (operation,ts)/(ts), degrado non-fatale).
- **Handler di cattura:** `EventPersistenceHandler` (observability/capture.py — logging.Handler che cattura i LogRecord strutturati di log_event, estrae i campi non-standard via `_RESERVED`, ignora i log non strutturati). GUARDIA DI RE-ENTRANCY contro la ricorsione (avviso di guasto store è a sua volta un evento).
- **Wiring:** composition.py `build_observability_store` + `enable_observability` (attach idempotente SOLO se SERTOR_OBSERVABILITY=true; abbassa la soglia del logger a INFO per CATTURARE gli eventi INFO senza aggiungere rumore su stderr); manopola Settings `observability_enabled` (default off); export da __init__.
- **Realizzazione DA-O-f:** l'handler è additivo (log_event invariato), non-fatale GRATIS (logging non propaga le eccezioni handler), redazione GRATIS (extra già redatto). VERIFICATO rischio ricorsione (RNF-006).
- **Qualità:** 11 test nuovi (test_observability_store 5 + test_observability_capture 6: cattura end-to-end, ignora log non strutturati, redazione segreti nello store, default-off senza handler/store, idempotenza, resilienza a store corrotto). Suite intera 406 root + 85 packages verdi, ruff pulito. Constitution PASS 10/10.
- **Distill (flusso principale):** [[ports-adapters]] aggiornata (7ª porta ObservabilityStore + meccanismo handler-observer). [[il-pannello-di-controllo]] già presente (resta «in sviluppo»: F1 persiste ma report/TUI sono feature successive).
- **Polish:** docs/install.md (sezione observability) + template .env (SERTOR_OBSERVABILITY commentato).
- **Roadmap:** F1 implementata su branch.
- **Prossimo:** PR di F1 oppure proseguire con F2 (consuma lo store via query_events).

## [2026-06-14] record | PR #34 mergiata: F1 osservabilità persistente in produzione — re-index dogfood

PR #34 mergiata su master (merge commit a1789b9): feature 020 (F1 epica osservabilità = strato di osservabilità persistente con store SQLite) è in produzione. Rituale post-merge completato: roadmap EXEC aggiornato, distill fatto, corpus dogfood re-indicizzato.

- **Feature F1:** strato persistente degli eventi strutturati (`log_event`) emessi durante indexing/embedding/retrieval, abilitabile via `SERTOR_OBSERVABILITY` (default off), 7ª porta `ObservabilityStore` pronta per FEAT-002 (aggregazione/report).
- **Roadmap:** F1 ✅ mergiata; ora IN PROGRESS = F2 aggregazione/report + F3/F4 pannelli TUI; il rituale di step 7 (explainer non tecnico) è documentato in `CLAUDE.md`.
- **Distill pre-merge:** pagine `[[ports-adapters]]` (7ª porta + meccanismo handler-observer) aggiornate.
- **Re-index dogfood:** cache 019 ha prodotto — 3889 hit / 212 miss, 68.823 token (vs ~770k a freddo), ~16s; corpus a 435 doc / 4101 chunk; contesto RAG allineato a master. SERTOR_OBSERVABILITY NON attivato sul dogfood (si potrà per dare dati reali a F2).
- **Verification:** PR #34 merge commit a1789b9; Constitution Check PASS 10/10; suite test 406 root + 85 packages verde.

## [2026-06-14] record | Feature 021 implementata: servizio aggregazione/report osservabilità (F2) — SpecKit completo in autonomia

Implementazione completata della feature 021-osservabilita-report: i due Must dell'epica osservabilità (F2 = aggregazione/report) via pipeline SpecKit full (specify→plan→tasks→analyze→implement) in un'unica giornata. Codice su branch `021-osservabilita-report`, NON ancora mergiato su master. È ciò che rende LEGGIBILI i dati che F1 conserva.

- **Servizio ObservabilityReports (services/observability_report.py):** legge gli eventi via la porta `ObservabilityStore` di F1 (già su master) e li aggrega in 5 report canonici (funzioni PURE/deterministiche). Implementazione: D1 facciata con un metodo per report (no selettore stringa); D2 aggregazione in memoria con funzioni pure (lo store resta query-engine); D3 bucket UTC deterministico; D4 percentili nearest-rank (no interpolazione, riproducibile); D5 risparmio = STIMA (caveat dedup in-call della 019); D6 dati assenti → report VUOTO esplicito (zeri), mai eccezione.
- **I 5 report:** (1) **CACHE** — hit/miss timeline, risparmio stimato in token per day, "missing vs hit" richiesto; (2) **COSTO** — token per provider e per bucket temporale (metriche aggregate, privacy-by-default); (3) **SALUTE corpus** — ultimo index: documents/chunks/dimensione medio + serie storica per day; (4) **LATENZE** — p50/p95 nearest-rank per operation (index/retrieve) da elapsed_ms, bucket UTC; (5) **AFFIDABILITÀ** — conteggi retry/low_confidence/errori + abstention_rate per operation.
- **Design:** D1 facciata con un metodo per report; D2 aggregazione in memoria; D3 bucket UTC deterministic; D4 percentili (no interpolazione); D5 stima €; D6 dati assenti → report vuoto (zeri). build_observability_reports in composition.py riusa build_observability_store; export da __init__.py.
- **Qualità:** 9 test nuovi (i 5 report su eventi simulati via InMemoryObservabilityStore in fixtures/mocks; test vuoto esplicito; test determinismo; test helper percentili/bucket; test wiring reale su store vuoto). Suite 415 root + 85 packages verdi, ruff pulito. Constitution PASS 10/10.
- **Fuori ambito (feature successive):** pannello TUI (F3/F4 renderanno questi report), export OTel (F5), conversione € (FEAT-007 si appoggia a CostReport), freschezza-vs-repo (host-specifica).
- **Note:** distill di una pagina-concetto dedicata all'osservabilità rinviato a quando l'epica matura (F3/F4); per ora l'explainer [[il-pannello-di-controllo]] copre il quadro non tecnico.
- **Prossimo:** PR di F2 oppure proseguire con F3 (TUI live).

## [2026-06-14] record | PR #35 mergiata: F2 report osservabilità in produzione — re-index dogfood

PR #35 mergiata su master (merge commit 064a66f): feature 021 è in produzione — il servizio `ObservabilityReports` (aggregazione e 5 report canonici) rende LEGGIBILI i dati che F1 conserva nel store persistente. Constitution PASS 10/10 (415+85 test).

- **Rituale post-merge:** roadmap EXEC aggiornato (F1 ✅ + F2 ✅; IN PROGRESS = F3 «TUI live»; prima superficie visibile, crea il frame per le decisioni di design seguenti su extra [tui] Textual).
- **Re-index corpus dogfood `sertor`:** cache 019 attiva, reindex su master (standing rule); reindex incrementale cache 019 → 4099 hit / 95 miss (solo chunk toccati F1+F2 via PR #34 e F2 code); 445 doc / 4194 chunk; elapsed ~11s.
- **Prossimo:** SpecKit su F3 (TUI live) in autonomia del flusso — due decisioni di design che flusso principale cattura e segnala: (1) framework Textual (extra [tui] isolato); (2) meccanismo "live" di F3 (DA-O-c risolto o aperto?).

## [2026-06-14] record | Feature 022 implementata: pannello TUI vista live (F3) — la prima superficie visibile, SpecKit in autonomia

Implementazione completata della feature 022-osservabilita-tui-live (F3 epica osservabilità): il pannello TUI di controllo che mostra in diretta i report di osservabilità. Branch `022-osservabilita-tui-live`, NON ancora mergiato su master. È la PRIMA interfaccia visibile dell'osservabilità: consuma il store di F1 e i report di F2, li espone in un cruscotto self-updating da terminale.

- **Modello di stato PURO + rendering:** nuovo modulo `observability/live.py` con classe `LiveSnapshot` e funzioni pure `live_snapshot(reports) → LiveSnapshot` e `render_snapshot() → str`. Testabilità garantita SENZA terminale (test_observability_live.py: stato corrente, vuoto onesto, recent_events, render determinista).
- **TUI Textual (extra `[tui]` opzionale):** `ObservabilityApp` in `observability/tui.py` (ereditato da `App`); refresh su timer (~2s default `SERTOR_OBSERVABILITY_REFRESH`) rilegge i report via `build_observability_reports()` (nessun tail del log: disaccoppiato); read-only, non-intrusivo. Launcher `run_observability_panel` in composition.py (import lazy, errore ConfigError se extra assente).
- **Sottocomando aggiunto:** `sertor-rag observe` apre il pannello TUI.
- **Additivo a F2:** `ObservabilityReports.recent_events` (ultimi N eventi per scrolling), manopola `Settings.observability_refresh_s` (SERTOR_OBSERVABILITY_REFRESH, default 2s).
- **Degradazione onesta:** persistenza spenta (osservabilità disabilitata) → pannello mostra stato vuoto esplicito con call-to-action (non crash); extra `[tui]` assente → ConfigError con istruzione (come rerank/graph). FR-003/FR-008 entrambi satisfatti.
- **Qualità:** 18 test nuovi (test_observability_live = modello puro senza textual: stato corrente, recent_events, render; test_observability_tui = errore extra-mancante via monkeypatch + smoke headless via Pilot). Suite 424 root + 85 packages verde, ruff pulito. Constitution PASS 10/10.
- **Ambiente:** `uv sync --extra dev` per i test (incluso extra [tui] con textual 8.2.7 già verificato installabile). pyproject.toml: nuovo extra `[tui] = ["textual"]`; textual aggiunto a `[dev]`.
- **Distill rinviato:** pagina-concetto dedicata a osservabilità rinviata a fine epica; explainer [[il-pannello-di-controllo]] (già presente, marcato «in sviluppo») copre il quadro non tecnico (la "vista live" ora esiste; F4 aggiunge i report sfogliabili).
- **Verificazione:** commit c7b5c2e (su branch); suite verde; Constitution Check PASS 10/10.
- **Prossimo:** PR di F3 oppure F4 (TUI report sfogliabili).

## [2026-06-14] record | PR #36 mergiata: F3 pannello TUI live in produzione — re-index dogfood

PR #36 mergiata su master (merge commit abcd568): la feature 022 (F3 = pannello TUI vista live dell'osservabilità) è in produzione. Rituale post-merge completato: roadmap EXEC aggiornato, distill già fatto pre-merge.

- **Feature F3:** pannello TUI live che mostra in tempo reale i report di osservabilità (cache hit/miss, costo token, latenze, affidabilità, salute corpus); extra `[tui]` opzionale Textual 8.2.7; lanciabile con `sertor-rag observe`; degradazione onesta se persistenza spenta oppure Textual assente.
- **Rituale post-merge:** roadmap EXEC aggiornato — F1 ✅ + F2 ✅ + F3 ✅; ora IN PROGRESS = F4 «pannello TUI report» (viste storiche sfogliabili) che chiude l'MVP dell'osservabilità (F1→F4 tutti Must). Distill pre-merge via flusso principale.
- **Re-index corpus dogfood `sertor`:** cache 019 attiva, reindex su master (standing rule); cache 019 → 4194 hit / 85 miss, 68.823 token; corpus a 457 doc / 4279 chunk; contexto RAG allineato a master.
- **Verification:** PR #36 merge commit abcd568; Constitution Check PASS 10/10; suite 424 root + 85 packages verde. `sertor-rag observe` lancia il cruscotto live (demo_mode senza store + solo F3 codice, pronto per abilitare SERTOR_OBSERVABILITY su ospiti).

## [2026-06-14] record | Feature 023 implementata: pannello TUI report sfogliabili (F4) — ultimo Must osservabilità

Implementazione completata della feature 023-osservabilita-tui-report (F4 epica osservabilità): il pannello TUI che rende i report di osservabilità sfogliabili — cache, costo, corpus, latenze, affidabilità — con cambio intervallo temporale. È l'ULTIMO Must dell'MVP osservabilità (F1→F4): chiude l'epica come primo prodotto visibile. Branch `023-osservabilita-tui-report`, NON ancora mergiato. Al merge l'MVP dell'osservabilità è completo.

- **Estensione F3:** `ObservabilityApp` trasformata in TabbedContent (schede: Live/Cache/Cost/Corpus). Le viste report rendono i report di F2: Cache (hit/miss timeline + risparmio estimato), Cost (token per provider e giorno), Corpus (ultimo stato + freschezza, degrada onesta se € assente). Tasto `t` cicla l'intervallo temporale (all → 7g → 24h, mostrato in sub-title).
- **Architettura:** funzioni pure in `observability/live.py` (render_cache_report, render_cost_report, render_corpus_report + helper time_window/next_window — testabili senza terminale); schede Textual in `observability/tui.py` (thin wrapper). Sola lettura; degradazione onesta.
- **Qualità:** test nuovi (resa report per intervallo + funzioni pure; smoke Textual via Pilot: 4 schede, ciclo intervallo). Suite 433 root + 85 packages verdi, ruff pulito. Constitution PASS 10/10. Nessuna nuova dipendenza/manopola.
- **Distill + aggiornamento [[il-pannello-di-controllo]] rinviati al POST-MERGE** (quando l'MVP F1→F4 è completo su master).
- **Verification:** suite 433 root + 85 packages verde; Constitution Check PASS 10/10; branch `023-osservabilita-tui-report`.
- **Prossimo:** PR di F4 → al merge, MVP osservabilità completo; poi Should/Could dell'epica (export OTel, metriche, web) oppure multi-assistente / memoria conversazioni.

## [2026-06-14] record | MVP Osservabilità completo su master (F1→F4, PR #34/#35/#36/#38) + fix wiki/wiki/ (PR #37)

Mergiati su master: F4 (PR #38, pannello TUI report sfogliabili con schede Live/Cache/Cost/Corpus e intervalli temporali) e il fix del resolver wiki (PR #37, deriva wiki/wiki/ + refactor auto-discovery). Con F4 l'MVP dell'epica osservabilità (F1→F4) è COMPLETO su master: F1 strato persistente (store SQLite + 7ª porta ObservabilityStore, cattura via logging.Handler) → F2 servizio ObservabilityReports (5 report da query_events, funzioni pure) → F3 pannello TUI vista live (sertor-rag observe, extra [tui] Textual) → F4 report sfogliabili a schede (Live/Cache/Cost/Corpus, tasto t intervallo all/7g/24h, freschezza). Privacy-by-default; architettura modello/aggregazione puri nel core + guscio Textual sottile; sola lettura; degradazione onesta. Constitution PASS 10/10 ×4.

- **Fix wiki/wiki/ (PR #37):** causa = auto-discovery di _resolve_config cwd-sensibile (dalla feature 016 la config è in wiki/; lanciato da cwd dentro wiki/ risolveva la root a wiki/wiki/, creando un log misfiled). Fix = guard samefile che ancora la root al genitore; + refactor a uscita unica (estratti _launched_inside_wiki e _discover_config). 2 test di regressione, verificato live. La directory spuria wiki/wiki/ è stata eliminata.
- **Rituale post-merge:** re-index del corpus dogfood `sertor` (cache → 4273 hit/83 miss, 465 doc/4356 chunk). Roadmap EXEC: osservabilità da IN PROGRESS a ✅ DONE (nuova voce DONE dettagliata; IN PROGRESS svuotato). Distill di chiusura: explainer [[il-pannello-di-controllo]] aggiornato da «in sviluppo» a «fatto (MVP)».
- **Verification:** Constitution Check PASS 10/10 ×2; suite 433 root + 85 packages verde; corpus `sertor` re-indicizzato.
- **Prossimo:** emendamento alla costituzione (chiarimento Principio VII su nesting/guard-clause vs SESE) su branch+PR, su richiesta utente. Poi: Should/Could osservabilità (export OTel, metriche, stima €, web) o decomporre memoria conversazioni.

## [2026-06-14] record | Emendamento Costituzione v1.1.1 — chiarimento Principio VII (nesting/guard-clause, SESE non obbligatorio)

Emendamento PATCH alla Costituzione di Sertor: v1.1.0 → v1.1.1 (su branch+PR `025-constitution-nesting`, come da workflow di produzione). Nessun nuovo vincolo: chiarimento di un principio esistente.

- **Cosa:** chiarito il **Principio VII** (Leggibilità) sullo stile delle funzioni — DEVONO essere piccole e a **bassa profondità di annidamento**; si **preferiscono i guard clause / early return** alla nidificazione profonda, estraendo un helper nominato quando un blocco cresce/si annida; il **single-exit dogmatico (SESE) NON è richiesto**: il problema da evitare è il *nesting*, NON il numero di `return` (la structured programming di Dijkstra bandiva il GOTO, non i return multipli). Allinea la regola alla pratica già in uso nel codebase (Clean Code).
- **Origine:** il refactor di `_resolve_config` (wiki_tools) e la discussione con l'utente su SESE vs nesting — l'utente ha chiesto di codificare la regola.
- **Artefatti:** `.specify/memory/constitution.md` (Principio VII + Sync Impact Report + footer v1.1.1, Last Amended 2026-06-14); template dipendente `.specify/templates/plan-template.md` (rif. v1.1.0→v1.1.1, gate invariati); pagine wiki [[constitution]] (changelog v1.1.1 + pointer) e [[mission-vision]] (pointer). Verificato: nessun test inchioda la versione; lint wiki pulito.
- **Prossimo:** PR verso master; al merge, la costituzione v1.1.1 è in vigore.

## [2026-06-14] record | Osservabilità cablata e accesa (fix wiring + accendi)

Feature 020 (F1 osservabilità persistente) era completata su branch ma non produceva effetti end-to-end: la funzione `enable_observability` era definita ma nessun consumatore la chiamava. Fix: cabrata nei consumatori — CLI (`src/sertor_core/cli/__main__.py` in `_cmd_index` e `_cmd_search`, dopo `_check_backend`) e MCP server (`src/sertor_mcp/server.py` in `main()` allo startup) — e aggiunto il wrapper `call_builtin_function` a `composition.py` per errori di configurazione. A persistenza disabilitata (default) è ancora no-op (zero overhead, backward-compatible). Test regressione aggiunti: `test_index_wires_observability` e `test_main_wires_observability`. 437 test (no-cloud) verdi, ruff clean. Mergiata PR #40 (08f162c).

Poi accensione: `SERTOR_OBSERVABILITY=true` aggiunto al `.env` (non versionato) e re-index reale del corpus dogfood `sertor` (465 doc, 4360 chunk, embedding_dim 3072) + 3 ricerche. Verificato: lo store SQLite locale (`.index-sertor/observability.sqlite`) ha catturato **11 eventi end-to-end** (embeddings ×4, embeddings_cache ×1, graph_build ×1, hybrid_query ×3, index ×1, ingest ×1). Osservabilità viva sul dogfood, pronta per FEAT-002 (aggregazione/report). NB: il server MCP va riavviato per codice nuovo.

- **Wiring:** CLI + MCP server, wrapper di errore ConfigError, default off.
- **Accensione:** SERTOR_OBSERVABILITY=true nel .env, 11 eventi catturati in store SQLite locale, verifica live OK.
- **Qualità:** 437 test verdi, Constitution PASS 10/10, ruff clean.
- **Prossimo:** decomporre memoria conversazioni oppure Feature 021 (aggregazione/report), a scelta utente.

## [2026-06-14] record | Decomposta epica memoria-conversazioni — FEAT-001/002 a requisiti EARS

Epica `requirements/memoria-conversazioni/epic.md` (memoria episodica / "terzo livello", pattern Hermes) decomposta a livello FEATURE (requisiti EARS) per le due Must:

1. **FEAT-001 «Cattura & archiviazione locale dei transcript»** → `requirements/memoria-conversazioni/cattura-archiviazione/requirements.md` (27 REQ EARS, 8 NFR, 6 SC misurabili). Fondamento: Claude Code persiste già le sessioni come JSONL in `~/.claude/projects/<progetto>/<session-id>.jsonl` → la "cattura" per Claude Code = leggere quei JSONL dietro un adapter host-specifico (Principio X: la cattura è host-specifica, archiviazione è generica). Privacy-by-default (off di default), scrub segreti esteso al contenuto dei transcript, idempotenza, archivio conservato (no rotazione).

2. **FEAT-002 «Ricerca episodica full-text locale»** → `requirements/memoria-conversazioni/ricerca-episodica-fulltext/requirements.md` (21 REQ EARS, 8 NFR, 7 SC). Query full-text lessicale LOCALE (no embedding → contenuto on-machine, privacy+robustezza); la ricerca SEMANTICA resta FEAT-004 (Should, opt-in).

MVP = FEAT-001 → FEAT-002. Decisioni APERTE catalogate: granularità unità di memoria (sessione vs turno — DA-M-b), cattura tutto-con-opt-in vs selettiva "remember this" (DA-M-c), relazione con i corpora (confine episodio ↔ corpus — DA-M-d). Scelte di design rinviate al plan di ciascuna feature. Backlog epica aggiornato a "decomposte FEAT-001/002 con link ai requirements".

- **Epica creata:** `requirements/memoria-conversazioni/epic.md` (8 feature MoSCoW, privacy condivisa con osservabilità, 6 DA aperte).
- **FEAT-001 requisiti:** 27 REQ + 8 NFR, cattura host-specifica (adapter JSONL/etc) dietro config (Principio X).
- **FEAT-002 requisiti:** 21 REQ + 8 NFR, FTS locale senza embedding (privacy by design).
- **Stato backlog:** entrambe decomposte, ready per clarify/plan.
- **Prossimo:** risolvere DA-M prioritarie (utente) oppure proseguire con FEAT-021 (aggregazione/report) in parallelo.

## [2026-06-14] record | Affidabilità del dogfood MCP — riparazione + segnalazione errori

Affidabilità del server MCP `sertor-rag`: riparazione + segnalazione degli errori su tre strati.

Durante il test post-rotazione della key Azure sono emersi due guasti distinti. **Causa 1 (http 401):** il server MCP era lanciato prima della rotazione della key e manteneva in memoria la chiave vecchia — risolto con reconnect. **Causa 2 (No module named 'rank_bm25'):** il venv `.venv-core` (usato dal server secondo `.mcp.json`) era stantio rispetto al venv di progetto `.venv` (privo di `rank-bm25`, dipendenza base del motore ibrido FEAT-004); sincronizzato con `uv pip install --python .venv-core/Scripts/python.exe -e ".[mcp,graph]"`. **Causa radice operativa:** i due venv divergono facilmente (footgun); opzioni aperte = (1) ripuntare `.mcp.json` a `.venv` (un solo env) OPPURE (2) documentare/scriptare il sync (fuori scope presente). **Perché emerso tardi:** il ramo `.venv-core` è esercitato quasi solo dal MCP; CLI/test girano su `.venv` sano; inoltre i subagent, su errore MCP, ripiegavano in silenzio su Read/Grep, sopprimendo il segnale; i guasti erano "a strati" (il 401 mascherava il modulo mancante).

- **Fix consegnato (FEAT-001 affidabilità-dogfood):** segnalazione degli errori MCP su tre strati: (1) helper `_guard` avvolge i 7 tool in `src/sertor_mcp/server.py` → su errore emette evento `mcp.<tool>.error` nello store di osservabilità (feature 020, persistenza già su master) e **ri-solleva invariato** (visibilità, non swallowing); così gli errori compaiono nel report affidabilità (F2 feature 021) e nel pannello TUI (F3/F4 feature 022/023); (2) `_self_test()` allo startup del server esercita una ricerca end-to-end (embedding+BM25): un guasto (key invalida, extra mancante, store rotto) emerge loud su stderr al reconnect, non-fatale; indice assente NON è un errore (degrada a lista vuota, coerenza con policy facade); (3) governance anti-silenzio: i 4 agenti che usano sertor-rag (`requirements-analyst`, `speckit-plan`, `speckit-implement`, `speckit-analyze`) e `CLAUDE.md` (sezione dogfooding) ora impongono di **segnalare esplicitamente** gli errori MCP invece di degradare in silenzio.
- **Qualità:** 441 test non-cloud verdi (+4 nuovi su _guard/_self_test), ruff pulito.
- **Azione:** il self-test e gli eventi d'errore diventano attivi al prossimo reconnect del server (serve codice nuovo). Pagina [[mcp-server]] aggiornata (sezione Troubleshooting).

## [2026-06-14] record | FEAT-001 memoria consegnato — cattura & archiviazione local dei transcript

Implementazione completata della prima metà dell'epica «Memoria delle conversazioni» (pattern Hermes): il tier episodico grezzo che cattura il transcript di ogni sessione, lo ripulisce dai segreti e lo conserva in un archivio SQLite locale per progetto. Mergiata su master (PR #45, commit c1fa95d).

- **Architettura:** 8ª porta `TranscriptCaptureAdapter` (host-agnostico, Principio X) + adapter Claude-Code (legge JSONL di sessione da ~/.claude/projects) + store SQLite `MemoryArchive` (idempotente, conservato, gitignored). Entità di dominio pure: `SessionRef`, `TranscriptTurn`, `TranscriptContent`, `ArchivedSession`. Granularità ibrida (sessione = unità archiviata, turni = grana di ricerca).
- **Scrub:** Funzione pura `scrub_text` estende la redazione al contenuto libero. Pattern: API key, token bearer, `KEY=VALUE`, header Authorization. Ripiego conservativo. Configurabile. Mai bypassabile.
- **Configurazione:** 5 manopole in `Settings` (default privacy-by-default, lazy import). Wiring in composition root SOLO se `SERTOR_MEMORY=true`.
- **Qualità:** 29/29 task, 488 test non-cloud verdi, ruff clean. Constitution Check PASS 10/10. Tutti i 27 REQ EARS + 8 NFR testati.
- **Distill:** Nuove pagine wiki — `[[memoria-conversazioni]]` (concetto), `[[transcript-capture-adapter-e-storage]]` (componenti), `[[scrub-segreti-in-contenuto]]` (tecnica), `[[memoria-negli-agenti]]` (explainer). Aggiornato `[[index.md]]`.
- **Prossimo:** FEAT-002 (ricerca episodica full-text locale), FEAT-003 (distillazione).

## [2026-06-14] record | FEAT-002 ricerca episodica consegnato — query full-text locale sui transcript

Implementazione completata della seconda metà dell'MVP dell'epica «Memoria delle conversazioni»: ricerca full-text **locale** sull'archivio dei transcript conservato da FEAT-001. Rende interrogabile l'archivio episodico (domande: «ne avevamo già parlato?», «com'è finita tre settimane fa?») via full-text lessicale senza embedding, mantenendo privacy e robustezza offline. Mergiata su master (PR [oggi], commit [commit-hash]).

- **Architettura:** Servizio `EpisodicSearch` + indice FTS5 nativo SQLite con trigger di sincronizzazione. Input: query testuale + vincoli opzionali (finestra temporale since/until, ordinamento, limite, lunghezza snippet). Output: lista ordinata di turni corrispondenti con sessione padre, timestamp, ruolo, indice, snippet BM25-ranking, latenza. Offline (zero rete), non-distruttivo (indice derivato, ricostruibile), robusto (archivio assente/vuoto/malformato → stato vuoto + warning).
- **Privacy by design:** Nessun embedding, nessun LLM, nessuna rete nel percorso di query → contenuto non lascia la macchina. Evento osservabilità registra query **hashata** (sha256[:16]), mai il testo in chiaro.
- **Entità:** `SearchQuery` (query + since/until + limit + snippet_tokens + order_by), `EpisodicHit` (session_key, captured_at, role, turn_index, snippet, score), `EpisodicResults` (hits + latency_ms), eccezione `InvalidTimeWindowError` (since > until).
- **Configurazione:** 2 manopole in `Settings` (`SERTOR_EPISODIC_LIMIT` default 20; `SERTOR_EPISODIC_SNIPPET_TOKENS` default 12). Wiring `build_episodic_search(settings)` in composition root.
- **Prova live:** Eseguita su dogfood di produzione (36 sessioni / 5062 turni archivati). Query «Langfuse» → trovato turno reale di 6h fa, score 9.57, ~78ms. Query «GraphRAG» → 4 turni in sessioni diverse, ordinamento pertinenza verificato. Finestra temporale «hardening» ultimi 2 giorni → 8 turni su 35 globali, range corretto.
- **Qualità:** 27 test nuovi, 515 test non-cloud verdi, ruff clean. Constitution Check PASS 10/10.
- **Distill:** Nuove pagine wiki — `[[feat-002-ricerca-episodica-fulltext]]` (experiment), `[[ricerca-episodica-fts5]]` (tech FTS5). Aggiornato `[[memoria-conversazioni]]` (stato FEAT-002 ✅), `[[memoria-negli-agenti]]` (explainer: ricerca ora possibile, temporale, FEAT-004 semantica futura), `[[index.md]]`.
- **MVP memoria completo:** FEAT-001 (cattura + archivio) + FEAT-002 (ricerca locale) = memoria episodica interrogabile funzionante end-to-end. Prossimi tier (FEAT-003/004/005/006/008): distillazione, ricerca semantica, marcatura, retention, multi-assistente.

## [2026-06-14] record | FEAT-035 — Superficie CLI memoria + hook SessionEnd (MVP completo)

Completamento MVP della memoria conversazioni (FEAT-001 cattura + FEAT-002 ricerca episodica) con superficie d'uso (CLI) e grilletto automatico (hook SessionEnd). La memoria episodica passa da componente di libreria a strumento quotidiano d'uso.

- **Superficie CLI:** comandi `sertor-rag memory search "<query>"` (filtri `--since`/`--until`/`-k`, output umano/JSON) e `sertor-rag memory archive` (idempotente); thin-consumer importa da core via factory `build_*`, pattern coerente con MCP-server.
- **Hook SessionEnd:** `.claude/hooks/memory-capture.ps1`, legge `SERTOR_MEMORY`, lancia `archive`, exit 0 sempre (non-bloccante); wiring in `.claude/settings.json`.
- **Privacy-by-default:** memoria OFF se `SERTOR_MEMORY` non configurato; hook is no-op (exit 0); scrub segreti nel contenuto.
- **Validazione:** SpecKit completo (specify→plan→tasks→analyze→implement), 33/33 task, 12 test nuovi, 527 test non-cloud verdi, ruff pulito, Constitution 10/10 ✅.
- **Pagine evergreen aggiornate:** [[memoria-conversazioni]] (stato MVP ✅), [[memoria-negli-agenti]] explainer (linguaggio comune: «si usa da terminale e si salva da sola»).
- **Nuova pagina:** [[feat-035-superficie-cli-memoria-hook-sessionend]] experiment record.
- **Index aggiornato:** entry experiments FEAT-035 + frontmatter updated.
- **Fuori scope:** hook per altri assistenti (pattern FEAT-008), distribuzione agli ospiti (FEAT-012 update), ricerca semantica (FEAT-004), distillazione (FEAT-003), retention (FEAT-006).
- **Prossimo:** il flusso principale aggiorna roadmap EXEC (memoria DONE) e re-index corpus `sertor`.

## [2026-06-14] record | FEAT-036 - Aggancio distillazione all'archivio episodico (loop cattura-distill chiuso)

FEAT-036 chiude il loop cattura->distillazione dell'epica Memoria conversazioni: l'archivio episodico (FEAT-001 cattura + FEAT-002 ricerca) diventa una FONTE legale per l'operazione `distill` del wiki, finora teorica (pretendeva trascrizione grezzo scritto a mano).

- **Consegna:** PR #51 mergiata su master (88088fe); thin consumer additivo (zero nuove porte); SpecKit completo specify->plan->tasks->analyze->implement in giornata.
- **Core:** metodo `MemoryArchive.list_recent(limit)` recency-first (turn_count con fallback); entita `SessionSummary`; eccezione `SessionNotFoundError`; factory `build_memory_reader` gated su `SERTOR_MEMORY`; manopola `Settings.memory_list_limit` (default 20).
- **CLI:** comandi `sertor-rag memory list [-k N]` e `sertor-rag memory show <session_key>` con output umano/JSON; gate `None->ConfigError` exit 1 distinto da sessione vuota exit 0; evento osservabilita registra conteggi+chiave opaca (mai contenuto).
- **Wiring distill:** `.claude/skills/wiki-author/ops/distill.md` modalita "from conversation" attinge archivio via `memory list`/`show`; **vincolo FR-013 ribadito:** distillazione SEMPRE su singola sessione mirata, mai intero archivio, mai automatica (cattura economica I/O, distillazione costosa LLM, disaccoppiate).
- **Qualita:** Constitution PASS 10/10, 558 test non-cloud verdi (31 nuovi), ruff clean, validazione live su archivio reale (36 sessioni / 5062 turni).
- **Asset:** spec/plan/research/tasks in `specs/036-aggancio-distillazione/`; asset anti-drift in `packages/sertor/.../assets/claude/skills/wiki-author/ops/distill.md`.
