# Log 2026-06-23

Log entries for 2026-06-23.

## [2026-06-23] record | E10 debiti — FEAT-012/013/003: governance nel corpus, layout `.sertor/`, CI Windows

Attacco a tre debiti dell'epica E10 `debito-tecnico`, operazione `record`: governance nel corpus RAG (FEAT-012), allineamento layout config dogfood↔ospite (FEAT-013), CI GitHub Actions Windows (FEAT-003).

### FEAT-012 — Governance (costituzione) nel corpus RAG (✅ DONE)

**Problema:** la costituzione non era nel corpus dogfood interrogabile. Causa-radice: il `.env` del dogfood impostava `SERTOR_EXCLUDE_PATTERNS` che includeva `.specify` (oltre a `.claude`/`prototype`/`raw`); il default del codice (`_DEFAULT_EXCLUDES` in `src/sertor_core/config/settings.py`) **non** esclude `.specify` (intenzionalmente host-agnostico), e il template installer nemmeno — gli ospiti erano già a posto. **Fix (dogfood-only):** rimosso `.specify` dagli exclude del `.env` (file gitignored, nessun artefatto tracciato qui). **Verifica:** `sertor-rag eval validate-path` conferma `.specify/constitution.md` + `.specify/plan-template.md` ora nell'indice (markdown indicizzabili). Nota latente: il template installer omette `.git`/`.hg`/`.svn` dagli exclude (basso impatto, riportato).

### FEAT-013 — Allineamento layout config dogfood↔ospite (✅ DONE)

**Problema:** il dogfood leggeva `.env` da ROOT con override `SERTOR_INDEX_DIR=.index-sertor` (non era `.sertor/`); gli ospiti usano `.sertor/.env` + `.sertor/.index`. **Decisione utente:** migrare il dogfood sotto `.sertor/` per mangiare il proprio dog-food anche sul layout. **Implementazione (due punti):**
1. Esteso `_resolve_env_path` in `settings.py` per cercare anche `./.sertor/.env` sotto cwd (additivo, host-agnostico: trova `.sertor/.env` dalla root anche se venv NON è annidato sotto `.sertor/`, come il dogfood `./.venv`).
2. Spostato `.env` dogfood → `.sertor/.env`, rimosso override `SERTOR_INDEX_DIR` → l'indice ora si ancora a `.sertor/.index` per convenzione.

**Test aggiunto:** `test_cwd_sertor_dotenv_resolved_when_venv_not_nested` in `tests/unit/test_settings_runtime.py` (6/6 verdi).

**Re-index full nel nuovo percorso:** 935 documenti / 10268 chunk, collezione `sertor__azure_text_embedding_3_large` (metadata puliti post-fix FEAT-004).

**Finding (segnalato per il smoke-test standing):** il server MCP `.mcp.json` serve ancora dalla vecchia `.index-sertor` (config caricata all'avvio) → richiede **RECONNECT del server** per servire dalla nuova `.sertor/.index` (regola «errori MCP = finding, mai rumore» della costituzione).

### FEAT-003 — CI GitHub Actions Windows (✅ DONE, Linux = prossima fase)

**Problema:** non esisteva alcun workflow CI (`.github/workflows/` assente). Suite girava **solo in locale**. **Decisione utente:** prima abilitare CI Windows su GitHub, poi Linux (fase 2).

**Creato `.github/workflows/ci.yml`:**
- Matrix `os` contiene **SOLO `windows-latest`** (Linux = aggiunta di una riga, prossimo step).
- Step: setup-uv, Python 3.12 pin, `uv sync --all-packages --extra dev`, `uv run ruff check .`, suite per-package con `-m "not cloud and not integration"` (root + sertor + install-kit + sertor-flow).

**Lint fix:** aggiunto `extend-exclude = ["prototype"]` a `[tool.ruff]` in `pyproject.toml` (il prototipo congelato/read-only → fuori scope lint). Ruff ora pulito.

**Nota:** CI non ancora eseguita su GitHub (richiede push, scelta utente pendente).

### Bonus — FEAT-008 debito chiuso (epica memoria-conversazioni)

**Scoperta:** guardia auto-derivante `packages/sertor/tests/unit/test_env_template_memory.py` (di FEAT-004) rilevava **2 test ROSSI su master** — il knob `SERTOR_MEMORY_COPILOT_SESSION_DIR` (aggiunto da FEAT-008) mancava nei template `.env` dell'installer. Debito dichiarato di FEAT-008 promosso a FEAT-009 (tracciato); `master` era rosso.

**Fix:** aggiunti `# SERTOR_MEMORY_COPILOT_SESSION_DIR=` e menzione adapter `copilot-cli` in entrambi i template (`env.local.tmpl`, `env.azure.tmpl`). Suite verde.

### Esiti

- **Suite (tutti `not cloud and not integration`):** root 977 ✓ · sertor 329 ✓ · install-kit 131 ✓ · sertor-flow 134 ✓ · ruff pulito.
- **Constitution Check:** N/A (non nuovo design, tutti fix meccanici).
- **Lint A (strutturale):** 0 broken wikilink, 0 frontmatter issues.
- **Pagine aggiornate:** `requirements/debito-tecnico/epic.md` (sezione 8, tre righe stato), `wiki/index.md` (updated date).

### FEAT-003 — primo run CI Windows: ROSSO → fix ermetico → verde (il payoff)

Pushato il branch + aperta **PR #96**; la CI Windows ha girato per la **prima volta in assoluto** su GitHub. **Esito: ROSSO** allo step «Tests — core (root)» — `3 failed, 974 passed` in `test_engine_selection.py` con `GloveUnavailableError [reason=cache_miss]` (+ ~3 min di hang).

**Causa-radice (test non-ermetico, NON regressione delle modifiche):** il default `glove` (FEAT-011) risolve la **cache GloVe in fase di costruzione** dell'embedder; in un checkout CI pulito non c'è cache → fail. In locale passava **solo** perché la macchina dev ha la GloVe in cache. Questi test verificano la **selezione del motore** (provider-agnostica), non l'embedding.

**Fix:** `tests/unit/test_engine_selection.py` — il helper `_settings` usa ora il provider **`hash`** (stdlib, zero-download), coerente con l'intento dichiarato del file («embedder costruito ma mai invocato: niente rete»). 8/8 verdi in 0.9s.

**Lezione:** è esattamente il valore di FEAT-003 — la CI su ambiente pulito coglie la non-ermeticità che il Windows locale (con cache) nasconde. Pattern da sorvegliare: ogni test che costruisce l'embedder di default tocca la risoluzione GloVe; deve forzare `hash` o iniettare un fake. *(Verifica «watch exit 0» fuorviante: lo stato reale del run va confermato con `gh run view`/`gh pr checks`, non col solo exit code del watch.)*

### FEAT-003 — esito finale: Windows + Linux VERDI (✅ DONE)

- **Run Windows (post-fix):** `28015551078` → `success` (5m18s), tutti gli step ✓. Confermato con `gh run view --json status,conclusion` (non col solo exit del watch).
- **Fase 2 — Linux:** aggiunto `ubuntu-latest` alla matrix (`fail-fast: false`). Run `28015943729` → `success` su **entrambi** i job: `test (windows-latest): success` · `test (ubuntu-latest): success`. Nessun problema Linux-specifico (path/case-sensitivity) emerso.
- **Esito:** FEAT-003 chiuso — prima CI del progetto, verde su Windows **e** Linux. Resta su E10 il solo Should **FEAT-011** (hook freschezza RAG).

## [2026-06-23] lint | Rifresco del drift nei backlog `epic.md` (stato ↔ realtà di master)

PR #96 mergiata su `master` (merge `537307e`, branch eliminato): i tre debiti E10 (FEAT-012/013/003) sono ora su master. A valle, **lint semantico** sui backlog d'epica: la colonna *Stato* di molti `requirements/<epica>/epic.md` era **stantia** — feature già consegnate su master ancora marcate "decomposta"/"🔄 implementata". Una lettura letterale sovrastimava l'aperto. Riallineate alla realtà (19 righe → ✅ DONE):

- **E1 sertor-core (5):** FEAT-001 nucleo · FEAT-002 baseline · FEAT-007 manutenzione wiki + tema lingua · FEAT-009 refresh incrementale (`3ec47f1`) · FEAT-MCP server MCP.
- **E3 osservabilità (6):** FEAT-001/002/003/004 MVP F1–F4 (PR #34/#35/#36/#38) · FEAT-005 export OTel (feature 061) · FEAT-015 visibilità RAG (PR #88).
- **E4 memoria (5):** FEAT-001 cattura (PR #45) · FEAT-002 full-text (PR #47) · FEAT-003 distillazione (PR #51) · FEAT-008 cattura Copilot (`7dd1182`) · FEAT-009 distribuzione installer (`a36ba89`).
- **E5 retrieval-qualità (2):** FEAT-001 eval (PR #92) · FEAT-011 graph-eval.

Doc-only (nessun codice/test toccato). **Aperto reale residuo** per epica: E1 nessuno · E3 FEAT-006/007/009/011/012/016 · E4 FEAT-005/006/007/010 · E5 FEAT-002/004/005/006/007/010 + leva `search_docs`. *(E2 già allineato; E6–E11 genuinamente da aprire; E10 già aggiornata in #96.)* Su branch dedicato → PR.

### Prossimi step

- **PR #96** ✅ MERGIATA su master (`537307e`).
- FEAT-013: smoke-test standing richiede reconnect MCP (non automatico, già segnalato a flusso principale).
- FEAT-003 fase 2: aggiunta Linux al matrix `os` (.github/workflows/ci.yml).
- Altro E10: FEAT-001 host-agnosticità residua (scoperta case-by-case), FEAT-011 hook freschezza RAG (enforcement), FEAT-005 igiene wiki (guidata da lint C).

## [2026-06-23] record | Nuova epica E12 `usabilità` (esperienza setup/config/uso)

Esplorata e creata una **nuova epica** (la 12ª): **`usabilità`** — l'esperienza utente di setup,
configurazione e uso di Sertor, oggi inadeguata (attriti reali ma sparsi, nessun flusso unificato che
previene/spiega prima). Lavoro in plan mode (2 esplorazioni Explore della UX reale + superfici
agentiche) → 3 bivi decisi con l'utente → epic.md.

- **Identità & lente:** strato di **UX agentica** (skill focalizzate + agente *concierge*) sopra i
  vehicle deterministici, vincolo **D↔N** (il core non chiama mai un LLM), + poche **primitive
  deterministiche** mancanti (su tutte `sertor-rag doctor`) + **documentazione self-describing
  dual-audience** (agente + umano non-interno, tema sollevato dall'utente). Ancoraggio missione:
  adozione/portabilità (Principio X) — un agente che sa installare/configurare/diagnosticare Sertor da
  solo È host-agnosticità reale.
- **Confine = owner del layer UX** (scelta utente): E12 possiede l'**esperienza** e assorbe gli item
  UX-facing da E2 (`--check`→`doctor`, ergonomia), E3 (diagnosi user-facing), E10 (freschezza
  user-facing); i **meccanismi** restano nelle epiche d'origine come dipendenze (no duplicazione).
  Aggiunti **cross-ref banner** nei §8 di `sertor-cli`/`osservabilita`/`debito-tecnico` (CS-7).
- **Forma:** skill + agente concierge a strati; riusa il pattern di distribuzione dual-target.
- **Backlog A–E (9 FEAT)** con MoSCoW; **MVP = FEAT-001 `sertor-rag doctor` + FEAT-002 guided-setup**
  (Must); ordine 1-3-4-2.
- **Artefatti:** nuovo `requirements/usabilita/epic.md`; riga **E12** in roadmap (tabella epiche +
  legenda E1..E12). Pipeline: esplorazione (PR #98, merge `932f0e2`) → cross-ref + log (qui) →
  decomposizione MVP (in corso).
- **Lint A:** wikilink `[[...]]` non usati nell'epic.md (link relativi Markdown verso le epiche
  d'origine, coerenti con lo stile dei requirements).
- **Re-index:** batchato a fine sessione (dopo la decomposizione MVP), per non pagare gli embeddings
  Azure due volte su step ravvicinati (regola «calibra al valore»).

### Prossimi step (E12)
- Decomporre **FEAT-001 `sertor-rag doctor`** e **FEAT-002 guided-setup** in
  `requirements/usabilita/<feature>/requirements.md` (EARS).
- Re-index del corpus dogfood (requirements/ + wiki/ indicizzati) + smoke test MCP.

## [2026-06-23] record | E12-FEAT-001 sertor-rag doctor — diagnostica di salute deterministica (IMPLEMENTATA)

Implementazione completa della **primitiva centrale** dell'epica E12 `usabilità`: il comando **`sertor-rag doctor`** diagnostica quattro aree (config/env, provider embeddings, indice RAG, server MCP) con verdetto per-area **pass/warn/fail**, causa + rimedio concreto per ogni problema, output umano + JSON schema `doctor.report/1`, exit code gate per scripting (≠0 se critico).

**Decisioni fissate:**
- **DA-D4** — Severità: CRITICO = env mancante O indice assente/incompatibile; WARN = provider irraggiungibile/indice stantio/MPC non registrato; exit 0 se tutti ≥ WARN.
- **DA-D5a** — Probe provider `--online` opt-in, sentinella embed, zero download GloVe, fallback offline-safe. Costruito via vehicle `build_provider_probe` (Principio XI).
- **DA-D5b** — Rilevamento stantio-MCP best-effort; se non osservabile → report `unknown` (Principio XII, non fake).

**Architettura:** servizio puro `services/doctor.py` (entità frozen + funzioni diagnosi per-area) → formatter puro `format_health_report` in `cli/output.py` → handler thin in `cli/__main__`. Helper sola-lettura nel composition root per manifest FEAT-009, probe provider, registrazione MCP, stat file.

**Esiti:** root 1118✓/3skip, sertor 333✓ (+34 doctor), Constitution 12/12 ✅, ruff pulito. File: `doctor.py` · `test_doctor.py` · `test_cli_doctor.py` · `test_output_doctor.py` (nuovi); 5 file modificati (errors/composition/cli/output/configure).

**Backlink:** [[valutazione-e-non-regressione]] (gemello vehicle) · [[mission-vision]] (Principio X operativo) · [[indexing-and-retrieval]] (manifest FEAT-009) · [[mcp-server]] (diagnostica registrazione).

**Debito promosso:** server MCP self-check freschezza indice → FEAT-011 epica E10 (enforcement-hooks). Ospiti ricevono `sertor-rag doctor` come parte dell'installer (FEAT-009 follow-up).

**Nota di processo:** branch `074-doctor-salute`, SpecKit completo specify→plan→tasks→implement.

## [2026-06-23] record | E12-FEAT-002 guided-setup — implementazione agentica

Implementazione completa della **prima feature agentica** dell'epica E12 `usabilità`: la skill `guided-setup` e l'agente concierge che guidano l'utente da repo non configurato a RAG verificato con 6 step deterministici, orchestrando SOLO i vehicle CLI (`sertor install`, `sertor configure`, `sertor-rag doctor/index`, mai reimplementati).

- **Skill `guided-setup`:** host-agnostica (no path/comando Claude hardcoded), 6 step sequenziali (detect → choose provider → install → configure → index → verify), euristica provider minimale + consenso esplicito, read-only libero/mutazioni su conferma.
- **Agente `concierge`:** orchestratore a ramo unico verso skill (FEAT-009 futura espanderà a multi-ramo), model pin `sonnet` su Claude (omesso su Copilot).
- **Distribuzione:** riuso standard del meccanismo agenti del kit (FEAT-007/009), estensione minima install_rag.py + guardia parità (no model-leak su Copilot), closure per-nome depositato.
- **Confine D↔N:** puro, vehicle-only, host-agnostico (Principio X).

**Esiti:** 363 test (invariato, sertor-core untouched), Constitution 12/12 ✅, ruff pulito. File nuovi: skill + agente + 2 guardia test. Modificati: install_rag.py + test parità esteso.

**Backlink:** [[assistant-targeting]] (pattern dual-target) · [[feat-074-doctor-salute]] (doctor usato per verify) · [[sertor-flow]] (modello skill+agente) · [[mission-vision]] (host-agnosticità reale).

**Non-bloccante:** prova live ospiti reali (R-4), FEAT-009 concierge multi-ramo (prossimo step).

**Nota di processo:** branch `075-guided-setup`, SpecKit completo specify→plan→tasks→implement. Merge pendente.

## [2026-06-23] record | E12-FEAT-001 doctor — fix falso positivo di freschezza (content-hash)

Fix della FEAT-001 sertor-rag doctor: il rilevamento della freschezza dell'indice falsava positivi su git checkout/merge/pull (file toccati a contenuto identico). La primitiva doctor valutava solo l'mtime; ora conferma col content-hash come l'indicizzatore incrementale (FEAT-009).

- **Bug:** doctor segnalava cronicamente `index_stale` dopo operazioni git (mtime rebumped a hash invariato), falso positivo proprio su caso d'uso quotidiano. Divergeva da DA-D2 («coerente col refresh incrementale»).
- **Causa-radice:** assenza del content-hash nella verifica di freschezza; il manifest FEAT-009 usa mtime+hash per rilevare cambiamenti reali.
- **Fix:** `current_source_stats` in `composition.py` calcola content-hash dei **soli** file con mtime cambiato, riusando pipeline dell'indicizzatore (`read_source` + `content_hash`); `freshness_from_manifest` marca `stale` solo se *mtime AND hash differenti* (o file sparito).
- **Nuovo test:** `test_freshness_touched_but_unchanged_not_stale` (regressione per il caso esatto di mtime avanzato/hash invariato).
- **Verifica live:** bump mtime di file indicizzato a contenuto invariato → doctor resta `index pass` (prima: `warn index_stale`).
- **Verifica:** root 1121 ✓, ruff clean.
