# Log 2026-07-27

Log entries for 2026-07-27.

## [2026-07-27] record | Aperto E10-FEAT-045: la macchina git per l'ancora esiste già, a due file di distanza

Consegnata la riqualificazione di **E10-FEAT-045 a Must/P0** (PR #238, docs-only: la riga di backlog
ora descrive un **deadlock**, non un'incoerenza fra strumenti) e aperta l'implementazione.

- **Ricognizione col RAG prima di leggere** (`search_combined`): dritto a `scan.py:41-58`
  (`_latest_log_mtime`) **e** alla decisione storica che lo motiva — **research D3 di `specs/006`**:
  *«mtime, non git»*, scelto **perché host-agnostico**. Il vincolo del Principio X non è una mia
  inferenza: è scritto nella decisione originale e ripetuto nel docstring del modulo (`scan.py:4`).
  Il flusso `graph` di `search_combined` (v0.2.0) ha aggiunto `_file_mtime` → chiamato solo da
  `_latest_log_mtime`: superficie di cambiamento delimitata senza aprire un file.
- **🔑 Il fatto che cambia la stima: la macchina git esiste già, a due file di distanza.**
  `ritual_check.py` ha `_git`, `_default_base_candidates` (E10-FEAT-033 — default branch rilevato a
  runtime, mai `master` hardcoded), `_resolve_base`, `_changed_repo_paths`. Il fix è un **riuso con
  estrazione**, non nuova infrastruttura. E i due strumenti che **misurano realtà diverse** vivono
  nello stesso pacchetto: è esattamente la firma del Principio XIV — un fatto derivabile tenuto come
  **proxy** accanto al derivatore, senza che nessuno confronti i due.
- **Il contratto `wiki.scan/1` va toccato, e non per comodità:** oggi `anchor` è un ISO-timestamp e
  **non dichiara la propria natura**. Se l'ancora diventa un commit dove è derivabile, sostituire un
  proxy silenzioso con un altro sarebbe ripetere il difetto nel fix che lo chiude: l'output deve
  **dire** se ha derivato o se sta stimando. È il XIV applicato al nostro stesso contratto.
- **Bivio di design portato all'utente** (non deciso qui): se accettare come chiusura del gate anche
  un **giudizio registrato** («ho guardato, non va registrato»). Rende il gate non-binario.

## [2026-07-27] distill | no dichiarato: l'entità candidata nasce dal fix, non dall'intenzione

**Nessuna entità nuova distillata**, e il «no» è **costato**: `ritual-check` riporta **0 candidati
distill / 0 drift** sullo scope `9cb2fb1...HEAD` (una sola pagina toccata, `syntheses/roadmap.md`),
ma il tool trova e io giudico — quindi ho valutato a mano ciò che la ricognizione ha fatto emergere.

**Candidati considerati e scartati:**

- **«Il derivatore e il proxy convivono nello stesso pacchetto»** — `ritual_check.py` deriva l'ancora
  dal diff git, `scan.py` la stima col mtime, e nessuno confronta i due. È la firma esatta del
  **Principio XIV**, già distillato ieri in [[constitution]]: questa è un'**istanza**, e la sua casa
  è la riga di backlog E10-FEAT-045. Una pagina propria direbbe «vedi il XIV».
- **«Un gate insoddisfacibile insegna la via d'uscita sbagliata»** — già considerato e scartato ieri
  per la stessa ragione (è una proprietà del caso, non un concetto autonomo). Non lo ri-promuovo
  oggi solo perché è ricomparso: sarebbe promuovere per ripetizione, non per identità.
- **«L'ancora deve dichiarare la propria natura»** (derivata vs stimata) — è il candidato **più
  vicino a meritare una pagina**, e lo tengo **in sospeso fino all'implementazione**: se il fix lo
  realizza come forma riusabile del contratto (`anchor` che porta *come* è stata ottenuta, non solo
  *quale* valore), allora è un'entità e si distilla **allora**, con il codice a sostenerla. Oggi
  sarebbe una pagina su un'intenzione.

**Perché non forzo la distillazione oggi:** lo step di oggi è **consegna di una riqualificazione**
(docs-only) più la ricognizione; l'entità vera, se c'è, nasce dal fix. Distillare un'intenzione
produrrebbe una pagina che il codice non conferma — la deriva che il lint semantico esiste per
cogliere.
