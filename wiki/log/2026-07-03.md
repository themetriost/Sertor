# Log 2026-07-03

Log entries for 2026-07-03.

## [2026-07-03] record | A-05 → E10-FEAT-027: dogfood come client Sertor fedele (fix de-reference RITIRATO)

**Operazione:** record (item A-05 audit SWOT — diagnosi + ritiro del primo fix + promozione a debito).

**Arco dello step:**
- **A-05 nasceva come «9 skill speckit fantasma»:** i 9 agenti dogfood `.claude/agents/speckit-*.md`
  dichiaravano «procedura autorevole in `.claude/skills/speckit-<fase>/SKILL.md`», ma quelle skill non
  esistono nel repo (pivot launch-installer 045: le materializza `specify init` sull'host, mai nel dogfood).
- **Primo fix (de-reference condizionale + guardia di root) — RITIRATO.** Su domanda dell'utente
  («abbiamo risolto o creato uno special-case?») è emerso che il fix **incistava** lo special case: la
  guardia *benediva* la divergenza dal client invece di eliminarla. Ritirato → i 9 agenti ripristinati a
  `master`, guardia `test_dogfood_agent_skill_refs.py` rimossa.
- **Causa reale = il dogfood NON è un client Sertor fedele:** (a) gli **mancano** skill+script+template
  SpecKit che ogni ospite riceve da `sertor-flow install`→`specify init`; (b) **porta** 9 agenti
  hand-authored che nessun client riceve (`test_no_vendored_speckit_agents`) e che puntano a quelle
  skill+script **assenti**. Residuo pre-pivot 045.
- **Materializzazione verificata (isolata, temp dir):** `specify init --ai claude` v0.8.18 con overlay
  UTF-8 `_UTF8_ENV` (senza cui aborta `UnicodeEncodeError` cp1252 → conferma il valore del guard di
  produzione) produce le 9 skill + `.specify/scripts/powershell/*` che **risolvono** sia i link-skill sia
  i riferimenti-script. Rischio costituzione confermato (l'upstream deposita una costituzione-*template*
  che clobbererebbe la nostra v1.4.0 → solo isolata + copia chirurgica; `plan-template.md` è Sertor-custom
  → preservare).
- **Promosso a E10-FEAT-027** (`debito-tecnico`, Should P0): conversione fedele-al-client (materializza
  machinery via install isolato · gitignore del rigenerabile · rimuovi agenti orfani · doc setup · guardia
  «no agenti speckit hand-authored») — **feature-sized**, da decomporre via SpecKit. L'esecuzione inline è
  stata **bloccata (giustamente) dall'auto-mode** (integra codice esterno in config + cancella config →
  richiede processo, non cowboy).

**Decisione/principio utente:** «il workspace Sertor lavori come un progetto-cliente di Sertor; gli
special-case sono debito». Memoria salvata (`feedback_dogfood_client_fedele`).

**Rituale:** record ✅ (questa voce) · **distill:** non serve (il principio è governance → salvato in
memoria + epica; nessuna nuova entità di dominio) · **lint semantico:** PULITO — corretto in più il drift
**A-04** (🔄→✅, era già mergiato PR #145); roadmap/epica riflettono il ritiro e FEAT-027 · **EXEC roadmap:**
aggiornato (A-04 ✅, A-05 → debito/FEAT-027, prossimo A-06) · **re-index/smoke:** hook SessionEnd (toccati
wiki/requirements; i 9 agenti ripristinati a `master`, nessun cambio di codice).

**Prossimo:** commit branch+PR (solo doc/governance: roadmap, epica, log, index — **niente cambi di codice**).
Poi A-06, oppure decomporre E10-FEAT-027 se prioritario.

## [2026-07-03] record | E10-FEAT-027 IMPLEMENTATA — dogfood come client SpecKit fedele

**Operazione:** record (implement di E10-FEAT-027, SpecKit completo requirements→specify→plan→tasks→implement).

**Cosa:** reso il dogfood di Sertor un **client SpecKit fedele**, chiudendo lo special-case diagnosticato in A-05. Prima: al dogfood mancavano skill/script SpecKit che ogni ospite riceve da `specify init`, e portava 9 agenti `speckit-*` hand-authored che nessun client ha (residuo pre-pivot 045), puntanti a skill+script assenti.

**Implementazione (branch `087-a05-dogfood-client-debt`, zero `sertor-core`):**
- **Script** `scripts/dev/materialize-speckit.ps1`: materializza la machinery via `specify init --ai claude` (pin `SPECKIT_VERSION` letto da fonte unica `profile.py`) in **dir isolata** + copia **selettiva** della sola machinery rigenerabile + overlay UTF-8 (`PYTHONUTF8`/`PYTHONIOENCODING`) + **verifica fail-loud** che `constitution.md` v1.4.0 / `plan-template.md` custom / `feature.json` restino byte-identici (mai copiati). Idempotente.
- **`.gitignore`**: blocco per la machinery rigenerabile (skill `speckit-*`, `.specify/scripts,workflows,integrations`, template non-custom, json) — non vendorata (modello `.venv`, evita re-vendoring/pin drift, NFR-1).
- **Rimossi** i 9 `.claude/agents/speckit-*.md` orfani (End-state fedele = skill native, invocate in-context come un client).
- **Guardia** `tests/unit/test_dogfood_speckit_fidelity.py` (offline, presence-agnostica): 0 agenti speckit hand-authored + 0 machinery rigenerabile tracciata + artefatti Sertor-authored tracciati.
- **Doc setup** in `CLAUDE.md` (sezione Sviluppo): lo step di materializzazione come `uv sync`; puntatore-piano → specs/085 (blocco SPECKIT snellito).

**Accettazione (verificata dal vivo):** script eseguito → 9 skill + 5 script materializzati e **gitignorati** (git status vuoto sulla machinery); artefatti Sertor-authored **byte-identici** (`git diff --quiet`); guardia **3/3 verde**; **1057 unit di root verdi** (1 skip noto); ruff pulito; `test_assets_sync` (21) e `test_no_vendored_speckit` (4) invariati. Il sistema ora espone le **skill native `speckit-*`** come Skill (il dogfood è un client fedele).

**Constitution:** 12/12 + missione PASS (XII rafforzato: rimossa la causa, non tollerata; X: dogfood-interno, asset distribuiti invariati; XI: usa `specify init` upstream, mai import del core). Complexity Tracking vuoto.

**Rituale:** record ✅ · **distill:** non serve (il principio «dogfood = client fedele» è già in memoria + epica; nessuna nuova entità di dominio) · **lint semantico:** pulito (roadmap/epic/CLAUDE coerenti con l'implementazione reale) · **EXEC roadmap:** aggiornato (A-05 → FEAT-027 implementata su branch) · **re-index/smoke:** hook SessionEnd (toccati `.claude/`, `scripts/`, `tests/`, `CLAUDE.md`; il server MCP andrà riavviato per servire le skill nuove, non serve al rituale).

**Prossimo:** commit + PR (su ok utente); al merge, verifica LIVE del setup su clone fresco.

## [2026-07-03] query | Audit di fedeltà dogfood↔client + epica E15 fedelta-dogfood

**Operazione:** query/audit (su richiesta utente «il punto era su TUTTO Sertor — abbiamo fatto su tutto?» dopo E10-FEAT-027).

**Cosa:** audit di fedeltà dogfood↔client su 3 superfici (RAG · wiki · governance) con 3 ricognitori paralleli + verifica empirica. **Verdetto: no, non su tutto** — E10-FEAT-027 ha reso fedele la sola fetta SpecKit. La fedeltà ha 2 livelli: **asset-fidelity** (stessi file, via `sertor_installer.sync`, oggi **parziale/disuniforme** — il sync copre solo `assets/claude/**`, non `assets/rag/**`; guardia su 3 hook RAG soli) e **process-fidelity** (prodotto dai veri installer, oggi **assente ovunque** — il dogfood non esegue mai `sertor install rag/wiki` né `sertor-flow install`; il processo è testato solo su host sintetici `tmp_path`).

**Correzione empirica decisiva:** il report governance e il mio script/FEAT-028 assumevano che `specify init --force` clobberasse la costituzione → **falso** (test isolato): `constitution.md` **preservata** (create-if-absent), `plan-template.md` **clobberato**, `feature.json` intatto. → il rischio governance è ristretto a `plan-template.md`; un vero `sertor-flow install` preserverebbe la v1.4.0. FEAT-028 ristretta di conseguenza. Lezione: l'assunzione stava nel mock dei test e nel docstring del mio script — l'ha smentita solo l'esecuzione reale (essenza del progetto).

**Gap per superficie:** RAG — mancano nel dogfood hook rag-usage+wiring, guided-setup, concierge, sertor-cli-reference, .sertor-version, blocco RAG-USAGE; `.mcp.json` dev-venv-form vs runtime `.sertor/`-form. Wiki — asset-fidelity forte, ma config super-set (dogfood AVANTI al template: explainers/audit/strings/roles/rag), CLAUDE.md prosa senza blocco marker. Governance — SpecKit via script isolato (FEAT-027), costituzione curata by-design.

**Deliverable:** nuova **epica E15 `fedelta-dogfood`** (`requirements/fedelta-dogfood/epic.md`) + sintesi `wiki/syntheses/audit-fedelta-dogfood-2026-07-03.md`. Backlog: FEAT-001 harness process-fidelity (Must, cuore: installa in sandbox e confronta) · FEAT-002 sync completo+guardie (Must) · FEAT-003 artefatti RAG mancanti · FEAT-004 divergenze dev↔client · FEAT-005 installer preservante plan-template (≡E10-FEAT-028) · FEAT-006 staleness inversa template. 3 domande aperte (modello sandbox vs repo-output · dichiara-o-elimina le divergenze-dev · priorità asset-first vs harness-first).

**Rituale:** record ✅ · distill: l'audit È la sintesi (nessuna nuova entità di dominio, il concetto «asset-fidelity vs process-fidelity» è nella synthesis) · lint semantico: l'audit stesso è il lint (i gap trovati sono finding, non correzioni d'ufficio; corretti solo il mio drift su FEAT-028/script) · EXEC roadmap: E15 aggiunta alla tabella epiche · re-index/smoke: hook SessionEnd.

## [2026-07-03] record | E15-FEAT-002 IMPLEMENTATA — sync completo + guardie (assorbe F3-file + chiude FEAT-025)

Chiuso il buco dell'asset-fidelity RAG del dogfood: sync esteso (`sync_assets`) da `claude/**` a tutti i subtree byte RAG (`hooks/`, `skills/`, `agents/`). Guardia esaustiva auto-derivante + riconciliazione dal fork IT stantio al canon EN (E10-FEAT-025).

- **Sync esteso:** `packages/sertor/src/sertor_installer/sync.py` — mapping da subtree RAG (hooks/skills/agents) → `.claude/` con esclusione per costruzione di process-fidelity (`env*`/`mcp*`/`settings*`/`claude-md-block*`/`sertor-cli-reference.md`)
- **Guardia esaustiva:** `tests/unit/test_assets_rag_dogfood_sync.py` — auto-derivante, enumera subtree RAG (11 casi), fallisce nominando asset + comando fix
- **Asset creati/allineati:** `concierge.md`, `sertor-rag-usage-check.ps1`, `guided-setup/SKILL.md` (assorbe F3); `eval-suite-author`/`eval-feedback` riconciliati a canon EN
- **Test riconciliati:** `test_skill_eval_feedback.py` — asserzioni IT → EN (`"assente"` → `"absent"`, etc.)
- **Scoperta:** estensione sync ha fatto emergere due debiti preesistenti (F3 + FEAT-025), chiusi insieme — il valore della guardia
- **Verification:** guardia 11/11 ✅ · `test_assets_sync` 18 invariato ✅ · 1065 unit root verdi (1 skip noto) ✅ · ruff clean ✅ · sertor-core byte-identico ✅

## [2026-07-03] record | Direttiva utente: dogfood SOLO via Sertor Install da version bump → riorientata E15

**Operazione:** record (decisione/direttiva utente che riorienta l'epica E15 `fedelta-dogfood`).

**Contesto:** subito dopo il merge di E15-FEAT-002 (#146), l'utente ha stabilito una **regola standing**: da ora il progetto Sertor fa dogfood **solo passando da Sertor Install** (in qualsiasi forma), sulla **versione INSTALLATA** derivata da un **version bump**. *«Non voglio più dare spazio ad ambiguità.»*

**Cosa cambia (pivot di E15):**
- **Modello target: da SANDBOX → REAL-INSTALL DA VERSION BUMP.** Il runtime del dogfood (MCP/hook/skill/SpecKit/asset) gira **solo sull'installato**; il dogfood **È** l'output d'install, non un confronto sandbox. Supera la decisione «sandbox» presa poche ore prima.
- **Due meccanismi decisi dall'utente:** (1) **cadenza** = version bump → self-install → re-index/smoke **ad ogni merge su master** (nuovo passo del rituale post-merge); (2) **confine dev↔dogfood** = test/sviluppo sull'editable `.venv`, runtime dell'agente **solo sull'installato**.
- **F2 (sync esteso) e FEAT-027 (script SpecKit isolato) diventano INTERIM/dev-tooling:** le guardie restano, ma la **via di fedeltà** passa all'install reale; il sync-come-fedeltà si deprecca quando il self-install è in piedi.
- **Divergenze del runtime = ELIMINA (non dichiara):** `.mcp.json` → runtime `.sertor/` installato, non venv monorepo.

**Backlog E15 riorientato:** FEAT-001 riframe «harness sandbox» → «il dogfood prodotto dal vero `sertor install` da version bump (self-install)» (il cuore); +FEAT-007 (repoint runtime `.sertor/`), +FEAT-008 (rituale post-merge bump→self-install); FEAT-005 (≡E10-FEAT-028 plan-template preservante) = **prerequisito reale**; rischi R-1/R-3 aggiornati (l'install gira SUL dogfood, non in sandbox). Memoria salvata `feedback_dogfood_solo_via_install_versionbump`.

**Rituale:** record ✅ · distill: non serve (la decisione è in memoria + epica) · lint: pulito (epica/roadmap/memoria coerenti col pivot) · EXEC roadmap: riga E15 annotata col riorientamento · re-index/smoke: già fatti post-#146 (verdi).

**Prossimo:** commit branch+PR (doc-only: epica + roadmap + log); poi decomporre il prerequisito FEAT-005 (installer preservante `plan-template`) o FEAT-001 (self-install).

## [2026-07-03] query | Spike empirico: i veri installer su host pulito — forma reale del runtime installato

Spike empirico per E15: eseguiti i 3 veri installer (`sertor-flow install`, `sertor install rag --backend local --mcp-scope project`, `sertor install wiki`) su target pulito isolato (scratchpad). Obiettivo: osservare la forma reale del runtime che il dogfood dovrebbe ESSERE sotto il nuovo modello.

- **Runtime .sertor/ forma:** progetto `uv` a sé con `pyproject.toml` + `uv.lock` + `.env` + `.sertor-version`; installa da git HEAD (nessun tag), confermando che il dogfood traccia HEAD mentre ospiti esterni hanno versione pinned.
- **MCP runtime:** `.mcp.json` in forma client (`uv run --directory .sertor python -m sertor_mcp.server`) vs forma-dev attuale nel dogfood; confermato F7 (repoint).
- **Governance clobber:** solo `plan-template.md` (riconfermato): su target pulito riceve starter neutro; su host con v1.4.0 la costituzione sopravvive (F5).
- **Self-install dogfood (non-pulito):** blocco marker SDLC/step-ritual si appende a prosa hand-written (duplicazione F4 da riconciliare); merge `.mcp.json` salta se server esiste (F7); `wiki.config` preservato (F6).
- **Incoerenza minore:** `.sertor/pyproject.toml` pinna `requires-python >=3.12`, core supporta 3.11+.
- **Client asset (.claude/):** agents (`requirements-analyst`, `configuration-manager`, `wiki-curator`, `concierge`); skills (`requirements`, `eval-*`, `guided-setup`, `wiki-author`, `speckit-*` 9 da specify init). Dogfood combacia (F2+FEAT-027).
- **Valore spike:** F5/F7/F1 ancorati a realtà (install pulito gira, runtime è uv HEAD, solo clobber plan-template). Modello pivot confermato eseguibile, zero errori.

## [2026-07-03] record | E15-FEAT-005 ≡ E10-FEAT-028 IMPLEMENTATA — installer preservante su plan-template.md

**Cosa:** `sertor-flow install` esegue `specify init --force` (Step 0) che clobbera `.specify/templates/plan-template.md`, e il file non è nel piano Sertor → la customizzazione (mission-gate/Constitution Check) andava persa. Prerequisito perché il dogfood (e ospiti che customizzano il template) possano fare il **vero** install. Verificato: è l'**unico** artefatto curato clobberato (costituzione/`feature.json` salvi).

**Implementazione** (packages/sertor-flow, zero sertor-core): meccanismo backup/restore in `execute_governance_plan` — legge `plan-template.md` prima dello Step 0, lo ripristina dopo se pre-esistente e il launch l'ha cambiato; host-agnostico (preserva ciò che c'era), fail-loud (Principio XII), esito onesto in `InstallReport`. Fresh host → resto upstream (no regressione). `launch_speckit` solo in install path → upgrade non ri-clobbera.

**Correzione:** avevo scritto che `FakeSpecifyRunner` non modellava il clobber → errore mio. Il fake **già** scrive `plan-template.md` incondizionatamente (`conftest.py:72-75`); è la costituzione a essere create-if-absent. Nessuna modifica al fake; spec/requisiti corretti.

**Accettazione:** 2 nuovi test in `test_non_destructive.py` (plan-template customizzato preservato byte-identico · fresh-host lascia upstream, nessun restore); **sertor-flow 142 verdi**, ruff pulito, `sertor-core` invariato. Constitution 12/12 + missione.

**Verification:** record ✅ · distill: non serve (fix installer; nessuna nuova entità di dominio) · lint: pulito (epic E15/E10 + roadmap coerenti) · re-index/smoke: hook SessionEnd (toccati packages/sertor-flow + specs/requirements/wiki).
