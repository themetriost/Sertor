# Log 2026-06-26

## [2026-06-26] record | Merge 3 PR su master: E2-FEAT-013 version-check, E12-FEAT-012 install host-aware, E10-FEAT-015 (backlog)

Mergiate 3 PR su `master` chiusura ciclo sessione-precedente (policy git ritirata: push-diretto forbidden, sempre PR).

- **E2-FEAT-013 version-check (DONE):** auto-update deterministica del binario `sertor`. Due hook host-facing depositati via `sertor install rag`: (1) `version-check.ps1` SessionEnd deterministico — GET cachata (~24h) del file `/VERSION` su master, confronta con `.sertor/.sertor-version` scritto a install-time, verdetto in `.sertor/.version-check.json` (non bloccante, exit 0 sempre); (2) `version-check-start.ps1` SessionStart (Claude/Copilot nativo) — legge verdetto, avvisa se behind, mai auto-upgrade. RFC-012 risolto (fine della dipendenza update→interazione agente). Riuso pattern hook rag-usage FEAT-009. SpecKit completo specify→plan→tasks→impl. Constitution 12/12 ✅, sertor/kit verdi. Merge: PR #113 (`8d951cd`).
- **E12-FEAT-012 install host-assistant-aware (DONE):** fix reale emerso in dogfooding (Copilot CLI) — le skill `guided-setup`/`concierge` determinavano l'host (`os.name`/`sys.platform`) ma non lo passavano ai subcomandi installer (rag/wiki/flow) → riuscivano sempre col default `claude` (layout sbagliato, `.claude/` vs `.github/`, MCP paths inesatti). Soluzione: flow agente leggere host a runtime e anteporre `--assistant <host>` a ogni invocazione installer downstream, guida estesa per wiki e sertor-flow. Corpi skill byte-identici (parità FEAT-056). **+ NRT nuova** `test_assets_ux_host_aware.py` — anti-regressione per-PR; testa il buco esatto (skill sceglie l'host, passa il flag) che lo smoke e2e non copre (smoke testa l'installer col flag dato, non il mezzo di scelta autonoma della skill). Verde sul fix, rosso sul pre-fix (verifica puntuale della causa-radice). Merge: PR #115 (`030c695`).
- **E10-FEAT-015 backlog (promoted):** refresh (FEAT-009) non disinstalla gli artefatti obsoleti su alcuni percorsi. Tagliata dal scope E10 (Could), promossa a backlog esplicito in epic.md con ricerca aperta (bug vs gap di copertura: re-install vs upgrade semanticamente diversi? A chiarire). Non bloccante né sulla FEAT-009 già mergiata né su E10 chiusura. Reference: issue backlog.
- **HEAD master = `8d951cd`**, CI verde, ruff clean nei tre pacchetti. Policy git aggiornata: **niente più push diretti su `master`** — **sempre PR** (eccezione prototipo ritirata, passaggio a produzione + governance via SpecKit). Re-index del corpus: **ora enforced da hook `rag-freshness.ps1` SessionEnd** (FEAT-011 enforcement) — non manuale, **rimosso il passo 5 del rituale CLAUDE.md** (il rilancio meccanico diventa involontario agente/hook).

**Cambio rituale:** il passo 5 del rituale step (re-index corpus) e passo 8 (smoke RAG) sono ora **enforced dall'hook** → agente NON lo esegue a mano, **giudizio trasferito al momento di esecuzione dell'hook** (eseguire correzione su degradation il SessionStart se necessario). Reclassificazione già in CLAUDE.md (annotata «enforced via hook E10-FEAT-011» con nota D↔N).
