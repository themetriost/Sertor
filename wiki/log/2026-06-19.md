# Log 2026-06-19

Log entries for 2026-06-19.

## [2026-06-19] record | Parità Copilot via agent-skills NATIVE + verifica LIVE su Spike (FEAT-001, 056)

Rifatta la parità funzionale Copilot CLI della capacità wiki (FEAT-001, epica `debito-tecnico`, feature 056) col **meccanismo NATIVO agent-skills** di Copilot, dopo aver letto la documentazione ufficiale (`docs.github.com/copilot` CLI add-skills): l'implementazione precedente (`b6e85b7`) appiattiva la skill in un custom-agent + container `.github/sertor/wiki-author/` + placeholder `{SKILL_DIR}` — una **reinvenzione** del meccanismo nativo. La capacità wiki su Copilot è ora **una sola skill nativa** auto-scoperta dal client.

- **Decisioni (sui doc, non a intuito):** auto-discovery Copilot è **per-cartella-skill** → topologia = **una skill** `.github/skills/wiki-author/**` il cui `SKILL.md` è il **dispatcher 8-operazioni** reso dalla fonte unica `commands/wiki.md` (la skill assorbe il command `/wiki`, che sulla CLI non ha veicolo nativo); payload (playbook/ops/craft) byte-copiato; riferimenti **relativi co-locati** (container paralleli `.claude/skills/` ↔ `.github/skills/`). `wiki-curator` resta custom-agent.
- **Codice:** nuovo renderer kit `render_native_skill`; riscritto `install_wiki._build_copilot_wiki_plan`; rimossi render skill→custom-agent, `.github/sertor/`, `{SKILL_DIR}`; `sertor_owned_paths` Copilot → owned_dir `.github/skills/wiki-author`. Guardia di parità (closure basename-based) invariata; 4 test del vecchio design riscritti. `sertor-core` INVARIATO.
- **SpecKit:** requirements + spec + plan + tasks (056) riscritti al meccanismo nativo; `wiki/tech/assistant-targeting.md` aggiornata («Parità by construction» nativa).
- **Verifiche:** ruff clean · sertor 292 · kit 131 · sertor-flow 120. Deposito verificato su dir temp pulita **e su Spike reale** (0 leak, 0 residui, idempotente; ripuliti i residui del vecchio approccio + stray `wiki/playbook/wiki-playbook.md`).
- **Prova LIVE su Copilot CLI 1.0.63 reale (Spike):** dai log di sessione (`~/.copilot/session-state/.../events.jsonl`) il tool nativo **`skill` invocato con `{"skill":"wiki-author"}`**, **playbook letto** al path co-locato `.github/skills/wiki-author/wiki-playbook.md`, 8 operazioni nominate; bonus: SessionStart hook carica roadmap/index/log e `agentStop` hook `success:true` → **gli hook Sertor funzionano su Copilot**. SC-008 ✅.
- **Lezione codificata:** per integrare un tool esterno, leggere la doc ufficiale PRIMA di progettare (non inferire da `--help`/log).
- **Stato:** commit `0fd8d09` (additivo su PR #80). Push su PR #80 dopo questo record.

## [2026-06-19] record | Fix distribuzione costituzione-starter neutra (replace-if-placeholder) + rifinitura principi (FEAT-009, 058)

Scoperto e corretto un bug di distribuzione: la **costituzione-starter neutra** di `sertor-flow` **non arrivava mai** sull'ospite (FEAT-009, epica `debito-tecnico`, feature 058). Emerso mentre esaminavamo *quali principi della nostra costituzione sono agnostici* (per distribuirli): la verifica empirica su host Spike + install pulito ha mostrato che l'ospite riceve il **placeholder di spec-kit** (`# [PROJECT_NAME] Constitution`, `[PRINCIPLE_1_NAME]`), non il nostro starter curato.

- **Causa:** dopo il pivot launch-installer (FEAT-045), `execute_governance_plan` lancia `specify init` (Step 0) che scaffolda un `.specify/memory/constitution.md` **placeholder**; poi il nostro CONFIG `CREATE_IF_ABSENT` (`_apply_config`) lo trova e fa **skip** → starter mai depositato. Lo stesso in upgrade (FR-040 preservava il placeholder).
- **Perché invisibile ai test:** il mock `FakeSpecifyRunner` (conftest) **non** depositava il constitution placeholder → i test offline non riproducevano la collisione. Reso fedele (placeholder create-if-absent), così la guardia ora cattura il bug.
- **Fix (replace-if-placeholder):** helper puro `_is_speckit_placeholder` (sentinelle `[PROJECT_NAME]`/`[PRINCIPLE_1_NAME]`/`[CONSTITUTION_VERSION]`) + `_apply_constitution(dest, starter, dry_run)` condiviso da install (`_apply_config`) e upgrade (`_apply_gov_upgrade`): placeholder → **sovrascrivi** con lo starter (UPDATED); costituzione **reale** → **preserva** (SKIPPED, Principio VI); idempotente. Uninstall invariato.
- **Rifinitura starter** (la richiesta originale): aggiunti due principi generici de-RAGizzati dalla costituzione di Sertor — **«Replaceable Details / No Vendor Lock-In»** (kernel del Principio II) e **«Consume Through Stable Interfaces, Not Internals»** (generalizzazione del Principio XI) — + allineamento leggibilità (guard-clause/SESE). Versione 0.1.0 → 0.2.0; esclusi i principi Sertor/RAG-specifici (X, veicoli, motori, hit@k).
- **Analisi a monte:** mappa dei nostri 11 principi → agnostico?/già-nello-starter? Lo starter copriva già 8 principi de-RAGizzati + Sicurezza + Governance; esclusi a ragione II(parziale), X, XI. I due kernel generici mancanti ora aggiunti.
- **Verifiche:** ruff clean · sertor-flow 132 · kit 131 · sertor 292; guard no-dipendenza `sertor-flow`→`sertor-core` verde. Empirica end-to-end (path reale): placeholder → starter (UPDATED), placeholder rimosso, 2 nuovi principi, v0.2.0. `sertor-core` INVARIATO.
- **SpecKit:** requirements EARS + spec + plan (Constitution Check 11/11) + tasks. Branch `058-distribuzione-costituzione`.
- **Resta:** commit + PR + merge; (env) `specify init` in dev locale dà `layout_missing` — quirk d'ambiente, non della feature (su Spike il layout combaciava).

## [2026-06-19] record | Host-agnosticità asset governance (asse Sertor↔ospite): via sertor-rag hardcoded + scope-prototipo (FEAT-010, 059)

Audit + neutralizzazione della **host-agnosticità degli asset governance** distribuiti da `sertor-flow`, sull'asse **Sertor↔ospite** (project-coupling), distinto dall'asse Claude↔Copilot coperto da FEAT-001/056 (FEAT-010, epica `debito-tecnico`, feature 059). Emerso da una domanda dell'utente — «distribuiamo anche `configuration-manager`?» — che ha innescato la verifica.

- **Scoperta:** la guardia di parità di FEAT-056 controlla la coupling all'**assistente** (`.claude/`, slash, nomi-modello Claude), **non** al **dominio/progetto**. Tre asset distribuiti avevano project-coupling: `configuration-manager` («del workspace **RAG**» + scope-esempi = cartelle-prototipo Sertor `01-baseline`/`02-hybrid-reranking`/`03-graphrag`/`shared`/`wiki` + «al momento senza remote»); `requirements-analyst` hardcodava 7 tool `mcp__sertor-rag__*` nel frontmatter `tools:` + sezione «Dogfooding — usa sertor-rag»; `requirements/SKILL.md` citava «Dogfooding». `claude-md-block-sdlc.md` pulito.
- **Divergenza prevenuta (grazie utente):** l'utente aveva ipotizzato «flow ha rag+wiki come prerequisito, quindi i tool ci sono» — la ground truth contraddice: `sertor-flow` dipende **solo** da `sertor-install-kit` (guard `test_no_core_dependency`), `install_governance` non installa rag/wiki; la frase vale solo nel dogfood. Confermata **opzione 1: mantieni l'indipendenza**, RAG **opzionale via discovery a runtime** (l'LLM dietro l'agente capisce se c'è un MCP e lo usa).
- **Fix:** `configuration-manager` → «del progetto», scope generico, rimosso «senza remote»; `requirements-analyst` → via i `mcp__sertor-rag__*` dal `tools:` (restano Read/Write/Edit/Glob/Grep/Bash), descrizione e sezione «Ancorare i requisiti al codice (opzionale)» rese host-agnostiche (Grep/Read + MCP se presente, errore→segnala non degradare in silenzio); `SKILL.md` «Dogfooding»→«Ancoraggio al codice». Asset canonico → **ri-sync `.claude/`** (il dogfood gira sulla versione installabile; guard anti-drift verde).
- **Pin di regressione** (non la guardia generale, esclusa dall'utente): `test_governance_assets_host_agnostic.py` — 0 `mcp__sertor-rag`/scope-prototipo/«workspace RAG» nei 3 asset; `tools:` del requirements-analyst solo universali.
- **Verifiche:** ruff clean · sertor-flow **134** (+2) · parità Copilot 18 verdi; guard anti-drift e no-core-dependency verdi. `sertor-core` INVARIATO; indipendenza `sertor-flow`↛RAG preservata. Branch `059-host-agnosticita-asset-governance`.
- **Nota:** FEAT-001 (asse assistente) era DONE; questo è un residuo di classe diversa (asse progetto) → nuova FEAT-010. La guardia di parità NON è stata estesa al project-coupling (scelta utente) — eventuale estensione = follow-up.
