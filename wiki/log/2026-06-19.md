# Log 2026-06-19

Log entries for 2026-06-19.

## [2026-06-19] record | Parità Copilot via agent-skills NATIVE + verifica LIVE su Spike (FEAT-001, 056)

Rifatta la parità funzionale Copilot CLI della capacità wiki (FEAT-001, epica `debito-tecnico`, feature 056) col **meccanismo NATIVO agent-skills** di Copilot, dopo aver letto la documentazione ufficiale (`docs.github.com/copilot` CLI add-skills): l'implementazione precedente (`b6e85b7`) appiattiva la skill in un custom-agent + container `.github/sertor/wiki-author/` + placeholder `{SKILL_DIR}` — una **reinvenzione** del meccanismo nativo. La capacità wiki su Copilot è ora **una sola skill nativa** auto-scoperta dal client.

- **Decisioni (sui doc, non a intuito):** auto-discovery Copilot è **per-cartella-skill** → topologia = **una skill** `.github/skills/wiki-author/**` il cui `SKILL.md` è il **dispatcher 8-operazioni** reso dalla fonte unica `commands/wiki.md` (la skill assorbe il command `/wiki`, che sulla CLI non ha veicolo nativo); payload (playbook/ops/craft) byte-copiato; riferimenti **relativi co-locati** (container paralleli `.claude/skills/` ↔ `.github/skills/`). `wiki-curator` resta custom-agent.
- **Codice:** nuovo renderer kit `render_native_skill`; riscritto `install_wiki._build_copilot_wiki_plan`; rimossi render skill→custom-agent, `.github/sertor/`, `{SKILL_DIR}`; `sertor_owned_paths` Copilot → owned_dir `.github/skills/wiki-author`. Guardia di parità (closure basename-based) invariata; 4 test del vecchio design riscritti. `sertor-core` INVARIATO.
- **SpecKit:** requirements + spec + plan + tasks (056) riscritti al meccanismo nativo; `wiki/tech/assistant-targeting.md` aggiornata («Parità by construction» nativa).
- **Verifiche:** ruff clean · sertor 292 · kit 131 · sertor-flow 120. Deposito verificato su dir temp pulita **e su Spike reale** (0 leak, 0 residui, idempotente; ripuliti i residui del vecchio approccio + stray `wiki/playbook/wiki-playbook.md`).
- **Prova LIVE su Copilot CLI 1.0.63 reale (Spike):** dai log di sessione (`~/.copilot/session-state/.../events.jsonl`) il tool nativo **`skill` invocato con `{"skill":"wiki-author"}`**, **playbook letto** al path co-locato `.github/skills/wiki-author/wiki-playbook.md`, 8 operazioni nominate; bonus: SessionStart hook carica roadmap/index/log e `agentStop` hook `success:true` → **gli hook Sertor funzionano su Copilot**. SC-008 ✅.
- **Lezione codificata:** per integrare un tool esterno, leggere la doc ufficiale PRIMA di progettare (non inferire da `--help`/log).
- **Stato:** commit `0fd8d09` (additivo su PR #80). Push su PR #80 dopo questo record.

## [2026-06-19] record | Fix distribuzione costituzione-starter neutra (replace-if-placeholder) + rifinitura principi (FEAT-009, 058)

Scoperto e corretto un bug di distribuzione: la **costituzione-starter neutra** di `sertor-flow` **non arrivava mai** sull'ospite (FEAT-009, epica `debito-tecnico`, feature 058). Emerso mentre esaminavamo *quali principi della nostra costituzione sono agnostici* (per distribuirli): la verifica empirica su host Spike + install pulito ha mostrato che l'ospite riceve il **placeholder di spec-kit** (`# [PROJECT_NAME] Constitution`, `[PRINCIPLE_1_NAME]`), non il nostro starter curato.

- **Causa:** dopo il pivot launch-installer (FEAT-045), `execute_governance_plan` lancia `specify init` (Step 0) che scaffolda un `.specify/memory/constitution.md` **placeholder**; poi il nostro CONFIG `CREATE_IF_ABSENT` (`_apply_config`) lo trova e fa **skip** → starter mai depositato. Lo stesso in upgrade (FR-040 preservava il placeholder).
- **Perché invisibile ai test:** il mock `FakeSpecifyRunner` (conftest) **non** depositava il constitution placeholder → i test offline non riproducevano la collisione. Reso fedele (placeholder create-if-absent), così la guardia ora cattura il bug.
- **Fix (replace-if-placeholder):** helper puro `_is_speckit_placeholder` (sentinelle `[PROJECT_NAME]`/`[PRINCIPLE_1_NAME]`/`[CONSTITUTION_VERSION]`) + `_apply_constitution(dest, starter, dry_run)` condiviso da install (`_apply_config`) e upgrade (`_apply_gov_upgrade`): placeholder → **sovrascrivi** con lo starter (UPDATED); costituzione **reale** → **preserva** (SKIPPED, Principio VI); idempotente. Uninstall invariato.
- **Rifinitura starter** (la richiesta originale): aggiunti due principi generici de-RAGizzati dalla costituzione di Sertor — **«Replaceable Details / No Vendor Lock-In»** (kernel del Principio II) e **«Consume Through Stable Interfaces, Not Internals»** (generalizzazione del Principio XI) — + allineamento leggibilità (guard-clause/SESE). Versione 0.1.0 → 0.2.0; esclusi i principi Sertor/RAG-specifici (X, veicoli, motori, hit@k).
- **Analisi a monte:** mappa dei nostri 11 principi → agnostico?/già-nello-starter? Lo starter copriva già 8 principi de-RAGizzati + Sicurezza + Governance; esclusi a ragione II(parziale), X, XI. I due kernel generici mancanti ora aggiunti.
- **Verifiche:** ruff clean · sertor-flow 132 · kit 131 · sertor 292; guard no-dipendenza `sertor-flow`→`sertor-core` verde. Empirica end-to-end (path reale): placeholder → starter (UPDATED), placeholder rimosso, 2 nuovi principi, v0.2.0. `sertor-core` INVARIATO.
- **SpecKit:** requirements EARS + spec + plan (Constitution Check 11/11) + tasks. Branch `058-distribuzione-costituzione`.
- **Resta:** commit + PR + merge; (env) `specify init` in dev locale dà `layout_missing` — quirk d'ambiente, non della feature (su Spike il layout combaciava).

## [2026-06-19] record | Host-agnosticità asset governance (asse Sertor↔ospite): via sertor-rag hardcoded + scope-prototipo (FEAT-010, 059)

Audit + neutralizzazione della **host-agnosticità degli asset governance** distribuiti da `sertor-flow`, sull'asse **Sertor↔ospite** (project-coupling), distinto dall'asse Claude↔Copilot coperto da FEAT-001/056 (FEAT-010, epica `debito-tecnico`, feature 059). Emerso da una domanda dell'utente — «distribuiamo anche `configuration-manager`?» — che ha innescato la verifica.

- **Scoperta:** la guardia di parità di FEAT-056 controlla la coupling all'**assistente** (`.claude/`, slash, nomi-modello Claude), **non** al **dominio/progetto**. Tre asset distribuiti avevano project-coupling: `configuration-manager` («del workspace **RAG**» + scope-esempi = cartelle-prototipo Sertor `01-baseline`/`02-hybrid-reranking`/`03-graphrag`/`shared`/`wiki` + «al momento senza remote»); `requirements-analyst` hardcodava 7 tool `mcp__sertor-rag__*` nel frontmatter `tools:` + sezione «Dogfooding — usa sertor-rag»; `requirements/SKILL.md` citava «Dogfooding». `claude-md-block-sdlc.md` pulito.
- **Divergenza prevenuta (grazie utente):** l'utente aveva ipotizzato «flow ha rag+wiki come prerequisito, quindi i tool ci sono» — la ground truth contraddice: `sertor-flow` dipende **solo** da `sertor-install-kit` (guard `test_no_core_dependency`), `install_governance` non installa rag/wiki; la frase vale solo nel dogfood. Confermata **opzione 1: mantieni l'indipendenza**, RAG **opzionale via discovery a runtime** (l'LLM dietro l'agente capisce se c'è un MCP e lo usa).
- **Fix:** `configuration-manager` → «del progetto», scope generico, rimosso «senza remote»; `requirements-analyst` → via i `mcp__sertor-rag__*` dal `tools:` (restano Read/Write/Edit/Glob/Grep/Bash), descrizione e sezione «Ancorare i requisiti al codice (opzionale)» rese host-agnostiche (Grep/Read + MCP se presente, errore→segnala non degradare in silenzio); `SKILL.md` «Dogfooding»→«Ancoraggio al codice». Asset canonico → **ri-sync `.claude/`** (il dogfood gira sulla versione installabile; guard anti-drift verde).
- **Pin di regressione** (non la guardia generale, esclusa dall'utente): `test_governance_assets_host_agnostic.py` — 0 `mcp__sertor-rag`/scope-prototipo/«workspace RAG» nei 3 asset; `tools:` del requirements-analyst solo universali.
- **Verifiche:** ruff clean · sertor-flow **134** (+2) · parità Copilot 18 verdi; guard anti-drift e no-core-dependency verdi. `sertor-core` INVARIATO; indipendenza `sertor-flow`↛RAG preservata. Branch `059-host-agnosticita-asset-governance`.
- **Nota:** FEAT-001 (asse assistente) era DONE; questo è un residuo di classe diversa (asse progetto) → nuova FEAT-010. La guardia di parità NON è stata estesa al project-coupling (scelta utente) — eventuale estensione = follow-up.

## [2026-06-19] record | Export OpenTelemetry: handler additivo gemello di F1, GenAI semconv, privacy metrics-only (FEAT-005, 061)

Implementato l'**export OpenTelemetry** (FEAT-005, epica `osservabilita`, feature 061): gli eventi che il core già emette via `log_event` sono esportati **anche** verso un backend OTel esterno (Langfuse/Phoenix/Grafana), **in aggiunta** allo store locale F1 (REQ-E4). Scelto dall'utente come prossima feature di osservabilità.

- **Ricerca prima del design** (lezione "leggi i doc ufficiali"): GenAI semantic conventions OTel da `opentelemetry.io` (la pagina spec è migrata su repo GitHub; attributi confermati dal registry): `gen_ai.operation.name`, `gen_ai.provider.name`, `gen_ai.request.model`, `gen_ai.usage.input_tokens`, `gen_ai.embeddings.dimension.count`, `gen_ai.retrieval.*`.
- **Design = gemello di F1.** Studiato il seam reale (`EventPersistenceHandler` + `enable_observability`): F1 è un `logging.Handler` additivo/non-fatale/privacy-safe. FEAT-005 si innesta identico: nuovo `OtelExportHandler` (`observability/otel.py`) attaccato in `enable_observability` SOLO con extra `[otel]` + manopola `SERTOR_OBSERVABILITY_OTEL` (default off). `log_event`/call-site/F1 INVARIATI.
- **Mappatura:** evento→span; attributi GenAI semconv dove applicabile (`embeddings`/`search`→`retrieval`), namespace `sertor.*` altrove (index/rerank/...). Mappa centralizzata (R-1). **Privacy metrics-only:** esporta solo numerici/bool + whitelist categorica; **mai** testo libero/query/path (oltre alla redazione già fatta da `log_event`).
- **Opzionalità:** dipendenze OTel in extra `[otel]` (lazy import; assente+richiesto → `ConfigError` come `[tui]`); core importabile senza OTel (verificato: 0 `opentelemetry` in sys.modules importando il modulo). Endpoint/trasporto dalle env standard OTel. Non-bloccante (BatchSpanProcessor), non-fatale (handleError).
- **Verifica offline** con `InMemorySpanExporter` (8 test: mapping puro, emissione e2e via handler, privacy, plain-log→no span, disabilitato→0 handler, extra-assente→ConfigError). Manopola aggiunta ai template `.env` dell'installer (corollario "feature completa = installabile").
- **Gap dichiarato:** span **flat post-hoc** (uno per evento) — niente tracing nidificato con context propagation (follow-up). Onesto nello spec/plan.
- **Verifiche:** ruff clean; **580 unit / 627 root non-cloud** verdi; sertor 292 · kit 131 · flow 134. `sertor-core` invariato salvo: modulo `observability/otel.py`, manopola `Settings`, ramo in `enable_observability`, extra `[otel]` in pyproject (+dev). Constitution 11/11 (pre+post). NB: 2 test `test_packaging` falliscono in locale finché il branch non è pushato (installano da `git+url@<branch>`) — artefatto, non regressione.
- **SpecKit completo:** requirements EARS + spec + plan (Constitution Check) + (tasks impliciti nelle fasi). Branch `061-export-otel`.

## [2026-06-19] record | Arricchimento span OTel: status dal livello + service.name + hybrid_query→retrieval (FEAT-013, 062)

Arricchimento degli span OTel (FEAT-013, epica `osservabilita`, feature 062) — polish additivo di FEAT-005, **emerso dal dogfooding su Jaeger**: si vedeva il nome dell'operazione ma non l'**esito** né la severità.

- **Trigger:** verifica live su Jaeger (FEAT-005) — gli attributi qualitativi (documents/chunks, dense/lexical/fused_k, tokens, elapsed) erano già presenti come **Tag** dello span, ma mancava l'esito (OK/errore) e la severità, e gli span di errore apparivano "verdi" come gli altri.
- **Enrichment (tutto in `observability/otel.py`, additivo):** (1) **span status dal livello del log** — un evento ERROR (`embeddings_error`/`store_error`/...) diventa uno span **rosso** con la reason (`error`/`reason`/messaggio); (2) **`sertor.level`** su ogni span (INFO/WARNING/ERROR); (3) **`service.name=sertor` di default** via Resource (helper `_otel_resource`; `OTEL_SERVICE_NAME` env ha precedenza) — niente più env obbligatoria perché Jaeger non mostri `unknown_service`; (4) mappa **`hybrid_query`/`retrieve`→`gen_ai.operation.name=retrieval`** (etichetta GenAI corretta; i conteggi già fluivano).
- **Conferma:** i conteggi "hit/miss" e "quanti" erano già negli eventi (`retrieve.results`, `hybrid_query.fused_k`, `index.documents/chunks`, `low_confidence.best_score/candidates`) — nessun call-site cambiato.
- **Verifica:** 4 nuovi test offline (status ERROR, `sertor.level`, hybrid_query→retrieval, resource default+env-override); 12 test OTel verdi · 584 unit verdi · ruff clean. **Live su Jaeger:** lo span della search è ora `retrieval` con `sertor.level=INFO` e `fused_k`.
- **Processo:** niente SpecKit (calibra al valore: enrichment additivo piccolo); tracciamento leggero = riga backlog FEAT-013 + questo record + PR. Constitution inline: additivo, nessuna dipendenza nuova, core invariato fuori da otel.py. Branch `062-otel-enrichment`.
- **Resta:** tracing nidificato (span padre/figlio attorno alle operazioni live) = feature separata se richiesto.

## [2026-06-19] record | TUI: eventi recenti in tabella allineata invece del dict grezzo (FEAT-014, 063)

Resa più leggibile la scheda **Live** della TUI di osservabilità (FEAT-014, epica `osservabilita`, feature 063) — **emerso dal dogfooding sulla TUI**: gli eventi recenti erano stampati come dict Python grezzo (`operation: {'k': v, ...}`), poco leggibile.

- **Fix:** nuova funzione pura `_render_events_table(events)` in `observability/live.py` — rende gli eventi recenti come **tabella allineata** `TIME · OPERATION · DETAILS`, con i campi resi `k=v` (niente graffe/apici del dict). `render_snapshot` la usa al posto del loop dict-repr.
- **Confine:** solo la **resa** (layer puro testabile, `Static` invariato); nessuna modifica a store/eventi/report F2. Gli altri tab (Cache/Cost/Corpus) erano già formattati.
- **Verifica:** nuovo test `test_recent_events_rendered_as_table_not_raw_dict` (header + `k=v` + niente dict repr); 585 unit verdi, ruff clean. Anteprima reale OK.
- **Processo:** niente SpecKit (UX polish piccolo, calibra al valore); tracciamento = riga backlog FEAT-014 + record + PR. Branch `063-tui-events-table`.

## [2026-06-19] record | Requirements FEAT-015: visibilità del RAG nella TUI (dimostrabilità, opt-in raw-text)

Fase **requirements** di una nuova feature osservabilità (FEAT-015, epica `osservabilita`): **Visibilità del RAG nella TUI (dimostrabilità)** — `requirements/osservabilita/visibilita-rag-tui/requirements.md`. Richiesta utente: nella TUI vedere **query · operazione MCP · risultato · verdetto hit/miss** per dimostrare *come* funziona il RAG (NON audit/tracciamento).

- **Inquadramento:** realizza l'**opt-in raw-text** già previsto dall'epica (REQ-E9/E8). Decisione cardine utente: dati LOCALI, TUI-user ≡ LLM-user → per uso locale niente da nascondere; si rilassa la privacy-by-default come **opt-in esplicito** (default privacy-safe **preservato** ovunque, e ovunque su ospiti terzi).
- **Prodotto:** 12 requisiti EARS + NFR + MoSCoW; FEAT-015 aggiunta al backlog (stato *decomposta*).
- **6 forche risolte con l'utente:** (1) hit/miss = **3 stati** hit/miss/**astenuto** (sotto soglia `min_score`); (2) risultato = **top-k path+score + snippet del 1°**; (3) gate = **manopola dedicata, default off, richiede lo store**; (4) TUI = **scheda dedicata nuova** ("RAG"), "Live" resta metriche; (5) eventi MCP includono l'**argomento query** sotto opt-in; (6) retention = **a numero di voci**, configurabile.
- **Forche di design rimaste (→ `/speckit-plan`):** criterio di correlazione query↔risultato↔MCP-op; nome manopola; schema persistenza contenuto.
- **Invarianti:** additivo (F1/F2/eventi invariati nel default); privacy-by-default default; host-agnostico; thin consumer; `sertor-core` importabile; export OTel resta metrics-only.
- **Processo:** fase requirements via skill `requirements` (livello feature). Prossimo: `/speckit-plan`. Branch `064-visibilita-rag-tui`.

## [2026-06-19] record | FEAT-015 visibilità del RAG nella TUI: opt-in contenuto + scheda RAG hit/miss/astenuto (064)

Implementata la feature **Visibilità del RAG nella TUI (dimostrabilità)** (FEAT-015, epica `osservabilita`, feature 064) — design (`/speckit-plan`) + implementazione + verifica live, sullo stesso branch dei requirements.

- **Design (plan, Constitution 11/11):** 4 forche risolte — D1 evento di retrieval **self-contained** (query+risultato+verdetto in un evento → niente correlazione cross-evento); D2 op MCP per adiacenza (hard-link = follow-up); D3 manopola dedicata `SERTOR_OBSERVABILITY_CONTENT` (default off, richiede lo store); D4 verdetto **3 stati** da `results`/`fused_k`+`abstained`.
- **Data layer:** helper puro condiviso `content_fields()` in `services/retrieval.py` (query scrubbata + `results_preview` top-k `path|score` + `snippet` top-1 scrubbato/capped 200) — riusato da **facade** (`_search`, evento `retrieve`) e **engine ibrido** (`_log_query`, evento `hybrid_query`); `abstained` (bool) aggiunto **sempre** ai 3 call-site dell'ibrido + al facade. Flag cablato dal composition (`build_facade`: `content_enabled = observability_content_enabled AND observability_enabled`); l'ibrido legge `self._settings`.
- **Vista TUI:** `retrieval_verdict()` (hit/miss/astenuto) + `render_rag_report()` puri in `observability/live.py`; nuova **scheda "RAG"** in `tui.py` (query · verdetto · top result+snippet · operazioni MCP); empty-state onesto quando il content è off.
- **Privacy/invarianti:** default off ovunque (REQ-012); `scrub_text` su query+snippet (REQ-003); l'**export OTel resta metrics-only** (l'handler scarta query/snippet — testo libero, già garantito + da pinnare); additivo (eventi/store/F2 invariati nel default).
- **Manopola** nei template `.env` dell'installer (corollario installabile), con nota "solo locale, non audit".
- **Verifiche:** 9 test nuovi (content_fields opt-in+scrub, verdetto 3 stati, render+empty-state+MCP); 594 unit verdi; ruff clean. **Live sul dogfood** (content acceso inline): evento `hybrid_query` porta query+preview(5)+snippet(200ch)+abstained=False, verdetto=`hit`. Dogfood default resta off.
- **SpecKit:** requirements (commit `8fc2030`) → plan (Constitution 11/11) → implement, sullo stesso branch `064-visibilita-rag-tui`.
- **Follow-up dichiarati:** MCP query-arg negli eventi `mcp.<tool>` (REQ-006 Should; il render è già pronto a mostrarlo); correlazione hard MCP↔retrieval; retention dello store col contenuto.

## [2026-06-19] record | Server MCP stantio dopo re-index: diagnosi + fix refresh client + rituale MCP-first/smoke-test

**Diagnosi:** indagine su fallimento `search_code`/`search_docs` nel server MCP `sertor-rag` (error `InternalError` su query con filtro `doc_type`), mentre `search_combined` (senza filtro) reggeva. Causa: il process server (avviato alle 08:44) teneva un client Chroma in memoria divergente dallo store riscritto su disco dal re-index giornaliero, riproducibile **solo** tracciando il path `where` del filtro metadata (che causa I/O reale); il client stantio ripiegava a `InternalError`. Sintomo gemello: `find_symbol` tornava righe obsolete (code-graph caricato all'avvio, non refresh).
- **Fix di prodotto:** `ChromaStore.query()` ora si auto-guarisce — su errore, se il client è "posseduto" (non iniettato nei test), ricrea il client via `client_factory` (nuovo, chiama `SharedSystemClient.clear_system_cache()` e una nuova connessione SQLite vede lo store riscritto) e riprova una volta; errore persistente → `VectorStoreError` come prima. Nuovo evento osservabile `store_client_refreshed`. Test: 3 nuovi in `test_vectorstore_errors.py` (logica refresh+retry, valida la ricreazione). Limite dichiarato: il caso reale (altro processo riscrive SQLite sotto un client aperto) non è ripetibile in unit — validazione via dogfood live (riconnessione già confermata verde).
- **Rituale di step:** 2 regole standing aggiunte a `CLAUDE.md`: (1) **MCP-first di apertura** (line 287) — quando uno step chiede di orientarsi nel codice, prima mossa = interrogare il RAG via server, solo poi `Read` (è il test che lo strumento funzioni; ogni guasto emerge invece di marcire); (2) **punto 8 smoke test di chiusura** (line 388) — allo stesso momento del commit, esercitare `search_code` E `search_docs` (il path `where` è il fragile) + `find_symbol` su simbolo a riga nota (freschezza grafo), segnalare errore/stantio, riconnettere, ri-verifica; obbligatorio dopo re-index/merge `master`.
- **Pagine aggiornate:** [[mcp-server]] — nuova sezione Troubleshooting nota 3 (`Client Chroma stantio dopo re-index`), rimedio + fix prodotto; [[step-ritual]] — le due regole standing registrate.
- **Verifiche:** 599 unit / 1 skip · ruff clean · live dogfood search_code/docs/find_symbol verdi post-riconnessione.

## [2026-06-19] record | Follow-up staleness MCP: code-graph auto-reload (mtime) + regola MCP-first propagata agli ospiti (branch 066)

Completamento della diagnosi del 2026-06-19 con due fix additivi e lezione di completamento (feature realizzabile su ospite).

- **Fix 1 — Code-graph auto-reload su mtime:** `NetworkxCodeGraph._load` cacheia il grafo per corpus usando una chiave `(st_mtime_ns, st_size)` dell'artefatto su disco. Se il file viene riscritto da un re-index parallelo, la prossima query lo rileva e ricarica senza riavvio del server — **staleness del code-graph eliminata** (gemello del fix Chroma di PR #89). 1 test deterministico (`test_load_reloads_when_artifact_rewritten_on_disk`).
- **Fix 2 — Blocco RAG-USAGE host-generalizzato:** asset `claude-md-block-rag-usage.md` (depositato da `sertor install rag`) adesso include la sezione **"Search first, MCP-first"** — la disciplina "interroga il RAG prima di leggere file; errori del tool = segnale, non degradare silenziosamente" — host-generalizzata (nessun path Sertor). È il corollario "una feature è completa solo se installabile su ospite"; la regola vive già nel rituale di step di Sertor da ieri.
- **Lezione:** quando una feature tocca il rituale (regole standing), **propagarla agli ospiti via installer** perché la disciplina sia coerente dovunque.
- **Pagine aggiornate:** [[mcp-server]] (nota 3 troubleshooting: code-graph ora auto-refreshed) · [[code-graph]] (cache su mtime+size, niente staleness) · [[sertor-installer]] (blocco SERTOR:RAG-USAGE + disciplina "Search first").
- **Verifiche:** core 600 passed (1 skip) · sertor 293 · ruff pulito · smoke test MCP verde post-riconnessione. Branch `066`.
