# Log 2026-07-18

Log entries for 2026-07-18.

## [2026-07-18] record | Ripresa: merge #194/#195, roadmap+lint (drift FEAT-026), decisioni analisi setup (FEAT-038 nuova, 036 folded)

Ripresa sessione (utente riavviato). Verifica stato → merge del lavoro sospeso → roadmap col lint →
scioglimento delle decisioni sull'analisi del setup.

### 1. Verifica dello stato (in prima persona, non dalla prosa)

Ricostruita l'ultima sessione dai transcript reali di Claude Code (`0c9d52c7`, 17/07): i 9 turni utente
combaciano con log e resume-pointer, nessuna discrepanza. Verificato su git/codice: `master`=`95e35f8`,
PR #194/#195 aperte e mergeable, FEAT-033..037 in `epic.md`, `doctor` non ancorato (`cli/__main__.py:574`
`Path.cwd()`), `ritual_check.py:52` `master` hardcoded, cattura-auto memoria ancora ferma al 09/07.

### 2. Merge del lavoro sospeso (#194 + #195)

Gate pre-merge verde su entrambe (detect + 3 job `test` Ubuntu/Windows PASS; smoke skip corretto).
Mergiate: **#194** (`c003e40`, `.claude/settings.json` agli hook ancorati — chiude il wiring rotto vivo) +
**#195** (`d313b9b`, tracciamento FEAT-033..037 + analisi). `master` = **`d313b9b`**. Sync locale ff a HEAD.

**Rituale post-merge:** re-lock runtime `95e35f8→d313b9b` · re-index incrementale **1373 doc / 14977 chunk**
(tutto `unchanged`: i doc di #195 erano già indicizzati a fine sessione scorsa) · nessun lock stantio (il
bug FEAT-035 non si è ripresentato) · smoke MCP verde: `search_docs` serve la pagina d'analisi, `search_code`
serve `settings_merge.py`, `find_symbol("merge_settings")`→ riga **149** = file reale (code-graph fresco).

### 3. Roadmap col lint (vista E1..E15)

Per la regola registrata: **prima lint, poi vista**. Estratte 177 feature su 15 epiche (conteggi per-epica =
grep, nessun drop silenzioso). **Lint — 1 drift corretto:** **E10-FEAT-026** era «🔄 decomposta» in `epic.md`
ma ✅ CONSEGNATA nell'EXEC (merge `e906e34`/PR #176, `ritual_check.py` esiste); corretta a ✅ + «vedi EXEC»
(regola A-12) → **PR #196** (`104-lint-fix-feat-026-stato`, `a596140`). Gli altri 🔄 (E2-006/010/017,
E12-009, E14-002/003, E15-003/007) coerenti EXEC↔epic.md. Totali: 85 ✅ · 9 🔄 · 77 📋 · 3 🔎 · 3 ❌.

### 4. Decisioni sull'analisi del setup (E2-017/018 · E10-036/037) — SCIOLTE

Discussa l'analisi `wiki/syntheses/setup-dichiara-presunzione-non-azione.md` (tesi: *le superfici del setup
dichiarano ciò che presumono, non ciò che è successo* — Principio XII). **Tre decisioni utente:**
1. **Riordino accettato:** `doctor` ancorato PRIMA di 033/034 (è l'unico P0: verdetti inaffidabili +
   prerequisito di 034; 033 senza scadenza esterna dura).
2. **036 assorbita in E2-018** (folded, non cancellata).
3. **034 sequenziale**, non fusa col doctor (doctor = fix P0 a sé; 034 = feature host-facing separata).

**Tracciamento durevole applicato:**
- **Nuova E10-FEAT-038** — `doctor` non ancorato alla root (P0 bug, PRIMO della coda), scorporata dalla
  FEAT-037.
- **E10-FEAT-037** marcata investigata: `last_index` non-letto da nessun check (`doctor.py:224`) ⇒ un corpus
  fermo non invecchia (non-bug); lo swing warn/pass = probabile FEAT-034; il cwd-bug → FEAT-038.
- **E10-FEAT-036** folded in **E2-FEAT-018** (traccia mantenuta).
- **E2-FEAT-018** scope allargato: P1 esito-descrive-azione (`skipped:identical` vs `skipped:divergent`,
  riusa il confronto di `lifecycle.py:159`) + P2 log `.sertor/.install-log.jsonl` (estende `log_event`).
- EXEC + analisi aggiornati con la coda: **FEAT-038 → FEAT-033 → E2-018(036 folded) → FEAT-034**.

**Prossimo passo:** avviare FEAT-038 (doctor ancorato).

### Rituale

**record ✅** (questa voce via vehicle `append-log`) · **distill: non serve** — l'entità durevole dello step
(«il setup dichiara ciò che presume») ha già la sua pagina (l'analisi-sintesi) + FEAT-038; qui si sono prese
decisioni e tracciate, non fatta emergere una nuova entità con identità propria · **lint ✅** — drift
FEAT-026 corretto (PR #196) + edit di questo step verificati contro le decisioni utente · **explainer: non
innescato** (tracciamento/decisioni, nessuna capacità nuova utente-facing ancora implementata).

## [2026-07-18] implement | E10-FEAT-038 implementata — doctor ancorato alla radice (SpecKit completo, gate verde)

Implementata E10-FEAT-038 «doctor ancorato alla radice» via pipeline SpecKit completa
(requirements → specify → clarify → plan → tasks → implement), branch `106-feat-038-doctor-ancorato-root`.

### Il bug e il fix

`_cmd_doctor` (`cli/__main__.py`) fissava `root = Path.cwd()` e lo passava a `current_source_stats` e
`read_mcp_registration`. Il manifest si caricava bene da ogni cwd (via `settings.index_dir` self-localizing),
ma sorgenti e `.mcp.json` venivano ri-risolti contro il cwd → stesso indice, `index: pass` dalla root e
`index: warn (index_stale)` da `src/`; `registered` oscillava True→False. Stesso difetto d'ancoraggio di
FEAT-031 (hook), mai corretto per `doctor`; già sfiorato e archiviato «not-a-bug» come E15-FEAT-009.

**Fix:** nuovo `_resolve_project_root()` + campo `Settings.project_root`, risolto in `Settings.load` con la
**stessa** self-localization di `.env`/`.index`: (1) `CLAUDE_PROJECT_DIR` se dir esistente (parità hook), (2)
altrimenti il parent della `.sertor/` che possiede l'indice risolto (`index_dir.parent.parent`, cwd-indip.
perché `index_dir` è ancorato via `sys.prefix`), (3) altrimenti `None`. `_cmd_doctor` usa
`settings.project_root`; se `None` → **fail-loud** (`ConfigError`, exit 1) prima di stampare qualunque
verdetto (Principio XII). Una sola semantica di root nel CLI → `doctor` concorda con `index`/`search`.

### Decisioni di scope (forcella clarify, decise dall'utente)

Ancoraggio = **riuso self-localization** (non walk-up, non `--root`); `--root` esplicito rinviato (Could).

### Verifica

- **5 unit test nuovi** verdi: `test_settings_runtime.py` (derivazione root cwd-indip., `CLAUDE_PROJECT_DIR`
  override, assenza→None) + `test_cli_doctor.py` (helper ancorati a `project_root` non a cwd, fail-loud senza
  verdetto). Fixture `wired` aggiornata (installazione sana = root risolvibile).
- **Gate:** `pytest -m "not cloud"` = **1193 passed** (1 xfail packaging noto); `ruff check .` pulito.
- **Live:** handler gira dalla root; da `src/` col workspace venv (privo dell'ancora `.sertor/.venv`) →
  **fail-loud** `exit 1` «cannot resolve the project root» (REQ-004 provato dal vivo).
- **Nessuna regressione a valle:** `rag-freshness` invoca doctor via runtime installato
  (`uv run --project .sertor`, `cwd=root`) → ancora via `sys.prefix` da ogni cwd.

**Nota onesta:** l'invarianza «stesso verdetto da ogni cwd» vale per il **runtime installato**
(`.sertor/.venv`), provata dai unit test; il workspace venv (`.venv`) da sottocartella fa fail-loud (non un
falso verdetto). Consegna reale all'utente/dogfood da verificare LIVE dopo merge + re-lock.

### Sequenziamento git

Mergiata prima **#197** (decisioni + tracciamento FEAT-033..038 + log) → `master` = `57d7acd`; poi rebasato
`106` su master aggiornato. FEAT-038 (P0) chiude il **primo** item della coda dell'analisi
[[setup-dichiara-presunzione-non-azione]]; restano FEAT-033 → E2-018 (036 folded) → FEAT-034.

### Rituale

**record ✅** (questa voce via vehicle) · **distill: non serve** — il fix applica la self-localization
**già esistente** (documentata in `settings.py` + le pagine di FEAT-031), non fa emergere una nuova entità con
identità propria · **lint ✅** — edit consistenti con requisiti/decisioni; l'aggiornamento di stato FEAT-038
(implementata→consegnata) in EXEC/epic.md è il post-merge, dopo il merge di `106` · **explainer: non
innescato** (bug fix interno, nessuna capacità nuova utente-facing).

## [2026-07-18] record | E10-FEAT-038 post-merge — re-lock, consegna LIVE verificata, re-index, smoke, EXEC aggiornato

Post-merge di E10-FEAT-038 (merge `7075a0f`/PR #198 su `master`). Rituale eseguito:

- **Re-lock runtime** `d313b9b` → `7075a0f` (il `.sertor` installato ora contiene il fix).
- **Consegna LIVE verificata (il gap onesto chiuso):** `doctor` via runtime **installato**
  (`uv run --project <abs>/.sertor`) da root e da `src/` → **verdetti identici** (`config=pass · provider=pass ·
  index=warn · mcp=warn`). Prima del fix divergevano col cwd; ora l'invarianza regge sul path reale dell'utente,
  non solo nei unit test. *(Nota d'invocazione emersa: `--project .sertor` è relativo al cwd → da una
  sottocartella serve il path assoluto al runtime; non è un difetto del fix.)*
- **Re-index** incrementale: 1378 doc / 15042 chunk (added=1, updated=10 — i file di FEAT-038). Corpus fresco.
- **Smoke MCP** verde: `search_code` serve `_resolve_project_root`; `search_docs` serve plan/spec/log;
  `find_symbol("_resolve_project_root")` → `settings.py:60` (simbolo nuovo, code-graph auto-ricaricato, riga
  esatta). Nessun lock stantio.
- **Stato aggiornato:** EXEC (tabella consegnate + coda: FEAT-033 ora prossimo) · `epic.md` FEAT-038 ✅ ·
  analisi setup · riga-epica E10.

**Coda residua analisi setup:** FEAT-033 (`ritual-check` default branch) → E2-018 (esito-azione + log, 036
folded) → FEAT-034 (`rag-freshness` rimisura, ora sbloccata).

## [2026-07-18] implement | E10-FEAT-033 implementata — ritual-check rileva il default branch (SpecKit completo)

Implementata E10-FEAT-033 «`ritual-check` rileva il default branch» via pipeline SpecKit completa, branch
`108-feat-033-ritual-check-default-branch`. 2° item della coda dell'analisi
[[setup-dichiara-presunzione-non-azione]] (dopo FEAT-038).

### Il bug e il fix

`_resolve_base` (`src/sertor_core/wiki_tools/ritual_check.py`) faceva `merge-base HEAD master` con `master`
**hardcodato** → su un repo con default `main` nessun merge-base → fail-loud `no merge-base with 'master'`.
Violazione Principio X (un assunto sull'ospite dentro un tool che legge tutto da config), e colpisce il
**primo** tool che un ospite tocca con la forced declaration del rituale (E10-FEAT-026). Segnalato da
**Noetix** (Acta), verificato sul codice.

**Fix:** nuovo helper `_default_base_candidates()` — rileva il default a runtime: (1) `origin/HEAD` via
`git symbolic-ref --short refs/remotes/origin/HEAD` (autorevole, il default *dichiarato dal remote*), poi
(2) i ref **esistenti** `origin/main`/`origin/master`/`main`/`master` (dedup, remoto prima di locale, `main`
prima di `master`). `_resolve_base` itera i candidati e ritorna il **primo con merge-base**; `--base`
invariato; fail-loud finale invariato (ora nomina i ref tentati). Il dogfood (`master`) è coperto dal path
primario (`origin/HEAD` del repo punta a `master`) → nessuna regressione.

### Decisioni di scope (forcella clarify, decisa in-flow)

Ordine di risoluzione = origin/HEAD → candidati ordinati esistenti → fail-loud (suggerimento Noetix,
sensato; nessun dubbio bloccante). Override del default in `wiki.config.toml` rinviato (Could).

### Verifica

- **4 test nuovi** in `test_ritual_check.py` (repo git tmp costruito nel test): default `main` **e** `master`
  → base risolta senza `--base` (SC-001/002); `--base` esplicito onorato (SC-003); orphan senza merge-base →
  fail-loud (SC-004). I test esistenti (che usano `--pages` o si aspettano il fail-loud in tmp non-git) restano verdi.
- **Gate:** `ruff check .` pulito. `pytest -m "not cloud"` = **1195 passed** + 1 xfail packaging noto; **2
  fallimenti SOLO ambientali** (`test_clean_install_uv[sertor]`/`[sertor-flow]`): `uvx --from git+…@108-…`
  non risolve finché il branch non è pushato su origin — non toccano `ritual_check.py`, passano una volta
  pushato il branch (come su branch 106). Confine D↔N invariato (tool trova, agente giudica); sola lettura; zero-LLM.

### Rituale

**record ✅** (questa voce via vehicle) · **distill: non serve** — il fix elimina un assunto hardcodato
(gemella lato-tool di FEAT-038), non fa emergere una nuova entità durevole con identità propria · **lint ✅**
— la claim esterna (Noetix: `master` hardcoded) verificata sul codice prima di accettarla; edit consistenti
con requisiti/decisioni · **explainer: non innescato** (bug fix interno di governance, nessuna capacità
utente-facing nuova).

## [2026-07-18] record | E10-FEAT-033 post-merge — re-lock, LIVE dogfood, re-index, smoke, EXEC aggiornato (coda → E2-018)

Post-merge di E10-FEAT-033 (merge `546cb22`/PR #200 su `master`). Rituale eseguito:

- **Re-lock runtime** `b0c9d87` → `546cb22`; nessun lock stantio.
- **Verifica LIVE sul dogfood** (default `master`): `ritual-check --json` → `scope: git:546cb22...HEAD`,
  **exit 0**, schema valido. Il default è **rilevato** via `origin/HEAD` (→ `master`), non assunto: nessuna
  regressione sul dogfood. *(La correzione per il caso `main`-default è provata dai 4 unit test, che
  costruiscono repo git `main`/`master` in tmp.)*
- **Re-index** incrementale: 1382 doc / 15094 chunk (added=4, updated=3 — i file di FEAT-033). Corpus fresco.
- **Smoke MCP** verde: `find_symbol("_default_base_candidates")` → `ritual_check.py:48` (simbolo nuovo,
  code-graph auto-ricaricato, riga esatta); `search_docs` serve requirements/spec di FEAT-033.
- **Stato aggiornato:** EXEC (delivered table + coda: E2-018 ora prossimo) · `epic.md` FEAT-033 ✅ ·
  analisi setup · riga-epica E10.

**Coda residua analisi setup:** E2-FEAT-018 allargata (esito-descrive-azione + log `.install-log.jsonl`,
036 folded) → E10-FEAT-034 (`rag-freshness` rimisura, sbloccata dal doctor ancorato).
