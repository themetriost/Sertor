# Log 2026-07-23

Log entries for 2026-07-23.

## [2026-07-23] lint | Audit semantico completo del wiki ↔ realtà (2 giri): ~32 pagine allineate

Lint semantico a tappeto dell'intero wiki (108 pagine) contro il codice reale **e** l'archivio delle
conversazioni — innescato dall'utente («il wiki sarà completamente obsoleto, lo lasci a marcire»), dopo che
la pagina `concepts/constitution.md` era ferma a 11 principi/v1.2.0 mentre la costituzione era a
v1.5.0/13 (colto da lui, non da me — vedi [[feedback_documentazione_inaffidabile]]).

**Metodo (2 giri, D↔N):** giro 1 = 4 sweep paralleli (concepts×2, tech, syntheses+explainers) → referto
per-pagina; correzioni applicate (4 esecutori, ognuna verificata sul codice) + 3 mie. Giro 2 (richiesto
dall'utente, «più a fondo, codice + storico conversazioni») = 3 sweep: ri-verifica dei fix (accurati al
100%, membro-per-membro), audit experiments/sources, cross-check con l'**archivio memoria episodica**.

**Drift trovato (~20 pagine giro 1 + 4 residue giro 2):** radici ricorrenti = superficie MCP 7→10 tool
(aggiunti memory_*), runtime `.venv`→`.sertor/` install-based (E15), hook `.ps1`→`.py` (A-09),
composition (glove+hybrid default), costituzione v1.5.0/13 principi, stati feature (memoria v0.1.2,
asset-install, distill-audit), rename enum/simboli (`hashing.py`/`HashingEmbedder`, ArtifactKind…),
FEAT-029 hook SessionStart config-driven. Il giro 2 ha stanato residui che il giro 1 aveva mancato
(`sessionstart-hook` ancora hardcoded, `code-graph`/`index.md` «7/3 tool», falsamente CURRENT).

**Esito:** ~32 pagine allineate alla realtà, verificate due volte; l'archivio conversazioni **corrobora**
il wiki (nessuna decisione non riflessa). Record datati (experiments, audit-*) lasciati come storia
corretta (non degradati a «stale»). Broken-link 17→14 (zero aggiunti; i residui sono rumore: esempi
`[[x]]`, link a concetti-memoria).

**Lezione (fix di processo):** il lint semantico non ha rete (a differenza del distill col pavimento
FEAT-039) → è il passo che salto. Regole dure salvate: [[feedback_lint_semantico_stesso_step]],
[[feedback_documentazione_inaffidabile]]. Possibile FEAT futura: una rete deterministica per il lint
semantico. Vedi [[step-ritual]], [[lint-semantico-host-agnostico]].

## [2026-07-23] distill | no: audit di correzione, nessuna nuova entità durevole (candidati nominati)

**«No» costato del giorno (pavimento distill FEAT-039, dogfood su se stesso).** Lo step di oggi è
l'**audit semantico correttivo** del wiki (~32 pagine riallineate al codice) + la **registrazione** di
FEAT-030 (costituzione v1.5.0/EN). Nessuna delle due fa emergere **nuove entità durevoli** che meritino
una pagina oggi. Candidati **considerati e scartati/rinviati**:

- **`lint-semantico-host-agnostico`** (una rete deterministica per il lint semantico, gemella del
  pavimento distill) — è un'**idea di FEAT futura**, non un meccanismo ancora reale: **rinviata** a
  roadmap/backlog, non merita una pagina-concetto oggi. Vedi la lezione nella voce `lint` di oggi.
- **Entità di FEAT-030** (`product-plane-vs-fixture-plane`, `fail-loud-fix-cause`, `ritual-check`):
  **già distillate** in pagine proprie ieri (2026-07-22) — oggi le si corregge/registra, non le si crea.
- Il resto dell'audit **corregge pagine esistenti** (superficie MCP, runtime `.sertor/`, hook `.py`,
  costituzione, stati feature): zero entità nuove.

Verdetto: **distill non dovuto oggi** — dichiarato, non omesso (è proprio ciò che il pavimento esige).
Questa voce è il primo caso in cui la merge-gate FEAT-039 agisce sul **proprio** dogfood.

## [2026-07-23] reorg | EXEC roadmap snellito: 37 KB → 2,5 KB (digest per epica, dettaglio non iniettato)

Il blocco **EXEC** di `wiki/syntheses/roadmap.md` (iniettato a ogni SessionStart) era cresciuto a **37 KB**,
violando la sua regola vincolante («executive, una schermata, non un changelog»): la tabella «Capacità
consegnate» era di fatto un changelog di ~42 righe-paragrafo con hash-PR e dettaglio tecnico. Su scelta
utente (forma **«digest per epica»**), l'EXEC è stato **compattato a 2,5 KB / 30 righe**: 🔄 In progress ·
✅ Consegnato (digest per epica, una riga per epica + riga di stato E1–E15) · 📋 Prossime direzioni.

**Metodo lossless:** nessuna storia cancellata — le tabelle di dettaglio (capacità per-feature, 15 epiche,
release notes, governance) sono state **spostate SOTTO `EXEC:END`** in una nuova sezione «📊 Dettaglio —
stato completo (non iniettato, leggibile su richiesta)». `EXEC:END` risalito subito dopo le Prossime
direzioni. Marker verificati (un solo START/END; block size confermata a 2572 byte). La vista completa
E1..E15 con tutte le feature **resta** (on-demand, sotto i marker) — distinta dall'EXEC iniettato.

**Non è nuovo:** l'item **A-04** (`e83c6de`/PR #145) aveva già spostato `EXEC:END` prima del changelog
(55k→10k token); il changelog era **ri-strisciato dentro** i marker nelle sessioni successive. Questo
ripristina la forma compatta e la **deriva-magnete** resta un rischio (ogni feature aggiunge una riga
all'EXEC → tenere il digest, non il changelog). Vedi [[roadmap]], la regola EXEC in [[step-ritual]].

## [2026-07-23] record | E10-FEAT-040 wiki-guard — gate Stop bloccante per record + lint semantico

Implementata **E10-FEAT-040 `wiki-guard`**: hook host-facing che dà a **record + lint semantico** la
stessa rete hard che [[daily-distill-floor]] dà al distill, ma **allo Stop** (fine turno) invece che al
merge. Nasce dalla richiesta utente («le istruzioni del CLAUDE.md vengono ignorate, voglio enforcement
deterministico») dopo che il nudge non-bloccante `wiki-pending-check` veniva regolarmente saltato.

**Design (confermato leggendo i doc ufficiali, non inferendo):** letti [Claude Code hooks](https://code.claude.com/docs/en/hooks)
e [GitHub Copilot hooks reference](https://docs.github.com/en/copilot/reference/hooks-reference).
Verificato: Claude `Stop` + `{"decision":"block","reason"}` (JSON parsato solo con exit 0) **e** Copilot
`agentStop` + `{decision:block,reason}` → **parità piena** (l'utente aveva ragione: Copilot ha lo stesso
blocco allo Stop). **Scartato** l'handler `agent`/`prompt` per un verify semantico *deterministico*:
Claude-only (Copilot ha `prompt` solo su `sessionStart`, auto-submit) **e** sposterebbe il **giudizio**
dentro l'hook, contro il confine D↔N (Principio XI). Scelta: **presence-gate `command` + reason ricco** —
l'hook esige, il main agent giudica.

**Meccanismo:** rileva **riusando** `sertor-wiki-tools scan` (`wiki.scan/1`, `pending`) — niente
reinvenzione di git-status/timestamp; se `pending>0` allo Stop → blocco con reason che esige record +
distill + **lint semantico**. `stop_hook_active`-guard, fail-open (no config/scan → non intrappola,
+breadcrumb), `pending==0` (read-only) chiude normale, exit 0 sempre, stdlib-only. Sostituisce il wiring
**Stop** di `wiki-pending-check` (che resta su SessionEnd).

**Consegna:** script `wiki-guard.py` (portabile via `_hooklib`), wiring Claude `settings.hooks.json` +
Copilot `_copilot_wiki_hook_specs` in `install_wiki.py`, 8 test, sync bundle. **Gate verde** (root 1244 ·
sertor 564 · ruff clean). **Prova LIVE** contro lo script reale: sessione con lavoro non registrato →
`{decision:block}` con «2 files newer than the last log entry»; `stop_hook_active=true` → nessun output
(anti-loop). **Debito tracciato FEAT-041:** l'`upgrade` sugli ospiti esistenti deve rimuovere il vecchio
wiring Stop (gemello FEAT-031→032); fresh install e dogfood sono corretti. Entità distillata:
[[wiki-guard]]. **explainer:** non serve (tooling di governance interno). Implementazione delegata a un
fork con verifica in flusso principale.

## [2026-07-23] record | E10-FEAT-041 — upgrade supersede il wiring Stop di wiki-pending-check (+ FEAT-042 finding dry-run)

Fix **E10-FEAT-041** (completamento di [[wiki-guard]] FEAT-040) + due finding dal **dogfooding via
installer**. Origine: l'utente ha chiesto di aggiornare il dogfood con l'installer; ho eseguito
`uvx --from git+… sertor upgrade wiki` reale, e l'ispezione del risultato ha stanato due bug.

**FEAT-041 — doppio-fire allo Stop (fixato).** L'`upgrade` aggiunge `wiki-guard` allo Stop (merge additivo
per stem) ma **non rimuoveva** la vecchia entry `--mode Stop` di `wiki-pending-check` (stem diverso) →
entrambi gli hook Stop firavano. Confermato **LIVE** sul dogfood (`.claude/settings.json` con due entry
allo Stop). **Fix:** `_apply_wiki_upgrade` (ramo `SETTINGS_MERGE`) ora rimuove quella entry **prima** del
merge, via `remove_hook_entries_by_command_substring` con substring **assistant-specifico**
(`wiki-pending-check.py" --mode Stop` su Claude, senza virgoletta su Copilot — la resa del path differisce);
la **SessionEnd resta** (rimozione mirata, non purga per basename). Test `test_upgrade_wiki_stop_supersede.py`.
Gemello di [[feat-032-hook-stem-identity|FEAT-031→032]].

**FEAT-042 — `upgrade --dry-run` mente sui settings (tracciato, non fixato ora).** Nel ramo UPGRADE il
dry-run ritorna hardcoded `SKIPPED "idempotent"` per `SETTINGS_MERGE`/`CONFIG` senza simulare il merge:
il dry-run diceva «idempotent» mentre il run reale faceva `+1`. Chi si fida del `--dry-run` per i settings
viene ingannato. Rinviato a FEAT-042 (per scelta utente: fixare FEAT-041 ora, tracciare il dry-run a parte).

**Prova poetica del dogfooding:** dopo aver attivato `wiki-guard` sul dogfood, l'hook ha **bloccato il mio
stesso Stop** in questa sessione (avevo cambiato il codice del fix senza registrarlo) — la feature che
blocca il suo autore, dal vivo, il giorno stesso della nascita. Onorato, non bypassato: questa voce e
l'aggiornamento di [[wiki-guard]] sono la risposta al blocco. *Se Sertor non usa Sertor, chi dovrebbe.*

## [2026-07-23] lint | Audit wiki ↔ realtà (3 agenti paralleli): 11 drift di contenuto corretti + struttura

Lint semantico a scala su richiesta utente («rendi il wiki migliore e più vicino alla realtà»), **dopo** le
due release wiki-guard v0.1.4/v0.1.5. Metodo: **3 agenti paralleli** (concepts · tech+syntheses ·
experiments+explainers) che *trovano* il drift contro codice/requirements/git; **io giudico e correggo**
(giudizio nel flusso principale) + deterministica (`lint` strutturale, `distill-audit`).

**Esito: wiki in buona salute** (i fix del giro precedente tengono; concepts/tech in larga parte verificati
accurati). Drift **nuovo, concentrato e concreto — 11 correzioni di contenuto:**
- `thin-consumer`: `Settings.backend` (inesistente) → `embed_provider`/`store_backend`.
- `valutazione-e-non-regressione`: «fusione code+doc = Principio XII» → è la *sezione Missione/North Star*
  (XII è «Fail Loud»); contraddiceva se stessa.
- `vector-retrieval`: FEAT-006 «resta futuro» → consegnata in **forma composita** (agente esterno + MCP).
- `explainers/memoria-negli-agenti` (HIGH): FEAT-008 multi-assistente spostata da «Il futuro» a **fatto**;
  cattura generalizzata a Claude **+ Copilot CLI**; aggiunta la **lettura via MCP** (`memory_*`).
- `explainers/il-pannello-di-controllo`: export OTel non più «pendente» (FEAT-005 fatto); data frontmatter
  changelog-style → data secca (A-13).
- `explainers/sertor-in-parole-semplici`: aggiunta la riga **memoria conversazioni** (mancava).
- `tech/sertor-installer`: `.mcp.json … --directory .sertor` → `--project`.
- `syntheses/sistema-wiki-fonte-unica`: hook Stop **wiki-pending-check → wiki-guard**; aggiunto il
  `distill-floor` (PreToolUse); conteggio hook corretto (2 non-bloccanti + 2 bloccanti).
- `syntheses/roadmap` (EXEC): ferma a v0.1.3 → **v0.1.4 + v0.1.5**; FEAT-041 ✅, residuo **FEAT-042**.

**Struttura:** corretti i 3 link rotti che avevo introdotto in `wiki-guard.md` (`wiki-pending-check`/`feat-032`/
memory-link) + frontmatter mancante di `nunzio-summaries-handoff`. Broken-link 17→14, frontmatter 7→6; i
residui sono rumore (memory-links intenzionali, esempi di sintassi, record datati) e orfani `processed/`
(by design). **Nessun record datato back-editato** nel contenuto (solo link/frontmatter). Vedi [[step-ritual]].
