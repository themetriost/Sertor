# Log — 2026-07-08

## [2026-07-08] record | Post-merge dedup: la misura sul runtime rivela la leva «doc-correlati» + re-baseline

Post-merge di E5-FEAT-003 dedup (PR #158, `67b4177`). Rituale: re-lock runtime a HEAD (`51dca4f→67b4177`) +
re-index (1302 doc) + **misura del lift sul runtime installato** (prova end-to-end, non solo editable).

**Finding onesto (la misura ha rivelato più del previsto):** sul corpus **fresco 1302** (i +10 doc di *questo
stesso merge*: spec 090, log dedup), `search_docs` **hit@3 è 0.625 con dedup ON *e* OFF** → il gate era
tornato rosso, ma la causa **NON è dedup**. A/B sul runtime: dedup ON dà @5 0.75→**0.88**, MRR 0.55→0.56,
union 1.00 (il dedup **funziona**); ma @3 non si muove perché il doc sepolto ora è scavalcato da contenuto
**diverso, non duplicato** (spec/log della stessa feature) — la **competizione tra doc correlati** al confine
k=3. Il dedup, per costruzione, non tocca i non-duplicati. Inoltre il **baseline era stantio** (registrato a
~813 doc; corpus ora 1302, +60%): `hit@3` a k=3 oscilla ±1 doc a ogni merge.

**Decisione utente:** **re-record del baseline** allo stato attuale sano (dedup on, union 1.00, `search_code`
@3 1.00, `search_docs` @5 0.88 / @3 0.625) come pavimento del corpus 1302 → **gate PASS**. Non è «seppellire»
(anti-XII): il fix è landato e provato (A/B), il baseline era genuinamente vecchio, e il **segnale è tracciato**
— la leva «doc-correlati» è promossa esplicitamente in roadmap (E5, distinta dal dedup, contextual retrieval /
diversità / query-handling, solo se lift misurato). Da qui in poi le regressioni vere si colgono dal nuovo
baseline. NB: l'eval Azure è segnale **dogfood**, non blocca la CI (che gira la suite unit non-cloud); il merge
era corretto.

**Lezione (misura-prima, di nuovo):** ogni merge fa crescere il corpus e sposta i ranking al confine k → il
`hit@3` è intrinsecamente rumoroso a questa scala; l'`union hit-rate` (metrica della fusione = la missione) è
il segnale robusto (1.00). *Lint:* corretto il bullet EXEC «FEAT-003 → search_docs (Should aperto)», ora ✅ +
leva-residua tracciata.
