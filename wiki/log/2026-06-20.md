# Log 2026-06-20

Log entries for 2026-06-20.

## [2026-06-20] record | FEAT-001 retrieval-qualita riformulata + decomposta a requisiti EARS (suite di valutazione host-side)

Epica E5 `retrieval-qualita`, FEAT-001 (l'unica Must) decomposta a livello feature. Riformulazione cardine dell'utente: non «un comando che carica un file e calcola hit@k», ma la **capacità di ciclo di vita di una suite di valutazione del retrieval, LATO OSPITE** (qualunque progetto, non solo il dogfood): dopo l'indicizzazione → genesi della suite (interattiva oppure delega a un LLM che genera candidati query→atteso dal corpus indicizzato, da approvare) → artefatto-dato versionato del progetto (NON fixture Python) → run ripetibile via vehicle (hit@k/MRR deterministici) → non-regressione (riferimento + gate sul degrado) → feedback esplicito che raffina gli `expected`.

- **Ancoraggio al codice esistente (promozione, non reinvenzione):** l'harness `evaluate()`/`EvalReport`/`QueryableEngine` esiste già in `src/sertor_core/engines/evaluation.py`; il ground-truth esiste come fixture di test `tests/fixtures/ground_truth.py` (11 coppie symbol/nl + `relative_to`); confronto baseline/ibrido in `tests/integration/test_baseline_quality.py`. Precedente per la genesi LLM-da-corpus-da-approvare: skill `derive-entity-types`.
- **Vincoli onorati:** Principio XI (run via vehicle, mai import engine fuori test), Principio X + corollario installabile (suite e comando host-side), confine D↔N (run deterministico nel core/CLI; genesi LLM + feedback = giudizio/skill), additività I/III, local-first.
- **Prodotto:** `requirements/retrieval-qualita/ground-truth-valutazione/requirements.md` (EARS — gruppi A suite-artefatto · B genesi interattiva · C genesi LLM · D run+metriche · E non-regressione · F feedback · G host/installabile; MoSCoW: Must=A/B/D/E/G, Should=C/F + confronto locale 2 config; 6 RNF; 8 rischi; 6 domande aperte di design). Epica aggiornata: FEAT-001 → decomposta, DA-Q-a risolta (file-dato fornibile dall'ospite), promosse FEAT-008 (genesi LLM) e FEAT-009 (feedback) nel backlog. Roadmap EXEC E5 aggiornata.
- **Processo:** fase requirements via skill `requirements` (livello feature). Prossimo passo: `/speckit-plan`. Forche di design aperte: formato artefatto (TOML/JSON), riferimento non-regressione (baseline-file vs soglia), genesi LLM (skill nuova vs estende derive-entity-types), superficie comando (`sertor-rag eval` + skill authoring), validazione `expected_path` contro l'indice.
- **Niente codice scritto** (fase a monte del design). Nessun re-index ancora (toccato `requirements/`+`wiki/`): da fare a fine sessione/merge.
