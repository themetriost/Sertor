# Log 2026-07-12

Log entries for 2026-07-12.

## [2026-07-12] record | E13 Fase 1 COMPLETA — doc utente (why/index/tutorial/troubleshooting/changelog/reference)

Scelta utente dopo la chiusura dell'audit A-01..A-20: prossimo fronte = **E13 completare la doc utente
Fase 1** («considera che abbiamo già fatto uno step con gli A in questo senso» → A-18 aveva consegnato i
2 Must), **processo leggero** (authoring diretto, niente SpecKit — «calibra la documentazione al valore»).

**Consegnato (i 6 restanti di Fase 1, FEAT-003..008):**
- **FEAT-003 `docs/why-sertor.md`** — «cos'è e perché» per non-tecnici. Controparte **esterna** di
  [[sertor-in-parole-semplici]] (analogia del *bibliotecario*), **riscritta** autosufficiente (DA-DM-d),
  apre col differenziatore code+doc + esempio `search_combined`, 0 wikilink interni (CS-5). *(io, flusso
  principale — flagship voice.)*
- **FEAT-004 `docs/README.md`** — indice «dove andare per cosa» (GitHub landing di docs/), spotlight su
  getting-started come ingresso unico, 3 sezioni + «if you want to…». *(subagent, briefing ancorato.)*
- **FEAT-005 `docs/tutorial.md`** — end-to-end guidato con output atteso a ogni passo (install→index→
  doctor→search→search_combined), entrambe le varianti CLI. *(io — command-heavy.)*
- **FEAT-006 `docs/troubleshooting.md`** — 9 voci symptom/cause/fix, complemento **statico** di
  `sertor-rag doctor`; comandi verbatim, il subagent ha **verificato i flag `--online`/`--json` nel
  sorgente CLI**. *(subagent.)*
- **FEAT-007 `CHANGELOG.md`** (radice) — release notes utente, Keep-a-Changelog; **onesto**: `[Unreleased]`
  vuoto + `[0.1.0]` baseline con nota interim `git+url` (no PyPI), zero storia inventata, zero PR/hash.
  *(subagent.)*
- **FEAT-008 `docs/reference.md`** — comandi (2 livelli: installer `uvx` · runtime `uv run --project
  .sertor`) + manopole `.env` chiave, rimando a install.md per il set completo. Sottocomandi `sertor-rag`
  verificati via `--help`. *(io — knob/command-heavy.)*

**Metodo (batch):** flagship + command-heavy scritti nel flusso principale (why, tutorial, reference); i
3 a minor rischio-drift delegati a subagent paralleli con **briefing ancorato alle fonti reali +
istruzione anti-drift stretta** («copia i comandi verbatim, non inventare»), poi **review nel flusso
principale** di ogni file contro gli asset reali.

**Verifica (SC-004/005):** link relativi in README/CHANGELOG/docs/*.md → **0 rotti**; check CS-5 →
**0 link a `wiki/`/`specs/`** nei doc utente (corretto **1 drift pre-esistente**: `docs/install.md:269`
puntava a `specs/011-.../quickstart.md` → sostituito con `reference.md`). Root README aggiornato (rimandi
a why-sertor + indice docs + changelog).

**Confine:** solo documentazione (radice + `docs/`); nessun codice `sertor-core`/CLI/installer → suite
non impattata (0 codice). **Distill:** non innescato (i doc *sono* la Fase 1; gli explainer interni
esistono già). **Lint semantico:** *è servito* — trovato+corretto il drift CS-5 in install.md.
**Explainer:** non innescato (E13 Fase 1 *è* lo strato explainer esterno).

**Fold:** questo commit porta anche la **finalizzazione post-merge di A-19** (roadmap A-19 ✅ + bullet
audit → ✅ COMPLETO, nota post-merge in `2026-07-11.md`).

**File toccati:** `docs/why-sertor.md`·`docs/README.md`·`docs/tutorial.md`·`docs/troubleshooting.md`·
`docs/reference.md`·`CHANGELOG.md` (nuovi), `docs/install.md` (fix CS-5), `README.md` (rimandi),
`requirements/documentazione-marketing/epic.md` (FEAT-003..008 ✅), `wiki/syntheses/roadmap.md` (E13 Fase 1
completa + capacità consegnate + finalize A-19), `wiki/log/2026-07-11.md` (nota post-merge A-19), questo log.

**Prossimo:** commit su branch + PR + merge → post-merge (re-index doc utente + smoke). Con questo,
**E13 Fase 1 è interamente consegnata**; la Fase 2 (marketing) resta gated sul go-public.

*(PR #175 mergiata su `master` `3bcb8b1`; CI verde — 3 job test pass, smoke skipped (docs-only). Post-merge
fatto: re-lock `e6096e4→3bcb8b1` · re-index incrementale 1337doc/14607chunk (7 added = i 6 doc + log ·
5 updated) · smoke MCP verde — `search_docs` rende `docs/why-sertor.md#0` (bibliotecario) in testa,
`search_code` serve `select_for`, `find_symbol("AssistantProfile")` → assistant.py:109 code-graph fresco.
Roadmap: capacità consegnate + E13 row aggiornate con merge hash. **E13 Fase 1 interamente su `master`
(FEAT-001..008).**)*

## [2026-07-12] record | E10-FEAT-026 — rituale wiki anti-skip: sottocomando `ritual-check` + dichiarazione forzata

Scelto il fronte **debito tecnico**, item **FEAT-026** (Should P1, l'unico Should aperto di E10), MVP
**parte 1+3**, **processo formale SpecKit**. Origine: 2 feedback ospiti convergenti (`hermes-nunzio-ha` +
`Noetix`, 2026-07-01) — i passi di **giudizio** del rituale (distill/lint) si saltano **in silenzio**;
danno reale: contraddizione nell'EXEC per un'intera sessione.

**Pipeline SpecKit completa** (requirements→specify→clarify→plan→tasks→implement). Clarify: git-diff-vs-base ·
nuovo sottocomando `ritual-check` · il tool emette lo scaffold. Plan risolve DA-3 (segnali drift).

**Implementato (parte 1 — il tool, `src/sertor_core/wiki_tools/`):**
- **`ritual_check.py`** (nuovo): sottocomando **deterministico, sola lettura, zero-LLM**. Scope dello step
  via `git diff --name-only <base>...HEAD` (default merge-base con `master`) filtrato ai `source_dirs`;
  override `--pages`; **fail-loud** (`ConfigError`) se scope indeterminabile (REQ-006). **Candidati distill:**
  ≥2 pagine changed con ≥2 **nuovi** backlink incrociati (HEAD∖base via `git show`) e 0 nuove pagine
  distill-home (`concepts/`/`tech/` da tassonomia, o `[ritual].distill_dirs`). **Candidati drift:**
  `stale-updated` (frontmatter `updated:` più vecchio della pagina changed più fresca), `neighbor-of-change`
  (pagine linkate da una pagina changed **non-hub** — hub esclusi via `hub_threshold` per non annegare il
  segnale, R-1), `capability-exec` **config-driven** (`[ritual].capability_globs`+`exec_page`: capacità
  cambiate ma EXEC non toccato — il danno reale, ma **non hardcodato**, host-agnostico). **Scaffold di
  dichiarazione** pre-popolato coi conteggi.
- `contracts.py`: `RitualCheckResult` (`wiki.ritual_check/1`). `profile.py`: sezione `[ritual]` opzionale.
  `__main__.py`: registrato `ritual-check` (`--base`/`--pages`/`--json`) + summary umano.
- **Test (+9):** `test_ritual_check.py` — distill ± + soppressione-da-nuova-pagina · drift
  stale/neighbor/hub-exclusion/capability-exec (con/senza config) · **fail-loud** su scope indeterminabile ·
  **sola lettura** (nessuna pagina modificata) · schema/scaffold.

**Implementato (parte 3 — contratto host-facing):** regola di **dichiarazione forzata** a fine step
(`Ritual: record · distill · lint`, «non serve» va **dichiarato**) + rimando a `ritual-check`, aggiunta al
blocco `SERTOR:WIKI-RITUAL` (sorgente `claude-md-block.md` + dogfood `CLAUDE.md`) e alla tabella operazioni
del `wiki-playbook.md` (dogfood + bundle sincronizzato). Budget-altitude rispettato (paragrafo condensato,
guardia verde).

**Confine D↔N onorato (dogfood in azione):** `ritual-check` su **questo stesso step** trova **0 distill**
(ho creato *codice* `src/`, non pagine wiki cross-linkate) e **0 drift** (roadmap.md è hub → esclusa) — ma
il **mio giudizio** ha distillato `ritual-check` in [[wiki-tools]]. Il tool *trova i segnali strutturali*,
l'agente *giudica il bisogno reale*: esattamente il meccanismo che la feature intende rendere sistematico.

**Confine:** solo `sertor-core`/`wiki_tools` (codice, installabile per costruzione) + asset host-facing
distribuiti/sincronizzati; engine di retrieval **INVARIATO**. **Fuori scope (follow-up):** parte 2
(wiki-curator restituisce sempre candidati) + parte 4 (propagazione ai moduli `ops/*.md`).

**Rituale: record ✅ · distill ✅** (`ritual-check` → [[wiki-tools]]; il tool ha trovato 0 perché è codice —
giudizio mio) **· lint ✅** (nessun drift introdotto; corretto en-passant l'`updated:`-changelog di
`wiki-tools.md`, anti-pattern A-13). **Explainer:** non innescato (feature di tooling/governance interna).

**Esiti:** ruff clean; suite `not cloud` **verde** (dopo fix budget-altitude); `test_assets_sync` +
`test_claude_md_block_budget` + `test_ritual_check` (9) verdi.

**File toccati:** `src/sertor_core/wiki_tools/{ritual_check.py (nuovo),contracts.py,profile.py,__main__.py}`,
`tests/unit/test_ritual_check.py` (nuovo), `packages/sertor/.../assets/claude-md-block.md`, `CLAUDE.md`,
`.claude/skills/wiki-author/wiki-playbook.md` (+ bundle sync), `wiki/tech/wiki-tools.md`,
`wiki/syntheses/roadmap.md`, questo log.

**Prossimo:** commit su branch + PR + merge → post-merge (re-lock/re-index/smoke).
