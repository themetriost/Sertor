# Log 2026-07-28

Log entries for 2026-07-28.

## [2026-07-28] record | Consegnate #243 e #245: la coda del 27, e il gate che cambia giornata

Consegnate le due PR rimaste in coda dalla sessione del 27: **#243** (E10-FEAT-052 — la cache degli
embedding conserva `float64` dove lo store usa `float32`) e **#245** (EXEC post-audit + registro).

- **Cosa portano:** la riga di backlog della cache a doppia precisione — **407 MB dove ne basterebbero
  ~204**, su ogni host che accende `SERTOR_EMBED_CACHE` — e l'EXEC che mette davanti a un agente a
  freddo le sei righe nuove del backlog, **E10-FEAT-049 marcata pronta** con i suoi quattro casi di
  prova, e la decisione sospesa su speclift/specaudit.
- **Nessun contenuto nuovo**: è la consegna di lavoro già svolto, registrato e distillato ieri.

- **📌 Nota sul gate, perché è la seconda volta in due giorni che insegna qualcosa.** Cambiando
  giornata il `distill-floor` ha bloccato il merge, come da progetto: la partizione del 28 non
  esisteva. **Ed è il comportamento giusto**, non un attrito — chiude una giornata di lavoro
  chiedendo di aprire la successiva con una dichiarazione, invece di lasciar scivolare la consegna in
  un giorno che non ha registrato nulla. *(Ieri lo stesso gate aveva bloccato il proprio autore due
  volte, e in entrambi i casi aveva ragione.)*

## [2026-07-28] distill | no dichiarato: la consegna mette su master ciò che era già distillato

**«no» dichiarato, e costato.** Lo step è la **consegna** di due PR il cui contenuto è stato scritto,
registrato e distillato ieri: non fa emergere entità nuove, le mette su `master`.

**Candidati considerati e scartati:**

- **«Un riassunto invecchia quando cresce ciò che descrive»** — resta il candidato più forte in
  circolazione (cinque superfici: indice wiki · EXEC · CHANGELOG · doc utente · blocchi distribuiti;
  quattro istanze provate ieri). **Confermo il rinvio deciso ieri**, con la stessa ragione e senza
  ammorbidirla: sarebbe una pagina che descrive un rimedio **non ancora costruito**. Si distilla
  quando **E10-FEAT-049** avrà il codice. *(È la seconda volta che lo dichiaro: se dovesse tornare una
  terza senza che 049 sia partita, il rinvio diventerebbe un modo per non decidere, e allora andrebbe
  scritta comunque come «problema aperto» invece che come soluzione.)*
- **«Il gate che cambia giornata»** — comportamento corretto e già descritto in [[wiki-guard]] e
  [[daily-distill-floor]]; l'episodio di oggi è una conferma, non una nozione nuova. Sta nel record.

**Lint semantico ✅.** Verificato che l'EXEC in consegna dica il vero **al momento del merge**: le
righe E10-FEAT-052..057 esistono nel backlog, `/VERSION` è **0.3.2**, `master` è a `3fcabf2`, e i due
rilevatori citati come «in prototipo» sono davvero solo prototipi — l'EXEC li descrive così, non come
capacità consegnate. Nessuna pagina viva contraddetta da questa consegna: non introduce comportamento,
solo backlog e stato.

## [2026-07-28] record | #245 e il contenuto di #243: un conflitto interamente spurio, e la domanda giusta

Consegnate **#245** (`7d81d9f`, EXEC post-audit) e il contenuto di **#243** (riga E10-FEAT-052). La
seconda ha richiesto una diagnosi, e la diagnosi vale più della consegna.

- **La #243 risultava non mergiabile**: branch di ieri, conflitto su `wiki/log/2026-07-27.md`, che nel
  frattempo era cresciuto di ~300 righe. La reazione ovvia sarebbe stata **rebasare**.
- **🔍 Ma il conflitto era interamente spurio.** Confrontando il branch con `master`: le sue **due voci
  di log erano già su master, identiche, alle stesse righe** (622 e 652) — vi erano arrivate col merge
  del branch dell'audit, quando risolvendo *quel* conflitto avevo tenuto il mio contenuto, che le
  includeva. L'unica cosa realmente mancante era **una riga** di backlog: la 052, che nella stessa
  risoluzione avevo escluso di proposito perché «vive sulla #243».
- **Rimedio scelto:** estrarre quella riga e consegnarla da `master`, chiudendo la #243 come
  **superata, non abbandonata**, con il rimando alla PR che ne porta il contenuto. Un rebase avrebbe
  ricreato un conflitto per riconciliare **testo già identico**.
- **La lezione operativa, che è generale:** davanti a un conflitto la domanda utile non è *«come lo
  risolvo?»* ma **«cosa manca davvero?»**. Qui la risposta era *una riga su 59*, e trovarla è costato
  due `git show`. *(Ieri lo stesso errore d'ordine — reagire prima di misurare — era costato un
  `reset --hard` proposto da un agente delegato, che avrebbe cancellato l'intero audit.)*

## [2026-07-28] distill | no dichiarato: il conflitto spurio è un'istanza dell'errore d'ordine già distillato

**«no» dichiarato.** Lo step è consegna + una diagnosi; non fa emergere entità durevoli nuove.

**Candidati considerati e scartati:**

- **«Davanti a un conflitto, chiedi cosa manca davvero prima di come risolverlo»** — è il candidato
  vero della giornata e ha **due istanze in due giorni** (oggi: 58 righe di conflitto per **una** riga
  mancante · ieri: un `reset --hard` proposto per due conflitti il cui lato upstream era **vuoto**).
  Ma è un'**istanza dell'errore d'ordine** già distillato il 26 su [[dogfood-fidelity]] — *«decidere
  prima, verificare dopo»* — applicato a git invece che a una segnalazione esterna. La sua casa è il
  record di oggi, e il rimando al concetto esiste già.
- **«Un contenuto può arrivare a destinazione per un'altra strada e rendere il conflitto spurio»** —
  descrittivo del caso, non riutilizzabile: dipende dall'aver risolto un conflitto precedente tenendo
  quel contenuto. Sta nel record.

**Lint semantico ✅.** La riga E10-FEAT-052 consegnata è **la stessa** verificata ieri sul database
reale (`length(vector)` = 24.576 byte = 3072 × 8 → `float64`; Chroma conserva in `float32`): nessun
claim nuovo da verificare. L'EXEC appena mergiato con la #245 **elenca già** la 052 fra le direzioni
nuove, quindi indice e backlog ora concordano — prima la citava senza che la riga esistesse su
`master`, ed è **la stessa classe** (riassunto che afferma qualcosa sul dettaglio) che ho corretto
quattro volte ieri. Qui si è chiusa da sé con la consegna.

## [2026-07-28] record | claude doctor: igiene dell'harness, e 4 istruzioni che insegnavano una manopola rimossa

Passata di **`/doctor`** sull'harness Claude Code (installazione, estensioni, memoria, hook, permessi).
Esito sull'harness: **sano** — un solo install nativo (`~/.local/bin/claude`, nessun residuo npm),
tutti i file di config parsano, 4 agenti validi senza collisioni di nome, versione **2.1.220 = latest**,
`defaultMode: auto` già attivo. Due sole azioni, e la seconda è quella che conta.

- **Una skill utente spenta:** `webapp-testing` (0 usi *lifetime*, 0 nella finestra di 12 giorni) →
  `skillOverrides` in `~/.claude/settings.json`. Reversibile.
- **`CLAUDE.md`: −92 righe / −3.684 caratteri** (839→747), **~1.570 token risparmiati a ogni sessione**
  (misura reale da `/context`: 25,8k token per 60.708 caratteri ≈ 2,35 char/token — la regola
  `char/4` **sottostimava di ~60%**). Sette tagli, tutti su contenuto **derivabile**: stack+dipendenze
  (→ `pyproject.toml`) · albero delle cartelle (→ `ls`) · diagramma dei layer del core (→
  `ls src/sertor_core/`, e le sue annotazioni erano già nelle «Regole architetturali» sotto) ·
  template `.env` (→ `.env.example`) · tabella dei 4 approcci del prototipo (→ i nomi delle cartelle) ·
  `## Convenzioni di codice` (generico) · bullet «Switch backend».

**🔍 Il difetto vero, trovato leggendo il file invece di fidarsi: `CLAUDE.md` insegnava `RAG_BACKEND`
in quattro punti.** Ma `RAG_BACKEND` **non è più onorata** da `Settings.load` (`settings.py:373`):
viene **ignorata** con un WARNING `config_rag_backend_ignored` che nomina i knob sostitutivi
(`SERTOR_EMBED_PROVIDER` + `SERTOR_STORE_BACKEND`). Uno dei quattro punti la dava come **condizione per
far passare i test** (`i test devono passare con RAG_BACKEND=local`). `.env.example` — la copia
versionata — era **già corretta**: a divergere era solo la prosa always-loaded.

- **Non era un'omissione recente:** l'albero delle cartelle ometteva **sette** directory tracciate
  (`src/`, `packages/`, `docs/`, `specs/`, `tests/`, `scripts/`, `eval/`) e annotava il corpus MCP come
  `prototype`. Un albero che descrive un repo di due epiche fa.
- **Perché nessuna guardia l'ha visto:** il `lint` strutturale non legge la prosa, e il lint semantico
  è **giudizio** — vede solo ciò che qualcuno decide di guardare quel giorno. Distillato in
  [[riassunto-invecchia-senza-riconciliatore]] come **sesta superficie**, la peggiore delle sei perché
  la legge l'agente a **ogni** sessione.

**Non toccato, e per una ragione:** i 7 blocchi marker `SERTOR:*` (posseduti dall'installer, già sotto
budget-righe in CI) e la prosa dogfood che vi si sovrappone — il file stesso dice *«Non riconciliare
cancellando la prosa»*. Le 17 skill di progetto **restano tutte**: le `speckit-*` sono git-ignorate e
rigenerate dall'installer, le altre otto sono **asset spediti agli ospiti** — spegnerle farebbe divergere
il dogfood da ciò che riceve un client, che è il debito che E15 esiste per prevenire
([[dogfood-fidelity]]). `speclift`/`specaudit` inutilizzate restano una **decisione di prodotto
tracciata** nell'EXEC, non un interruttore nei settings.

**Anche i permessi: niente da fare.** ~150 negazioni in 12 giorni, **nessuna** allowlistabile: sono
scritture/distruttive correttamente bloccate (`gh pr merge`, `git branch -D`, `reset --hard`, `sed -i`)
oppure passano da `uv run`, un package runner — una regola prefissa lì pre-approverebbe qualunque cosa
`uv run` sappia lanciare. La frizione più alta misurata è il nostro `ritual-check` (21 blocchi fra Bash
e PowerShell): reale, ma non si chiude con una allow-rule.

**Verifica che Sertor funzioni ancora (richiesta dell'utente) — ✅ verde.**
`doctor` prima: **WARN** su `index` + `mcp`, ed era **corretto** — segnalava proprio il mio edit a
`CLAUDE.md` («sources changed since the last index»). Re-index via vehicle
(`sertor-rag index .`, Principio XI): **incrementale**, `updated=1` — esattamente il file toccato —
`added=0 removed=0 unchanged=1471`, 7,1s, 27 cache hit. `doctor` dopo: **PASS su 4/4 aree**.
Smoke MCP sul path del filtro metadata: `search_code` ✅ · `search_docs` ✅ · `find_symbol` ✅ due volte
con controllo di freschezza (`Settings` → `settings.py:162` = riga del decoratore; `GloveEmbedder` →
`glove.py:51`, entrambe combaciano col file reale). Nessun errore MCP da segnalare.

**Nota su `ritual-check`:** ha risposto `distill=0 drift=0` perché confronta `master...HEAD`, cioè il
**diff committato** — il mio lavoro era nell'albero di lavoro, quindi invisibile. È la divergenza
`ritual-check` ↔ `wiki-guard` già in coda (E10-FEAT-045 nella lista riparazione): lo stesso difetto
d'ancoraggio che per `scan` è stato chiuso derivando dalla storia **unita** alle modifiche non
consegnate. Qui la seconda metà manca ancora.

## [2026-07-28] distill | il rinvio dichiarato due volte è scaduto: «un riassunto invecchia» ha una pagina

**Distillazione reale**, non un «no». Il candidato **«un riassunto invecchia quando cresce ciò che
descrive»** era stato rinviato **due volte** (27/07 e 28/07) con la stessa ragione — *sarebbe una pagina
che descrive un rimedio non ancora costruito* — e il log del 28 aveva messo per iscritto il termine:

> *«se dovesse tornare una terza volta senza che 049 sia partita, il rinvio diventerebbe un modo per non
> decidere, e allora andrebbe scritta comunque come "problema aperto" invece che come soluzione.»*

È tornato una terza volta, E10-FEAT-049 non è partita, e **il caso di oggi è la sua istanza peggiore**.
Quindi: pagina scritta, **come problema aperto** — con un banner che dichiara che il rimedio non esiste.

**→ [[riassunto-invecchia-senza-riconciliatore]]** — la faccia in **prosa** del Principio XIV. Sei
superfici con istanze reali (indice wiki · EXEC · doc utente/CHANGELOG · blocchi distribuiti ·
`wiki/log/index.md` · **prosa always-loaded di `CLAUDE.md`**), la firma del difetto (la copia *non si
rompe*: si legge bene ed è obsoleta), e perché la sesta è la peggiore — la legge l'agente a **ogni**
sessione, e la manopola che documentava non solleva ma **ignora**, quindi produce il comportamento
plausibile invece di un errore.

**Il contenuto nuovo che giustifica la pagina — la riparazione ricade nello stesso difetto.**
Correggendo i quattro punti su `RAG_BACKEND` ho scritto che `Settings.load` «**fallisce loud**». È
**falso**: ignora e segnala. Lo `search_code` dello smoke test l'ha smentito nel giro di un minuto (tre
test lo dicono nel nome: `test_rag_backend_residual_warns_and_ignored`) e
`specs/068-embedder-locale/quickstart.md` lo affermava già in modo indipendente. Corretto in
`CLAUDE.md` con la citazione della fonte.

> **Riscrivere un riassunto a memoria è esattamente l'operazione che l'ha fatto invecchiare.** La copia
> non era stantia perché vecchia, ma perché **riformulazione non ancorata**. Una riformulazione nuova,
> fatta oggi, è già il prossimo drift.

Da cui il corollario operativo, che è il vero valore della pagina: **quando ripari una copia derivata,
non riformulare — punta**. I sette tagli del 28/07 non hanno riscritto meglio: hanno **eliminato la
copia** rimandando alla fonte (`pyproject.toml`, `.env.example`, `settings.py`, `ls`). Quelle 92 righe
**non possono più invecchiare**. Dove la copia serve davvero (le manopole non ovvie), resta **con la
citazione** (`settings.py:373`) — un riconciliatore leggibile a mano.

**Scoperta collaterale sullo scope dei rimedi in coda:** né E10-FEAT-049 né E13-FEAT-014 coprono la
sesta superficie. La prosa `CLAUDE.md` non ha un indice che la punti né un `sources:` da confrontare,
quindi non c'è niente da rilevare. Il presidio praticabile lì è **strutturale, non diagnostico**: non
scrivere nel file always-loaded ciò che è derivabile a due `Read` di distanza. Vale come nota di scope
quando le due FEAT verranno progettate insieme.

**Candidati considerati e scartati:**

- **«L'harness dell'agente è un artefatto di progetto come gli altri»** (il `/doctor` ha trovato un
  difetto di prodotto-documentale, non di harness) — vero ma sottile, e già implicito in
  [[dogfooding]]. Sta nel record.
- **«La regola `char/4` sottostima la prosa italiana di ~60%»** — fatto utile e misurato (2,35
  char/token), ma è una **nota di misura**, non un'entità: la sua casa è il record di oggi.

**Lint semantico ✅ — con una correzione applicata e una segnalazione.**

- **Corretto nello stesso step** (regola: la pagina che descrive un artefatto si aggiorna quando
  l'artefatto cambia): il mio «fallisce loud» → «ignora ed emette `config_rag_backend_ignored`», più il
  nome reale del knob (`SERTOR_STORE_BACKEND`, `settings.py:399`), che avevo citato come `store_backend`
  (nome del campo, non della variabile d'ambiente).
- **Verificato che i tagli non abbiano rotto claim veri:** i 7 blocchi `SERTOR:*` sono intatti (7/7),
  tutte le sezioni «regola SEMPRE attiva» sono al loro posto, e le annotazioni di design del diagramma
  rimosso sopravvivono nelle «Regole architetturali» che restano.
- **⚠️ Segnalato, non corretto:** `CLAUDE.md:67` afferma che gli indici sono namespaced
  `.index` vs `.index-prototype`, ma su disco **esiste solo `.index`** (e il re-index di oggi ha scritto
  la collezione `sertor__azure_text_embedding_3_large`). Il namespacing reale è **per collezione**, non
  per cartella. Non toccato perché riguarda il corpus del prototipo e non l'ho verificato a fondo: è un
  candidato per il prossimo lint, ed è **la stessa classe** appena distillata.
