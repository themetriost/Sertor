# Log 2026-06-18

Log entries for 2026-06-18.

## [2026-06-18] record | Unificazione venv (E10-FEAT-002): .venv canonico, dev superset, mcp ri-puntato

Risolto il problema dei due virtualenv divergenti del workspace: il repo aveva `.venv` (creato da `uv sync`, default) e `.venv-core` (creato a mano per il server MCP) — il non-allineamento causava errori silenziosi del server (`No module named …`). Unificazione mirata (non SpecKit completo): `dev` extra in `pyproject.toml` ora contiene mcp+graph per assicurare il server MCP sia pronto dopo un `uv sync --all-packages --extra dev` unico; `.mcp.json` ripuntato al venv canonico. Per Azure rest opt-in. Nuovo guard test `test_single_venv_guard` (SC-3).
- **Cosa:** extra `dev` superset di sviluppo (mcp≥1.2, networkx≥3 + test/lint existing); `.mcp.json` aggiornato; `.venv-core/` eliminato (gitignored finché il server della sessione corrente non si chiude).
- **Documentazione interna:** note operativa in `wiki/tech/mcp-server.md` + troubleshooting; `CLAUDE.md` (frase "due venv"→uno, regola standing) + `README.md` Development.
- **Sicurezza:** nuovo `test_single_venv_guard` fallisce se `.mcp.json` ritorna a un path non canonico (SC-3, anti-drift).
- **Backlog:** FEAT-002 debito-tecnico epic aggiornato → DONE; nuova `requirements/debito-tecnico/unificazione-venv/requirements.md`; roadmap EXEC.
- **Discovery:** installer NON toccati — creano un runtime isolato `.sertor/.venv` per ospite (verificato); `.venv-graphrag` prototipo isolato intenzionalmente (numpy<2), fuori ambito.
- **Verifiche:** 621 test root (+1 xfail noto) · kit 131 · sertor 282 · sertor-flow 120; ruff produzione pulito; query dogfood live dal venv canonico OK.

## [2026-06-18] record | Verifica empirica install su Spike (Claude+Copilot) + finding residuo uninstall .claude

Dopo l'unificazione venv (E10-FEAT-002) mergiata su master (commit `8cad346`), eseguita verifica empirica LIVE su ospite reale **Spike** (`C:\Workspace\Git\Spike`, progetto HTML 3 prototipi) per attestare che install/uninstall con ENTRAMBI gli assistenti (Claude + Copilot CLI) funzionino end-to-end senza regressioni. Scopo: confidenza sulla **FEAT-007** (distribuzione Copilot) e **FEAT-012** (installer multi-assistente) post-merge.

**Premessa confermata empiricamente:** l'unificazione venv riguarda SOLO l'ambiente DEV di dogfooding; sull'ospite l'installer crea un runtime isolato per-assistente `.sertor/.venv` (mai due venv) — install protocol INVARIATO. Test non-regressione tutti verdi.

**Dogfooding:** server MCP da `.venv` canonico avviato; probe self-test `mcp.status=ok`, 7 tool serviti.

**Spike test 1 — install Claude rag (live):** `.sertor` bootstrap da git+url (sertor-core[azure,mcp,graph,rerank]) → creds Azure → index live 41 doc/269 chunk → query OK → MCP probe (7 tool + search_combined) OK.

**Spike test 2 — install Copilot-CLI rag (live):** uninstall Claude (pulizia segnalata under) → install Copilot-CLI (native `.mcp.json` + hook JSON) → creds → index live 40 doc/268 chunk → query OK → MCP call OK.

**Spike ripristinata:** stato originale (Copilot rag+wiki+governance; codice HTML intatto).

**FINDING (tracciato, commit `c30400b`):** `sertor uninstall rag --assistant claude` su ospite senza `.claude/` preesistente lascia `.claude/` orfano vuoto (`settings.json`={} + `hooks/` vuota) — stessa classe di PR #77 ma su file CONDIVISO. Ripulito a mano. Fixato come gemello di PR #77 (issue per sequenza).

**Dogfood post-merge:** re-index incrementale 760 doc/7854 chunk (+3/~10); FEAT-009 manifest OK.

- **Test:** Spike CLI+installer verdi; suite root +1 (SC-2); 621 test dogfood; 99 non-cloud verificati live (Copilot, MCP, rag).
- **Regressione:** nessuna (prototipo intatto, install/uninstall/upgrade idempotenti, query deterministiche).
- **Debito:** finding uninstall → issue tracciato, fixato in prossimo step.
- **Roadmap:** FEAT-007 ✅ fine test empirico; FEAT-012 ✅ fine test empirico; E10 chiuso.
