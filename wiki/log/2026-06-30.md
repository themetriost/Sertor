# Log 2026-06-30

Log entries for 2026-06-30.

## [2026-06-30] record | FEAT-018 portabilità OS degli hook implementata

Implementazione completata di E10-FEAT-018: il Principio XII (Fail Loud, Fix the Cause) + Principio X (portabilità reale) applicati a hook e superfici cross-piattaforma.

- **Guardia `pwsh` + gap dichiarato:** nuovo modulo puro `sertor-install-kit/host_env.py` (stdlib-only, mockabile offline) fornisce `is_windows`/`pwsh_available` e `PWSH_INSTALL_URL`; `install_rag`/`install_wiki` emettono nota azionabile su non-Windows senza PowerShell Core via meccanismo `InstallReport.notes` (primo uso reale). Gating short-circuit (nessun falso allarme su Windows).
- **Onestà surface inerti:** audit corretto del SessionStart Copilot (è funzionale, non inerte) e della memoria-capture Copilot (inerte finché FEAT-009 memorie non distribuisce manopola `SERTOR_MEMORY_ADAPTER=copilot-cli`); note d'install segnalano il gap, zero dissimulazione.
- **Doc utente:** `docs/install.md` (prerequisito pwsh + tabella operatività-per-target + limite tecnico dichiarato), `docs/install-copilot.md`, tabella capability `packages/sertor/docs/install.md`.
- **Guardie:** test offline (OS/pwsh mockati), tabella verità guardia, form/substringhe note, NR Claude.
- **Esiti:** SpecKit completo, Constitution 12/12 + missione; schema `install.report/1` invariato (additivo); nessuna nuova porta/ArtifactKind/Surface/WriteStrategy/dipendenza/env; sertor **451** · kit **139** · root **1131 passed** (3 skip packaging), ruff pulito; `sertor-core` invariato.
- **Pagina experiment:** [[feat-018-portabilita-os-hook]] creata; backlink wikilink (constitution, deterministic-vs-judgment, sertor-install-kit, feat-019); index e epic debito-tecnico aggiornati.
- **Prossimo passo:** prova LIVE su ospite macOS/Linux senza `pwsh` (giudizio LLM, non-delegabile); follow-up dichiarato: fix wiring Claude portabile (candidato, non in ambito).

## [2026-06-30] record | Finding: l'avvisatore versione (FEAT-013) è inerte senza bump di /VERSION

Spiegando all'utente come funziona l'avvisatore d'aggiornamento (E2-FEAT-013), emerso un gap latente: l'hook confronta lo stamp installato `.sertor/.sertor-version` col `/VERSION` su `master`, ma `/VERSION` è **manuale** (bump tools scartati YAGNI in FEAT-001) e fermo a `0.1.0`.

- **Verificato:** `/VERSION` = `0.1.0`, ultimo (e unico) touch `7f1c89f` (2026-06-17, FEAT-001), **+273 commit** da allora invariato — incluse tutte le feature successive.
- **Conseguenza:** lo stamp installato == remoto == `0.1.0` → verdetto **sempre `up-to-date`**; FEAT-013 **non scatta mai `behind`**. Idraulica corretta ma muta finché non c'è una disciplina di bump.
- **Tensione:** distribuzione `git+url@master` → ogni merge è di fatto una nuova versione, ma `/VERSION` non lo riflette.
- **Documentato come voce durevole:** nuova **E2-FEAT-014** nel backlog `requirements/sertor-cli/epic.md` (disciplina di versioning: bump manuale vs automatico — decisione aperta), Should, 📋 da decomporre.
- **Non è un bug di FEAT-013:** è un gap di processo/policy a monte. Nessun codice toccato; solo backlog + log (questo).
