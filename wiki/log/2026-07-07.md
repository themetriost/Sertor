# Log — 2026-07-07

## [2026-07-07] record | A-06 (audit SWOT P0) — doc `configure` + quick-start su GloVe

Chiuso l'item **A-06** dell'assessment SWOT 2026-07-02 (P0, casa E13 documentazione-marketing): i due gap
doc utente rilevati nei *Weaknesses* dell'audit — «`sertor configure` invisibile» e «quick-start Claude
che manda a Ollama quando il default è GloVe».

- **`docs/install.md`**: aggiunta la sezione **«Filling `.sertor/.env`: `sertor configure`»** (superficie CLI
  reale verificata sul codice: `--backend {azure,local}` con mapping `azure→azure, local→glove`, `--store`,
  `--set KEY=VALUE`, `--overwrite`, `--non-interactive`, `--check` deferred, `--json`; resolution
  flag→env/esistente→prompt-if-TTY→default; additivo/non-distruttivo, segreti mascherati). Corretti gli spot
  stantii «local = Ollama»: prerequisiti (ora **glove default zero-config** + azure/ollama opt-in espliciti),
  commento «# base (local: Ollama+Chroma)» → glove, e la variante **sbagliata** `--backend local # Ollama`
  → local-first (glove). La sezione provider-table (glove/hash/ollama/azure) era già corretta.
- **`docs/install-claude.md`**: prerequisiti riscritti su **glove** (zero-config, nulla da installare;
  azure/ollama espliciti) + step **`sertor configure --backend azure`** nel quick-start (guidato, no editor;
  saltabile con `--backend local` perché glove non ha segreti).
- **README**: nessun drift residuo (verificato).

**Verifica comportamento (MCP-first, non inventato):** `--backend local` mappa a `glove` (`__main__.py`
help + `configure._embed_provider_for`), non a Ollama; `sertor configure` esiste (`configure.py`); GloVe è
il default local-first (FEAT-011, `RAG_BACKEND` rimosso → `SERTOR_EMBED_PROVIDER`).

Doc-only (nessun codice/test toccato). Roadmap: A-06 → ✅; corretto anche il drift **A-05** (🔄→✅,
E10-FEAT-027 mergiato `4a8bd33`, tema poi assorbito da E15 asset-install).

## [2026-07-07] query | A-07 misura-prima — `search_docs` degrada per duplicazione di contenuto

Avviato **A-07** (audit SWOT P1, E5-FEAT-003) con la disciplina *misura-prima*. `sertor-rag eval run
--fused` sul corpus live (`azure:text-embedding-3-large`, 1292 doc): `search_code` MRR **0.74** (robusto),
`search_docs` MRR **0.55** (la metà debole confermata). **Il gate di non-regressione è ROSSO** — confronto
**equo** (stesso provider del baseline 2026-06-21): `search_docs` hit@3 0.75→0.625, union 1.00→0.667.

**Diagnosi (MCP-first, non a intuito):** il corpus è cresciuto **+59%** (813→1292 doc, tutto E15/spec/log)
e ha **sepolto** doc attesi. Guardando i rank reali: le pagine concetto canoniche (`wiki/concepts/step-ritual.md`
→ rank 4, `wiki/concepts/constitution.md` → rank 6) sono scavalcate dalle **copie di distribuzione** del
bundle installer `assets/**` (`assets/claude-md-block.md`, `assets/rag/claude-md-block-rag-usage.md` — byte-copie
dei blocchi `CLAUDE.md`). **Causa = duplicazione di contenuto nel corpus**, non l'algoritmo. `.claude` è già in
`SERTOR_EXCLUDE_PATTERNS`; `assets` **no** → il bundle è indicizzato e inquina. *Ironia: l'asset-install di oggi,
aggiungendo i blocchi a `CLAUDE.md` byte-identici al bundle, ha peggiorato la duplicazione.*

**Calibrazione ground-truth (misura-prima, non circolare):** un solo miss era **suite-narrowness** — «fusion
coverage how it is specified» aveva `expected` troppo stretto; `specs/069/research.md §3 «Fusion coverage:
definizione operativa»` è una risposta **indipendentemente legittima** (rank 2). `eval amend-case` → union
0.667→**0.833**. **NON** ho allargato la GT ai duplicati (`.specify/memory/constitution.md`, ecc.): sarebbe
circolare e seppellirebbe il problema. Il residuo è **retriever reale**.

**Leva tracciata (E5-FEAT-003, non implementata qui — scelta utente):** (1) *quick win dogfood* — escludere
`assets` da `SERTOR_EXCLUDE_PATTERNS`; (2) *fix di prodotto* — **dedup near-duplicate** nel top-k del retrieval,
vale per ogni host con `CLAUDE.md` + wiki. **Gate NON ri-baselinato** (seppellirebbe il segnale, Principio XII).
Committata la sola calibrazione GT (`eval/suite.toml`).

## [2026-07-07] record | E5-FEAT-003 dedup risultati — MVP esatto insufficiente, fuzzy consegna il lift

Implementata la leva A-07 (pipeline SpecKit completa, spec/plan/tasks in `specs/090-retrieval-result-dedup/`,
branch `090`). Funzione **pura** `dedup_results` in `src/sertor_core/services/dedup.py`, applicata a
**query-time** nei **5 siti** di retrieval (ibrido main+fallback, facade fallback+fused, baseline) **prima del
cut**, dietro manopola `SERTOR_DEDUP` (default on, `Settings.dedup_enabled`). **Punto chiave:** serve un
pool > k (riuso `rerank_pool`) o la dedup ridurrebbe sotto k senza backfill. Indicizzazione invariata.

**Misura-prima ha ribaltato l'ipotesi MVP** (il valore della disciplina): l'MVP **esatto** (sha1 sul testo
normalizzato del chunk) ha dato lift **marginale** (search_docs @5 0.75→0.88, ma **hit@3 invariato**, gate
rosso). Causa **verificata dal vivo**: lo stesso blocco chunkato da `CLAUDE.md` (#33, immerso) vs
`assets/claude-md-block.md` (#0, file a sé) ha **confini diversi** → testi chunk non byte-identici → l'esatto
non li collassa (entrambe le copie restavano a rank 1-2 con dedup on). → estensione a **fuzzy**:
shingle 5-word + **containment coefficient** (overlap/min, robusto a lunghezze diverse) soglia 0.8,
case-preserving, fallback esatto per testi < k parole. **Lift consegnato:** `search_docs` **hit@3 0.62→0.75**
(baseline ristabilito), MRR 0.55→0.57, `search_code` **intatto** (no falso-dedup, SC-004), **gate `--fused`
PASS**. 1 residuo union («incremental refresh»: doc-fratelli `specs/046`, NON duplicati) chiuso con
calibrazione GT legittima (`specs/046/spec.md` = risposta valida). **Gate NON ri-baselinato**: è verde perché
il lift è reale. Manopola documentata nel template `.env` installer + `docs/install.md`. Test: `test_dedup.py`
(8, incl. fuzzy) + integrazione engine (dup collassati/backfill/bypass) + suite **1166 verde**, ruff clean,
`sertor-core` invariato fuori dai siti di retrieval. Constitution 12/12 + missione (forte). Follow-up: near-dup
a scala (MinHash), leva «doc-fratelli». Pagina [[dedup-risultati]] (da distillare).
