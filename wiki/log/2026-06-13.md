# Log 2026-06-13

Voci di registro del 2026-06-13.

## [2026-06-13] record | FEAT-006 chiusa come ✅ soddisfatta in forma composita (decisione utente)

Decisione utente (2026-06-13): FEAT-006 (motore RAG agentico) passa da ⏸ rinviata a ✅ **soddisfatta in forma composita**. La capacità agentic RAG esiste già senza codice nuovo: il sistema [server MCP a 7 tool] + [agente client frontier] pianifica, seleziona i tool, itera e cita — è agentic RAG. Nessun motore incorporato da costruire (un loop con modello minore non migliorerebbe l'orchestratore frontier).

- **Framing del bollino verde (giudizio, flusso principale):** distinti due livelli — (1) la *capacità* agentic RAG = ✅ soddisfatta in forma composita (i «4 motori» dell'epica si chiudono: vettoriale + ibrido + code-graph + agentico); (2) l'*agenzia incorporata* nel core (porta `LLMProvider`, `sertor-rag ask`, digest MCP) = dote differita (Could), 36 REQ già elicitati in requirements/sertor-core/motore-agentico/. Verde scritto come "in forma composita", non `✅ master` secco, per non lasciar credere a un motore incorporato consegnato.
- **Lint semantico:** verificato che non esiste `engines/agentic` né porta `LLMProvider` in `src/` → il claim "nessun motore incorporato costruito" è vero, nessuna deriva introdotta.
- **Artefatti aggiornati:** requirements/sertor-core/epic.md (FEAT-006 in §8) e wiki/syntheses/roadmap.md (tabella exec, IN PROGRESS/PLANNED/DONE, mappa epica, fasi, frontmatter).
- **Prossimo step:** igiene radice ospite (`requirements/sertor-cli/igiene-radice-host/`) → `/speckit-specify`.

## [2026-06-13] record | igiene-radice-host: retrocompat ospiti esterni FUORI AMBITO (D4), Sertor one-shot

Decisione utente (2026-06-13) sui requirements di `igiene-radice-host` prima di entrare in SpecKit: la **retrocompatibilità degli ospiti esterni** già installati con `wiki.config.toml` in root è **fuori ambito** — nessun percorso/comando di migrazione, la base installata è trascurabile e si riallinea con un nuovo `install`. **Unica eccezione: Sertor stesso** (dogfood), il cui `wiki.config.toml` si sposta in `wiki/` **one-shot** come fix diretto dell'implementazione (riallineando i ~10 asset `.claude/` che lo invocano + i test di sync nello stesso passaggio), senza costruire un meccanismo riusabile.

- **Modifiche a requirements/sertor-cli/igiene-radice-host/requirements.md:** NFR-2 riscritta (fuori ambito + eccezione Sertor one-shot), R-2 ridotto a "coerenza del dogfood", nuova voce "Fuori ambito" sulla migrazione esterna, decisione D4 aggiunta, header STATO → PRONTO PER SPECIFY.
- **Verifica a terra:** `wiki.config.toml` di Sertor è in root; ~10 file in `.claude/` lo referenziano (skill wiki-author + moduli ops, hook wiki-pending-check.ps1, comando /wiki, agente wiki-curator) → confermano l'ampiezza del fix one-shot.
- **Prossimo step:** `/speckit-specify` su igiene-radice-host (la spec dovrà trattare lo spostamento + asset-sync della mossa #1, ora senza il peso della migrazione esterna).

## [2026-06-13] record | Spec 016 creata: igiene-radice-host (specify, checklist verde)

Avviata la pipeline SpecKit della feature `igiene-radice-host` (asse DOVE / collocazione, epica sertor-cli): fase specify completata sul branch `016-igiene-radice-host` (creato dall'hook before_specify `speckit.git.feature`; `.specify/feature.json` ripuntato a 016, resta locale per policy).

- **Spec:** `specs/016-igiene-radice-host/spec.md` — 3 user story (P1 radice host minima e prevedibile dopo l'install = MVP; P2 wiki autocontenuto: `wiki.config.toml` in `wiki/` + ogni invocazione asset riallineata, Sertor stesso one-shot; P3 meccanismo `--mcp-scope project|local` con fail-fast su local non realizzabile), 8 FR mappati 1:1 sui REQ-301..307 (+ FR-008 per l'eccezione D4), 5 edge case, 6 SC misurabili.
- **Checklist qualità:** `checklists/requirements.md` tutta verde al primo passaggio; zero NEEDS CLARIFICATION (D1..D4 già chiuse nei requirements). Assunzione esplicita: il default di `--mcp-scope` resta di `installer-multiutente` (qui solo il meccanismo).
- **Numerazione:** il numero SpecKit **016** — informalmente "prenotato" per FEAT-007 manutenzione-wiki — va a igiene-radice-host, che entra prima in pipeline (numerazione per ordine d'implementazione, non per ordine epica).
- **Tensione vocabolario accettata (come feature 015):** per un installer gli artefatti e i loro percorsi (`.sertor/`, `wiki/`, file MCP) SONO il valore, non leak implementativo.
- **Roadmap:** EXEC aggiornato (igiene-radice-host requirements-pronti → spec creata, IN PROGRESS con dettaglio; prossimo `/speckit-plan`).
- **Prossimo step:** `/speckit-plan` (clarify saltabile).

## [2026-06-13] record | Plan 016 completato: igiene-radice-host (auto-discovery config + --mcp-scope, Constitution PASS senza deroghe)

Fase plan della feature `016-igiene-radice-host` completata sul branch omonimo: 7 artefatti di design (plan, research, data-model, contracts/cli-commands, quickstart + spec/checklist già presenti), Constitution Check PASS 10/10 senza deroghe.

- **Punto cardine (verificato a terra):** `WikiProfile` risolve i path relativi rispetto a `config_dir` (= `config_path.parent`). Spostare `wiki.config.toml` in `wiki/` rompe `source_dirs`/`root` se non si usa il `root_override`. Soluzione a due meccanismi: (1) convenzione esplicita `--config wiki/wiki.config.toml --root .`; (2) **auto-discovery** nel CLI `sertor-wiki-tools` (`wiki_tools/__main__`): cerca `./wiki.config.toml` poi `./wiki/wiki.config.toml`, e se trovato sotto `wiki/` imposta root=CWD. (2) rende vero SC-2 ("senza intervento manuale") e riduce R-1 — le invocazioni ad-hoc/umane non si rompono.
- **--mcp-scope project|local (REQ-304/305):** default project (invariato, MCP_MERGE in radice); local registra via `claude mcp add-json … --scope local` dietro il `CommandRunner` già iniettabile (nuovo `ArtifactKind.MCP_REGISTER`/`REGISTER_CLI` al posto di MCP_MERGE), fail-fast `McpRegistrationError` se `claude` manca + comando manuale, NESSUN `.mcp.json` silenzioso; idempotente (skip-if-present via `claude mcp get`). Il default "vero" resta di installer-multiutente.
- **.sertor/ unica sede (REQ-301):** già realtà (015), si aggiunge guardia di test sul piano (nessun artefatto runtime in radice oltre .mcp.json/.gitignore).
- **Fix Sertor one-shot (FR-008):** `_CONFIG_TARGET`→`wiki/wiki.config.toml` + mkdir parents in `_apply_config` + `root_override=target_root` in `_apply_structure`; asset (claude/**, claude-md-block, hook ps1) alla nuova sede; git mv del `wiki.config.toml` di Sertor + ri-sync `.claude/` (guardia test_host_agnostic) + CLAUDE.md/playbook; nessun meccanismo di migrazione per ospiti esterni.
- **Dettaglio da confermare live:** mappatura del nostro flag `local`→`--scope local` di Claude Code (come la validazione uvx della 015, il meccanismo è indipendente dal nome).
- **Riferimento piano:** CLAUDE.md (blocco SPECKIT) aggiornato 015→016.
- **Prossimo step:** `/speckit-tasks`.

## [2026-06-13] record | Tasks 016 generati: igiene-radice-host (25 task, 6 fasi, MVP=radice minima)

Fase tasks della feature `016-igiene-radice-host` completata: `specs/016-igiene-radice-host/tasks.md` con 25 task (T001..T025) in 6 fasi, organizzati per user story.

- **Struttura fasi:** setup (T001 baseline verde) → foundational (T002 FakeCommandRunner per `claude`, T003 helper asserzione radice host) → **US1 MVP radice minima** (T004-T008: `_CONFIG_TARGET`→`wiki/wiki.config.toml`+mkdir, root_override in structure, test collocazione + guardia REQ-301 + set radice REQ-306) → US2 wiki autocontenuto (T009-T016: auto-discovery `--config` nel CLI core, test, hook ps1 + asset prosa/esempi + tmpl, fix Sertor one-shot git mv + ri-sync .claude/ + CLAUDE.md, guardia sync test_host_agnostic, guardia no-legacy-path REQ-303) → US3 scope MCP (T017-T021: ArtifactKind.MCP_REGISTER, RagInstallOptions.mcp_scope, McpRegistrationError+_apply_mcp_register idempotente/fail-fast, arg --mcp-scope, test project/local/fail-fast/idempotenza) → polish (T022 docs residenti+--mcp-scope, T023 suite+ruff, T024 quickstart+validazione live mappatura scope, T025 re-index post-merge).
- **Test inclusi** (Principio V): tutti senza rete (FakeCommandRunner, fixtures tmp_path).
- **Coverage FR:** FR-001→T007 · FR-002→T004/T005/T009 · FR-003→T011/T012/T016 · FR-004→T017-T020 · FR-005→T019/T021 · FR-006→T008/T022 · FR-007→T015 · FR-008→T014/T015.
- **Percorso critico:** ~10 passi (setup→foundational→US1; US2/US3 parallelizzabili dopo, con cautela su T014 che tocca asset condivisi con US2). MVP = US1.
- **Prossimo step:** `/speckit-analyze` su spec+plan+tasks, poi implement.

## [2026-06-13] record | Analyze 016: copertura 100%, 0 critical — 4 ritocchi applicati a tasks/data-model

Analisi trasversale spec/plan/tasks della feature `016-igiene-radice-host` (speckit-analyze, read-only): copertura requisiti→task 8/8 FR + 6/6 SC buildable = 100%, 0 task non mappati problematici, 0 CRITICAL/HIGH, 0 conflitti costituzionali.

- **Finding F1 (MEDIUM):** `MCP_REGISTER.target_rel` era il sentinel `"<client mcp registry>"`, ambiguo nel render del report. **Fix:** sentinel leggibile `"(mcp: client registry)"` + nota di verifica render in data-model §1 e T017.
- **Finding F2 (MEDIUM):** la sintassi CLI del client (`claude mcp add-json … --scope local`, nome scope local vs user) è assunta, non verificata. **Fix:** T019 chiede di verificare la CLI reale PRIMA di chiudere `_apply_mcp_register` (il CommandRunner la rende sostituibile); resta anche la validazione live T024.
- **Finding F3 (LOW):** T014 diceva "git mv" ma il rename git lo registra il configuration-manager. **Fix:** T014 chiarito (sposta file + riallinea; staging/commit al configuration-manager).
- **Finding F4 (LOW):** FR-007 host-agnostico coperto solo da T015. **Fix:** T010 asserisce host-agnosticità dell'auto-discovery (solo path relativi a CWD).
- **Finding F5 (LOW):** drift terminologico spec ("file di registrazione MCP") vs plan (`.mcp.json`/`MCP_REGISTER`); mantenuto (spec user-facing, plan tecnico).
- **Esito:** via libera a `/speckit-implement`.

## [2026-06-13] record | Feature 016 implementata: igiene-radice-host 24/25 — config in wiki/, auto-discovery, --mcp-scope

Implementazione della feature `016-igiene-radice-host` completata sul branch omonimo: 24/25 task (T025 re-index = post-merge), 84 (packages) + 326 (root, not cloud) = 410 test verdi, ruff pulito.

- **US1 — radice host minima:** `install_wiki._CONFIG_TARGET` → `wiki/wiki.config.toml` (+ mkdir parents in `_apply_config`, `root_override=target_root` in `_apply_structure`). Test: config in wiki/ non in radice, root hygiene set `{.claude, CLAUDE.md, wiki}`, guardia REQ-301 (nessun artefatto RAG in radice oltre .mcp.json/.gitignore).
- **US2 — wiki autocontenuto + invocazioni:** auto-discovery del `--config` nel CLI core (`wiki_tools/__main__._resolve_config`): cerca `./wiki.config.toml` poi `./wiki/wiki.config.toml` (root=CWD), `ConfigError` se nessuna; `--config`/`--root` espliciti hanno precedenza. Asset aggiornati (hook ps1 con fallback legacy, prosa/esempi a `--config wiki/wiki.config.toml --root .`, tmpl commentato) + sync .claude/ (6 file). **Fix Sertor one-shot:** `wiki.config.toml` spostato in `wiki/` (filesystem; rename git al configuration-manager); auto-discovery verificata dal vivo (`sertor-wiki-tools scan` e `append-log` funzionano senza flag dalla radice repo). Guardia no-legacy-path (REQ-303) + guardia sync (test_host_agnostic, test_assets_sync) verdi.
- **US3 — --mcp-scope project|local:** `ArtifactKind.MCP_REGISTER`/`REGISTER_CLI`, `RagInstallOptions.mcp_scope` (validato → ConfigError), `McpRegistrationError` + `_apply_mcp_register` (idempotente via `claude mcp get`, fail-fast se `claude` assente + comando manuale, nessun .mcp.json silenzioso), `build_rag_plan(mcp_scope)` seleziona REGISTER vs MERGE, flag `--mcp-scope` nel CLI. **F2 risolto dal vivo:** `claude mcp add-json <name> <json> --scope local` confermato (scope local valido, default). FakeCommandRunner esteso (claude_available, claude_has_server). 5 test US3.
- **Polish:** docs/install.md §7 "Igiene della radice host: cosa resta e perché" + `--mcp-scope` nelle varianti. T025 (re-index corpus sertor) resta obbligato dopo il merge su master.
- **Prossimo step:** PR verso master su conferma utente; al merge: distill (pagina installer/igiene), re-index, riavvio MCP.

## [2026-06-13] record | PR #26 mergiata: igiene radice host in produzione — distill, roadmap a DONE, re-index

PR #26 mergiata su master (merge commit 988a9f2, branch 016-igiene-radice-host conservato): la feature 016 (igiene radice host, asse DOVE/collocazione) è in produzione. SpecKit completo specify→merge in giornata.

- **Distill (rituale punto 2):** [[sertor-installer]] — nuova sezione "Igiene radice host (feature 016)": config in `wiki/` + auto-discovery del CLI, `--mcp-scope project|local` (5° ArtifactKind MCP_REGISTER via `claude mcp add-json`, idempotente/fail-fast), residenti inevitabili a root documentati, principio "uv.lock sta sempre accanto al suo pyproject.toml" (ospite: .sertor/; repo Sertor: radice); retrocompat esterni fuori ambito (D4), Sertor one-shot. Frontmatter + tabella install wiki aggiornati.
- **Roadmap a DONE:** riga a colpo d'occhio ✅ master PR #26; IN PROGRESS svuotato (resta FEAT-007); tabella idee aggiornata; nuova voce DONE dettagliata.
- **Config di Sertor spostata:** `wiki.config.toml` → `wiki/wiki.config.toml` (rename R100 nel commit f9f871b); auto-discovery serve il mio stesso tooling senza flag (verificato dal vivo: scan/append-log).
- **Post-merge (regola standing):** re-index del corpus sertor lanciato su master (il grafo si ricostruisce nello stesso passaggio).
- **Coda:** riavvio del server MCP per servire l'auto-discovery del CLI ai client (il codice del server non cambia, ma il re-index aggiorna l'indice). L'utente vuole ora installare su un altro repo: con 016 su master, `uvx … sertor install rag/wiki` pesca il nuovo comportamento.

## [2026-06-13] record | Tema lingua: asset installer e output host-facing tradotti in inglese (pass mirato)

Affrontato il tema lingua con un pass mirato (decisione utente): **tooling/infrastruttura in inglese canonico, contenuto del wiki nella lingua scelta dall'host**. Scope scelto = "bundle + output host-facing" (NON big-bang del codice interno).

- **Asset installati → inglese** (delegato a 3 agent Sonnet in parallelo, con glossario e regole rigide): i 14 file di `skills/wiki-author/**` (SKILL, wiki-playbook, page-craft, wiki-craft, log-craft, ops/*), `agents/wiki-curator.md`, `commands/wiki.md`, `hooks/wiki-pending-check.ps1` (commenti + messaggi), `claude-md-block.md` (blocco rituale), commenti di `wiki.config.toml.tmpl`. Identificatori/comandi/path/schemi/wikilink lasciati invariati. Ri-sync in `.claude/` (17 file).
- **Output host-facing CLI → inglese** (flusso principale): `report.py` (resa umana installer), help/description/errori di `sertor` (installer), `sertor-rag` (`cli/__main__`) e `sertor-wiki-tools` (`wiki_tools/__main__`); wrapper d'errore `errore:`→`error:`. Le error-string profonde nel dominio/servizi restano in italiano (traduzione graduale Boy-Scout, fuori scope per decisione).
- **`[strings]` di Sertor → inglese** (messaggi del hook = tooling).
- **Guardia di lingua:** nuovo `test_assets_english.py` (denylist di parole italiane ad alto segnale sugli asset installati) → verde. Aggiornate le asserzioni dei test sul wrapper d'errore (`errore`→`error`, 6 punti).
- **Qualità:** 85 (packages) + 326 (root) test verdi, ruff pulito. Guardie sync e host-agnostic invariate verdi.
- **Residuo (follow-up):** seed localization di `structure init` it/en (D3, lato contenuto/host-language) + traduzione graduale di error-string profonde e docstring interni. FEAT-007 si restringe (parte asset-EN fatta).
- **Implicazione:** `.claude/` di Sertor è ora inglese (derivato); il wiki interno di Sertor resta in italiano (lingua di contenuto scelta da Sertor-host).

## [2026-06-13] record | Runtime auto-localizzante + traduzione .env template (fix cwd + completamento lingua)

Due fix emersi dalla validazione live su Kaelen, consegnati insieme.

- **Traduzione dei template `.env` (completamento pass lingua):** `assets/rag/env.azure.tmpl` e `env.local.tmpl` avevano commenti italiani (sfuggiti perché sotto `assets/rag/`, non `assets/claude/`) → tradotti in inglese. La guardia `test_assets_english` ora scansiona anche `assets/rag/`. `.gitignore` header già inglese, `.mcp.json` è JSON (niente commenti).
- **Runtime auto-localizzante (fix cwd/.env, root cause del fallback a Ollama):** `Settings.load` ora risolve il `.env` con `_resolve_env_path`: file indicato/cwd `.env` → poi `.env` accanto al venv del runtime (`Path(sys.prefix).parent` = `.sertor/` per un install, radice repo in dev). L'`index_dir` è ancorato alla cartella del `.env` risolto (quando assoluto). Così `sertor-rag` carica `.sertor/.env` (backend Azure) e tiene l'indice in `.sertor/.index` DA QUALSIASI cwd — non solo lanciato da dentro `.sertor/`. **Fail-loud:** se non trova `.env` né `RAG_BACKEND`, emette WARNING `config_no_env_found` invece del fallback silenzioso a `local`/Ollama (la confusione vista su Kaelen). `env_file=None` (isolamento test) preservato identico; il caso cwd `.env` resta relativo (`.index`) retro-compatibile.
- **Test:** nuovo `tests/unit/test_settings_runtime.py` (4 casi: env_file=None preservato, runtime .env da cwd diverso, precedenza cwd, _resolve None). Suite: 85 packages + 330 root verdi, ruff pulito.
- **Diagnosi Kaelen:** l'agente aveva lanciato l'exe da radice host → `.env` cercato in radice (assente) → default `local` → `ollama UnsupportedProtocol`. Causa = cwd, non i segreti. Ora risolto alla radice.
- **Docs:** docs/install.md (nota runtime auto-localizzante) + [[sertor-installer]] (sezione follow-up).

## [2026-06-13] record | D3: seed di structure init localizzato per lingua host (it/en, fallback inglese)

Chiuso il follow-up D3 (seed localization), pass mirato (approccio A: tabella in modulo).

- **Cos'era:** `init_structure` scriveva `wiki/index.md` e `wiki/log.md` con due frasi seed HARDCODED in italiano ("Indice del wiki. Aggiornato dalle operazioni di registro." / "Registro append-only del wiki."), ignorando `profile.language`. Il seed è CONTENUTO del wiki → deve seguire la lingua dell'host; un host `--language en` riceveva seed italiano (incoerente).
- **Fix (structure.py):** tabella `_SEED_STRINGS` {en, it} selezionata da `profile.language` (normalizzato: `it-IT`→`it`), fallback inglese `_SEED_FALLBACK` per lingue non in tabella. Le intestazioni (`# {root}`, `# {log_file}`) restano dalla config. Inglese canonico: "Wiki index. Updated by the log operations." / "Append-only wiki log.".
- **Test:** 4 nuovi in test_wiki_tools_structure (en, it, fallback fr→en, sottotag it-IT→it). 85 packages + 334 root verdi, ruff pulito.
- **Coerenza modello lingua:** asset/tooling inglese + contenuto (incluso seed) nella lingua dell'host. Sertor (host it) → seed italiano invariato.
- **Residuo lingua:** solo la traduzione GRADUALE (Boy-Scout) delle error-string profonde del dominio (fuori scope per decisione).

## [2026-06-13] record | Record retroattivo delle fix lingua+runtime; FEAT-007 ridotta ai gruppi B/C/D

Su richiesta utente, tracciate a posteriori le fix consegnate fuori dal flusso SpecKit.

- **Nuovo doc:** requirements/sertor-cli/tema-lingua-runtime/requirements.md — record retroattivo (STATO: CONSEGNATO) con Parte A (requirement tema lingua: REQ-L1..L5 — asset/CLI/seed in inglese, contenuto in lingua host) e Parte B (bug runtime auto-localizzante: REQ-B1..B3 — .env/indice ancorati al venv del runtime, non al cwd; fail-loud). Mappa la consegna su PR #27/#28/#29 e sui gruppi E/F di FEAT-007.
- **FEAT-007 (manutenzione-wiki):** aggiunto banner — gruppi E (seed REQ-030..034) ed F (asset EN REQ-035..037) GIÀ CONSEGNATI a parte; residuo effettivo = gruppo B (move-con-link REQ-010..015), gruppo C (reconcile + collect+status REQ-020..027), gruppo D (trigger periodico Could). Gruppo A (probe freschezza) resta Won't (D1).
- **A che punto è FEAT-007:** requirements completi e D1..D4 risolte; NON ancora in SpecKit (nessuno spec/plan/tasks). Parte dello scope (E+F) consegnata in anticipo via il pass lingua. Prossimo passo: /speckit-specify (numero 017) sul solo residuo B/C/D.

## [2026-06-13] record | Spec 017 creata: manutenzione wiki (move/reconcile/collect+status) — specify, checklist verde

Avviata la pipeline SpecKit del residuo di FEAT-007 (manutenzione wiki deterministica): fase specify sul branch `017-manutenzione-wiki` (creato dall'hook before_specify; `.specify/feature.json` → 017, locale per policy).

- **Spec:** `specs/017-manutenzione-wiki/spec.md` — 3 user story (P1 `move`-con-link = MVP: sposta una pagina e riscrive tutti i wikilink/link entranti, dry-run, idempotente, errore se dest esiste, slug-matching condiviso col lint, contratto wiki.move/1; P2 `reconcile` sola-lettura delle pagine `status: superseded` + estensione `collect` col campo status, contratto wiki.reconcile/1, lista vuota→clean:true, mai modifica; P3 trigger periodico Could = solo doc + delega allo scheduler ospite), 15 FR mappati sui REQ EARS (gruppi B/C/D), 8 SC misurabili, edge case (alias, link relativi, stato parziale, successore frontmatter vs banner).
- **Ambito:** gruppi E (seed) ed F (asset EN) FUORI ambito (già consegnati PR #27/#28/#29); gruppo A (probe freschezza) Won't (D1).
- **Checklist:** `checklists/requirements.md` verde; zero NEEDS CLARIFICATION (decisioni D1..D4 già chiuse). Unica assunzione da confermare in plan: fonte del "successore" (campo frontmatter vs banner).
- **Numerazione:** 017 (016 = igiene-radice-host).
- **Roadmap:** EXEC aggiornato (FEAT-007 → spec creata; prossimo `/speckit-plan`).
- **Prossimo step:** `/speckit-plan`.

## [2026-06-13] record | Plan+Tasks+Analyze 017: manutenzione wiki — Constitution PASS, copertura 100%, D6 successore=frontmatter

Fasi plan/tasks/analyze della feature 017 (manutenzione wiki deterministica) completate inline sul branch `017-manutenzione-wiki`.

- **Plan:** 5 artefatti (plan/research/data-model/contracts/quickstart). Constitution Check PASS 10/10 senza deroghe. Decisioni chiave: D1 riscrittura wikilink form-preserving (mappa old→new sulle 3 forme di `_link_targets`, preserva alias/anchor); D2 link relativi via `posixpath.relpath`; D3 processa pagine+indice, non i log; D5 rewrite-then-move + recovery (collisione dest+src=errore REQ-013; src assente+dest presente=recovery REQ-014); D6 successore = SOLO frontmatter `superseded_by` (banner scartato, deterministico) — risolve l'unica assunzione aperta della spec; D8 collect+status additivo; D9 trigger periodico = solo doc.
- **Tasks:** 14 task in 6 fasi (US1 move = MVP; US2 reconcile+collect/status; US3 doc; polish+re-index). File condivisi contracts.py/__main__.py editati in serie. Coverage 15/15 FR.
- **Analyze (inline, read-only):** copertura 100%, 0 CRITICAL/HIGH. Finding F1 (MEDIUM): incoerenza spec↔research sul successore (spec diceva "frontmatter o banner") → spec allineata a D6 (solo frontmatter). F2/F3 LOW, nessuna azione.
- **CLAUDE.md:** marker SPECKIT aggiornato 016→017.
- **Prossimo:** implement (in corso, autonomia utente fino a implement).

## [2026-06-13] record | Feature 017 implementata: move/reconcile/collect+status — 434 test verdi

Implementazione della feature 017 (manutenzione wiki deterministica, residuo FEAT-007) completata inline sul branch `017-manutenzione-wiki`: 13/14 task (T014 re-index = post-merge).

- **Consegnato:** `contracts.py` +`MoveResult`(`wiki.move/1`)/`ReconcileResult`(`wiki.reconcile/1`); `collect._page_meta` +campo `status` (additivo, forward-compatible); `move.py` — `move()` con riscrittura wikilink **form-preserving** (mappa old→new sulle 3 forme, preserva `|alias`/`#anchor`) + link relativi Markdown (`posixpath.relpath`), file processati pagine+indice (non i log), `rewrite-then-move` + recovery da stato parziale, collisione dest+src=errore, `--dry-run` no-op; `reconcile.py` — read-only, filtra `status: superseded`, legge `superseded_by`/`updated`, `clean` se vuoto; wiring CLI (`move`/`reconcile` in `_OPS`, 2° positional `dest`, `--dry-run`, dispatch, `_human`).
- **Test:** test_wiki_tools_move (10: forme path/stem/alias, link relativo, dry-run, collisione, sorgente assente, recovery, lint post-move, CLI dispatch+requires-dest), test_wiki_tools_reconcile (4: solo superseded, clean, read-only snapshot, CLI contratto), test_wiki_tools_collect (+status). **434 test verdi** (349 root + 85 packages), ruff pulito.
- **Docs:** docs/install.md — sezione "Manutenzione del wiki (feature 017)" con move/reconcile + nota schedulazione periodica (gruppo D, delega all'ospite).
- **Coerenza:** `move`↔`lint` stessa nozione di target; `reconcile` non-distruttivo (Principio VI); tutto offline/stdlib (Principio V).
- **Prossimo:** PR verso master su conferma utente; al merge: re-index (T014).

## [2026-06-13] reconcile | Riconciliata pulizia-pycache-e-diagnosi-mcp: successore dichiarato (mcp-server)

Riconciliata l'unica pagina superata emersa da `sertor-wiki-tools reconcile` (il primo uso reale del comando appena implementato in feature 017).

- **Pagina:** experiments/pulizia-pycache-e-diagnosi-mcp.md (status: superseded dal 2026-06-10, ma `superseded_by` vuoto).
- **Risoluzione (giudizio, flusso principale):** mantenuta come record storico datato (testimonianza, playbook §4 — non si cancella); aggiunto `superseded_by: mcp-server` nel frontmatter (successore primario; il banner cita anche [[wiki-tools]]); `updated` → 2026-06-13. Supersessione ora completa: banner + status + superseded_by (machine-readable).
- **Verifica:** `reconcile` ora riporta `superseded_by: mcp-server`; `clean=false` resta (la pagina è correttamente segnalata come storica, non eliminata).
- **Nota:** modifica su branch 017 (cavalcherà la PR della feature); re-index del corpus alla prossima occasione/merge.

## [2026-06-13] record | PR #30 mergiata: FEAT-007 manutenzione wiki in produzione — distill, roadmap a DONE

PR #30 mergiata su master (merge commit 945c6ea, branch conservato): feature 017 (manutenzione wiki deterministica) in produzione. Chiude FEAT-007, l'ultimo Should del core.

- **Distill (rituale punto 2):** [[wiki-tools]] aggiornata — tabella operazioni con `move` (wiki.move/1) e `reconcile` (wiki.reconcile/1), riga `collect` con campo `status`; frontmatter + sources aggiornati.
- **Roadmap a DONE:** FEAT-007 ✅ master PR #30 (tabella a colpo d'occhio + nuova voce DONE dettagliata); IN PROGRESS svuotato (nessuna feature in progress: i Must/Should del core sono tutti su master).
- **Chiusura dote FEAT-007:** gruppo A (probe freschezza) Won't; E (seed)/F (asset EN) consegnati a parte (PR #27/#28/#29); B (move)/C (reconcile+collect-status)/D (trigger periodico=doc) consegnati qui.
- **Post-merge (regola standing):** re-index del corpus sertor su master (T014). Riavvio MCP non necessario per move/reconcile (sono wiki_tools, non il server di retrieval; il re-index aggiorna comunque l'indice).
- **Qualità:** 434 test verdi (349 root + 85 packages), ruff pulito.

## [2026-06-13] record | Rituale di step: nuovo step 6 — mostra la roadmap dopo il merge su main (fallback: chiedi e crea)

Esteso il rituale di step (richiesta utente): nuovo step "Mostra la roadmap dopo il merge su main".

- **Cosa:** quando uno step si chiude con un merge su master/main, il flusso principale mostra all'utente l'executive summary della roadmap (blocco EXEC:START/END di wiki/syntheses/roadmap.md) — così dopo ogni consegna si vede subito «dove siamo e cosa fare adesso». Si innesca SOLO al merge, non a ogni step. Presuppone che lo step 4 (Executive summary) abbia già reso vero l'EXEC.
- **Fallback:** se la roadmap non esiste, non inventarla a freddo → chiedere all'utente (con contesto) e crearla su conferma (struttura: blocco EXEC con tabella a colpo d'occhio + IN PROGRESS/PLANNED/DONE, poi mappa feature × stato).
- **Dove:** CLAUDE.md (fonte unica del rituale) come nuovo punto 6 ("altre azioni" → 7); pagina [[step-ritual]] allineata (stessa numerazione). L'asset distribuito claude-md-block.md NON è toccato: è un sottoinsieme deliberato (solo record/distill/lint; non ha nemmeno gli step Executive/Re-index) → la roadmap-post-merge è fuori dal suo scope host.
- **Nota:** modifica di sola governance/doc su master; re-index differibile (modifica minima, ~10 righe in 2 file).
