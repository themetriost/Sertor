# Log 2026-07-14

Log entries for 2026-07-14.

## [2026-07-14] record | Prep PyPI (rifinitura metadati 4 pacchetti) + decisione E14-FEAT-002 (casa = sertor-flow)

Prosecuzione del go-public: **prep a costo-zero** per essere release-ready al 1° ago (publish PyPI resta
gated a CI verde). Niente codice sorgente — solo `pyproject`/README/doc, mergeable come #182.

**Nomi PyPI — verificati LIBERI (2026-07-13):** `sertor`, `sertor-core`, `sertor-install-kit`, `sertor-flow`
(API PyPI → 404 = liberi). Nessun bloccante di naming.

**Build + `twine check` — PASSED puliti (tutti e 4).** Primo giro: `twine check` avvisava `long_description
missing` su `sertor`/`sertor-flow`/`sertor-install-kit` (pagina PyPI vuota). Rifinitura:
- **README nuovi** per `sertor` e `sertor-install-kit`; `sertor-flow` aveva già il README → aggiunto solo il
  campo `readme` nel pyproject (+ corretto en-passant `--assistant copilot`→`copilot-cli`, va su PyPI pubblico).
- **`sertor-core`**: aggiunti `[project.urls]` (Repository + Documentation) + `classifiers` + `keywords` (è la
  libreria-prodotto user-facing; prima nudi).
- **Version constraints** sulle deps interne: `sertor-core>=0.1.0`/`sertor-install-kit>=0.1.0` (così un
  `sertor 0.1.0` tira versioni compatibili). Corretta anche la `description` di `sertor` (diceva «installer del
  sistema-wiki» — undersell: installa anche RAG).
- **`sertor-install-kit`** resta **interno** (DA-P4): README minimale che rimanda a `sertor`/`sertor-flow`.
Secondo giro: `uv build --all-packages` OK, `twine check` dei 4 = **PASSED** senza warning.

**Finding operativo per il workflow di publish:** `uv build --all-packages` builda **6** pacchetti — inclusi
**`speclift`/`specaudit`** (vendored E14). Il workflow di release deve **selezionare i 4 Sertor**, non
`--all-packages`. Registrato in `requirements/sertor-cli/go-public-pypi/` (§In-ambito).

**Decisione E14-FEAT-002 (utente):** casa di distribuzione di **SpecLift/SpecAudit = `sertor-flow` (fold nel
pacchetto)** → diventano moduli + console-script di `sertor-flow`, **non** pacchetti PyPI a nostro nome (evita
di pubblicare una capacità vendorata handoff). **La tensione temuta** («prerequisito RAG che romperebbe
l'ortogonalità di sertor-flow al RAG») **NON si applica**: verificato che `speclift`/`specaudit` sono
**zero-deps** e usano il RAG **via MCP** (Adapter B), **senza importare `sertor-core`** — esattamente come
sertor-flow. **Effort implementazione = feature MEDIA** (fold ~3900 righe zero-conflitto + distribuzione skill
dual-target via `sertor-flow install` + test/guardie/doc) → **da decomporre** (specify/plan), NON necessaria per
la release. Aggiornati `requirements/speclift/epic.md` (riga FEAT-002 + §9 domanda RISOLTA).

**Stato go-public dopo questo step:** i 4 pyproject sono **release-ready** (build+twine puliti, nomi liberi).
Resta al 1° ago (CI verde): workflow OIDC trusted-publishing (i 4) → publish + pip fallback reale + 2ª
release/bump. E13 Fase 2 marketing sbloccata ora.

**Confine:** solo `pyproject`/README/`requirements`; **zero codice sorgente**. Suite/gate: build+twine verdi;
suite non-cloud verde (i pyproject non toccano runtime).

**Rituale: record ✅ · distill: non serve** (prep packaging + decisione; nessuna entità durevole tecnica nuova
— la decisione E14 vive in `speclift/epic.md`) **· lint ✅** (risolta la §9 «casa non decisa» ora obsoleta;
corretta description `sertor` e README `sertor-flow` stantii) **· explainer: non innescato** (packaging/decisione
interna; il valore utente «installabile da PyPI» sarà spiegabile al publish reale).
