# Log 2026-07-11

Log entries for 2026-07-11.

## [2026-07-11] record | A-18 — doc utente MVP: getting-started unico + README di valore (E13 Fase 1)

Item SWOT A-18 (E13 `documentazione-marketing`, EVO/P2), scelta utente: **processo formale SpecKit** +
**nuovo `docs/getting-started.md`**. Consegna i **due Must** di Fase 1 (FEAT-001 getting-started +
FEAT-002 README di valore) come **una feature accoppiata** — branch `096-doc-utente-mvp`.

**Pipeline SpecKit completa (requirements→specify→clarify→plan→tasks→implement).**
- **requirements:** `requirements/documentazione-marketing/doc-utente-mvp/requirements.md` (REQ-001..015,
  NFR, MoSCoW); backlog E13 FEAT-001/002 → `decomposta`. Commit `5c27c97`.
- **specify:** `specs/096-doc-utente-mvp/spec.md` (3 user story, FR-001..015, SC-001..006) + checklist
  qualità (tutti pass, 0 NEEDS CLARIFICATION).
- **clarify (2 forche):** (DA-1) esempi CLI → **entrambe le varianti Claude+Copilot affiancate** (non il
  default Claude-only che avevo proposto); (DA-2) esempio code+doc → **illustrativo generico
  host-agnostico** (query sul repo dell'utente, non dogfood Sertor).
- **plan+research+tasks:** Constitution **12/12 + missione PASS** (authoring, D↔N, `sertor-core`/CLI/
  installer invariati); **DA-4 risolta** — oggi **non esiste** link/lint automatico per `docs/`
  (`sertor-wiki-tools validate` è scoped su `wiki/`, la CI non tocca `docs/`) → verifica
  manuale/scriptata in implement, linter automatico fuori scope. 18 task per user story. Commit `d7c42ae`.

**Implement (questo step).**
- **Nuovo `docs/getting-started.md`:** percorso unico host-agnostico a 4 tappe (prerequisiti → `sertor
  install rag` → `index` → prima query), **entrambe le varianti CLI affiancate** (Claude `--assistant`
  omesso · Copilot `--assistant copilot-cli`), **delega** il dettaglio ai per-assistente, e **termina con
  l'esempio concreto di fusione code+doc** (`search_combined` → tupla `docs`/`code`, illustrativa sul repo
  dell'utente). Comandi **copiati verbatim** dagli asset reali (anti-drift, FR-012).
- **Riscrittura `README.md` valore-first:** apre col **differenziatore fusione code+doc** (*il codice dice
  cosa fa, la documentazione dice perché*) + esempio, poi capacità/status **fattuali preservati** e
  riordinati, poi puntatore a `docs/getting-started.md` come **ingresso unico**.
- **Convergenza:** rimandi al getting-started in testa a `install-claude.md`, `install-copilot.md`,
  `retrieval.md` (no duplicazione; il dettaglio resta delegato).

**Verifica (SC-001..006):** link relativi in `README.md`+`docs/*.md` **tutti risolvono** (0 rotti);
0 blocchi duplicati verbatim (solo le righe-comando canoniche coincidono, per necessità). Gate pre-merge:
**ruff clean** + suite **1171 passed / 3 skipped** (packaging noti) — `sertor-core` invariato, suite non
impattata (0 codice).

**Distill:** nessuna entità di dominio durevole (getting-started/README *sono* doc utente, non pagine-
concetto). Non innescato. **Lint semantico:** *è servito* — corretti 3 drift: (1) la riga «In development
/ next» del README elencava capacità già consegnate (refresh incrementale, Copilot, ricerca semantica) →
resa accurata; (2) EXEC/SWOT davano A-14/A-16/A-17 «pre-merge» ma sono mergiati (PR #167/#169/#171) →
marcati ✅ con hash; (3) IN PROGRESS bullet «A-01→A-16» → «A-01→A-17». **Explainer:** non innescato — gli
artefatti stessi *sono* la doc in linguaggio comune.

**File toccati:** `README.md`, `docs/getting-started.md` (nuovo), `docs/install-claude.md`,
`docs/install-copilot.md`, `docs/retrieval.md`, `wiki/syntheses/roadmap.md` (EXEC/SWOT + A-18 in corso +
fix A-14/16/17), questo log. (Artefatti SpecKit già committati `5c27c97`+`d7c42ae`.)

**Prossimo passo:** PR del branch `096-doc-utente-mvp` → merge → post-merge (re-lock runtime + re-index +
smoke MCP) + mostra roadmap; poi **A-19**.

*(PR #172 mergiata su `master` `6e40ccc`; CI verde — 3 job test pass su ubuntu 3.11/3.12 + windows 3.12,
smoke skipped per docs-only. Post-merge fatto: re-lock `064c5eb→6e40ccc` · re-index incrementale
1330doc/14536chunk (9 added + 7 updated) · smoke MCP verde — `search_docs` rende `docs/getting-started.md#0`
in testa (indice fresco sul path filtro `where`), `search_code` sano, `find_symbol("remove_file_if_owned")`
→ lifecycle.py:145 code-graph fresco. EXEC/SWOT/epic E13 marcati ✅ A-18; questi ritocchi post-merge
viaggiano col commit del prossimo item.)*

## [2026-07-11] lint | A-20 — igiene: triage `sources/Human`, collisione `specs/077`, sweep frontmatter (3/5 già risolti)

Item SWOT A-20 (E10 `debito-tecnico`, FIX/P2, L×S), scelta utente «entrambi: A-20 poi A-19». Bundle di 5
sotto-item d'igiene. **Sondaggio iniziale: 3/5 erano già risolti** (probabilmente dopo l'audit 2026-07-02):
- **#1 `.last-hook-error` gitignore** → già in `.gitignore:88` (`.sertor/.last-hook-error`). ✅
- **#5 OTel senza collector** → `.sertor/.env:54` ha già `SERTOR_OBSERVABILITY_OTEL=false` con commento
  (il rumore `localhost:4318` a ogni comando era stato disattivato). ✅
- **#3 «6 wikilink rotti»** → `sertor-wiki-tools validate` riporta **`broken_links=0`** (già sistemati). ✅

**Fatto in questo step:**
- **#2 triage `sources/Human/`.** Il file era `Fable SWOT.md` — il **prompt dell'audit indipendente**
  (fonte del backlog A-01..A-20), **non tracciato**, non-kebab-case e senza frontmatter. Normalizzato in
  fonte propria **`wiki/sources/Human/fable-swot-audit-prompt.md`** (rename kebab + frontmatter
  `type: source`), pagina [[fable-swot-audit-prompt]]; aggiornate le 2 citazioni file-specifiche
  (`audit-swot-2026-07-02.md`, log `2026-07-02.md`). Azzera il `naming_violations=1`.
- **#4 collisione `specs/077`.** Due dir: `077-fail-loud-hook-agent` (E10-FEAT-019) e
  `077-version-update-check` (E2-FEAT-013), **entrambe storiche/merged** (branch `077` paralleli offline).
  **Decisione: accettata come artefatto storico, NIENTE rename** — `077-version-update-check` non ha
  riferimenti vivi fuori dai suoi `specs/` e dai log; rinominare una spec merged rompe i pointer storici
  ed è puro churn; l'allocatore di numerazione (scan `max+1` di speckit) **già previene** ricorrenze
  same-repo (la collisione fu un artefatto di branch paralleli offline, inerente alla numerazione
  decentralizzata, non un bug del tool). Documentata qui + in roadmap.
- **Bonus igiene:** sweep del **frontmatter** mancante su **9 file-fonte tracciati** (`input-other-agents/*`,
  `usersfeedback/processed/*` — delega mechanical al `wiki-curator`), per azzerare `missing_frontmatter`
  sul corpus **committato**. Restano fuori i **2 input non tracciati/non processati**
  (`nunzio-summaries-handoff`, ecc.) → appartengono al workflow di processamento input, non ad A-20.

**Confine:** solo file wiki/config; nessun codice `sertor-core`/CLI. **Distill:** nessuna entità durevole
(igiene). **Lint semantico:** *è* l'operazione (il lint stesso è lo strumento). **Prossimo:** verifica lint
pulito → commit su branch + PR + merge → post-merge; poi **A-19**.

*(PR #173 mergiata su `master` `6032cab`; CI verde — 3 job test pass, smoke skipped (wiki-only). Post-merge
fatto: re-lock `6e40ccc→6032cab` · re-index incrementale 1330doc/14541chunk (1 added `fable-swot-audit-prompt`
· 14 updated · 1 removed `Fable SWOT.md`) · smoke MCP verde — `search_docs` rende `fable-swot-audit-prompt.md#0`
in testa (indice fresco), `search_code` sano, `find_symbol("AssistantProfile")` → assistant.py:87 code-graph
fresco. Roadmap/EXEC marcati ✅ A-20; ritocchi post-merge viaggiano col commit di A-19. **Resta solo A-19**,
l'ultimo dell'audit. Detour memoria (2026-07-11): confermato che retention è hook non-enforced = E4-FEAT-006
Could; `.env` invariato.)*

## [2026-07-11] record | A-19 — de-binarizzazione del seam assistenti (`select_for` n-ario) — CHIUDE l'audit A-01..A-20

Item SWOT A-19 (E2 `sertor-cli`, EVO/P2, M×L), **l'ultimo** del backlog audit 2026-07-02. Scelta utente:
**processo leggero** (plan + parità come gate) — è un refactor **iso-funzionale**, non nuova capacità.
Ricognizione MCP-first: il seam esiste già (`AssistantProfile`), ma alcuni plan-builder sceglievano un
nome/valore per-assistente con **ternari binari** `X if CLAUDE else Y` (assumono *2* assistenti; un terzo
= Codex cadrebbe in silenzio nel ramo `else`).

**Fatto (7 punti, ~5 siti + 1 struttura dati + helper):**
- **`assistant.py`:** nuovo **`select_for(assistant, {AssistantId: valore})`** n-ario + fail-loud
  (`ConfigError` che nomina l'assistente su chiave mancante, Principio IV/XII) + metodo
  `AssistantProfile.select(mapping)`. Esportato dal kit `__init__`.
- **`install_governance.py`:** `_SERTOR_AUTHORED` da colonne `(claude_name, copilot_name)` →
  `dict[AssistantId, str]`; ternario `:157` + pre-check model `:152` → `select`/lookup.
- **`install_rag.py`:** concierge `:382` → `aprofile.select`.
- **`install_wiki.py`:** `_legacy_wiki_ps1_targets` `:98` + `owned_dirs` `:468` → `select_for`.
- **`__main__.py`:** enumerazione hardcoded `(CLAUDE, COPILOT_CLI)` `:377` → `iter(AssistantId)`.
- **Test (+3):** `test_assistant.py` — `select_for` sceglie il valore per-assistente · chiave mancante →
  `ConfigError` fail-loud · `AssistantProfile.select` legato al proprio assistente.

**Fuori scope (motivato, Principio III/YAGNI):** i guard *a singolo ramo* `if COPILOT_CLI:`
(governance:148, wiki:171, rag:427/842/860/1072) codificano **comportamento strutturale** dell'assistente
(hook nativi, render custom-agent), non selezione di valori → astrarli senza conoscere Codex sarebbe
speculativo. **Onestà:** A-19 rende Codex «aggiunta di dati» per nomi/target, ma i punti strutturali
richiederanno una decisione esplicita.

**Gate di parità (verde):** `select({CLAUDE:X, COPILOT_CLI:Y})[claude] = X`, identico al ternario →
output installato **byte-identico** per i 2 assistenti. Ruff clean; **suite completa 1173 passed** (1 xfail
packaging noto); seam test **17/17** (14 esistenti + 3 nuovi). `sertor-core` **non toccato** (è installer-kit).

**Confine:** solo installer package (kit/sertor/sertor-flow), nessun asset host-facing nuovo → nessun
aggiornamento doc utente. **Distill:** meccanismo `select_for` distillato in [[assistant-targeting]] (nuova
sezione «De-binarizzazione del seam») + corretto lì un drift pre-esistente (`AssistantId` elencava `copilot`
VS Code, rimosso in FEAT-012). **Lint semantico:** il drift VS Code sopra (corretto la riga entità; la
narrativa storica VS Code resta come storia). **Explainer:** non innescato (refactor interno).

**File toccati:** `packages/sertor-install-kit/src/.../assistant.py`, `.../__init__.py`,
`packages/sertor-flow/src/.../install_governance.py`, `packages/sertor/src/.../{install_rag,install_wiki,__main__}.py`,
`packages/sertor-install-kit/tests/unit/test_assistant.py`, `wiki/tech/assistant-targeting.md`,
`wiki/syntheses/roadmap.md`, questo log.

**Prossimo:** commit su branch + PR + merge → post-merge. **🎉 Con A-19 il backlog audit A-01..A-20 è
COMPLETO.**

*(PR #174 mergiata su `master` `e6096e4`; CI verde — 3 job test + **4 smoke E2E** (Claude+Copilot ×
ubuntu+windows) tutti pass = **parità confermata empiricamente su host reali**, la prova più forte per un
refactor del seam. Post-merge: re-lock `6032cab→e6096e4` · re-index incrementale 1330doc/14549chunk (10
updated) · smoke MCP verde — `find_symbol("select_for")` → assistant.py:24 (code-graph fresco sul simbolo
NUOVO di A-19), `search_docs` rende la sezione A-19 di [[assistant-targeting]]. Roadmap: A-19 ✅ + bullet
audit → **✅ COMPLETO** A-01..A-20. Ritocchi post-merge viaggiano col commit del prossimo fronte.)*
