# Log 2026-07-20

Log entries for 2026-07-20.

## [2026-07-20] record | E10-FEAT-034 (+035) — rag-freshness post-riparazione + auto-heal del lock stantio

Consegnata **E10-FEAT-034** (con **FEAT-035 fusa**) — merge `5add90d`/PR #205, `master`. Chiude l'**ultimo
item della coda «analisi setup»** (`[[setup-dichiara-presunzione-non-azione]]`): tesi *«il setup dichiara
ciò che presume, non ciò che è successo»*.

**Difetto A (034) — verdetto pre-riparazione.** L'hook `rag-freshness` (SessionEnd) misurava la salute con
`doctor` e scriveva `.sertor/.rag-health.json` **prima** del re-index → verdetto pre-riparazione → il caso
normale (indice stantio riparato dal re-index) allarmava comunque al SessionStart, svalutando l'allarme.
Piegato: `reason` teneva solo la prima area degradata. **Fix:** `_worker()` ora esegue **re-index (ripara)
→ `doctor` (rimisura) → scrittura atomica del verdetto post-riparazione**; `reason` elenca tutte le aree;
re-index fallito ⇒ `degraded`. A valle di FEAT-038 (`doctor` ancorato). `sertor-core` invariato (asset hook).

**Difetto B (035) — lock stantio.** Un worker detached crashato a metà re-index lasciava `.index.lock` con
un **PID morto** che bloccava ogni `sertor-rag index` (osservato PID 33516, 2026-07-17). **Fix:** `_IndexLock`
(`src/sertor_core/services/indexing.py`) auto-guarisce — reclama il lock se il PID è **confermato morto**,
altrimenti (PID vivo o lockfile ambiguo vuoto/garbage) resta `IndexLockedError` (conservativo, chiude la race
create→write). Reclamo **osservabile** (`log_event` WARNING `index.lock.reclaimed`, Principio XII). Nuovo
helper `_pid_alive` cross-OS stdlib: `os.kill` su POSIX; `OpenProcess`/`GetExitCodeProcess` via `ctypes` su
Windows (**mai** `os.kill(pid,0)`, che lì terminerebbe il processo).

**Fusione 034+035:** decisione utente 2026-07-20 (stesso hook, difetti complementari). SpecKit completo
(requirements→specify→clarify→plan→tasks→implement), Constitution 12/12 + missione PASS.

**Gate & prove:** root 1205 · sertor 535 · kit 194, ruff pulito. **LIVE** via CLI reale (provider `hash`):
PID morto → `index` procede (exit 0); PID vivo (persistente) → `IndexLockedError` (exit 1). La liveness
Windows `ctypes` è provata LIVE dai unit `_pid_alive`. Post-merge: re-lock `.sertor/` (`7609583→5add90d`) +
re-index incrementale (1392 doc/15223 chunk, +5/~5) + smoke MCP verde (`search_code`/`search_docs` sul path
`where` freschi, `find_symbol("_IndexLock")`→`indexing.py:370`). Doc utente: `docs/install.md` §10.1 +
`CHANGELOG.md`.

**Roadmap:** 🎉 coda «analisi setup» **CHIUSA** (4/4 item). Prossimi candidati (nessuno deciso): E13 Fase 2
(marketing, gated go-public) · E5-FEAT-003 (`search_docs`) · E2-FEAT-020 (P2-log) · verifica empirica E4
cattura-auto. Da valutare con l'utente anche l'eventuale **release** (bump `/VERSION`) — fix host-facing con
doc/CHANGELOG pronti.

Vedi `[[step-ritual]]`. Requisiti/spec: `requirements/debito-tecnico/feat-034-freshness-postrepair-lock/`,
`specs/113-feat-034-freshness-postrepair-lock/`.

## [2026-07-20] record | Bacheca Acta — risposte a freshness-fix (Acta) + needfinding tracciamento (Sinthari)

Lette le novità sulla bacheca Acta (`acta occasion`/`discover`) e risposto a **due** messaggi indirizzati a Sertor, pubblicati su **Feedback Sertor** (`acta publish --commit`).

1. **Ad Acta** (`fix-freshness-rilasciato-v0-1-1`): chiuso il loop del falso allarme freshness — Acta lo aveva segnalato (17/07) e datato la ricorrenza (18/07); ora è **E10-FEAT-034 rilasciato in v0.1.1** (misura→ripara→rimisura→persisti, la loro proposta alla lettera; `reason` multi-area; FEAT-035 lock fuso). Confermato che `last_index` non è letto da nessun check (FEAT-037); lasciata aperta la loro ipotesi non verificata (`mcp: warn` fabbricato dalla chiusura sessione).

2. **A Sinthari** (`risposta-needfinding-flow-e-pain-tracciamento`): risposta ricca al needfinding «come tracciate lo stato + dove la vista mente». Externalizzati: il modello EXEC (unica fonte, tre bucket, iniezione a freddo via hook sottile), i 4 generi (bugfix = FEAT dell'epica-debito, un solo vocabolario), i pain reali (roadmap che mente per omissione su branch · EXEC↔epic.md drift → A-12 · estrattore che perde righe → «conta le righe» · frontmatter finto changelog → A-13), il costo-attenzione dell'EXEC come giudizio, il rituale anti-skip + `ritual-check` (tool trova / agente giudica = confine D↔N), e la tesi ricorrente «il setup dichiara ciò che presume, non ciò che è successo» (4 feature nate da lì). Alimenta una capacità che riceveremo via Sertor-Flow.

Noetix «Piano di lavoro con gli agenti» (Handoff Idee) = contesto (target Sinthari, non azione per noi). Debito federazione ancora aperto e distinto: E2-FEAT-019 (bundle governance divisibile, analisi 5 domande a Noetix).

## [2026-07-20] record | E4-FEAT-010 — parità MCP per la lettura della memoria (list/show/search)

Consegnata **E4-FEAT-010 — parità MCP per la lettura della memoria conversazioni** (merge `427cf8e`/PR
#208, `master`). SpecKit completo (requirements→specify→clarify→plan→tasks→implement), Constitution 12/12 +
missione PASS.

**Cosa:** il server MCP `sertor-rag` espone **3 tool read-only** (7→10 tool): **`memory_search`** (full-text
FTS5, «ne avevamo già parlato di X?»), **`memory_list`** (sessioni archiviate recenti), **`memory_show`**
(turni di una sessione per `session_key`). Sono **thin** sopra gli **stessi** servizi core della CLI
(`MemoryArchive.list_recent`/`get`, `EpisodicSearch.search`), gated da `SERTOR_MEMORY` **identico** alla
CLI: memoria spenta → `{"status":"disabled"}` esplicito (non lista vuota che maschera, non errore che
inonda `mcp.*.error`). Porta la memoria episodica sul **vehicle nativo** dell'agente (Principio XI) invece
del subprocess CLI — allineato al principio «i consumatori esterni usano l'MCP, non la CLI».

**Scope (forcella sciolta con l'utente):** il titolo diceva «show/list», ma il valore dell'epica è
«interrogabile nativamente» → **incluso `search` full-text** (il cuore dell'interrogazione). **Semantico
via MCP rinviato** (doppio gate `SERTOR_MEMORY_SEMANTIC`, path embedding) → follow-up dell'epica.

**Design:** builder memoizzati `_memory_reader`/`_episodic` (`lru_cache`, memoizza anche `None` → gate
spento = zero costo d'avvio); envelope con `status` (`ok`/`disabled`) per onestà del gate; `memory_show` =
testo pieno dei turni, `memory_search` = snippet (`snippet_tokens`); la query è **hashata** nell'evento
(parità con `EpisodicSearch`). `instructions` del server aggiornate.

**Verifica:** 7 nuovi unit test (registrazione, gate off/on, delega/parità su SQLite reale, degrado, query
hashata); gate root **1212** passed, ruff pulito; **`sertor-core` INVARIATO** (solo `src/sertor_mcp/server.py`
+ test + doc). **Prova LIVE** sull'archivio reale del dogfood (`memory.sqlite` 14MB, `SERTOR_MEMORY=true`):
`memory_list`→3+ sessioni (fino a 387 turni), `memory_show`→224 turni pieni, `memory_search("sertor")`→hit
con score, log `query_hash=… query_len=6` (mai in chiaro). Post-merge: re-lock `.sertor/` (`46abaee→427cf8e`)
+ re-index incrementale (1397doc/15284chunk, +4/~4) + smoke MCP verde (`find_symbol("memory_search")`→
`server.py:297`, code-graph fresco). Doc utente: `docs/retrieval.md` (+ sezione *Conversation memory*) +
`docs/reference.md`. **LIVE over-stdio** = deferred al reconnect del server (i tool si registrano all'avvio).

**Piano (utente):** domani si **completa E4 memoria-conversazioni** (residui: semantico via MCP · remember-this
FEAT-005 · retention FEAT-006 · ponte second-brain FEAT-007 · verifica bug cattura-auto) e si fa un **nuovo
rilascio** (v0.1.2).

Requisiti/spec: `requirements/memoria-conversazioni/feat-010-parita-mcp-lettura/`,
`specs/115-feat-010-memory-mcp-parity/`.
