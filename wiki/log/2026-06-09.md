# Log 2026-06-09

Voci di registro del 2026-06-09.

## [2026-06-09] record | FEAT-009: store_backend disaccoppiato + indice dogfood sertor costruito

Per costruire l'indice di dogfooding del corpus di produzione `sertor` (mai costruito finora) servivano embeddings Azure + store Chroma locale, combinazione non esprimibile su `master` perché lo store era accoppiato a `RAG_BACKEND`. Risolto via SpecKit (lean, FEAT-009) e poi costruito l'indice.

- **Decoupling:** nuova manopola `store_backend` (`SERTOR_STORE_BACKEND`, default = `RAG_BACKEND`, retro-compatibile) distinta dal provider di embeddings; `build_store`/`collection_name` ora vi keyano. Rafforza Principi II e VIII.
- **Endpoint v1:** `AzureEmbedder` rileva `/openai/v1` e omette `api-version` (la v1 lo rifiuta con HTTP 400, verificato con probe live).
- **Branch stale non mergiata** (`feat/decouple-store-backend`): riusato il design, riscritto pulito (la branch regrediva FEAT-008 e rimuoveva `wiki_tools`).
- **Fix d'isolamento test:** `test_mcp_server._facade()` caricava il `.env` reale con `override=True`, inquinando `os.environ` (emerso ora che `.env` ha `RAG_BACKEND=azure`); neutralizzato `Settings.load` in `_use`.
- **Indice dogfood:** corpus `sertor` indicizzato per la 1a volta — 191 doc / 1578 chunk, dim 3072, collezione `sertor__azure_text_embedding_3_large` in `.index-sertor/`.

**Esito:** suite `not cloud` verde (135) + ruff pulito su src/tests; retrieval verificato via facade; PR `spec/009-store-backend-disaccoppiato` → master. Aperto: il server MCP live va riavviato per servire il nuovo indice (gira col codice pre-modifica). Vedi [[store-backend-disaccoppiato-feat009]].

## [2026-06-09] distill | FEAT-009: distill in ports-adapters/corpus-index-naming + lint semantico backend

Distillate le entità durevoli emerse da FEAT-009 dalle pagine evergreen, assottigliando il record datato a evento + esito + puntatori. Incluso il lint semantico di allineamento (claim del wiki ↔ realtà del repo): due pagine elencavano solo due combinazioni di backend (local↔Chroma+Ollama, azure↔AzureSearch+AzureOpenAI), ora superate dal decoupling.

- **[[ports-adapters]]:** la sezione "Il composition root sceglie" ora descrive le **due manopole distinte** (embedder da `backend`, store da `store_backend`) e la loro combinabilità (es. embeddings Azure + Chroma locale).
- **[[corpus-index-naming]]:** riga backend del prodotto corretta (tre combinazioni reali); aggiunti la collezione reale `sertor__azure_text_embedding_3_large` e la voce di storico "indice dogfood costruito" (2026-06-09).
- **CLAUDE.md:** corretta la regola architetturale che accoppiava store ed embeddings (era `azure→Azure AI Search+Azure OpenAI`).

**Esito:** record [[store-backend-disaccoppiato-feat009]] resta magro; nessun claim del wiki contraddice più la realtà del repo sul tema backend/indice.

## [2026-06-09] record | Roadmap di prodotto come pagina viva (commit)

Committata la pagina viva [[roadmap]] (`wiki/syntheses/roadmap.md`), riscritta su `master` e ancorata allo stato reale, finora non versionata. Punto d'ingresso unico per "dove siamo": mappa feature dell'epica × stato reale (✅ master / 💀 ramo morto / 🔜 / 💤), lavori abilitanti mergiati (incl. FEAT-009 indice dogfood), fasi, backlog e sezione *Nuove funzionalità da discutere*. Riconcilia esplicitamente le **due numerazioni** (epica `FEAT-NNN` ≠ `specs/NNN`).

- **`wiki/index.md`:** aggiunto il rimando in evidenza alla roadmap (callout in testa a *Pagine* + voce in *Syntheses*).
- Lint strutturale pulito; wikilink ai record per-feature già esistenti.

**Esito:** la roadmap entra in `master` come quadro d'insieme manutenibile. Restano aperte due questioni da discutere col flusso principale: cosa manca a FEAT-003 (automazione e2e FEAT-010, stranded su ramo morto `spec/005`) e l'orchestrazione automatica al commit.
