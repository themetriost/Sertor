# Log 2026-06-09

Voci di registro del 2026-06-09.

## [2026-06-09] record | FEAT-009: store_backend disaccoppiato + indice dogfood sertor costruito

Per costruire l'indice di dogfooding del corpus di produzione `sertor` (mai costruito finora) servivano embeddings Azure + store Chroma locale, combinazione non esprimibile su `master` perché lo store era accoppiato a `RAG_BACKEND`. Risolto via SpecKit (lean, FEAT-009) e poi costruito l'indice.

- **Decoupling:** nuova manopola `store_backend` (`SERTOR_STORE_BACKEND`, default = `RAG_BACKEND`, retro-compatibile) distinta dal provider di embeddings; `build_store`/`collection_name` ora vi keyano. Rafforza Principi II e VIII.
- **Endpoint v1:** `AzureEmbedder` rileva `/openai/v1` e omette `api-version` (la v1 lo rifiuta con HTTP 400, verificato con probe live).
- **Branch stale non mergiata** (`feat/decouple-store-backend`): riusato il design, riscritto pulito (la branch regrediva FEAT-008 e rimuoveva `wiki_tools`).
- **Fix d'isolamento test:** `test_mcp_server._facade()` caricava il `.env` reale con `override=True`, inquinando `os.environ` (emerso ora che `.env` ha `RAG_BACKEND=azure`); neutralizzato `Settings.load` in `_use`.
- **Indice dogfood:** corpus `sertor` indicizzato per la 1a volta — 191 doc / 1578 chunk, dim 3072, collezione `sertor__azure_text_embedding_3_large` in `.index-sertor/`.

**Esito:** suite `not cloud` verde (135) + ruff pulito su src/tests; retrieval verificato via facade; PR `spec/009-store-backend-disaccoppiato` → master. Aperto: il server MCP live va riavviato per servire il nuovo indice (gira col codice pre-modifica). Vedi [[store-backend-disaccoppiato-feat009]].

## [2026-06-09] distill | FEAT-009: distill in ports-adapters/corpus-index-naming + lint semantico backend

Distillate le entità durevoli emerse da FEAT-009 dalle pagine evergreen, assottigliando il record datato a evento + esito + puntatori. Incluso il lint semantico di allineamento (claim del wiki ↔ realtà del repo): due pagine elencavano solo due combinazioni di backend (local↔Chroma+Ollama, azure↔AzureSearch+AzureOpenAI), ora superate dal decoupling.

- **[[ports-adapters]]:** la sezione "Il composition root sceglie" ora descrive le **due manopole distinte** (embedder da `backend`, store da `store_backend`) e la loro combinabilità (es. embeddings Azure + Chroma locale).
- **[[corpus-index-naming]]:** riga backend del prodotto corretta (tre combinazioni reali); aggiunti la collezione reale `sertor__azure_text_embedding_3_large` e la voce di storico "indice dogfood costruito" (2026-06-09).
- **CLAUDE.md:** corretta la regola architetturale che accoppiava store ed embeddings (era `azure→Azure AI Search+Azure OpenAI`).

**Esito:** record [[store-backend-disaccoppiato-feat009]] resta magro; nessun claim del wiki contraddice più la realtà del repo sul tema backend/indice.

## [2026-06-09] record | Roadmap di prodotto come pagina viva (commit)

Committata la pagina viva [[roadmap]] (`wiki/syntheses/roadmap.md`), riscritta su `master` e ancorata allo stato reale, finora non versionata. Punto d'ingresso unico per "dove siamo": mappa feature dell'epica × stato reale (✅ master / 💀 ramo morto / 🔜 / 💤), lavori abilitanti mergiati (incl. FEAT-009 indice dogfood), fasi, backlog e sezione *Nuove funzionalità da discutere*. Riconcilia esplicitamente le **due numerazioni** (epica `FEAT-NNN` ≠ `specs/NNN`).

- **`wiki/index.md`:** aggiunto il rimando in evidenza alla roadmap (callout in testa a *Pagine* + voce in *Syntheses*).
- Lint strutturale pulito; wikilink ai record per-feature già esistenti.

**Esito:** la roadmap entra in `master` come quadro d'insieme manutenibile. Restano aperte due questioni da discutere col flusso principale: cosa manca a FEAT-003 (automazione e2e FEAT-010, stranded su ramo morto `spec/005`) e l'orchestrazione automatica al commit.

## [2026-06-09] record | Decisione D-18: rimozione per design di `manual_edited/` e `ingested_sources/`

Discutendo cosa manca alla feature Wiki (FEAT-003), decisione dell'utente di **semplificare lo scope**: eliminare le due convenzioni a cartelle-input `manual_edited/` (autoring umano immutabile) e `ingested_sources/` (import esterno non versionabile). Motivazione: semplificazione + **allineamento alla realtà** — il wiki di produzione aveva già consolidato la tassonomia in `sources/` (vedi [[sistema-wiki-fonte-unica]]), quindi le due cartelle sopravvivevano solo come *requisito pianificato*, non come realtà. La rimozione **chiude il divario piano↔realtà**, non è una regressione.

- **Modello risultante:** ingest torna alla semantica Karpathy (riassunto in `sources/`, FEAT-003 Gruppo C / REQ-020..023 riattivati); autoring umano nelle pagine normali; retrieval semplificato (indicizzato = wiki generato incluso `sources/` + codice, nessuna cartella-input da escludere); autorità sul "perché" = discussioni/SpecKit.
- **Metodo:** non cancellazione silenziosa ma marcatura esplicita `⛔ DELETED BY DESIGN` con motivazione (pattern di supersession già usato dal doc per i Gruppi C/E), più decisione canonica **D-18** che indicizza l'intero impatto.
- **Artefatti aggiornati:** `requirements/sertor-core/wiki-creazione/requirements.md` (D-18 + banner su D-1/D-6/D-11, semplifica D-4/D-5/D-7/D-9, elimina FR-007/015/016/020/021/030/031, riformula FR-001/009/012/017/022/023, SC-002 obsoleto, SC-010 e R-06, glossario/scope/MoSCoW/assunzioni, changelog); `requirements/sertor-core/wiki-llm/TODO.md` (riga N4). Memoria di sessione allineata.

**Esito:** lo scope della feature Wiki è più snello e ancorato alla realtà; nessun changelog/log storico riscritto (registrano la decisione di allora). Discriminante emersa prima della decisione: l'automazione e2e *con LLM* è fattibile solo via `claude -p` headless o standing-behavior in-loop (un hook non avvia un agente) — ma la gran parte di ex-FEAT-010 è codice D senza LLM.

## [2026-06-09] record | Decisione D-19: trigger del wiki = comando manuale `/wiki`, ambito ultimo commit

Chiusa la domanda aperta DA-FR004 (trigger del popolamento del wiki). Decisione dell'utente: trigger = **comando manuale `/wiki`** (scartati hook automatico e `claude -p` headless); ambito = **changeset dell'ultimo commit** (`git diff HEAD~1`); il **cosa modificare** è responsabilità della parte N (giudizio LLM) guidata da page-craft, wiki-craft e playbook. Mai rebuild completo.

- **Confine D/N confermato:** la parte D calcola il diff dell'ultimo commit (configuration-manager come fornitore di diff, non più invocatore); la parte N decide pagine/contenuti.
- **Superato il modello automatico via binding** (D-8 invocazione-al-commit, FR-026/027/028, D-16 binding, FR-040, SC-008, R-03): marcati `⛔` o riformulati in `requirements/sertor-core/wiki-creazione/requirements.md` (decisione canonica D-19); allineati FR-004/018/037/025, dipendenze, assunzioni, MoSCoW, D-13/D-14.
- **Nuova questione aperta DA-GATE:** il gate "blocca il commit" (D-17/FR-041/042/SC-004) non è coerente con un trigger manuale che gira *dopo* il commit; da decidere se eliminarlo, renderlo report non bloccante o pre-commit.

**Esito:** modello di trigger del wiki semplificato e coerente (manuale + incrementale sull'ultimo commit), allineato alla scelta "calibra al valore". Resta da sciogliere DA-GATE prima di implementare il gate.

## [2026-06-09] record | Decisione D-20: eliminato il gate al commit del wiki

Sciolta DA-GATE (opzione a). Il "gate al commit" (D-17) bloccava il commit se lint/freschezza superavano una soglia: presupponeva una generazione automatica al commit. Con D-19 il wiki si aggiorna con `/wiki` *dopo* il commit, quindi non c'è un commit da bloccare. Decisione dell'utente: **eliminare il gate**.

- **Superati** in `requirements/sertor-core/wiki-creazione/requirements.md`: D-17, FR-041, FR-042, SC-004 (marcati `⛔`/obsoleti); allineati scope §5 item 8, RNF-003 (soglia gate), R-01 (mitigazione), MoSCoW, D-14. Decisione canonica D-20.
- **Cosa resta:** lint + verifica di freschezza (FR-035/036/037) come **parte non bloccante** di `/wiki` — eseguono e riportano i problemi; l'utente decide se intervenire (human-in-the-loop, niente auto-fix).

**Esito:** modello del wiki coerente end-to-end col trigger manuale; nessuna domanda aperta residua su trigger/gate. Lo scope della feature Wiki è ora: generazione via `/wiki` (incrementale sull'ultimo commit, N decide il contenuto) + lint/freschezza non bloccanti + collezioni separate con query congiunta + `sertor wiki init` + ingest→`sources/`.

## [2026-06-09] record | `generate-from-diff` marcata COMPLETA come procedura (N8)

Decisione dell'utente: l'aggiornamento incrementale del wiki dalle modifiche si fa con l'operazione **`generate-from-diff`** così com'è — **procedura guidata dal flusso principale, senza codice nuovo**. Non si automatizza il resolver "file cambiato → pagine impattate"; quella mappatura resta giudizio.

- Cosa fa: scan + `git diff` (parte D, git delegato al configuration-manager) → aggiorna solo le pagine impattate (parte N, conforme a page-craft) → aggiorna `index.md` + voce di log `generate-from-diff` (parte D). Trigger = `/wiki` (D-19); lint/freschezza non bloccanti (D-20).
- **Marcata ✅** in `requirements/sertor-core/wiki-llm/TODO.md` (N8 = orchestrazione/come l'agente popola) e in `.claude/skills/wiki-author/ops/generate-from-diff.md`. Contestualmente: **N7 (gate) marcato ⛔ DELETED BY DESIGN** (D-20) e **domanda aperta FR-004 chiusa** (D-19) nel tracker.

**Esito:** il tracker FEAT-003-N è allineato alle decisioni D-19/D-20; N8 completa come procedura, N7 eliminato. Restano aperte le operazioni di contenuto (N1 da esercitare, N3, N4, N6) e i pezzi codice D (collezioni separate+query congiunta, `wiki init`, write-back index in CLI).

## [2026-06-09] lint | Allineamento wiki↔decisioni D-18/D-19/D-20 + roadmap

Lint semantico prima del push: le decisioni di oggi avevano reso stale alcune pagine evergreen (deriva creata da noi → correzione pre-approvata). Corretto:
- **[[architettura-wiki-llm]]**: roadmap interna e tabella aggiornate — 2a (FR-004 trigger) ✅ risolta (D-19), N7 gate ⛔ deleted (D-20), N4 ingest→`sources/` (D-18), N8 `generate-from-diff` completa; "prossimo passo" non parla più di "indice da costruire" (già costruito, FEAT-009).
- **[[lint-semantico-host-agnostico]]**: il trigger di B non è più "differito (FR-004)" ma comando manuale `/wiki` (D-19), non bloccante (D-20).
- **[[roadmap]]**: riga FEAT-003 annota lo scope snellito (D-18/19/20).

Verificato con `sertor-wiki-tools lint`: 0 broken/orphan/frontmatter/naming/stub. I record datati (log, `experiments/ponte-d-n`) non riscritti: sono storia.

**Esito:** wiki e roadmap coerenti con i requisiti aggiornati; pronti per push su origin/master.

## [2026-06-09] record | Esercizio N1 (page-craft) sulle 10 pagine evergreen più deboli

Esercizio dell'operazione N1 (record-contenuto / page-craft) richiesto dall'utente: migliorare le 10 pagine evergreen che ne avevano più bisogno. Triage delegato a 3 valutatori read-only (sonnet, non Haiku: è giudizio) che hanno scorato `concepts/`/`tech/`/`syntheses/` contro `page-craft.md` ancorando i claim al codice reale; i fatti chiave riverificati a mano (composition.py, dispatch.py, baseline.py, settings.json, wiki.config.toml). Le 10 pagine riscritte/corrette:

- **step-ritual** (score 4): distillato il concetto evergreen, espunto il diario datato (→ [[retrospettiva-interazione-2026-06-04]]), corretto `wiki/log.md`→`wiki/log/<data>.md`.
- **sistema-wiki-fonte-unica** (4): riscritta come sintesi evergreen; tolti "File toccati"/"Prossimi step"; corretti nomi stantii (skill `generate-wiki`→`wiki-author`, hook-key inesistenti, 6→9 operazioni).
- **sessionstart-hook** (3): niente più trascrizione verbatim del comando (deriva); documentato il **gap reale** (legge `wiki/log.md` inesistente → coda log no-op).
- **retrieval-core** (3): mapping backend corretto in **due manopole** (`embed_provider`/`store_backend`).
- **chunking-dispatch** (3): documentato il mapping `symbol_kind`→`node_type` ai due lati del dispatch.
- **wiki-role-da-w1** (2): rimosso il claim "GIÀ ATTIVO" impreciso (il dogfood `sertor` non indicizza `wiki/`; wiki = corpus `rag-sync` separato).
- **corpus-index-naming** (2): tolti storico-diario, footer di stato, snippet di config copiati.
- **constitution** (2): titolo de-versionato; corretto "9 principi"→10; framing atemporale.
- **wiki-tools** (2): corretta la conflazione `append-log`/`upsert_index` (la riga di `index.md` resta LLM-authored; `upsert_index` non è cablato in CLI).
- **vector-retrieval** (2): firma corretta `query(query, k=None)`; rimosse frasi al futuro.

Verificato con `sertor-wiki-tools lint`: 0 broken/orphan/frontmatter/naming/stub.

**Esito:** 10 pagine evergreen ancorate al codice reale e ripulite da diario/stato volatile; N1 esercitato su casi reali (la prova che mancava per consolidarlo). **Bug collaterale trovato (fuori scope, da decidere):** il SessionStart hook in `.claude/settings.json` legge ancora `wiki/log.md` (rimosso dalla rotazione FEAT-008) → non inietta più la coda del log.

## [2026-06-09] record | Fix del SessionStart hook: legge la partizione di log più recente

Risolto il bug emerso nell'esercizio N1: il SessionStart hook (`.claude/settings.json`) leggeva il path fisso `wiki/log.md`, inesistente dalla rotazione FEAT-008 → la coda del log non veniva più iniettata a inizio sessione (restava solo `index.md`).

- **Fix:** il comando ora seleziona la **partizione più recente** di `wiki/log/` (elenca `*.md`, esclude `index.md`, ordina per nome = ordine cronologico per `YYYY-MM-DD`, prende l'ultimo) con fallback al vecchio `wiki/log.md`. Implementato **senza regex** per non incappare nell'escaping dei backslash nel JSON (un primo tentativo con regex aveva rotto la validazione di `settings.json`).
- **Verificato:** il comando seleziona `wiki/log/2026-06-09.md` e ne stampa la coda.
- Allineata la pagina [[sessionstart-hook]] (sezione "Compatibilità con la rotazione del log").

**Esito:** a inizio sessione tornano a essere iniettati sia l'indice sia la coda del log corrente. Aperto: l'utente segnalerà ulteriori migliorie sullo startup.

## [2026-06-09] record | Executive summary della roadmap iniettata a inizio sessione

Migliorìa allo startup richiesta dall'utente: a inizio sessione il contesto si apre con un **riassunto executive** dello stato di prodotto (IN PROGRESS con dettagli + PLANNED + DONE). Implementate le tre parti che reggono la cosa:

- **Governance (`CLAUDE.md`):** nuova azione standing del rituale (punto 4) — il flusso principale tiene vero un blocco executive in `wiki/syntheses/roadmap.md` tra i marker `<!-- EXEC:START/END -->`; aggiornato nello stesso commit ogni volta che uno step cambia lo stato di una capacità; è giudizio → resta su Opus; non deve contraddire la mappa-feature sotto.
- **Contenuto ([[roadmap]]):** creato il blocco EXEC con lo stato reale al 2026-06-09 (IN PROGRESS = migliorie startup; PLANNED = pezzi codice D del wiki, operazioni N, nuovi motori RAG, CLY; DONE = nucleo/baseline/MCP/wiki/lavori abilitanti).
- **Trasporto (`.claude/settings.json`):** il SessionStart hook estrae il blocco tra i marker (`Select-String -SimpleMatch`, niente regex per non rompere il JSON) e lo stampa **per primo**, prima di indice e coda log; degrada in silenzio se i marker mancano.

**Verificato:** l'estrazione stampa il blocco corretto; lint strutturale 0/0/0/0/0. L'iniezione vera si vedrà al prossimo avvio/resume di sessione.

**Esito:** lo startup ora apre con "dove siamo e cosa fare adesso". Confine netto: l'hook trasporta, il rituale tiene il contenuto vero. Aperto: altre migliorie allo startup che l'utente segnalerà.

## [2026-06-09] record | Startup: tabella roadmap + dettaglio IN PROGRESS

Riscritto il blocco executive (`EXEC:START/END`) in `wiki/syntheses/roadmap.md` per cambiare la forma
dell'iniezione a inizio sessione, su richiesta dell'utente. Ora il blocco contiene: (1) una **tabella
"Roadmap a colpo d'occhio"** (Capacità · Pri · Stato, con legenda) e (2) una sezione **IN PROGRESS
(dettaglio)** con cosa/dove/prossimo passo/blocco; PLANNED e DONE restano in coda in forma sintetica.
L'hook SessionStart non è stato toccato: estrae e stampa il blocco verbatim, quindi il contenuto è
l'unica leva. Estrazione simulata col medesimo comando dell'hook: output conforme.

## [2026-06-09] record | Startup ridisegnato: hook sottile (direttiva-Read) supera il cap 10K del canale-hook

Lo startup non funzionava: l'utente ha segnalato che a inizio sessione non si caricava il contesto. Diagnosi: il SessionStart hook *stampava* su stdout index.md (~12 KB) + executive roadmap + coda log (~17 KB totali), ma il canale-hook e' limitato a ~10.000 caratteri -> l'harness salvava l'output su file e iniettava solo un preview da ~2 KB. Quindi entrava in contesto solo mezza roadmap, niente indice ne' log. La causa NON era il contesto della sessione (1M token), ma il cap del canale-hook.

- **Ridisegno (opzione A, scelta dall'utente):** l'hook diventa *sottile* e non trasporta piu' il payload. Emette una direttiva (~630 B) che istruisce il flusso principale a caricare i file col tool `Read` (`roadmap.md`, `index.md`, ultima partizione di `wiki/log/`) -- il cui output entra INTERO nel contesto, nessun cap -- e poi a mostrare all'utente l'executive summary della roadmap. Catena: l'hook *innesca*, il `Read` *trasporta*, il rituale tiene il *contenuto* vero.
- **Verificato:** comando del hook estratto da settings.json ed eseguito -> 5 righe, 632 byte, JSON valido. Eseguita la procedura in-sessione (Read dei 3 file + display della roadmap).
- **Derive create corrette (pre-approvato):** `CLAUDE.md` punto 4 (descrizione iniezione: hook sottile, non trasporta) · pagina [[sessionstart-hook]] ("Cosa fa" + nuova sezione sul cap 10K + sezione rotazione log) · blocco EXEC di [[roadmap]] (IN PROGRESS aggiornato col ridisegno).

**Esito:** lo startup carica il contesto via `Read` senza incappare nella troncatura del canale-hook. Lint strutturale 0/0/0/0/0. Verifica finale dell'iniezione vera al prossimo avvio/resume.

## [2026-06-09] record | Startup di sessione confermato e chiuso e' DONE

Confermato in sessione che lo startup funziona: l'hook SessionStart sottile (direttiva-Read) ha iniettato la direttiva, il flusso principale ha caricato roadmap/index/log con `Read` (output intero, nessun cap) e mostrato l'executive summary. La migliorìa-startup, unica voce IN PROGRESS, è quindi chiusa.

- **Blocco EXEC ([[roadmap]]):** spostata la migliorìa-startup da 🔄 IN PROGRESS a ✅ DONE (riga "Startup di sessione: hook sottile direttiva-Read, supera il cap ~10K"); IN PROGRESS ora vuoto (placeholder "scegliere il prossimo PLANNED").
- **Frontmatter:** `updated` annota la chiusura.
- Lint strutturale 0/0/0/0/0.

**Esito:** nessuna capacità in lavorazione; il prossimo step parte da un PLANNED. La verifica dell'iniezione, prima rimandata "al prossimo avvio reale", è avvenuta proprio in questa sessione.

## [2026-06-09] record | Prossimo step deciso: mini-SpecKit pezzi D 1+3 del wiki; pezzo 2 in epica CLI

Deciso il prossimo step con l'utente: i due pezzi deterministici (D) rimasti della feature Wiki (FEAT-003) si fanno con **un unico mini-SpecKit lean**, perché entrambi deterministici. Il terzo pezzo (`sertor wiki init`) è spostato nell'epica CLI. Marcati prioritari al riavvio nella roadmap.

- **Pezzo (1) — Query congiunta multi-collezione** *(vero contenuto ingegneristico):* wiki e codice sono già in collezioni RAG separate (namespacing per `(corpus, provider)`), ma `RetrievalFacade.search_combined()` (`services/retrieval.py`) gira su una sola collezione (il filtro `both` discrimina per `doc_type` dentro la collezione). Serve fan-out su 2 collezioni + merge dei top-k.
- **Pezzo (3) — Esporre `upsert-index` in CLI** *(piccolo):* `upsert_index()` esiste già (`wiki_tools/registry.py`) ma non è cablato in `__main__.py`. Aggiungere il sottocomando sul modello di `append-log`. Chiarito con l'utente: il **sommario resta LLM-authored** (giudizio); la CLI fa solo il **write idempotente**, ricevendo il testo come argomento — non si delega la generazione del contenuto a codice.
- **Pezzo (2) — `sertor wiki init`:** fuori da questo mini-SpecKit → epica CLI. La capacità `init_structure` esiste già (`sertor-wiki-tools structure init`); manca solo l'esposizione sotto la CLI top-level `sertor`, che non esiste (ramo morto). È lavoro-CLI, non lavoro-wiki.

**Aggiornati:** blocco EXEC di [[roadmap]] (PLANNED → voce "⏯️ PROSSIMO" con i due pezzi + nota CLI per il pezzo 2) e il puntatore "RIPRENDI QUI" in memoria. Lint strutturale 0/0/0/0/0.

**Esito:** prossima sessione parte con il mini-SpecKit dei pezzi D 1+3 già inquadrato e ancorato al codice reale.

## [2026-06-09] record | Correzione: i pezzi D 1+3 del wiki via SpecKit completo (non lean)

Correzione della decisione di poco fa: i due pezzi D del wiki (FEAT-003, query congiunta + `upsert-index` in CLI) si faranno con **SpecKit completo**, non con un "mini-SpecKit lean". Niente scorciatoie sul flusso: requirements→specify→clarify→plan→tasks→analyze→implement.

- Allineati il blocco EXEC di [[roadmap]] (voce "⏯️ PROSSIMO" + riga di sintesi) e il puntatore "RIPRENDI QUI" in memoria. Lint strutturale 0/0/0/0/0.

**Esito:** la pianificazione del prossimo step indica chiaramente SpecKit completo.
