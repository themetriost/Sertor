# Log 2026-06-17

Log entries for 2026-06-17.

## [2026-06-17] record | Decomposizione FEAT-001 packaging distribuibile + risolte DA-P1..P4

Decomposta a requisiti EARS la parte packaging/distribuzione di FEAT-001 della CLI (epica `sertor-cli`) — unica casella Must rimasta nella subepica dopo che feature `esecuzione`, `install rag` e `install governance` sono state consegnate. Verifica ground-truth confermata: 4 pacchetti buildano OK, wheel include correttamente gli asset, metadati YAML dichiarano MIT ma manca LICENSE e project-URL/classifiers bundlati. Risolte 4 domande aperte storiche (DA-P1..P4): versione unica allineata · `uv` primario + `pip` best-effort documentato (non gate Must) · `sertor-core`/`sertor-install-kit` sono sole dipendenze interne (scelta utente vs raccomandazione) · install-kit build-validato ma esonerato dai metadati user-facing.
- **Ambito:** formalizzare distribuzione interim `git+url` come percorso di prima classe — file LICENSE MIT mancante, metadati completi (urls/classifiers/keywords), validazione build (`uv build` → sdist+wheel), install pulito a un comando (uv & pip), documentazione. PyPI fuori ambito (FEAT-006).
- **File creato:** `requirements/sertor-cli/packaging-distribuibile/requirements.md` (18 REQ EARS, 5 DA, MoSCoW completo).
- **Pagine aggiornate:** `requirements/sertor-cli/epic.md` (FEAT-001 riga 101 marcata "pronta per `/speckit-specify`") · `wiki/syntheses/roadmap.md` (EXEC motore/capacità, sezione PLANNED).
- **Verification:** 4 DA risolte, packaging decomposto OK, pronto per il workflow SpecKit.

## [2026-06-17] record | FEAT-001 packaging distribuibile git+url CONSEGNATA (PR #68)

Pipeline SpecKit completa specify→implement (branch `047-packaging-distribuibile`, merge `cd3a809`, PR #68). Chiude l'UNICA casella Must rimasta dell'epica `sertor-cli`.
- **Cosa:** distribuzione interim `git+url` resa percorso di prima classe. File LICENSE MIT in radice + ogni pacchetto (incluso nelle wheel via PEP 639); versione unica da `/VERSION` (dynamic hatchling) sui 4 pyproject; metadati distribuzione (urls/classifiers/keywords) su `sertor`/`sertor-flow` (install-kit esonerato DA-P4); suite `tests/integration/test_packaging.py` (stdlib+subprocess, no import sertor_core, marker integration): licenza→metadati→build→install pulito reale.
- **Decisioni DA-P1..P4:** versione unica allineata; `uv` primario+gate / `pip` best-effort documentato (→FEAT-010); `sertor-core`/`install-kit` = dipendenze interne; install-kit build-validato esonerato dai metadati user-facing.
- **Verifiche:** `uv build` ×4 verde; install pulito `uv` validato live (2 passed), `pip` xfail documentato; 613 non-cloud verdi; Constitution 11/11. PyPI resta FEAT-006 (Won't).

## [2026-06-17] record | Robustezza chunking: tetto del chunk al budget token dell'embedder (PR #69 + #70)

Bug emerso dal DOGFOODING del re-index post-merge. I chunker strutturali (markdown per heading, codice per simbolo) potevano emettere un chunk oltre il limite dell'embedder: una sezione enorme del `CLAUDE.md` (8357 token) faceva fallire l'indicizzazione con `http 400 retriable=False` da text-embedding-3-large ("maximum input length is 8192 tokens"), bloccando OGNI re-index del corpus.
- **Diagnosi:** prima ipotesi (chunk vuoto) smentita ottenendo il body reale del 400; vera causa = chunk troppo lungo. Sonde dirette all'endpoint Azure per isolare (la query embeddava, l'indicizzazione no).
- **Fix:** due step — PR #69 cap iniziale (a caratteri); PR #70 cap in TOKEN (`SERTOR_MAX_CHUNK_TOKENS`, default 8191 — usa la finestra piena del large), conteggio/split preciso con tiktoken (cl100k_base) dietro l'extra opzionale `tokenizer` (import lazy) + fallback conservativo per carattere offline-safe (Principio II); `_logic_version` aggiornato (FR-013). Nuovo `src/sertor_core/services/chunking/_tokens.py`.
- **Verifiche:** max chunk del corpus ora 8191 token (era 8357 → 400), 0 oversized; 581 non-cloud verdi; ruff pulito. Corpus dogfood re-indicizzato pulito (incremental, 692 doc/6733 chunk, cache_hits 6715).
- **Effetto collaterale prezioso:** l'indicizzazione incrementale FEAT-009 si è confermata CORRETTA end-to-end (ha rilevato i file cambiati e ne ha embeddato i chunk; il manifest `sertor.manifest/1` con 680+ file era popolato e funzionante).
