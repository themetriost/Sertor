# Log 2026-06-17

Log entries for 2026-06-17.

## [2026-06-17] record | Decomposizione FEAT-001 packaging distribuibile + risolte DA-P1..P4

Decomposta a requisiti EARS la parte packaging/distribuzione di FEAT-001 della CLI (epica `sertor-cli`) — unica casella Must rimasta nella subepica dopo che feature `esecuzione`, `install rag` e `install governance` sono state consegnate. Verifica ground-truth confermata: 4 pacchetti buildano OK, wheel include correttamente gli asset, metadati YAML dichiarano MIT ma manca LICENSE e project-URL/classifiers bundlati. Risolte 4 domande aperte storiche (DA-P1..P4): versione unica allineata · `uv` primario + `pip` best-effort documentato (non gate Must) · `sertor-core`/`sertor-install-kit` sono sole dipendenze interne (scelta utente vs raccomandazione) · install-kit build-validato ma esonerato dai metadati user-facing.
- **Ambito:** formalizzare distribuzione interim `git+url` come percorso di prima classe — file LICENSE MIT mancante, metadati completi (urls/classifiers/keywords), validazione build (`uv build` → sdist+wheel), install pulito a un comando (uv & pip), documentazione. PyPI fuori ambito (FEAT-006).
- **File creato:** `requirements/sertor-cli/packaging-distribuibile/requirements.md` (18 REQ EARS, 5 DA, MoSCoW completo).
- **Pagine aggiornate:** `requirements/sertor-cli/epic.md` (FEAT-001 riga 101 marcata "pronta per `/speckit-specify`") · `wiki/syntheses/roadmap.md` (EXEC motore/capacità, sezione PLANNED).
- **Verification:** 4 DA risolte, packaging decomposto OK, pronto per il workflow SpecKit.

## [2026-06-17] record | FEAT-001 packaging distribuibile git+url CONSEGNATA (PR #68)

Pipeline SpecKit completa specify→implement (branch `047-packaging-distribuibile`, merge `cd3a809`, PR #68). Chiude l'UNICA casella Must rimasta dell'epica `sertor-cli`.
- **Cosa:** distribuzione interim `git+url` resa percorso di prima classe. File LICENSE MIT in radice + ogni pacchetto (incluso nelle wheel via PEP 639); versione unica da `/VERSION` (dynamic hatchling) sui 4 pyproject; metadati distribuzione (urls/classifiers/keywords) su `sertor`/`sertor-flow` (install-kit esonerato DA-P4); suite `tests/integration/test_packaging.py` (stdlib+subprocess, no import sertor_core, marker integration): licenza→metadati→build→install pulito reale.
- **Decisioni DA-P1..P4:** versione unica allineata; `uv` primario+gate / `pip` best-effort documentato (→FEAT-010); `sertor-core`/`install-kit` = dipendenze interne; install-kit build-validato esonerato dai metadati user-facing.
- **Verifiche:** `uv build` ×4 verde; install pulito `uv` validato live (2 passed), `pip` xfail documentato; 613 non-cloud verdi; Constitution 11/11. PyPI resta FEAT-006 (Won't).

## [2026-06-17] record | Robustezza chunking: tetto del chunk al budget token dell'embedder (PR #69 + #70)

Bug emerso dal DOGFOODING del re-index post-merge. I chunker strutturali (markdown per heading, codice per simbolo) potevano emettere un chunk oltre il limite dell'embedder: una sezione enorme del `CLAUDE.md` (8357 token) faceva fallire l'indicizzazione con `http 400 retriable=False` da text-embedding-3-large ("maximum input length is 8192 tokens"), bloccando OGNI re-index del corpus.
- **Diagnosi:** prima ipotesi (chunk vuoto) smentita ottenendo il body reale del 400; vera causa = chunk troppo lungo. Sonde dirette all'endpoint Azure per isolare (la query embeddava, l'indicizzazione no).
- **Fix:** due step — PR #69 cap iniziale (a caratteri); PR #70 cap in TOKEN (`SERTOR_MAX_CHUNK_TOKENS`, default 8191 — usa la finestra piena del large), conteggio/split preciso con tiktoken (cl100k_base) dietro l'extra opzionale `tokenizer` (import lazy) + fallback conservativo per carattere offline-safe (Principio II); `_logic_version` aggiornato (FR-013). Nuovo `src/sertor_core/services/chunking/_tokens.py`.
- **Verifiche:** max chunk del corpus ora 8191 token (era 8357 → 400), 0 oversized; 581 non-cloud verdi; ruff pulito. Corpus dogfood re-indicizzato pulito (incremental, 692 doc/6733 chunk, cache_hits 6715).
- **Effetto collaterale prezioso:** l'indicizzazione incrementale FEAT-009 si è confermata CORRETTA end-to-end (ha rilevato i file cambiati e ne ha embeddato i chunk; il manifest `sertor.manifest/1` con 680+ file era popolato e funzionante).

## [2026-06-17] record | Documentazione d'installazione: quickstart per-assistente (Claude / Copilot)

Guide d'installazione rapide separate per Claude e Copilot, ricablando l'ingresso nel README per dare priorità ai quickstart brevi rispetto alla documentazione completa di riferimento.
- **Pagine create:** `docs/install-claude.md` e `docs/install-copilot.md` — tre sezioni per guide (RAG, Wiki, Governance/SDLC), forma `uvx git+url`, happy-path conciso.
- **Guida Copilot:** supporta target `--assistant copilot` (VS Code → `.vscode/mcp.json`) e `copilot-cli` (CLI → `.mcp.json`); nota che `sertor-flow` supporta solo `claude|copilot` (no copilot-cli, follow-up).
- **Ingresso ricablato:** README «Installation» reindirizza ai due quickstart; banner in cima a `docs/install.md` che rimanda ai quickstart.
- **Verifiche:** doc-only, non distruttivo; nessun cambiamento di codice o comportamento.

## [2026-06-17] record | Convenzione: uv.lock ora versionato (riproducibilità)

Decisione versionamento: il lockfile `uv.lock` (finora untracked) passa a **committato**. Razionale: Sertor è un prodotto installabile (CLI `sertor`/`sertor-flow`, console-script) → lock garantisce riproducibilità (versioni esatte transitive su CI/macchine esterne/dogfood) e integrità hash. Coerente con la mission di framework production-grade. Rilevante ora con `pyproject` che include `tiktoken` (extra `tokenizer`, PR #70): senza lock un'altra macchina potrebbe risolvere una versione diversa.
- **Decisione:** inizia il versionamento di `uv.lock` su tutti i rami; i cambi dipendenze saranno visibili nel diff.
- **Verifica:** `uv lock --check` verde pre-commit (113 pacchetti sincronizzati con `pyproject.toml`).

## [2026-06-17] record | Chiuse Q1–Q4 FEAT-008 lifecycle installer (+ sertor-flow in ambito)

Chiuse tutte e 4 le domande aperte della decomposizione FEAT-008 "ciclo di vita installer" (upgrade e uninstall di sertor/sertor-flow). Decisioni assunte:
- **Q1 (a):** wiki NON rimosso di default da `uninstall wiki`; richiede flag `--purge-wiki` + `--yes`, non combinabile con `--dry-run`.
- **Q2 (a):** tracciatura artefatti obsoleti via diff a posteriori (lista statica di path Sertor-owned nei plan-builder), NESSUN manifest persistente.
- **Q3 (c):** `sertor uninstall` supporta forma all-in-uno (rimuove tutto) E per-capacità (specifico wiki/rag/governance).
- **Q4 (a):** `sertor-flow upgrade`/`uninstall` INCLUSO in questo ticket (contro raccomandazione di separarlo) — simmetria piena, primitive condivise nel sertor-install-kit, invariante "sertor-flow no sertor-core" preservato.
- **Esito:** `requirements/sertor-cli/lifecycle-installer/requirements.md` decomposto a 34 REQ EARS + 7 NFR (§5 chiusa completamente). Riga FEAT-008 dell'epica aggiornata. Pronta per `/speckit-specify`.

## [2026-06-17] record | FEAT-008 ciclo di vita installer CONSEGNATA (PR #71)

Pipeline SpecKit completa specify→plan→tasks→implement per FEAT-008 "ciclo di vita installer" (epica sertor-cli), branch `048-lifecycle-installer`, merge `8920870` (PR #71). Produttivizza i verbi `upgrade`/`uninstall` oltre al primo install, per `sertor` e `sertor-flow`. Chiude l'ultima Could rilevante dell'epica `sertor-cli`.
- **Cosa:** `sertor upgrade`/`uninstall` (tutto-in-uno + per-capacità, Q3) e `sertor-flow upgrade`/`uninstall` (Q4 — governance in ambito, simmetrica). Tipi artefatto A/B/C/D; report `install.report/1` esteso additivo (outcome `updated`/`removed`).
- **Design (D1–D4):** verbo ortogonale `LifecycleOp{INSTALL/UPGRADE/UNINSTALL}` + funzioni inverse PURE duali, una volta sola nel `sertor-install-kit` (D1 — niente raddoppio `WriteStrategy`/`ArtifactKind`); riuso degli stessi plan-builder percorsi col verbo (D2); diff a posteriori `sertor_owned_paths` + test invariante `plan ⊆ owned`, niente manifest (Q2); `--purge-wiki` opt-in CI-safe — senza TTY né `--yes` il `wiki/` è preservato, `--purge-wiki --dry-run` = usage error (Q1/D4).
- **Invariante duro:** `sertor-flow` senza dipendenza da `sertor-core`/`sertor` (verificato via AST + guard di simmetria a 0 divergenze); primitive solo nel kit.
- **Decisioni di requisito Q1(a)/Q2(a)/Q3(c)/Q4(a):** chiuse con l'utente a monte (una alla volta), poi cablate in spec/plan.
- **Verifiche:** Constitution 11/11 pre/post senza deroghe; 393 test non-cloud verdi (kit 108 · sertor 180 · sertor-flow 105); ruff pulito. `docs/install.md §10` produttivizzato (comandi automatici via primaria, script manuale → fallback §10.3).
- **Distill:** nuova pagina [[installer-lifecycle]] (tech) + pointer in index; EXEC roadmap aggiornato (FEAT-008 → DONE). Corpus dogfood re-indicizzato (incrementale: 712 doc/7141 chunk, +18 nuovi, 24 aggiornati, 23s — FEAT-009 confermato).

## [2026-06-17] record | Audit compatibilità Copilot: parità FEAT-007/009 NON piena (→ FEAT-011)

Audit di dogfooding (ospite reale Copilot CLI 1.0.63 + cross-check doc ufficiale GitHub) sulla distribuzione Copilot di Sertor. Trovato che la "parità funzionale piena" dichiarata da FEAT-007/009 è FALSA su più superfici: l'installer deposita artefatti in formato Claude non conformi allo schema Copilot.
- **Hooks** `.github/hooks/sertor-hooks.json` (asset `copilot/hooks/*.json`): formato Claude — manca `"version":1` (→ file scartato, NESSUN hook parte), struttura annidata `{matcher?,"hooks":[...]}`, `"shell":"powershell"`, `"statusMessage"`, `"timeout"`. Copilot vuole `{"version":1,"hooks":{<evento>:[entry piatta]}}` con `powershell`/`bash`/`command`/`timeoutSec`. 🔴 CONFERMATO.
- **Contratto output `.ps1`** (`wiki-pending-check.ps1`): emette `{"systemMessage"}` (Claude-only). Copilot vuole per-evento: sessionStart→`additionalContext`, agentStop→`decision/reason`, sessionEnd→nessun output. `preToolUse` è fail-closed (hook malformato → NEGA la tool call). 🔴.
- **SessionStart**: emette stringhe plain non JSON → zero contesto iniettato (serve `{additionalContext}` o `type:prompt`). 🟡.
- **Prompt file** `.github/prompts/*.prompt.md` (comandi /wiki, wiki-author, requirements): NON supportati da Copilot CLI (solo VS Code/JetBrains) → su copilot-cli i comandi NON esistono; veicolo corretto = custom-agent. Anche su VS Code frontmatter `mode:` invece di `agent:`. 🔴 su CLI.
- **Custom agent** `.github/agents/*.agent.md`: struttura OK, ma `model: haiku` (nome Claude) non valido su Copilot. 🟡.
- **MCP**: `.vscode/mcp.json`/`servers` (VS Code) corretto; `.mcp.json`/`mcpServers` (CLI) validato empiricamente da PR #66 ma doc indica `~/.copilot/mcp-config.json` → da riconfermare. ⚠️.
- **copilot-instructions.md** (marker block) e .env/.sertor/wiki/.specify/SpecKit: OK.
- **Causa di fondo:** FEAT-007 ha riusato gli script `.ps1` identici (corretto) ma NON ha tradotto il wiring JSON allo schema Copilot; i test Copilot verificano l'INSTALLAZIONE dell'asset, non la sua VALIDITÀ-schema → non hanno preso i bug.
- **Azioni:** aperta FEAT-011 "hardening compatibilità Copilot" (Must, in decomposizione → `compatibilita-copilot/requirements.md`); corretto il claim falso di "parità piena" in roadmap (EXEC + tabella CLI) ed epic.md (FEAT-007/009 annotate ⚠️). Metodo: due agenti paralleli (inventario in-repo + oracolo schema Copilot da doc ufficiale), sintesi matrice di compatibilità nel flusso principale.

## [2026-06-17] record | FEAT-011 compatibilità Copilot nativa CONSEGNATA (PR #73)

Pipeline SpecKit completa specify→implement (branch `049-compatibilita-copilot`, merge `ec2566d`, PR #73) per l'hardening della compatibilità Copilot. Corregge la falsa "parità piena" di FEAT-007/009 emersa dall'audit dogfooding su Copilot CLI 1.0.63.
- **Principio guida (utente):** supporto NATIVO per ogni tool, niente hack di compatibilità (vedi memoria standing). Il riuso è del CONTENUTO; il CONTENITORE/contratto è tradotto nativamente. FR-014 di FEAT-007 rilassato: corpo `.ps1` condiviso, output nativo per assistente.
- **Cosa:** nuovo `render_copilot_hooks` + `HookEntrySpec` nel `sertor-install-kit` generano hook in formato Copilot (`version:1`, entry piatte, `powershell`/`bash`, `timeoutSec`) → rimossi gli asset Claude-format `copilot/hooks/*.json`. Script `.ps1` condivisi con parametro `-Assistant` → contratto nativo per evento (sessionStart→`additionalContext`, agentStop→`decision:allow` non-bloccante, sessionEnd→nessun output, preToolUse→FAIL-OPEN). Comandi per-target: VS Code prompt-file (`agent:`), Copilot CLI custom-agent; `model:` omesso. Suite di validità-schema OFFLINE (gruppo G) che fallisce se un bug dell'audit rientra.
- **Design:** estensione MIRATA del seam `AssistantProfile`/`Surface` (non revisione profonda, YAGNI/Principio III — la revisione era autorizzata ma non necessaria perché il seam esteso produce comunque artefatti nativi).
- **Decisioni Q1–Q6:** Q1(b) SessionStart nativo, Q2(c) piano per-target, Q3(b) agentStop non-bloccante, Q4(b) script per-assistente via parametro, Q5 verifica MCP CLI inclusa, Q6(a) omettere model.
- **Verifiche:** Constitution 11/11 pre/post; 453 test non-cloud verdi (kit 126 · sertor 219 · sertor-flow 108); ruff pulito; gate non-regressione Claude (SC-010, 29 test) e `sertor-flow` senza dipendenza dal core (SC-011, 17 test). `sertor-core` invariato. Corpus dogfood re-indicizzato incrementale (731 doc/7437 chunk, +19/23).
- **Gap dichiarato (onestà, mai "parità piena"):** SessionStart VS Code `[ASSUNTO-VSC]` (il `type:prompt` è solo-CLI) e target MCP CLI da VERIFICARE EMPIRICAMENTE su ospite reale (Sentinel) → follow-up tracciato in roadmap. Decisione utente: «merge ora, verifica dopo».
- **Distill/governance:** corretto il claim falso di parità in roadmap + epic; `wiki/tech/assistant-targeting.md` aggiornato col rendering nativo per-assistente.

## [2026-06-17] record | Fix runtime Copilot + verifica empirica live su Copilot CLI 1.0.63 (PR #74)

Fix dei difetti di runtime della distribuzione Copilot, emersi da una VERIFICA EMPIRICA su ospite reale (Spike, Copilot CLI 1.0.63) — i test offline di FEAT-011 non li coprivano (uno addirittura codificava il bug). Branch `050-fix-copilot-runtime`, merge `9cde709`, PR #74.
- **3 difetti risolti e pinnati da test:** (1) il server MCP `sertor-rag` crashava con `-32000 Connection closed` su config incompleta — il warm-up eager non protetto in `main()` uccideva il processo prima di `mcp.run()`; ora è protetto, il server parte e l'errore azionabile arriva al tool-call. (2) L'hook SessionStart `type:"prompt"` metteva il testo nel campo `command` invece di `prompt` → Copilot lo ignorava; `render_copilot_hooks` ora usa il campo corretto e il dedup `_inner_commands` riconosce anche il payload `prompt` (idempotenza + uninstall corretti). (3) `render_custom_agent` non quotava la `description`: un `:` rompeva lo YAML e Copilot scartava il custom-agent (`wiki-author` non si caricava) → quoting via `_yaml_scalar`.
- **Test:** aggiornati i test che codificavano il vecchio comportamento; nuovo guard sugli ASSET VERI (`test_real_cli_agents_have_yaml_safe_frontmatter`) che avrebbe preso il difetto wiki-author; nuovo `test_main_starts_server_even_if_warmup_fails`. Sync del `.claude/` dogfood (drift FEAT-011: `wiki-pending-check.ps1` + nuovo `wiki-session-start.ps1`). Suite verde: kit 127 · sertor 221 · root unit 583 · packaging integration 2.
- **VERIFICA EMPIRICA LIVE (Spike, Copilot CLI 1.0.63):** confermato che l'approccio `.mcp.json` di progetto È auto-caricato da Copilot CLI in sessione INTERATTIVA (non in `-p`, dove il config di progetto non-fidato è saltato — normale). Run 1 (creds presenti): 3 agent custom caricati (incl. `wiki-author`), `sertor-rag` connesso (7 tool, self-test ok). Run 2 (creds azzerate): server `sertor-rag` CONNESSO comunque (niente `-32000`), `search_code` ritorna l'errore azionabile «incomplete Azure configuration … endpoint/api_key/deployment missing». Difetto del `-32000` = solo chiavi Azure mancanti (RAG non configurato).
- **Decisioni utente (da implementare in lavori successivi, tracciate):** distribuzione Copilot = **solo CLI** (drop del target VS Code, sorgente del `.vscode/mcp.json` che la CLI rifiuta); **naming unico `copilot-cli`** ovunque (oggi `sertor` usa copilot/copilot-cli, `sertor-flow` solo `copilot`); governance = installare i prompt SpecKit e **trasformarli in custom-agent** (i prompt-file non funzionano su Copilot CLI); niente fallback bash/cloud-agent. La causa-radice UX («installo → non funziona senza chiavi») è **FEAT-003 wizard di configurazione**, ora load-bearing.

## [2026-06-17] record | Copilot CLI verificato END-TO-END su ospite reale (hook/agent/MCP/SpecKit)

Verifica empirica finale della distribuzione Copilot CLI su ospite reale (Spike, Copilot CLI 1.0.63), dopo il fix runtime PR #74. Tutte le superfici confermate FUNZIONANTI sul client reale (log interattivi `~/.copilot/logs/`):
- **MCP** `sertor-rag`: connesso, 7 tool, self-test ok (con creds; senza creds resta connesso con errore azionabile — niente -32000).
- **Agent**: tutti i custom-agent caricano e sono invocabili — wiki/wiki-author/wiki-curator + requirements-analyst/configuration-manager + i 10 `speckit.*` — zero errori di frontmatter.
- **Hook**: TUTTI E 4 gli eventi scattano (provato nei log: `Executing hook: …`): SessionStart (prompt), PreToolUse (`sertor-rag-usage-check`, fail-open silenzioso), Stop e SessionEnd (`wiki-pending-check`, silenziosi quando non c'è lavoro wiki pending → sembravano "non scattare" ma il log mostra l'esecuzione).
- **Prova del fix**: un log precedente (pre-fix) conteneva `[ERROR] Invalid hook configuration … hooks.sessionStart[0].prompt: prompt hook prompt is required` — Copilot rifiutava il vecchio config (testo in `command` invece di `prompt`): conferma diretta che il bug e il fix di PR #74 erano esatti.
- **Discovery (importante):** Copilot CLI carica la config di progetto (`.mcp.json` + `.github/hooks/`) SOLO in sessione INTERATTIVA fidata, NON in `-p` non-interattivo. La verifica va fatta in interattivo (come fatto).
- **SpecKit:** `specify init --ai copilot` (spec-kit 0.8.18) deposita SIA prompt-file (VS Code) SIA `.github/agents/speckit.*.agent.md` (CLI) → i comandi SpecKit sono GIÀ invocabili su Copilot CLI. Ridimensiona la decisione utente #3: la trasformazione prompt→custom-agent serve solo per la nostra skill `requirements` (unico comando Sertor-authored ancora reso solo come prompt-file → non invocabile su CLI).
- **Esito:** la distribuzione Copilot CLI è completa e funzionante end-to-end. Residuo noto: skill `requirements` su CLI (prompt-file → custom-agent); refactor "CLI-only" (drop VS Code, naming `copilot-cli`) tracciato; FEAT-003 wizard config = causa-radice UX delle chiavi mancanti.

## [2026-06-17] record | FEAT-003 wizard di configurazione sertor configure CONSEGNATA (PR #75)

Pipeline SpecKit completa specify→implement per FEAT-003 (epica sertor-cli): comando guidato `sertor configure [rag]` che porta `.sertor/.env` da "segreti vuoti" a "pronto" senza editare a mano. Branch `051-configurazione-wizard`, merge `9314e93`, PR #75. È la causa-radice UX del RAG non configurato (il `-32000` visto su Copilot CLI quando mancano le chiavi Azure).
- **Cosa:** sub-parser `configure` in `sertor_installer` (3 moduli nuovi: configure.py / configure_fields.py / configure_report.py). Flag `--target`/`--backend`/`--store`/`--set KEY=VAL`/`--overwrite`/`--non-interactive`/`--check`/`--json`. Exit 0 completa&valida · 1 incompleta/invalida o probe fallito · 2 usage.
- **Decisioni Q1–Q4:** Q1(a) ibrido CI-safe (prompt con TTY, flag-driven senza TTY, mai bloccante); Q2(a) comando separato ri-eseguibile; Q3(a) validazione statica via `Settings.validate_backend()` + `--check` opzionale; Q4(a) solo provider/store del core (Azure/Ollama, Chroma/Azure Search).
- **Default coerente FEAT-009 (chiarito con l'utente):** backend azure → store `local` (Chroma), onorando il template `env.azure.tmpl` — embeddings Azure + store Chroma locale è il default voluto; "azure ovunque" SCARTATO.
- **Robustezza/sicurezza:** risoluzione per-campo (flag→env/esistente→prompt→default); campi richiesti da fonte unica `validate_backend()` con test di copertura catalogo↔validatore (no drift); scrittura additiva non-distruttiva (`merge_env`), overwrite gated; mascheramento segreti centralizzato `mask_secret` con test anti-leak (entità, render umano, JSON, stdout/stderr); install≠run (mai indicizza). install/upgrade/uninstall invariati.
- **Verifiche:** 293 test verdi (221 baseline + 72 nuovi), ruff pulito, Constitution 11/11.
- **Follow-up tracciato (US5 deferred):** `--check` (probe live) richiede un nuovo `sertor-rag check` in `sertor-core` (per Principio XI il wizard usa il vehicle, non importa `build_embedder`); oggi il flag c'è e degrada onestamente. Da promuovere a FEAT di sertor-core prima che `--check` conti come done.

## [2026-06-17] record | FEAT-012 consolidamento Copilot CLI-only CONSEGNATA (PR #76)

Pipeline SpecKit completa specify→implement per FEAT-012 (epica sertor-cli): la distribuzione Copilot è consolidata su **un solo target = la CLI**. Branch `052-copilot-cli-only`, merge `6c02dbd`, PR #76. Nasce dalla verifica empirica (Copilot CLI 1.0.63) e dalle decisioni utente del 2026-06-17.
- **Cosa:** rimosso `AssistantId.COPILOT` (VS Code) dall'enum del `sertor-install-kit` + il suo profilo + i rami di resa VS-Code (`.vscode/mcp.json`, root `servers`, prompt-file come veicolo COMMAND). `--assistant copilot` ora è un errore esplicito che nomina `copilot-cli`. Naming uniforme `claude|copilot-cli` su `sertor` e `sertor-flow` (rinomina diretta in sertor-flow). Skill `requirements` resa come **custom-agent** su CLI (non più prompt-file). Mapping `copilot-cli → --ai copilot` (upstream spec-kit) in un punto unico (`_SPECKIT_AI_FLAG` in `speckit_launch.py`) + `_EXPECTED_LAYOUT` aggiornato per l'idempotency. Docs (`install.md`/`install-copilot.md`) → solo `claude|copilot-cli` + nota di migrazione per ex utenti VS Code (cleanup manuale).
- **Decisioni Q1–Q4:** Q1(a) rimozione TOTALE VS Code · Q2 mapping confermato in un punto · Q3(a) solo nota di migrazione · Q4(a) rinomina diretta.
- **Verifiche:** 530 test verdi (kit 128 · sertor 282 · sertor-flow 120); gate non-regressione Claude verde; `sertor-core` invariato (diff vuoto); 0 residui VS Code; ruff pulito; Constitution 11/11. Il -11 netto in sertor è atteso (test VS Code rimossi → copertura `copilot-cli` equivalente).
- **Esito:** eliminato il footgun VS Code↔CLI e l'incoerenza di naming; la distribuzione Copilot è ora nativa e coerente su un solo target. Chiude l'item 2 del programma utente (1 FEAT-003 ✅, 2 FEAT-012 ✅; resta 4 fix uninstall + FEAT-010 in coda).

## [2026-06-17] record | Fix uninstall: file hook Copilot dedicato cancellato se vuoto (PR #77)

Bugfix del residuo di uninstall emerso dalla verifica empirica su Spike. Branch `053-fix-uninstall-empty-hooks`, merge `2ab824b`, PR #77.
- **Problema:** `sertor uninstall` rimuoveva le entry Sertor da `.github/hooks/sertor-hooks.json` ma lasciava il file come guscio `{"version":1}` (residuo orfano), perché `remove_settings_entries` riscriveva sempre il dict potato.
- **Fix:** parametro `delete_if_empty` in `remove_settings_entries`: per il file Copilot DEDICATO (`sertor-hooks.json`), se dopo la rimozione non resta contenuto (solo `version`/vuoto), il file viene CANCELLATO; il file CONDIVISO `.claude/settings.json` resta sempre (mai cancellato, preserva il contenuto utente). Cancella solo quando non resta NULLA: un hook utente residuo mantiene il file. Cablato nei caller wiki+rag (`dest.name == "sertor-hooks.json"`); governance non installa hook. 3 test nuovi (cancella-se-vuoto dedicato · mantiene-se-condiviso · mantiene-se-resta-contenuto-utente).
- **Verifiche:** kit 131 · sertor 282 verdi.
- **Programma utente 1·2·4 COMPLETO:** FEAT-003 wizard (PR #75) · FEAT-012 Copilot CLI-only (PR #76) · fix uninstall (PR #77). FEAT-010 ergonomia resta in coda; follow-up `sertor-rag check` (sblocca `--check`/US5 del wizard).
