# Log 2026-06-16

Log entries for 2026-06-16.

## [2026-06-16] record | Backlog audit → roadmap: 6 nuove epiche dal censimento del non-fatto

Convertito il backlog audit del 2026-06-15 (censimento di tutto il "citato ma non fatto") in struttura di epiche durevoli, su richiesta utente ("raggruppa i temi e crea epiche per ciascun raggruppamento, poi mettile in roadmap"). Decisioni utente sul raggruppamento (via domanda): granularità "più granulare (3-4)" per il cluster retrieval; "entrambe piene" per Second-brain e Debito tecnico.

- **6 nuove epiche create** (`requirements/<nome>/epic.md`, house-style EARS):
  - `retrieval-qualita` — misura+leve di qualità del retrieval: ground-truth+metriche (Must), search_code architetturale/calibrazione soglie/eval `cloud` (Should), HyDE+filtro-metadata+contextual-retrieval (Could, assorbe H7/H8/H11 da `hardening-produzione`).
  - `backend-store-scala` — nuovi store + scala: adapter PGVector (Should), Mongo/Atlas, multi-provider, fan-out >2 corpora, scala code-graph (Could).
  - `ingestione-estesa` — repo remoti URL, formati non-testo (PDF/DOCX/notebook), chunking sintattico PowerShell/T-SQL/PL-SQL/Bash, no-code-first (Could). Promuove la FEAT-010 di `sertor-core`.
  - `conoscenza-schema-sql` — schema SQL come corpus (Should) + schema-graph + fusione col codice applicativo (Could); bloccata a monte dal chunking SQL di `ingestione-estesa`. Ancorata a [[conoscenza-schema-sql-rag]].
  - `second-brain` — Meta-Sertor (epica piena, DA ESPANDERE): MVP catalogo flotta + query federata; harvest/promote/trust/asset-registry; bivi §9 da decidere prima di decomporre. Ancorata a [[second-brain-cross-progetto]].
  - `debito-tecnico` — epica interna (non capacità di prodotto): host-agnosticità asset residui, unificazione venv .venv/.venv-core, CI Linux nativo (Should); plugin rituale portabile, igiene wiki, selettività bundle sertor-flow (Could).
- **Leak minori (§6 audit) promossi nelle epiche esistenti:** `sertor-core` (cross-ref alle epiche-estensione + nota trasporti MCP non-stdio/tool health come estensione FEAT-MCP; FEAT-010 → promossa a `ingestione-estesa`); `sertor-cli` FEAT-010 nuova (ergonomia installer: pip fallback, avviso non-Python, hook Linux, multi-target install, reviewer clean-code); `osservabilita` FEAT-011 (export report CSV/MD + bucket "hour"); `memoria-conversazioni` FEAT-010 (parità MCP `memory show`/`list`).
- **Roadmap** (`wiki/syntheses/roadmap.md`): aggiunte 6 righe alla EXEC table; sezione PLANNED riorganizzata intorno alle nuove epiche + epiche esistenti in attesa; timestamp `updated` aggiornato.
- **Audit** marcato ✅ ESEGUITO (era "to-do sessione successiva").

Pagine wiki impattate: [[roadmap]], [[backlog-audit-2026-06-15]]. Aggiornato `wiki/index.md` (timestamp `updated` con una riga sintetica su questo step).

## [2026-06-16] record | Aggiornata docs/install.md per documentare FEAT-007/009 Copilot multi-assistente

Aggiornamento della guida pubblica d'installazione (`docs/install.md`): correzione di una deriva in cui le capacità multi-assistente (Copilot) FEAT-007 e FEAT-009 erano implementate e su master ma non documentate, lasciando la guida stantia.

- **Sezione aggiunta (§9):** tabella di mapping container Claude ↔ Copilot per ogni superficie logica (istruzioni, MCP, hook, comandi, agenti). Caveat esplicito: Copilot = VS Code, non standalone CLI; Codex PLANNED.
- **Correzione §8 (sertor-flow):** descrizione del pivot vendoring → launch-installer di spec-kit (FEAT-009): SpecKit non più vendorato, bensì ottenuto da `specify init --ai <assistant>` a versione pinnata, prerequisiti fail-fast (uvx su PATH + raggiungibilità rete).
- **Flag `--assistant` aggiunto:** esempi di `sertor install wiki` e `sertor install rag` aggiornati con `--assistant copilot`.
- **Derive risolte:** la documentazione è ora sincronizzata con lo stato di master (FEAT-007 PR #64 + FEAT-009 PR #65 mergiati il 2026-06-15).

## [2026-06-16] record | Aggiunto support GitHub Copilot CLI (`--assistant copilot-cli`) all'installer sertor

Aggiunto terzo target assistente `--assistant copilot-cli` al package `sertor` (installer rag/wiki). Fix su discovery dogfooding: GitHub Copilot CLI ha rimosso supporto a `.vscode/mcp.json` (root `servers`), legge solo `.mcp.json` con root `mcpServers`.

- **Implementazione:** nuovo `AssistantId.COPILOT_CLI` nel `sertor-install-kit` con profilo che mappi `MCP_SERVER` → `.mcp.json`/`mcpServers` e riusa superfici `.github/**` (istruzioni, prompt, agenti, hook). `install_rag.py` e `install_wiki.py` instradano famiglia Copilot (VS Code + CLI); help CLI aggiornato.
- **Non-distruttivo:** target `copilot` (VS Code) resta invariato, feature additiva pura.
- **Qualità:** kit 51 test verdi, sertor 138 test verdi, ruff pulito; nuovo test `test_install_rag_copilot_cli.py`.
- **Documentazione:** `docs/install.md` §9 riscritta con due target Copilot, esempi PowerShell single-line, banner aggiornato.
- **Scoperta operativa:** trovato dogfooding su host reale (Spike); fix manuale `.mcp.json` stopgap, poi sistemato in prodotto.
- **Merge:** PR #66, commit 5282e7f su master (2026-06-16).
- **Follow-up:** distribuzione governance via sertor-flow su Copilot CLI (FEAT-009, dipende supporto SpecKit per CLI; sertor-flow resta `claude|copilot`).

## [2026-06-16] record | FEAT-008 (arricchimento Wiki↔RAG) chiusa in forma composita — verificata live via dogfooding

Verificato — interrogando il RAG su sé stesso (dogfooding, tool MCP `sertor-rag`, nessun errore) + lettura del codice — che la feature FEAT-008 dell'epica `sertor-core` («Arricchimento bidirezionale Wiki↔RAG», finora Could «da decomporre») è **già soddisfatta in forma composita**, come fu per FEAT-006 (agentico). Decisione utente: chiuderla.

- **Evidenza raccolta:**
  - Wiki → RAG documentale: `search_docs` restituisce pagine wiki reali (`wiki/log/2026-06-10.md`, `wiki/tech/corpus-index-naming.md`, `wiki/concepts/wiki-role-da-w1.md`) come `source:doc`. Codice: `src/sertor_core/services/ingestion.py:84` classifica i `.md` come `DocType.DOC`. È il **modello a corpus unico** (D-21).
  - Sorgenti → fondamenta wiki: esiste l'operazione `generate` (`.claude/skills/wiki-author/ops/generate.md`).
  - Codice↔doc fusi: `get_context("build_facade")` ritorna definizioni+caller+callee **+ `docs`**; `related_docs("DocType")` include pagine wiki (`domain-model.md`, `index.md`).
  - Loop chiuso: regola standing di re-index a fine step; le pagine wiki sono nell'indice vivo.
  - CS-6 dell'epica (il RAG usa sia sorgenti sia wiki) risulta **dimostrato** live.
- **Decisione:** FEAT-008 → ✅ «soddisfatta in forma composita (2026-06-16)» (gemella di FEAT-006). Aggiornati `requirements/sertor-core/epic.md` (riga FEAT-008 con l'evidenza) e `wiki/syntheses/roadmap.md` (inventario epiche: Wiki↔RAG passa al consegnato del core; PLANNED: rimossa da residui core; FEAT-009 refresh incrementale resta l'unico residuo «da fare» reale).
- **Residuo scorporato (capacità NUOVA, non la FEAT-008 originale):** *rilevamento ATTIVO dei gap di documentazione* — il RAG/code-graph che rileva entità di codice senza pagina wiki e le propone al `wiki-author` (oggi il legame codice↔doc è passivo, non generativo). Registrato come idea in roadmap «Nuove funzionalità da discutere» (casa candidata: feature wiki dedicata o `debito-tecnico` FEAT-005 igiene-wiki).
- **Contesto:** è la prima feature affrontata nel giro «elaboriamo i residui di sertor-core»; prossimo candidato = FEAT-009 refresh incrementale (reale, da decomporre).

Pagine wiki impattate: [[roadmap]].

## [2026-06-16] record | Aggiunta §10 Refresh and clean uninstall a docs/install.md

Arricchimento della guida pubblica d'installazione (`docs/install.md`) con procedure di manutenzione e ciclo di vita: come aggiornare il deployment quando Sertor su `master` avanza (§10.1) e come rimuovere completamente ogni traccia del sistema da un host (§10.2).

- **§10.1 Refresh:** documenta che `uvx` con git URL non pinnato **cachea** la build — per prendere il codice nuovo da master serve `--refresh` (causa diagnosticata da dogfooding su host reale: installer MCP post-merge non era cambiato perché uvx serviva una versione pre-merge cached). Aggiunto: idempotenza installer su re-run (non sovrascrive) e manuale di pulizia asset obsoleti quando si cambia target assistente (es. `copilot` → `copilot-cli` lascia il vecchio `.vscode/mcp.json`). Consiglio di ri-index del corpus (`sertor-rag index .`) per freschezza.
- **§10.2 Disinstallazione pulita:** procedura manuale (nessun `sertor uninstall` tracciato come follow-up FEAT-008 epica sertor-cli) per rimuovere OGNI traccia di Sertor, organizzata per **tipo di asset** (A runtime isolato `.sertor/`, B asset standalone, C file shared da editare, D registrazione client) con inventario dettagliato per tre assistenti (Claude / Copilot VS Code / Copilot CLI). Incluso helper **script PowerShell** (selettivo sui file Sertor, rifiuta di cancellare dir condivise in blocco, grep finale di verifica). Caveat: non elimina il cache globale `uvx` (utente separa se vuole).
- **Chiarimenti di confine:** uninstall rimane manuale (no automazione fuori scope); lo script è un aiuto, non comando ufficiale; distinzione tra la **rimozione della capacità** (ciò che fa lo script) vs la **pulizia del venv universale** (cura dell'ospite).

Derive risolte: la documentazione di lifecycle era fragmentaria (solo hint sparse di "uninstall ≈ delete `.sertor/`"); il nuovo §10 la rende completa, strutturata e verificabile.

Pagine toccate: `docs/install.md` (§10.1–10.2 aggiunti).

## [2026-06-16] record | FEAT-009 (refresh incrementale dell'indice) decomposta a requisiti EARS — con ricerca prior-art

Decomposta a livello FEATURE la FEAT-009 dell'epica `sertor-core` («Refresh incrementale dell'indice RAG sui sorgenti», finora Could «da decomporre»), producendo `requirements/sertor-core/refresh-incrementale/requirements.md` (18 REQ EARS + MoSCoW + 5 domande aperte). Primo residuo reale di `sertor-core` affrontato dopo la chiusura di FEAT-008.

- **Problema:** oggi `index()` fa full rebuild dei 5 stadi (discover/chunk/embed/store/BM25+grafo); solo l'embed è incrementale (cache content-hash, FEAT-019). Su ospiti grandi il full a ogni modifica è minuti → contraddice l'essenza «contesto sempre reale».
- **Ricerca prior-art (web, 2026-06-16):** pattern convergente «ledger» in CocoIndex (lineage tracking, change detection mtime+fingerprint, cache step pesanti, `behavior_version` per invalidare su cambio-logica), LlamaIndex IngestionPipeline (docstore con hash, `upserts_and_delete`), record-manager di LangChain (ledger SQLite, `cleanup="full"`). Caso più analogo: CocoIndex realtime-codebase-indexing (tree-sitter, «edit one function → one chunk re-embedded»). Nodo Sertor-specifico: BM25 (statistiche di corpus) e code-graph (archi cross-file) non hanno analogo pulito nella prior art.
- **Decisioni utente (forche di scope):** **F1** = vettore incrementale (upsert/delete mirati) + BM25/code-graph **ricostruiti dal ledger** (chunk-id persistiti, niente re-chunk degli invariati) nel Must; vera incrementalità di BM25/grafo = Could futuro. **F2** = incrementale **di default**, full su `--full` (l'utente ha scelto CONTRO la raccomandazione dell'opt-in; tracciata l'implicazione: default-incrementale alza il rischio di drift silenzioso → safeguard non negoziabili nel Must: equivalenza col full REQ-012, fallback automatico al full se manifest assente/incompatibile REQ-011, invalidazione su cambio-logica REQ-013, conteggi delta osservabili REQ-015).
- **Meccanismo nei requisiti:** manifest persistito `file→(mtime, content-hash, chunk-id, versione-logica)`; rilevamento mtime pre-filtro + hash conferma; gestione cancellazioni (delete mirato); granularità a file (embed-cache copre il chunk-level); namespaced per (corpus, provider), gitignored; via vehicles (Principio XI).
- **Artefatti:** nuovo `requirements/sertor-core/refresh-incrementale/requirements.md`; aggiornato `requirements/sertor-core/epic.md` (FEAT-009 → decomposta); aggiornata `wiki/syntheses/roadmap.md` (inventario: FEAT-009 «requirements decomposti → /speckit-plan»). Resta l'evidenza in `refresh-incrementale/corollario-costo.md`.
- **Prossimo passo:** `/speckit-plan` (design) su FEAT-009.

## [2026-06-16] record | Tracciamento debito naming --assistant Copilot (FEAT-007 debito-tecnico)

Tracciato un debito di naming nella distribuzione Copilot: la CLI espone due valori `--assistant` (`copilot` e `copilot-cli`) per quello che l'utente percepisce come un solo assistente. Promosso a voce durevole: FEAT-007 dell'epica `debito-tecnico` con priorità Could.

- **Problema:** `--assistant copilot` (VS Code) vs `--assistant copilot-cli` (CLI) sono due target con contenitori MCP diversi (`.vscode/mcp.json` vs `.mcp.json`), ma l'utente vuole un naming coerente sotto uno solo.
- **Decisione di design aperta:** come riconciliare i due contenitori MCP sotto un nome unico — scrivere entrambi, o eleggere `.mcp.json` come canonico?
- **Casa durevole:** `requirements/debito-tecnico/epic.md` FEAT-007 (potenziale spettanza: quale contenitore sia "fonte di verità" per il CLI, per allineamento GitHub)
- **Stato:** registrato in §2 ambito + §8 backlog epica, user-flagged 2026-06-16

## [2026-06-16] record | FEAT-009 refresh incrementale dell'indice CONSEGNATA (merge 3ec47f1)

Consegnata su `master` (merge `3ec47f1`, branch `046-refresh-incrementale`) la FEAT-009 dell'epica `sertor-core` (refresh incrementale dell'indice RAG). Pipeline SpecKit completa specify→clarify→plan→tasks→implement in sessione. Chiude l'ULTIMO residuo reale di `sertor-core` (resta solo l'agenzia incorporata, Could differita).

- **Capacità:** `index()` incrementale di DEFAULT. Manifest SQLite namespaced `(corpus,provider)` (`<index_dir>/index_manifest.sqlite`, gitignored) che ricorda `mtime+content_hash+logic_version` e conserva le unità (Document/Chunk). I file cambiati → upsert/delete MIRATI sul vector store (riusa `VectorStore.delete` già esistente); BM25 e code-graph RICOSTRUITI dal manifest (decisione F1, niente re-chunk/re-read degli invariati). Granularità a file; embed-cache (FEAT-019) copre il chunk-level.
- **Safeguard:** fallback automatico al full su manifest assente/incompatibile; `--full`/`SERTOR_INDEX_INCREMENTAL=false` per forzare il full; invalidazione su cambio-logica; lock single-writer (`IndexLockedError`); riconciliazione off-default (`SERTOR_INDEX_RECONCILE_EVERY`); conteggi delta osservabili (added/updated/removed/unchanged/cache_hits).
- **Decisioni utente:** F1 (vettore incrementale + BM25/grafo dal manifest), F2 (incrementale di default + --full); clarify (full di riconciliazione off-default; lock single-writer, concorrenza avanzata → multiutente).
- **Qualità:** gate di equivalenza incrementale≡full byte-identico verde; 596 test non-cloud verdi (32 nuovi); ruff pulito; Constitution PASS 11/11 senza deroghe. Ricerca prior-art a supporto (CocoIndex/LlamaIndex/LangChain). Nessuna nuova porta (manifest = store concreto come EmbeddingCache).
- **Effetto:** genera la feature `osservabilita` FEAT-012 (rilevamento del drift dell'indice, il segnale per il trigger della riconciliazione).
- **File chiave:** nuovo `src/sertor_core/services/index_manifest.py`; esteso `services/indexing.py`, `ingestion.py`, `composition.py`, `cli/__main__.py`, `domain/{errors,entities}.py`, `config/settings.py`, `adapters/embeddings/cache.py`.
- **Distillazione (entità durevoli):** aggiunta sezione nel concetto [[indexing-and-retrieval]] sul refresh incrementale (manifest come stato persistito, 4 classi UNCHANGED/NEW/MODIFIED/DELETED, upsert/delete mirati vs reset+full, BM25/grafo ricostruiti dal manifest, safeguard, DEFAULT incrementale). Aggiornato wiki/index.md (timestamp `updated`, riga sintetica).

## [2026-06-16] record | Bugfix robustezza Windows UTF-8 — specify init stampa banner Unicode

Trovato e risolto un bug reale durante installazione su host Windows: `sertor-flow install --assistant copilot` falliva con `UnicodeEncodeError` nella libreria `rich` di SpecKit. La causa non era il valore `--assistant`, ma l'incapacità di Python di codificare i caratteri Unicode in console Windows legacy (code page cp1252).

- **Fix:** overlay d'ambiente `PYTHONUTF8=1` + `PYTHONIOENCODING=utf-8` iniettati da `speckit_launch` nel sottoprocesso `specify init`; `CommandRunner.run()` del `sertor-install-kit` esteso con parametro opzionale `env` e UTF-8 capture nella lettura dell'output.
- **Validazione:** esecuzione end-to-end `sertor-flow install --assistant copilot` esce 0, layout creato, output pulito (0 errors).
- **Artefatti:** nuovo test `test_command_runner.py` che verifica l'overlay env e il forcing UTF-8.
- **Qualità:** kit 54✓ · sertor-flow 88✓ · sertor 138✓ test verdi, ruff pulito.
- **Merge:** PR #67, commit 76757df su master (2026-06-16).
- **Workaround per l'utente:** `$env:PYTHONUTF8="1"` prima del comando.
