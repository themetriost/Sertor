# Log 2026-06-22

Log entries for 2026-06-22.

## [2026-06-22] record | FEAT-009 distribuzione memoria via installer — decomposizione a requisiti EARS

Decomposizione di FEAT-009 (epica memoria-conversazioni, Must, debito di completamento): la memoria conversazioni è accesa **solo sul dogfood** di Sertor, non negli asset dell'installer. Un ospite che fa `sertor install rag` **non riceve** la memoria — viola il corollario «una feature è completa solo se installabile» (CLAUDE.md, Principio X). Artefatto: `requirements/memoria-conversazioni/distribuzione-installer/requirements.md`.

- **Problema e contesto:** l'MVP memoria è implementato (FEAT-001/002/035/003 mergate), ma **vive solo nel repo di Sertor** — 8 manopole nel `.env` di dev, hook `memory-capture.ps1` in `.claude/`, voce `SessionEnd` in `.claude/settings.json`, agente ospite **ignaro**. Template `.env` dell'installer (env.local.tmpl/azure.tmpl) contiene RAG/osservabilità/eval, **zero memoria**. Recupera il rinvio A-009 di FEAT-035 tracciato solo in `specs/035-…`, mai promosso a backlog durevole.
- **Soluzione (rispecchiamento pattern):** il pattern d'installazione **esiste già** nel `sertor-install-kit` e in `install_rag.py` — l'hook PreToolUse `sertor-rag-usage-check.ps1` (feature 042/044/011) è `FILE` (create-if-absent) + `SETTINGS_MERGE` (merge dedup preservante), riusato per assistente (Claude `.claude/` ↔ Copilot CLI `.github/`), con lifecycle inverso e `sertor_owned_paths`. **Questa feature rispecchia quel meccanismo** per l'hook memoria + manopole `.env`.
- **Scope deciso (utente):** (1) memoria **cavalca `install rag`** (no comando `install memory` nuovo; condivide runtime, CLI, `.env`); (2) hook depositato **anche su Copilot** (parità/forward-compat), ma **wiring inerte** finché FEAT-008 non porta adapter che legga transcript Copilot (unico adapter oggi = `claude-code`).
- **Requisiti EARS (39 voci):** Must (manopole `.env`, script, wiring SessionEnd Claude, consapevolezza agente, privacy, lifecycle), Should (deposito/routing Copilot CLI, obsolete cross-assistente), Could (test anti-drift knob↔template). Tre domande aperte rinviate al design (forma cenno istruzioni; parametro `-Assistant` su hook; nuovo `ArtifactKind` vs riuso).
- **Pagina:** nuovo file `requirements/memoria-conversazioni/distribuzione-installer/requirements.md` (EARS, 217 righe, link via [[memoria-conversazioni]], [[sertor-installer]], [[sertor-install-kit]], [[installer-lifecycle]]).
- **Stato backlog epic:** aggiornato `requirements/memoria-conversazioni/epic.md` riga FEAT-009 da `da decomporre` a `decomposta`.
- **Executive roadmap:** aggiornato `wiki/syntheses/roadmap.md` blocco IN PROGRESS memoria da `MVP acceso (dogfood)` a `DECOMPOSTA, prossimo: /speckit-plan`. Prossimo passo: design e speckit (`/speckit-plan`).
- **Lint A:** 0 broken wikilink, no pagina wiki distillata (non è entità di dominio, è planning), index coerente.

## [2026-06-22] record | FEAT-009 distribuzione memoria via installer — implementazione SpecKit completa

Implementazione FEAT-009 (branch `071-distribuzione-memoria-installer`): chiude il debito di completamento dell'MVP memoria — rende la memoria conversazioni **installabile su ospiti** via `sertor install rag`, non solo accesa sul dogfood. Principio X ondeggioso fino a qui; ora saldato.

- **Cosa:** distribuzione della capacità di cattura e ricerca episodica ai consumatori di Sertor via l'installer standard. Quattro consegne, tutte additive (sertor-core INVARIATO): (1) 8 manopole memoria nei template .env (env.local.tmpl + env.azure.tmpl); `SERTOR_MEMORY` OFF di default (privacy-by-default), con descrizione d'uso in commenti; (2) hook di cattura `memory-capture.ps1` in asset installer + voce hook SessionEnd nei file settings per-assistente (Claude `.claude/settings.json` frammento, Copilot CLI `.github/hooks/sertor-hooks.json` entry), lifecycle install/upgrade/uninstall completo; (3) cenno condizionale nel blocco istruzioni RAG esistente `SERTOR:RAG-USAGE`: «Se `SERTOR_MEMORY=true`, i comandi `sertor-rag memory` diventano disponibili»; (4) riuso totale del pattern rag-usage (FILE + SETTINGS_MERGE, routing per-assistente, `sertor_owned_paths` esteso, guardia `plan ⊆ owned`).
- **Design:** tre forche risolte con l'utente (DA-a/b/c del brief): (a) cenno nel blocco esistente, no nuovo marker (rimane tematico), (b) corpo `.ps1` invariato da FEAT-035 (nessun parametro `-Assistant`, riuso diretto), (c) riuso tipi artefatto (FILE+SETTINGS_MERGE bastano, no `ArtifactKind` nuovo). Refactor minore: `_rag_hook_fragment(plan, art)` resa art-aware (dispatch su source), elimina raddoppio della funzione `_rag_settings_fragment`.
- **Esiti verificati:** 952 test non-cloud verdi (sertor 323 +30 nuovi: test_install_rag_memory.py +10 + unit/test_env_template_memory.py +20; aggiornato test_install_rag_usage.py per ≥1 SETTINGS_MERGE); ruff pulito su src/packages (gli 87 errori residui sono nel prototype/ congelato, pre-esistenti); Constitution Check 12/12 PASS (pre e post-design), nessuna deviazione.
- **Validazione:** ciclo di vita esercitato (install deposita hook+manopole, upgrade idempotente, uninstall rimuove hook+voce preservando env). Smoke test feature: plan builder include asset memoria, SETTINGS_MERGE fonde in settings con dedup, per-assistente routing corretto Claude/Copilot.
- **Gap dichiarato (onestà, Principio XII):** (1) Copilot CLI: script + wiring depositati, cattura INERTE finché FEAT-008 non porta adapter cattura Copilot (oggi esiste solo `claude-code`; il core non fallisce, hook scatta ma rimanere no-op utile); (2) invocazione ospite dell'hook (uv run da radice) condivide dipendenza generale di accesso CLI (non specifico, FEAT-003 wizard sbloccherà completamente).
- **Pagine aggiornate (wiki):** (1) nuovo file `wiki/experiments/feat-009-distribuzione-memoria-via-installer.md` (experiment, frontmatter completo, riassunto pattern+design+gap, backlink a sertor-installer/installer-lifecycle/memoria-conversazioni/assistant-targeting); (2) `wiki/index.md` entry per [[feat-009-distribuzione-memoria-via-installer]] aggiornata da «decomposizione EARS» a «implementazione SpecKit completa» + distill note; (3) index frontmatter `updated` aggiornato.
- **Lint A:** 0 broken wikilink (index → experiment page OK), index coerente.
- **Stato:** branch 071 NON YET MERGIATO (deliberato: attendere conferma step rituale).
