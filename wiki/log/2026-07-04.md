## [2026-07-04] record | Merge F8 + Dry-run installer fedeltà dogfood + FEAT-009 not-a-bug

Step multiplo: merge della PR #152 (E15-FEAT-008 re-lock runtime post-merge su HEAD), merge della PR #153 (requirements asset-install), dry-run empirico dei 3 veri installer del workspace (sertor/sertor-flow) per osservare la process-fidelity reale degli asset, e chiusura di una premessa di FEAT-009 come non-bug. Output principale: [[asset-install-installer-dry-run-2026-07-04]] (experiment).

- **Merge F8 (PR #152):** E15-FEAT-008 implementata e mergiata (squash b849a1f) — script `scripts/dev/relock-runtime.ps1` (check-then-act, fail-loud, dogfood-only), gitignore `.sertor/uv.lock` (lock volatile), `.sertor/pyproject.toml` versionato (spec stabile), rituale post-merge documented con gate «suite+ruff verdi pre-merge». Primo uso reale: re-lock sul proprio merge 879b688→b849a1f, re-index 1277 doc, smoke MCP verde.
- **Merge F4 requirements (PR #153):** E15 asset-install scope B scritto — asset host-facing depositati dai **veri** installer, non via sync interim; requisiti EARS completi, roadmap aggiornata.
- **Dry-run 3 installer:** eseguiti i veri installer da `git+url@master` via `uvx` come un ospite reale, contro il dogfood su branch usa-e-getta. **Verdetto:** per lo più idempotente; asset-fidelity byte confermata via sync (FEAT-002), residuo process-fidelity contenuto: (a) CLAUDE.md 3 blocchi generici vs prosa dogfood ricca → riconciliazione ibrida per-blocco (da fare step deliberato); (b) CRLF churn installer→LF dogfood → new E15-FEAT-010 (`.gitattributes` nel template); (c) `wiki/log.md` legacy vs rotazione `wiki/log/` → istanza di E15-FEAT-006 (template indietro).
- **FEAT-009 not-a-bug:** premessa della diagnosis (`.mcp.json --directory` → `registered=False`) era misdiagnosi; `registered=False` è artefatto della `cwd` del comando `doctor`, non difetto del template; `Settings.load` è self-localizing. Nessun cambio di codice, indagine chiusa, backlog cleared.
