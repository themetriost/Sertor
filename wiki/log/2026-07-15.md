# Log 2026-07-15

Log entries for 2026-07-15.

## [2026-07-15] record | E4-FEAT-011 — fix cattura memoria: encoding path (spazi/punti) + fail-loud su source assente

Scelta utente «andiamo di FEAT-11». Bug **MUST** diagnosticato dall'handoff Nunzio (processato ieri),
verificato sul codice, **validato empiricamente**, ora **corretto** (2 parti).

**Diagnosi + regola derivata empiricamente.** `encode_project_path` (`adapters/capture/claude_code.py`)
sostituiva solo `:`/`\`/`/` con `-`; Claude Code invece collassa **OGNI carattere non-alfanumerico**
(spazi **E** punti inclusi) quando nomina la cartella in `~/.claude/projects`. Regola derivata dai **15
nomi di cartella reali** della macchina (es. `…\Nunzio Summaries`→`…-Nunzio-Summaries`;
`…\Sinthari\.claude\…`→`…-Sinthari--claude-…`, il punto dà `--`). Adapter **`copilot-cli` NON affetto**
(associa via `cwd`/`gitRoot` con path-matching reale `os.path.normpath`).

**Implementato (sertor-core):**
- **(1) Encoding:** `encode_project_path` → `re.sub(r"[^A-Za-z0-9]", "-", path)`. **Validato DAL VIVO:**
  ora `…\Virtual Machines\VM-WorkingFolder` mappa alla cartella reale con le **22 sessioni** prima
  irraggiungibili (il vecchio encoding puntava a una cartella inesistente → 0 archiviati, il bug).
- **(2) Fail-loud (host-agnostico):** nuovo metodo del port `TranscriptCaptureAdapter.source_available()`
  (impl. in **entrambi** gli adapter); `ArchiveRunReport.source_absent` impostato in `archive_all()`
  (il **port riporta**, il servizio non ispeziona l'identità dell'adapter — FR-005); `format_archive_report`
  emette un **WARNING visibile** (human + `--json`) quando memoria on + source assente, invece del
  silenzioso `archived=0 errors=0`. **Exit resta 0** (fail-safe per l'hook SessionEnd, non lo si rompe).

**Test:** encoding sui 15 campioni reali + spazi/punti/trattini · `source_available` present/absent ·
`source_absent` flag nel servizio · warning nel formatter (human+JSON) · aggiornati 4 fake adapter con
`source_available()`. Suite non-cloud **verde** (i 2 fail `test_clean_install_uv` = **branch non pushato**,
NON codice: `sertor-flow` non importa `sertor-core` → provato che è contesto; si risolvono al push);
`ruff check .` clean. Requirements lean in `requirements/memoria-conversazioni/fix-cattura-encoding-e-fail-loud/`.

**Confine:** `sertor-core` (2 adapter, port, servizio archiver, report, cli output). Il 🐛 **auto-capture E4
distinto** (hook `memory-capture` non popolava l'archivio) **resta aperto** (fuori scope). **Rituale:
record ✅ · distill: non serve** (bugfix mirato; l'entità durevole «memoria» esiste già) **· lint ✅** (bug
verificato sul codice + fix validato dal vivo sui 15 campioni + le 22 sessioni) **· explainer: non innescato**
(fix interno, non capacità nuova utente-facing).

**Prossimo:** commit + push (risolve i packaging test) + PR + merge → post-merge (re-lock/re-index/smoke).

## [2026-07-15] record | fix hook wiring CWD-robusto — ancoraggio path (regressione A-09), parità Claude/Copilot (E10-FEAT-029)

Path relativo negli hook (regressione di A-09 dalla migrazione .ps1 → .py): il wiring perdeva l'ancoraggio
della directory radice, bloccando ogni comando quando la CWD dell'ospite deviava dalla repo root. Fix
nativo per assistente: Claude via `${CLAUDE_PROJECT_DIR}` in settings*.json; Copilot via campo `cwd` in
hook JSON + render engine. Confine: installer/asset + test anti-regressione, sertor-core invariato.

- **Diagnosi:** path relativo in `uv run --no-project python .claude/hooks/X.py` fallisce se CWD ≠ repo
  root; empiricamente bloccante da quando A-09 ha migrato `.ps1` → `.py` eliminando l'ancoraggio
  `Join-Path $env:CLAUDE_PROJECT_DIR` dei vecchi script.
- **Regressione latente 6 giorni** (commit 69d527c, 2026-07-09) finché non emersa nel dogfooding vivo con un
  `cd .sertor` (PreToolUse sull'hook bloccava Bash/Write/Edit).
- **Fix Claude (host-agnostico):** 7 asset `settings*.json` → `"${CLAUDE_PROJECT_DIR}/.claude/hooks/X.py"`
  (quotato per spazi); variabile sostituita dalla shell **prima** dell'esecuzione.
- **Fix Copilot (nativo):** nuovo campo `HookEntrySpec.cwd` in kit + `render_copilot_hooks` → emette `cwd="."` sui 6 command entry (rag ×4, wiki ×2);
  prompt SessionStart restano senza `cwd` (non serve, non invocano comando).
- **Test anti-regressione:** `test_claude_hook_wiring_anchored.py` (ogni comando Claude ancora a `${CLAUDE_PROJECT_DIR}`; forma nuda bandita) +
  `test_schema_copilot_hooks.py` esteso (validatore esige `cwd="."` sui command, assenza sui prompt).
- **Gate:** root sertor-core 1188 · sertor 531 · flow 142 · kit 178 · ruff clean.
- **Residuo tracciato (E10-FEAT-029):** verifica LIVE post-merge (dogfood re-install → hook sopravvive a `cd`; ospite Spike + Copilot CLI reale); installer legge asset dalla versione installata.
