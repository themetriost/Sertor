# Log 2026-07-02

## [2026-07-02] record | Pulizia memoria/CLAUDE.md + elaborazione 2 feedback ospiti → E10-FEAT-026

**Operazione:** `record` (igiene governance + elaborazione feedback → backlog).

**Cosa è stato fatto:**
- **Pulizia contesto always-on:** potato il blocco `<!-- SPECKIT START/END -->` di `CLAUDE.md`
  (catena `Storico:` 084→001, ~1180 righe morte già durevoli in `specs/*/plan.md`/wiki/git) → tenuto
  solo il puntatore al piano attivo. **1744 → 634 righe (−1110).** PR #138 mergiata su master (`1ec8011`,
  squash, CI Win+Linux verdi). *(Memoria dell'agente `~/.claude/…/memory`: 42→31 file, resume-pointer
  reso fonte unica `project_resume.md` — non versionato, fuori dal repo.)*
- **Elaborati 2 feedback ospiti** (`wiki/sources/usersfeedback/`, convergenza indipendente stesso giorno):
  `wiki-ritual-distill-lint-discrezionale.md` (host `hermes-nunzio-ha`) + `wiki-ritual-distill-ignorato-noetix.md`
  (host `Noetix`). **Stesso gap:** i passi di **giudizio** del rituale (`distill`, `lint semantico B`) sono
  prosa discrezionale nel `claude-md-block` → si saltano **in silenzio**, mentre `record` (meccanico/delegato)
  si fa. Danno dimostrato: contraddizione rimasta nel blocco **EXEC di `roadmap.md`** (mostrato a ogni
  SessionStart) per un'intera sessione; pattern notato in prosa mai distillato; moduli `ops/*.md` derivati
  non ereditano il rituale.
- **Promosso a backlog:** nuova **E10-FEAT-026** in `requirements/debito-tecnico/epic.md` (Should P1),
  gemella lato-giudizio di FEAT-011 (meccanico→hook) + Principio XII come FEAT-019; decomposizione D↔N a 4
  pezzi (scoperta deterministica candidati in `sertor-wiki-tools` · `wiki-curator` restituisce candidati ·
  dichiarazione forzata a fine step · propagazione ai moduli derivati). I 2 feedback spostati in
  `usersfeedback/processed/` (convenzione, status → elaborata).
- **Memoria comportamentale** salvata (`feedback_dichiara_distill_lint_a_fine_step`): dichiarare sempre il
  verdetto di distill/lint a fine step, gemella di `step-meccanici-e-mcp-first`.

**Rituale (dichiarazione esplicita — cfr. il gap appena elaborato):** record ✅ (questa voce) ·
**distill:** non serve (è un item di backlog, non ancora un concetto di design durevole) · **lint:** pulito
(nessuna contraddizione introdotta; EXEC roadmap non innescato — nessun cambio di stato di capacità, FEAT-026
è 📋 da decomporre).

**Pagine toccate:** `requirements/debito-tecnico/epic.md` (+FEAT-026), `wiki/sources/usersfeedback/processed/`
(+2 file), `CLAUDE.md` (prune, già su master via #138).

## [2026-07-02] query | Audit indipendente SWOT + backlog azionabile

**Operazione:** `query`/audit (analisi trasversale su richiesta utente, prompt in `wiki/sources/Human/Fable SWOT.md`).

**Cosa è stato fatto:** audit completo del workspace con 5 subagent paralleli (core+test · packages+CI ·
governance/skill/hook · requisiti/backlog · doc utente/wiki) + verifiche dirette (smoke MCP verde,
re-index 1191 doc/13305 chunk, spot-check skill speckit fantasma e collisione `specs/077`).
Deliverable: SWOT + backlog di 20 item P0–P2, archiviato in [[audit-swot-2026-07-02]] (syntheses).

**Finding di punta:** (P0) `sertor upgrade` default strappa l'assistente coesistente + capability creep;
licenza MIT apposta su speclift upstream senza licenza; BM25 senza auto-heal staleness nel server MCP;
~55k token obbligatori a SessionStart; 9 agenti speckit → skill inesistenti; doc `configure` invisibile +
quick-start Claude stale su GloVe. (Strategia) 9 feature meta consecutive vs `search_docs` MRR 0.55 fermo.
Repo privato ⇒ canale d'install per terzi oggi inesistente e version-check inerte.

**Rituale:** record ✅ (questa voce + pagina synthesis + index) · distill: non serve (l'audit È la
synthesis; nessuna nuova entità di design) · lint semantico: l'audit stesso è il lint — i drift trovati
(epic.md FEAT-015/E14-FEAT-001 stale, `CLAUDE.md` con `RAG_BACKEND` e policy-git contraddittoria,
fondo-roadmap zombie) sono FINDING in attesa di conferma utente, non corretti d'ufficio · EXEC roadmap:
non innescato (nessun cambio di stato capacità) · re-index: hook SessionEnd (enforced).

## [2026-07-02] record | A-01 upgrade safety multi-assistente (audit SWOT)

**Operazione:** `record` (fix host-facing installer, primo item del backlog audit SWOT).

**Cosa è stato fatto:** chiuso il footgun A-01 dell'audit — `sertor upgrade`/`uninstall` non fanno più
scelte silenziose. **Root cause verificata:** `install_rag.py:1074` usava `_union_owned(tuple(AssistantId))`
su ogni upgrade → l'obsolete-phase con `--assistant` default `claude` strappava un Copilot coesistente
(feature "assistant switch" FR-016/T042 innescata di default). **Fix (decisione utente 1–4 + b):**
(1) `--assistant` default → `None`; (2) primitiva di detection via marker autorevole nel file di istruzioni
(`SERTOR:RAG-USAGE`/`SERTOR:WIKI-RITUAL`) — distingue cruft da install reale; (3) verbo nudo = opera solo su
ciò che è installato (no capability creep, obsolete-scope = solo cruft di assistenti NON installati → coesistenza
preservata); (4) switch esplicito che rimuove un assistente coesistente = azione destructive con consenso
(`--yes`/TTY y-N, altrimenti UsageError exit 2 — **mai silenzioso**, decisione (b)). Simmetrico rag+wiki
(`execute_{rag,wiki}_lifecycle` parametrizzati con `obsolete_assistants`). Report aggregato deriva l'assistente
dai report reali (onesto: singolo→valore, multiplo→None).

**Test/esiti:** 5 nuovi test (coesistenza preservata su upgrade nudo · switch-senza-consenso rifiutato ·
switch-con-`--yes` funziona · no-capability-creep · clean-host no-op) + T042 aggiornato al consenso;
**491 test pacchetto `sertor` verdi**, kit lifecycle 23 verdi, ruff pulito. `sertor-core` **INVARIATO**
(Principio XI). Doc utente `docs/install.md §10.2` aggiornata (auto-detection + switch consentito).
Branch `086` (in apertura PR).

**Rituale:** record ✅ (questa voce) · **distill:** non serve (fix comportamentale, nessuna nuova entità
di dominio durevole; la meccanica lifecycle è già in [[installer-lifecycle]]) · **lint semantico:** pulito —
la doc utente e la roadmap (A-01 → 🔄) riflettono il cambiamento nello stesso step, nessuna contraddizione
introdotta · EXEC roadmap: A-01 già in IN PROGRESS · re-index/smoke: non innescati (nessun file del corpus
RAG toccato oltre wiki/roadmap, che l'hook SessionEnd re-indicizza).

## [2026-07-02] record | A-02 licenza speclift (in-repo) + handoff SpecAudit → E14-FEAT-003

**Operazione:** `record` (A-02 audit SWOT parte in-repo + elaborazione handoff Sinthari SpecAudit).

**Contesto:** dopo il merge di A-01 (`a9e84e3`/PR #141), l'utente ha chiesto di leggere l'handoff Sinthari
prima di A-02. L'handoff (`specaudit-handoff-sinthari.md`) consegna **SpecAudit** — gemello top-down di
SpecLift (verdetto per-requisito SODDISFATTO/PARZIALE/MANCANTE/DRIFTED/NON_DOCUMENTATO, moat strutturale,
non legge codice) — e ne chiede il vendoring in `sertor-flow`. Questo ha **riformulato A-02 dalla causa**:
il repo `themetriost/Sinthari` non ha LICENSE, e stiamo per vendorare un secondo pacchetto dallo stesso repo.

**Cosa è stato fatto:**
- **A-02 in-repo (✅):** l'utente ha confermato che Sinthari e Sertor hanno lo **stesso titolare legale**
  (org `themetriost`) → l'MIT apposto su `packages/speclift` è un **atto legittimo del titolare** (first-party),
  non una rilicenza di terzi. Reso onesto in `packages/speclift/VENDORING.md` (riga `upstream_license` +
  divergenza #5). **Azione residua tracciata (outbound, pendente ok+repo):** committare la LICENSE a monte nel
  repo Sinthari + re-pinnare, così i vendoring futuri (SpecAudit) la ereditano alla sorgente.
- **SpecAudit promosso:** `requirements/speclift/epic.md` FEAT-003 da «Could, capacità futura non decomposta»
  a **Should, 🆕 handoff ricevuto** (Sinthari `master` PR #8/`91c5a45`; runtime `src/specaudit/`, skill
  host-agnostica, 58 test); da vendorare in `sertor-flow` (o `packages/specaudit`, decisione stampo aperta).
  Prerequisito = SpecLift self-hosted (FEAT-001 ✅) → sblocca anche il loro T048/T049. Roadmap E14 + backlog
  audit (A-02 → 🔄) aggiornati; A-01 flippato a ✅ (post-merge).
- **Handoff archiviato** in `wiki/sources/input-other-agents/processed/specaudit-handoff-sinthari.md`
  (convenzione input-other-agents → processed).

**Rituale:** record ✅ · distill: non serve (SpecAudit ha già pagina concept a monte in Sinthari; qui è
backlog+provenienza, nessuna nuova entità di dominio Sertor) · lint semantico: pulito (VENDORING.md, epic.md,
roadmap coerenti tra loro e col fatto di titolarità) · EXEC roadmap: aggiornato (A-01 ✅, A-02 🔄, E14 FEAT-003) ·
re-index/smoke: hook SessionEnd (toccati solo requirements/wiki, re-indicizzati dall'hook).

## [2026-07-02] record | Vendoring SpecAudit in packages/specaudit (dogfood, E14-FEAT-003)

**Operazione:** `record` (vendoring nuovo membro workspace — self-host/dogfood di SpecAudit).

**Contesto:** su richiesta utente, **pausa dell'A-backlog** dell'audit per anticipare il vendoring di
**SpecAudit** (handoff Sinthari, E14-FEAT-003), così durante l'implementazione delle A si può testare la
coppia **SpecLift→SpecAudit** sui changeset reali (SpecLift traduce diff→requisiti bottom-up; SpecAudit
confronta col requisito originale top-down ed emette verdetto per-requisito).

**Cosa è stato fatto:** vendorato verbatim in **`packages/specaudit`** con lo **stampo di SpecLift**
(`packages/speclift`): `src/specaudit/**` (20 file) + test con nidificazione preservata (`tests/specaudit/`,
59 test) + skill + contratti + quickstart; nuovi `pyproject.toml` (scoped), `LICENSE` MIT, `VENDORING.md`
(pin Sinthari `e1bbdb2`). Registrato come membro `[tool.uv.workspace]` + `extend-exclude` ruff (verbatim,
non riformattato) + step CI per-pacchetto ("Tests/Lint — specaudit"). Skill copiata in
`.claude/skills/specaudit/SKILL.md` per il dogfood. **4 divergenze SOLO di packaging** (py `>=3.12`→`>=3.11`
verificato empiricamente; ruff `py312`→`py311`; pyproject scorporato dal monorepo Sinthari; LICENSE aggiunta).
`specaudit` NON importa `speclift` (consuma solo il suo `*.speclift.json`) → `specaudit → ∅`.

**Esiti (verificati indipendentemente):** **59 test verdi** su Python 3.12 e 3.11; ruff pacchetto pulito;
**ruff root pulito** (specaudit escluso); **non-regressione speclift 122 verdi**; `sertor-core` INVARIATO
(Principio XI — nessun import del core). Aggiustamento harness test: `pythonpath = ["src", "."]` + `tests/__init__.py`
(per far risolvere `from tests.specaudit.helpers` scorporando il pacchetto dal monorepo); nessuna logica di
test toccata.

**Collaterale (A-02 outbound):** Sinthari ha **già aggiunto la `LICENSE` MIT** al proprio repo (commit locale
`3e800a0`, recependo la nostra risposta) → A-02 risolto su entrambi i lati.

**Rituale:** record ✅ · **distill:** SpecAudit ha già la pagina concept [[speclift]] adiacente (pipeline
deterministica + moat); qui il durevole è la riga d'epica FEAT-003 + questo record — una pagina concept
[[specaudit]] dedicata è follow-up leggero se serve (calibrato al valore) · **lint semantico:** pulito
(epic.md, roadmap E14, backlog A-02, EXEC coerenti col vendoring reale e coi test verdi) · **EXEC roadmap:**
aggiornato (A-02 ✅, pausa A, FEAT-003 🔄 vendorato, prossimo A-03) · **re-index/smoke:** hook SessionEnd
(toccati requirements/wiki + nuovo codice `packages/specaudit`; il server MCP andrà riavviato per servire il
codice nuovo, non serve al rituale). **Prossimo:** commit branch+PR, poi ripresa A-03.

## [2026-07-02] record | A-03 BM25 staleness auto-heal (terza gamba MCP)

**Operazione:** `record` (fix `sertor-core`, ripresa A-backlog dell'audit dopo la pausa SpecAudit).

**Cosa era rotto (audit A-03):** `Bm25LexicalIndex._load` (`adapters/lexical/bm25.py`) cache-ava le entries
per collezione e le invalidava **solo** su `build`/`reset` in-process. Il server MCP memoizza la facade e il
motore di default è **ibrido** (BM25+denso) → dopo un re-index fatto da un **altro processo**, l'istanza
long-lived del server continuava a **fondere contro il corpus lessicale stantio**, in silenzio. Era l'unica
delle tre gambe dell'auto-heal a mancare: ChromaStore (ricrea il client su errore) e NetworkxCodeGraph
(reload su `(mtime_ns,size)`) già si auto-guarivano.

**Fix:** replicato il pattern del code-graph in `_load`: il cache ora è chiavato `token (mtime_ns,size) →
(entries,bm25,token_sets)`; a ogni `_load` si fa `stat` del sidecar e si ricarica se il token è cambiato;
un sidecar sparito invalida il cache stantio. Nessun riavvio del server necessario. Cambiamento chirurgico
(solo `bm25.py`), contratto di `query`/`lookup` invariato.

**Test/esiti:** 2 nuovi test in `test_bm25_lexical_index.py` — reload su rebuild cross-processo (il caso MCP
reale) + invalidazione su reset cross-processo (niente ritorno stantio). **1054 unit test verdi** (16 BM25),
ruff pulito. Questo è un fix **di** `sertor-core` (non un accesso via vehicle): legittimo, è codice di libreria.

**Rituale:** record ✅ · **distill:** non serve (l'entità durevole è [[hybrid-retrieval]]/[[mcp-server]], già
esistenti; l'auto-heal delle tre gambe è già descritto — al più una riga in [[mcp-server]] come follow-up
leggero) · **lint semantico:** pulito (roadmap A-03 coerente col fix reale e coi test) · **EXEC roadmap:**
A-03 → 🔄 · **re-index/smoke:** toccato codice `src/` → il server MCP andrà **riavviato** per servire il
codice nuovo (il fix riguarda proprio la freschezza; verificabile in dogfood post-merge). **Prossimo:**
commit branch+PR, poi girare SpecLift→SpecAudit sul changeset A-03 e mandare lo `speclift.json` a Sinthari.

## [2026-07-02] query | Primo dogfood end-to-end SpecLift→SpecAudit sul changeset A-03

**Operazione:** `query`/dogfood (primo uso end-to-end della coppia SpecLift+SpecAudit su un changeset reale).

**Contesto:** SpecAudit è stato anticipato (vendorato in `packages/specaudit`) proprio per testare la coppia
**durante** l'implementazione delle A dell'audit. Primo giro completo sul changeset di **A-03** (`572a509`,
BM25 staleness auto-heal).

**SpecLift** (Procedura B, agente+MCP): `changeset` → localizzazione via `find_symbol`/`search_code` →
`bundle --changeset --located` → EARS scritte dall'agente → `assemble`. Esito moat: **6 requisiti EARS,
6/6 àncore `verified`, 0 excluded**. Output reale `*.speclift.json` inviato a Sinthari (canale
`input-other-agents/`) per sbloccare il loro T049.

**SpecAudit** (intento originale A-03 in 2 bullet EARS REQ-001/002 vs le 6 EARS di SpecLift): `prepare` →
aggiudicazione dell'agente (allineamento N:M + verdetti) → `report`. Esito verificato: **SODDISFATTO 2 ·
PARZIALE/MANCANTE/DRIFTED 0 · NON_DOCUMENTATO 2**. I due SODDISFATTO confermano che A-03 ha consegnato il suo
intento; i due **NON_DOCUMENTATO** sono un finding reale: il codice gestisce **anche** il caso «sidecar
sparito» (reset) — consegnato ma non promesso dall'intento originale (rischio basso, da decidere se
promuovere a requisito esplicito).

**Valore dimostrato:** la coppia funziona su codice vero (non solo sui test upstream). SpecLift traduce il
diff in requisiti ancorati e riverificati; SpecAudit conferma la conformità e scova il consegnato-non-
documentato. È il ciclo lint-semantico che le due capacità promettono, esercitato sul dogfood.

**Rituale:** record ✅ (questa voce) · distill: non serve (capacità già in [[speclift]]; l'esperienza è
cronaca) · lint: n/a · re-index/smoke: n/a (nessun file del corpus toccato; artefatti in scratchpad + repo
Sinthari). **Nota:** merge A-03 in corso (PR #144); prossimo item audit = A-04.

## [2026-07-02] record | A-04 riduzione carico SessionStart (tagli sicuri; redesign EXEC rinviato cross-team)

**Operazione:** `record` (item A-04 audit SWOT — costo di contesto a ogni sessione).

**Cosa era il problema:** a ogni SessionStart si caricavano ~55k token (roadmap 97KB + index 50KB letti interi
+ CLAUDE.md 49KB always-on), e il blocco EXEC "mostrato" era degenerato a 542 righe (l'intero changelog DONE
dentro i marker).

**Cosa è stato fatto (tagli sicuri, senza preempire il redesign):**
- **`index.md` fuori dalla lettura obbligatoria** a SessionStart (on-demand): il catalogo si legge solo quando
  serve navigare il wiki. Applicato al hook fonte-unica `wiki-session-start.ps1` (dogfood + copia distribuita,
  byte-identiche) + direttiva inline `settings.json` allineata. ~13k token risparmiati/sessione, zero rischio.
- **`<!-- EXEC:END -->` spostato prima del changelog `### ✅ DONE`** → il blocco EXEC che si "mostra" a ogni
  sessione/merge torna un riassunto (197 righe vs 542); il DONE resta nel file come riferimento, fuori dai marker.
- **Potatura conservativa `CLAUDE.md`**: corretto il paragrafo git-policy stantio (diceva «push diretto su master»,
  contraddiceva la produzione → ora «branch + PR, mai push diretti», invariante configuration-manager) + condensati
  i due blockquote «ENFORCED VIA HOOK» verbosi a una riga (item rituali 5/8 intatti). −561 byte.

**Fuori scope (dichiarato):** la roadmap `roadmap.md` è **ancora letta intera** a SessionStart (~25k token): la
ristrutturazione profonda della *rappresentazione* EXEC è un **redesign cross-team (Noetix + Sinthari)** che
l'utente elabora e passerà — non preempito qui.

**Esiti:** hook eseguito OK (index on-demand); guardia sync dogfood↔asset **18 verdi**, assets **80 verdi**,
ruff root pulito; parità Claude/Copilot preservata. `sertor-core` invariato.

**Rituale:** record ✅ · distill: non serve (igiene di governance) · lint: pulito (roadmap/hook/CLAUDE coerenti) ·
EXEC roadmap: A-04 🔄, prossimo A-05 · re-index/smoke: n/a (toccati asset governance + wiki, non il corpus src).
**Prossimo:** commit branch+PR (bundlo il bookkeeping in sospeso), poi A-05.
