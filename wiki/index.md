---
title: Indice del Wiki — Produzione Sertor
type: index
tags: [produzione, wiki, index]
created: 2026-05-30
updated: 2026-07-15
sources: ["requirements/sertor-core/epic.md", "requirements/memoria-conversazioni/epic.md", ".specify/memory/constitution.md", "specs/001-nucleo-retrieval/**", "specs/002-rag-baseline/**", "src/sertor_core/**", "CLAUDE.md"]
---

# Wiki di Produzione — Sertor

Wiki della **fase di produzione** (costruzione del CLI `sertor`, vedi
[`../requirements/sertor-cli/epic.md`](../requirements/sertor-cli/epic.md)). È **nuovo** e
cumulativo: cresce a ogni sessione secondo lo schema in [`../CLAUDE.md`](../CLAUDE.md)
(sezione *Wiki & documentazione*), ispirato al pattern "LLM Wiki" di Karpathy.

> **Il wiki del prototipo è altrove e congelato.** I 4 motori RAG su FastAPI, il loro codice e il
> wiki storico vivono ora in [`../prototype/`](../prototype/) come **snapshot di sola lettura**.
> Quel wiki (`../prototype/wiki/`) è indicizzato nel **RAG di dogfooding**: per consultare il
> prototipo si interroga il server MCP **`sertor-rag`** (tool `search_code` / `search_docs` /
> `get_context` / `find_symbol` / …), non si modifica più a mano.

## 🧭 Da dove cominciare (percorsi di lettura)

Quattro porte d'ingresso, per intento — ogni percorso parte da una pagina-overview e scende al dettaglio:

| Vuoi… | Percorso |
|---|---|
| **Capire il prodotto senza gergo (non tecnici)** | [[sertor-in-parole-semplici]] (panoramica in parole semplici → una pagina per capacità) |
| **Sapere dove siamo e cosa fare adesso** | [[roadmap]] (executive summary in testa, poi mappa feature × stato reale) |
| **Capire il prodotto (il nucleo di retrieval)** | [[retrieval-core]] → le 4 entità: [[domain-model]] · [[ports-adapters]] · [[chunking-dispatch]] · [[indexing-and-retrieval]] → le modalità RAG: [[vector-retrieval]] · [[hybrid-retrieval]] (default) → la navigazione ortogonale: [[code-graph]] → le superfici: [[thin-consumer]] · [[mcp-server]] |
| **Capire il sistema-wiki (questo wiki)** | [[architettura-wiki-llm]] → la metà deterministica [[wiki-tools]] → il principio che li separa [[deterministic-vs-judgment]] → la disciplina d'uso [[step-ritual]] |
| **Conoscere le regole del gioco** | [[constitution]] (10 principi vincolanti) · [[mission-vision]] (il perché) · [[dogfooding]] (come ci usiamo) |

La **storia** (cosa è successo, quando) sta nei record di *Experiments* qui sotto e nel
[registro giornaliero](log/index.md).

## Come è organizzato

L'area si sceglie dalla **natura** della pagina (non dalla fase): vedi l'euristica di collocazione nel
playbook (`.claude/skills/wiki-author/wiki-playbook.md`, §3).

| Cartella | Contenuto |
|----------|-----------|
| `concepts/` | Astrazioni, fondamenta, principi (evergreen) |
| `tech/` | Tecnologie, strumenti, infrastruttura (evergreen) |
| `explainers/` | Descrizioni in **linguaggio comune** (per non tecnici): una lente sulle capacità |
| `experiments/` | Record datati di un'attività/step/feature svolta |
| `sources/` | Riassunti di fonti esterne ingerite |
| `syntheses/` | Viste d'insieme e confronti trasversali (la categoria più rara) |
| [`log/`](log/index.md) | Registro append-only, **un file per giorno** (rotazione, FEAT-008) |

## Pagine

> 🗺️ **[[roadmap]] — Roadmap & stato di prodotto (pagina viva).** Punto d'ingresso per "dove siamo":
> mappa delle feature (epica `sertor-core`/`sertor-cli`), cosa è su `master` vs su branch, lavori
> abilitanti mergiati, fasi e backlog. Tienila aperta come quadro d'insieme.

### Explainers (per non tecnici — linguaggio comune)

- **[[sertor-in-parole-semplici]]** — 🧭 **Panoramica per non tecnici.** Cos'è Sertor e perché, con
  immagini quotidiane e zero gergo; ingresso che rimanda a una pagina per capacità. Ogni explainer
  ha un rimando «dettaglio tecnico» alla pagina di concetto corrispondente.
- **[[ricerca-codice-e-documenti]]** — Il bibliotecario che trova le pagine giuste *per significato*, non per parola esatta.
- **[[i-modi-di-cercare]]** — Quattro tecniche dalla più semplice alla più furba (significato · mista · mappa delle connessioni · ragionata).
- **[[wiki-che-cresce]]** — La memoria scritta del progetto che cresce a ogni sessione invece di ripartire da zero.
- **[[collegamento-con-l-assistente]]** — La "presa universale" a cui l'assistente AI si attacca per usare Sertor.
- **[[installare-su-un-progetto]]** — Installazione con un comando, ordinata e non distruttiva, su qualunque progetto.
- **[[robusto-ed-economico]]** — Le migliorie di robustezza e costo: riprova sugli intoppi, sa dire «non lo so», memoria del già-fatto, mostra il costo.
- **[[il-pannello-di-controllo]]** — 🚧 *In sviluppo.* Il cruscotto da terminale che mostra costo, risparmio della cache e salute dell'archivio, con i numeri che restano per i report.
- **[[memoria-negli-agenti]]** — ✅ *Fatto (MVP).* La memoria della conversazione che cresce a ogni sessione: il registratore intelligente che conserva i transcript ripuliti dai segreti, base della ricerca episodica (domande-nel-passato) e della distillazione futura.
- **[[sertor-con-copilot]]** — ✅ *Fatto (FEAT-007).* Sertor ora si aggancia anche a **GitHub Copilot**, non solo a Claude: scegli l'assistente in installazione e ricevi gli stessi attrezzi nella forma che lui capisce. La "cassetta con due attacchi".

### Concepts (fondamenta e astrazioni)

- **[[retrieval-core]]** — Il **nucleo di retrieval** importabile (`sertor-core`), *il prodotto*: architettura Clean (domain/services/adapters/engines + porte `Protocol`), composition root guidato da `Settings`, backend `local`/`azure`, policy errori tollerante↔strict, collezioni namespaced per `(corpus, provider)`. CLI/MCP/wiki ne sono consumatori sottili. *Scomposto nelle 4 pagine-entità sotto.*
- **[[retrieval-vs-graph]]** — Guida di scelta: ricerca **ibrida** (scopri per significato) vs **code-graph** (naviga per struttura), pattern *scopri → naviga*, tabella domanda→tool. Ortogonali, non alternative.
- **[[domain-model]]** — Le **entità dati pure** del nucleo (`Document`, `Chunk`/`ChunkMetadata`, `EmbeddedChunk`, `RetrievalResult`, `IndexReport`; enum `DocType`/`ChunkerKind`): nessun SDK nel dominio, id stabili (path POSIX, `doc_id#index`) → idempotenza del rebuild.
- **[[ports-adapters]]** — Le **sei porte** `Protocol` (`EmbeddingProvider`, `VectorStore`, `LexicalIndex`, `Reranker`, `CodeGraph`, `RetrieverStrategy`) e gli **adapter** che le implementano (Ollama/Azure · Chroma/Azure Search · BM25 · FlashRank · networkx); il composition root sceglie da `Settings` con import lazy. Structural typing → mockabili.
- **[[chunking-dispatch]]** — Il **chunking** `Document`→`Chunk`: dispatch per tipo/lingua (markdown / sintattico tree-sitter / fallback dimensionale), 10 linguaggi sintattici, esclusione deliberata R-N2 di PowerShell/SQL, id stabile `doc_id#index`.
- **[[indexing-and-retrieval]]** — Le **due pipeline**: indicizzazione (ingest→chunk→embed→store, atomicità del rebuild, DEFAULT incrementale da FEAT-009 con manifest persistito) e la **facade** `search_code/docs/combined`, tollerante su indice assente (`[]`+warning). Punto d'ingresso dei consumatori via `build_facade()`.
- **[[vector-retrieval]]** — La **prima modalità RAG**: retrieval vettoriale (embed query → similarity top-k) realizzato dal motore baseline; policy errore *strict* (`IndexNotFoundError`) + valutazione hit-rate@k/MRR@10. Dal 2026-06-12 non è più il default (→ [[hybrid-retrieval]]) ma resta la baseline di confronto, selezionabile con `SERTOR_ENGINE=baseline`.
- **[[hybrid-retrieval]]** — La **seconda modalità RAG e nuovo default** (FEAT-004, PR #24): BM25 lessicale (porta `LexicalIndex`, sidecar atomico) + via densa fusi con RRF deterministico; degradazione onesta sui corpora pre-ibrido (warning, mai errore); reranking FlashRank come extra opzionale; consumatori invariati via strategia iniettata. Qualità: simboli hit@5 0→1.00, xfail storici chiusi strict.
- **[[retrieval-confidence]]** — Il **segnale di confidenza** (FEAT-018, PR #32): soglia di similarità opzionale (`SERTOR_MIN_SCORE`, default off) che esclude i risultati deboli ed emette `low_confidence`, dando all'agente il materiale per **astenersi** (grounding nel modello composito); additivo (contratto invariato). Asimmetria voluta: nell'ibrido filtra il pool denso **prima di RRF** (lo score RRF non è una similarità). Compagno: retry resilienza embedder (stessa 018).
- **[[valutazione-e-non-regressione]]** — La **capacità di valutazione del retrieval** (FEAT-001, epica retrieval-qualita): ground-truth versionato su disco, `sertor-rag eval` per hit-rate@k/MRR con gate di non-regressione su baseline+tolleranza, skill di genesi assistita e feedback pertinenza; ciclo di vita completo suite di valutazione host-side.
- **[[dedup-risultati]]** — La **collapse dei risultati near-duplicate** a query-time (E5-FEAT-003): fuzzy shingle a 5 parole + coefficiente di **containment** (≥0.8) prima del taglio top-k, `SERTOR_DEDUP` (default on), pool > k per backfill. Perché il dedup *esatto* falliva (copie byte-identiche `CLAUDE.md`↔`assets/**` con confini-chunk diversi) → il fuzzy ha dato il lift (`search_docs` hit@3 0.62→0.75). Cattura la leva residua distinta «doc-correlati» (spec/log affini che competono a k=3).
- **[[code-graph]]** — La **navigazione strutturale del codice** (FEAT-005, PR #25), ortogonale ai motori: grafo AST deterministico (porta `CodeGraph`, artefatto JSON per corpus, build dentro `index()` — mai stantio), copertura per-linguaggio DICHIARATA e verificata sui 10 linguaggi; i 4 tool MCP storici tornati (find_symbol/who_calls/related_docs/get_context); due semantiche di assenza, niente LLM/cloud.
- **[[thin-consumer]]** — Il pattern per cui le interfacce (CLI, server MCP, tool) espongono il [[retrieval-core]] importandolo e cablandolo dalle factory `build_*`, **senza reimplementare logica**: il prodotto è la libreria, l'interfaccia è un guscio sottile (host-agnostico, Principio X). Esempio realizzato: il server MCP.
- **[[speclift]]** — Pipeline **deterministica diff→requisiti ancorati** (Sinthari): 9 stadi meccanici (ingest→parse→filter→locate→bundle→verify→render) + giudizio agentico. Il moat riverifica ogni àncora sul filesystem. Due adapter locator: Adapter A via CLI Sertor, Adapter B via MCP (adatto al consumo esterno). Artefatto: `located.json` con field `verified`. Integrazione Sertor: three-gear agente-driven via MCP.
- **[[specaudit]]** — Il **gemello top-down** di [[speclift]]: dato un requisito ORIGINALE + l'output ancorato di SpecLift sullo stesso changeset, emette un **verdetto per requisito** (SODDISFATTO/PARZIALE/MANCANTE/DRIFTED/NON_DOCUMENTATO) **senza leggere codice** (confine D↔N: CLI deterministica prepara+verifica, agente allinea+giudica; cita-non-verifica le àncore = moat). Package proprio `packages/specaudit` (zero-deps + MCP); distribuzione folded in `sertor-flow` (E14-FEAT-002).
- **[[dogfooding]]** — Interrogare il progetto stesso col proprio RAG: Sertor indicizza il proprio codice/doc come corpus e li consulta coi suoi tool (server MCP `sertor-rag`) invece di leggerli a mano. Validazione continua + contesto ancorato.
- **[[dogfood-fidelity]]** — Il dogfood non basta che *interroghi* sé stesso: deve *essere* ciò che un ospite otterrebbe. Runtime installato `.sertor/` che traccia HEAD + re-lock post-merge, confine dev↔dogfood, asset- vs process-fidelity. Modello dell'epica E15 (F1/F5/F8 ✅, F4 asset-install 🔜).
- **[[esito-sull-host-vs-forma-dell-asset]]** — Una guardia può essere verde e cieca. Il buco: testare la **forma dell'asset** (il file è giusto?) non dice se il wiring **arriva** all'ospite quando aggiorna. Il caso reale (E10-FEAT-031/032): asset dichiarante corretto, merge che duplica/scarta. Regola: asserire l'**esito su un host che aggiorna** (non dal vuoto), non solo la forma spedita. Compagno: [[identita-hook-nel-merge]].
- **[[auto-heal-staleness]]** — L'invariante per cui un **client long-lived** (il server MCP) non deve **mai** servire dati stantii dopo un re-index **cross-process**. Le quattro «gambe» dell'auto-guarigione: Chroma refresh-on-error (PR #89) · code-graph reload su `(mtime_ns,size)` (PR #90) · BM25 token reload (A-03) · reclamo del lock d'indice con PID morto (FEAT-035). Pattern condiviso: rileva a basso costo · re-inizializza in trasparenza · resta conservativo se ambiguo. Complemento runtime dell'hook di freschezza.
- **[[osservabilita]]** — Il **sottosistema di osservabilità**: `log_event` come fonte unica, ogni capacità un `Handler` additivo — persistenza F1 → visibilità TUI → **export OTel** (`OtelExportHandler`, GenAI semantic-conventions, privacy metrics-only) — cablati uniformemente al confine dei vehicle via `enable_observability` (Principio XI). Perché esiste: rendere **dimostrabili** qualità/costo/freschezza del retrieval.
- **[[deterministic-vs-judgment]]** — Il confine **meccanico** (codice, zero LLM, testabile) ↔ **giudizio** (LLM: cosa scrivere, è una contraddizione?). Principio trasversale: massimizza il deterministico, riserva all'LLM solo il giudizio; guida anche la delega.
- **[[diary-vs-graph]]** — Le **due memorie** del wiki: il **diario** (log + record datati, append-only, «cosa è successo») vs il **grafo** (pagine-entità aggiornate in place, «cosa è vero adesso»); i tre strati scrivano/cronaca/travaso (`append-log` · `record` · `distill`) e i tre ingressi della distillazione (step · backlog · conversazione intera).
- **[[constitution]]** — Ratifica Costituzione v1.0.0 (2026-05-31) → v1.1.0 (2026-06-05, aggiunto Principio X host-agnostico); 10 principi vincolanti, gate Constitution Check. Governa design e produzione.
- **[[mission-vision]]** — Mission/Vision canonizzate in README.md; Principio X come vincolo operativo; backlog: refactor host-agnostico di skill wiki/playbook/rituale.
- **[[wiki-role-da-w1]]** — DA-W1 risolta: il wiki è CORPUS + SUPERFICIE; identità, autorità, confine MVP, ruoli 1–3.
- **[[step-ritual]]** — Rituale di step (Definition of Done): a ogni step → record + lint di allineamento + azioni standing estendibili. Standing behavior vs automazione unattended; fonte unica = `CLAUDE.md`. *(Retrospettiva estratta in [[retrospettiva-interazione-2026-06-04]].)*
- **[[memoria-conversazioni]]** — Il **tier episodico** della memoria: archivio conservato di sessioni (grezzo, prima della distillazione), fonte grezza per la ricerca episodica (FEAT-002) e la distillazione (FEAT-003). Hermes pattern. Privacy-by-default (default off), scrub segreti nel contenuto (no segreti mai), host-agnostico (porta + adapter).

### Experiments (record di attività/step/feature)

- **[[feat-010-hook-portabili]]** — ✅ **A-09 / E2-FEAT-010 portabilità POSIX degli hook** (2026-07-09, branch `095-portable-hooks-09`): **8 hook host-facing riscritti in Python portabile**, invocati via `uv run --no-project python <hook>.py` — eliminazione totale della dipendenza da PowerShell/`pwsh` (inerti su mac/Linux). Single-impl (rimozione `.ps1`), `sertor upgrade` migra legacy, E10-FEAT-018 supersessa, docs ospite aggiornate. Portabilità POSIX: installabile su Windows · macOS · Linux. Esiti: SpecKit completo, Constitution 12/12 ✅, sertor 510/kit 149/root 1166 verdi, ruff pulito, parity gate + smoke CI matrice (ubuntu+windows), prossimo: merge + live migration.
- **[[asset-install-installer-dry-run-2026-07-04]]** — 🔍 **Dry-run empirico dei 3 veri installer su dogfood** (2026-07-04, E15 asset-install): esecuzione di `sertor install rag/wiki` e `sertor-flow install` da `git+url@master` via `uvx` come un ospite reale, per osservare la process-fidelity degli asset prima di automatizzare. Verdetto: **per lo più idempotente**; asset-fidelity byte confermata (FEAT-002), residuo process-fidelity contenuto su 3 fronti (CLAUDE.md ibrido DA-1, CRLF churn FEAT-010, `wiki/log/` legacy FEAT-006). Log entry + pagina registrati.
- **[[asset-install-scope-b-implementation-2026-07-06]]** — ✅ **E15-FEAT-001 scope B implementazione** (2026-07-06, asset-install): trasformazione in realtà del modello «dogfood alimentato dai veri installer» — i 3 installer eseguiti su dogfood risolvono i 3 finding del dry-run (FEAT-010 .gitattributes LF, CLAUDE.md ibrido, wiki/log schema). **Zero special-case**: il dogfood produce sé stesso via processi reali. Pipeline SpecKit completa, idempotenza verificata (block:0), Constitution 12/12 ✅, 2122 test verdi, spec.md + tasks completati.
- **[[feat-084-speclift-self-host]]** — ✅ FEAT-084 (2026-07-01): self-hosting di SpecLift (Sinthari) come membro workspace. Handoff → feedback (CLI→MCP) → Sinthari rende engine pluggable (Adapter A/B) → vendoring puro (zero fork, convergenza upstream). Flusso three-gear agente-driven: `speclift changeset` + MCP search + `speclift bundle`. Deterministic pipeline (9 stadi) + moat riverifica → agenzia giudizio EARS. Branch `speclift`, PR #136, Constitution 12/12, 122+487+151+140 test verdi, pagina experiment + concept + log entry registrati.
- **[[feat-083-default-model-policy-copilot]]** — ✅ E2-FEAT-015 (2026-07-01): default model assignment per i 5 agenti Sertor-authored su Copilot CLI. Fonte unica versionata `model_policy.py` nel kit, fail-loud install-time, frontmatter `model:` nativo, guardie riconciliate. Branch `083`, Constitution 12/12, zero core, 1134 test verdi.
- **[[feat-024-parity-guard-budget]]** — ✅ E10-FEAT-024 (2026-06-30): parity guard esteso a `.ps1`/`.json` + budget righe per blocchi CLAUDE.md in CI. Tre guardie: shape wiring Copilot · budget per-blocco wiki=60/rag=58/sdlc=70 · no-payload hook `.ps1`. Test-only, Constitution 12/12, zero core.
- **[[feat-023-stub-copilot]]** — ✅ E10-FEAT-023 (2026-06-30): rimozione dello stub fuorviante `assets/copilot/` (4 `.gitkeep`, zero consumatori). Git rm + guardia anti-regressione. Zero core, Constitution 12/12.
- **[[epiche-sertor-core-e-cli]]** — Ristrutturazione: Sertor Core (MVP, capacità RAG + Wiki) primaria; Sertor CLI (distribuzione via CLI) secondaria. Questione aperta DA-W1 su ruolo wiki.
- **[[decomposizione-must-core]]** — Decomposizione dei 3 Must (FEAT-001/002/003); 6 decisioni di ambito MVP; nuova FEAT-009 su refresh incrementale.
- **[[chiusura-prototipo-dogfooding]]** — Isolamento del prototipo, motore corpus-aware, RAG di dogfooding su se stesso, MCP ri-puntato.
- **[[piano-nucleo-retrieval]]** — Piano SpecKit FEAT-001: architettura Clean, decisioni R1–R8, Constitution Check ✅ (Principi I+IV), modello dati, contratti, scope MVP vs post-MVP.
- **[[implementazione-nucleo-retrieval]]** — Record datato del completamento FEAT-001 (2026-06-03): 53 test, ruff clean, Constitution Check 9/9 ✅. **Distillato** (2026-06-08): l'architettura è migrata nelle 4 pagine-entità del nucleo; qui resta l'evento + esito.
- **[[motore-baseline-feat002]]** — Record FEAT-002 (2026-06-03): 67 test, Constitution 9/9 ✅, estensioni non-breaking al nucleo. **Distillato** (2026-06-08): entità in [[vector-retrieval]]/[[indexing-and-retrieval]].
- **[[nucleo-wiki-deterministico-feat003d]]** — Record FEAT-003-D (2026-06-05, PR #13): 11 moduli, 44 test, Constitution 10/10 ✅, SC-001 host-agnosticità dimostrata. **Distillato** (2026-06-08): entità in [[wiki-tools]].
- **[[ponte-d-n-host-agnostico]]** — Primo step FEAT-003-N (ponte D→N): il layer agentico (playbook + skill + comando + agente) reso host-agnostico (legge `wiki.config.toml`) e poggiato sulla CLI `sertor-wiki-tools` per il meccanico; all'LLM resta il giudizio. Rename coerente: `genera-wiki`→`wiki-author`, `playbook.md`→`wiki-playbook.md`, `wiki-keeper`→`wiki-curator` (+Bash). Tabella confine D↔N; scope leggero (zero codice).
- **[[server-mcp-produzione-feat-mcp]]** — Record FEAT-MCP (2026-06-06, PR #15): SpecKit completo, 6 test, Constitution 10/10 ✅, `.mcp.json` ri-puntato alla produzione (corpus `sertor`). **Distillato** (2026-06-08): entità in [[mcp-server]].
- **[[meccanica-log-feat008]]** — Record FEAT-008 (2026-06-08, PR #18): meccanica del log di [[wiki-tools]] — rotazione a un file/giorno, `append-log` curato in CLI, `migrate` dello storico. SpecKit completo, 22 test, Constitution 10/10 ✅. Attivazione su Sertor deferita post-merge.
- **[[store-backend-disaccoppiato-feat009]]** — Record FEAT-009 (2026-06-09): `store_backend` disaccoppiato dal provider di embeddings + `AzureEmbedder` su endpoint v1. Abilita embeddings Azure + store Chroma locale → **costruito il primo indice dogfood del corpus `sertor`** (191 doc / 1578 chunk). Distillato in [[ports-adapters]]/[[corpus-index-naming]].
- **[[spec-010-query-congiunta-e-upsert-index]]** — Feature 010 (`specs/010`, 2026-06-10): i pezzi D residui di FEAT-003 (query congiunta multi-collezione + `upsert-index` CLI) portati da requirements a **implementazione** in un SpecKit completo in giornata; 4 decisioni clarify (fail-fast su provider eterogenei, corpora da Settings, multilinea → errore, fan-out solo `search_combined`); 159 test verdi, validata live sul dogfood; PR #20.
- **[[pulizia-pycache-e-diagnosi-mcp]]** — Record del 2026-06-05: rimossi 16 dir `__pycache__` fantasma + diagnosi architetturale di `.mcp.json`. ⚠️ **Diagnosi superata il 2026-06-06** (banner nella pagina): `sertor_mcp` (PR #15) e `wiki_tools`/FEAT-003-D (PR #13) sono su master, `.mcp.json` ri-puntato alla produzione. *(Spostata da `tech/` a `experiments/`: è un record datato, non una tecnologia.)*
- **[[retrospettiva-interazione-2026-06-04]]** — Retrospettiva onesta sull'interazione del 2026-06-04 (pattern di ostruzione percepito, radici plausibili, correttivo adottato); separata dal design del rituale per atomicità.
- **[[feat-007-elicitazione-gap-analysis]]** — Elicitazione FEAT-007 (manutenzione wiki): gap analysis su dote dichiarata vs stato attuale (2026-06-12). Quattro decisioni risolte: D1 probe di freschezza eliminato (Won't), D4 tema lingua risolto (asset EN canonico + contenuto governato da config), D3 seed localizzati via tabella modulo, D2 trigger periodico Could. Perimetro finale: 23 REQ (move/reconcile/collect+status/seed it+en/asset coordination verso FEAT-012).
- **[[feat-001-memoria-cattura-archiviazione]]** — ✅ **Implementazione FEAT-001 memoria** (PR #45, 2026-06-14): il primo tier dell'epica «Memoria conversazioni» — cattura locale e archiviazione dei transcript in SQLite (`memory.sqlite`, gitignored, per-progetto, conservato). 8ª porta `TranscriptCaptureAdapter` host-agnostica, adapter Claude-Code, store idempotente. Privacy-by-default, scrub segreti nel contenuto. 29/29 task, 488 test, Constitution 10/10 ✅.
- **[[feat-002-ricerca-episodica-fulltext]]** — ✅ **Implementazione FEAT-002 ricerca episodica** (PR [oggi], 2026-06-14): il secondo tier — ricerca **full-text locale** sull'archivio di FEAT-001 a granularità di turno. Motore FTS5 nativo SQLite, BM25 ranking, vincoli temporali opzionali, privacy offline. 27 test, Constitution 10/10 ✅. Prova live: query «Langfuse» su 5062 turni → trovato in 78ms.
- **[[feat-035-superficie-cli-memoria-hook-sessionend]]** — ✅ **Completamento MVP memoria conversazioni** (2026-06-14): superficie CLI thin-consumer (`sertor-rag memory search/archive`) + hook SessionEnd automatico (cattura silenziosa a fine sessione). Privacy-by-default (off se `SERTOR_MEMORY` non configurato); hook non-bloccante (exit 0 sempre). SpecKit completo, 33/33 task, 12 test nuovi, 527 test non-cloud verdi, Constitution 10/10 ✅.
- **[[feat-036-aggancio-distillazione]]** — ✅ **Aggancio della distillazione all'archivio episodico** (PR #51, 2026-06-14): chiude il loop cattura→distillazione dell'epica memoria — archivio episodico diventa fonte legale per l'operazione `distill` del wiki (finora teorica su trascrizione grezzo). Nuovi comandi CLI `memory list`/`show`, metodo `list_recent` in MemoryArchive, entità `SessionSummary`. Thin consumer additivo, zero nuove porte. Vincolo FR-013: distillazione SEMPRE su singola sessione, mai automatica. SpecKit completo, 558 test, Constitution 10/10 ✅.
- **[[feat-004-ricerca-semantica-memoria]]** — ✅ **Implementazione FEAT-004 ricerca semantica sull'archivio episodico** (2026-06-22, branch `072-ricerca-semantica-memoria`): aggiunge il **tier semantico** all'archivio memoria — indicizzazione embedding dei turni (granularità turno), ricerca per **significato** opt-in separato (flag `--semantic` su `memory search`). Riuso puro primitive RAG (build_embedder/store), store dedicato, incrementalità append-only (marker = collezione), **gap chiuso:** `ChromaStore.contains_ids` per backfill Chroma (NFR-009). Privacy stratificato: `SERTOR_MEMORY_SEMANTIC=true` + provider locale (FEAT-011). Auto-index non-fatale a fine sessione. Esiti: 998 test non-cloud verdi (+48 feature), Constitution 12/12 ✅, ruff pulito. Gap dichiarato: manopole template `.env` installer P2 (FEAT-009 follow-up).
- **[[feat-008-cattura-copilot-cli]]** — ✅ **Implementazione FEAT-008 cattura transcript su GitHub Copilot CLI** (2026-06-22, branch `073-cattura-copilot-cli`): secondo adapter `CopilotCliTranscriptAdapter` dietro porta `TranscriptCaptureAdapter` — legge `events.jsonl` da `~/.copilot/session-state/`, parsing identico a Claude Code. Associazione progetto via `cwd`/`gitRoot` in session.start (asimmetrica, anti-misattribuzione). Privacy offline (niente cloud-sync). Rende vivo l'hook `SessionEnd` già distribuito su Copilot da FEAT-009. Esiti: 1039 test non-cloud verdi (+32 feature), Constitution 12/12 ✅, ruff pulito. Gap dichiarato: manopole installer FEAT-009 phase-2. Vedi anche [[copilot-cli-session-storage]] (source ricognizione tecnica).
- **[[feat-009-distribuzione-memoria-via-installer]]** — ✅ **Implementazione FEAT-009 distribuzione memoria via installer** (2026-06-22, branch `071-distribuzione-memoria-installer`): chiusura del debito di completamento — l'MVP memoria (FEAT-001/035) era vivo sul dogfood, ma non negli asset dell'installer. Ospiti che facevano `sertor install rag` **non ricevevano la memoria** → violava Principio X. Soluzione: pattern dell'hook rag-usage (FILE + SETTINGS_MERGE, lifecycle, per-assistente), riuso totale tipi artefatto. Consegne: (1) 8 manopole memoria nei due template .env (off di default), (2) hook `memory-capture.ps1` in asset + voce SessionEnd per-assistente (Claude/Copilot CLI), (3) cenno condizionale nel blocco `SERTOR:RAG-USAGE`, (4) ciclo di vita completo (install/upgrade/uninstall). Esiti: 952 test non-cloud verdi (sertor 323 +30 nuovi), Constitution 12/12, ruff pulito. Gap dichiarato: adattatore cattura Copilot richiede FEAT-008 corpo (implementato), invocazione ospite dipende da FEAT-003 wizard. **Non ancora mergiato.**
- **[[feat-074-doctor-salute]]** — ✅ **Implementazione E12-FEAT-001 sertor-rag doctor** (2026-06-23, branch `074-doctor-salute`): **primitiva di diagnostica deterministica** per rispondere a «ha funzionato?» post install/configure. Quattro aree (config/env, provider embeddings, indice RAG, MCP) → verdetto per-area **pass/warn/fail**, causa + rimedi concreti. Sola lettura, offline-safe (probe --online opt-in), exit code gate. DA-D4/D5a/D5b decise. Vehicle CLI (Principio XI), servizio puro + formatter + helper nel composition. Esiti: 1118 test root (333 sertor +34 doctor), Constitution 12/12 ✅, ruff pulito. Backlink [[valutazione-e-non-regressione]]/[[mission-vision]]/[[indexing-and-retrieval]]/[[mcp-server]].
- **[[feat-075-guided-setup]]** — ✅ **Implementazione E12-FEAT-002 guided-setup** (2026-06-23, branch `075-guided-setup`): **prima feature agentica** dell'epica usabilità — skill `guided-setup` + agente concierge orchestrano 6-step deterministic (detect→choose→install→configure→index→verify), euristica provider minimale, consenso mutazioni, riuso pattern FEAT-007 distribuzione dual-target, vehicle-only (CLI no reimplementazione), host-agnostico (Principio X). Esiti: 363 test (invariato, core untouched), Constitution 12/12 ✅, ruff pulito. Backlink [[assistant-targeting]]/[[feat-074-doctor-salute]]/[[sertor-flow]]/[[mission-vision]].
- **[[feat-018-portabilita-os-hook]]** — ✅ **Implementazione E10-FEAT-018 portabilità OS hook + onestà surface inerti** (2026-06-30, branch `078-portabilita-os-hook`): realizzazione di Principio XII + Principio X — guardia deterministica `pwsh_available` (stdlib-only nel kit), note ospite azionabili per mancanza PowerShell Core su non-Windows, onestà sui surface Copilot inerti (memory-capture = gap FEAT-009, SessionStart = NON inerto, solo UX degradata). Zero core, audit drift corretto. Esiti: SpecKit completo, Constitution 12/12 ✅, sertor 451/kit 139/root 1131 verdi, ruff pulito, prossimo: prova LIVE ospite. Branch `078`.
- **[[feat-019-fail-loud-hook-agent]]** — ✅ **Implementazione E10-FEAT-019 fail-loud + fallback agente** (2026-06-29, branch `077-fail-loud-hook-agent`): realizzazione del Principio XII — breadcrumb JSON ispezionabile `.sertor/.last-hook-error` su 4 hook (non-bloccanti), fallback uniforme agente STOP+segnala (concierge/wiki-curator/requirements-analyst byte-identico Claude↔Copilot). Zero core, 4 guardie anti-regressione. Esiti: SpecKit completo, Constitution 12/12 ✅, 1131 test root verdi, ruff pulito, prossimo: prova LIVE ospite (giudizio). Branch `077`.
- **[[feat-022-pulizia-stile-skill]]** — ✅ **Implementazione E10-FEAT-022 pulizia stile skill distribuite** (2026-06-30, branch `080-pulizia-stile-skill`): igiene host-facing, ALL-CAPS→imperativo+why (5 skill: guided-setup/eval-suite-author/eval-feedback/wiki-playbook/requirements), condensazione What-NOT sezioni (rimozione duplicati), ToC wiki-playbook + pointer centralizzato eval-skills a `sertor-cli-reference.md`, rimozione orfani wikilink (Principio X). Zero core, zero cambio semantico. Guardie: ALL-CAPS=0, zero orfani, pointer eval presenti. Riduzione: guided-setup −23 (−17%), eval-suite −15 (−12%), eval-feedback −3; totale skill ~541→~519 (−4%). Esiti: SpecKit completo, Constitution 12/12 ✅, sertor 473/kit 139/root 1131 verdi, ruff pulito. **Finding dichiarato:** fork IT dogfood eval-skill non-sincronizzato promosso FEAT-025 backlog. Prossimo: merge branch `080`. Branch `080`.
- **[[feat-021-altitude-claude-md]]** — ✅ **Implementazione E10-FEAT-021 ridurre altitude CLAUDE.md + fonte unica "How to invoke"** (2026-06-30, branch `079-altitude-claude-md`): igiene host-facing, fattorizzazione «How to invoke» triplicato in asset canonico `sertor-cli-reference.md` (deposito `.sertor/`, owned), blocchi CLAUDE.md ridotti a direttiva+pointer. Applicazione Principio III DRY. Riduzione: RAG-usage 72→49 (−23) · wiki-ritual 71→52 (−19) → totale 208→~166 righe (−20%). Zero core, 1 file in install_rag.py (CREATE_IF_ABSENT), zero `ArtifactKind`/Surface/WriteStrategy. Guardie: fonte unica assert+grep, closure pointer, parità Copilot offline. Esiti: SpecKit completo, Constitution 12/12 ✅, sertor 456/kit 139/root 1131 verdi (3 skip), ruff pulito, prossimo: prova LIVE ospite. Branch `079`.

### Sources (riassunti di fonti esterne ingerite)

- **[[copilot-cli-session-storage]]** — Ricognizione tecnica GitHub Copilot CLI: struttura di memorizzazione delle sessioni, transcript storage in `~/.copilot/session-state/<uuid>/`, associazione progetto via metadata, sincronizzazione cloud. Fonte per FEAT-008 (adapter cattura multi-assistente).
- **[[rag-production-audit-framework]]** — Framework di audit production-readiness per RAG (freeCodeCamp, Paulo Dichone): 14 pillar (A–I baseline, J–N rigor), 8 hard gate non-negoziabili, 4 tier (Prototype → Hardened), report fisso. Validazione esterna vs best practice; mappa i gap di Sertor alla roadmap.
- **[[karpathy-llm-wiki]]** — La **fonte fondativa** del sistema-wiki: gist originale di Karpathy (aprile 2026) — tre strati (raw/wiki/schema), tre operazioni, indice+log, il claim "sotto ~50-100k token il contesto batte il RAG" (tensione segnalata in [[wiki-role-da-w1]]), radice nel Memex di Bush.
- **[[llm-wiki-v2-agentmemory]]** — Estensione "grassa" del pattern: memory lifecycle (confidence/supersession/decay), typed knowledge graph, hybrid search con RRF, automazione event-driven + le critiche dei commentatori. Input di design per N6 e FEAT-004/005.

### Syntheses (viste d'insieme e sintesi trasversali)

- **[[audit-fedelta-dogfood-2026-07-03]]** — 🔍 **Audit di fedeltà dogfood↔client (2026-07-03).** Tre ricognitori paralleli (RAG, wiki, governance) verificano se il dogfood di Sertor riflette il prodotto distribuito agli ospiti. Verdetto: no, non su tutto. Due livelli di fedeltà — asset-fidelity (stessi file, parziale via sync) e process-fidelity (processi reali installer, assente ovunque). Correzione empirica: `specify init --force` preserva la costituzione v1.4.0 (solo `plan-template.md` a rischio). Deliverable: epica E15 `fedelta-dogfood` con 6 FEAT backlog.
- **[[audit-swot-2026-07-02]]** — 🔍 **Audit indipendente SWOT + backlog (2026-07-02).** Cinque audit paralleli (core · packages/CI · governance · backlog · doc/wiki): core production-ready su local/Chroma; footgun `upgrade` multi-assistente, skill speckit fantasma, ~55k token a SessionStart, deriva processo>prodotto (leva `search_docs` MRR 0.55 ferma); 20 item prioritizzati P0–P2.
- **[[roadmap]]** — 🗺️ **Roadmap & stato di prodotto (pagina viva).** Mappa feature epica × stato reale (master/branch), lavori abilitanti mergiati (incl. FEAT-009 indice dogfood), fasi, backlog e idee da discutere. Reconcilia le due numerazioni (epica `FEAT-NNN` ≠ `specs/NNN`).
- **[[conoscenza-schema-sql-rag]]** — Ricognizione prior art (DataHub/WrenAI/Vanna/RASL/SchemaGraphSQL): il panorama dei sistemi che interrogano la conoscenza-schema di un database (DDL, stored procedure, pattern d'accesso). Nessun sistema esistente fonde (a) schema, (b) SP/viste documentate, (c) query buone, (d) **codice applicativo** in un unico corpus RAG. Lo spazio di Sertor: schema-graph parallelo al code-graph, nuovo sorgente ingestione nel corpus unico, parsing sintattico di T-SQL/PL-SQL (prerequisito noto, oggi escluso).
- **[[second-brain-cross-progetto]]** — 💡 **Idea da espandere: il «Sertor dei Sertor» (Meta-Sertor).** Visione del second brain cross-progetto: Sertor ricorsivo a tre altitudini (L0/L1/L2), conoscenza e asset come stessa sostanza a diversi gradi di cristallizzazione, i due flussi (harvest/promote ↑, seed/apply ↓), la sintesi N→1 di asset da più progetti, la query federata con escalation, il modello a due strati intento/binding + verifica, la tensione grounding↔astrazione e la sua riconciliazione (provenance). Con diagrammi mermaid, prior art, rischi e bivi aperti. Sertor da autore a **giardiniere della flotta**.
- **[[architettura-wiki-llm]]** — 🗺️ **Vista d'insieme + roadmap.** Architettura del Wiki LLM dopo il ponte D→N: nucleo deterministico (`wiki_tools`) + layer agentico (4 entità host-agnostiche) + hook, separati dal confine D (meccanico) ↔ N (giudizio); una sola config. Schemi a strati, confine per operazione, lint a tre livelli (A strutturale / B semantico / C organizzativo). **Roadmap** con grafo di dipendenze e priorità. Pagina d'ingresso all'architettura.
- **[[sistema-wiki-fonte-unica]]** — Consolidamento del wiki (fonte unica playbook + tre interfacce sottili + automazione hook). Tassonomia consolidata; convenzioni esplicite; operazioni del playbook. **Modularizzato (2026-06-07):** playbook = indice + moduli `ops/*.md` caricati on-demand (progressive disclosure, resta DRY e portabile).
- **[[lint-semantico-host-agnostico]]** — 🔍 **Estensione del lint a audit globale.** 4 `kind` di artefatti (`wiki`/`requirements`/`spec`/`tracker`) dichiarati in config `[[audit]]` con profili universali nel playbook; per ogni `kind`, tassonomia di coerenza e procedura ripetibile. Host-agnostico: la rete di anti-deriva è globale, non solo wiki.
- **[[lint-organizzativo-e-reorg]]** — 🧭 **Lint livello C (organizzativo) + reorg.** La terza categoria di deriva (collocazione, atomicità, coerenza `type`↔natura, disciplina link) oltre igiene (A) e claim (B). Perché è tutto giudizio (cartella e `type` mentono insieme sul contenuto); principio "grafo non albero"; esercizio 2026-06-06 (`syntheses/` da 16/20 a 4/3/9/4, 0 link rotti).

### Tech (tecnologie e infrastruttura)

- **[[sessionstart-hook]]** — Hook SessionStart di Claude Code: carica indice + log a inizio sessione. Ruolo 1 di DA-W1 (contesto iniettato).
- **[[tree-sitter-language-pack]]** — Il binding Python (wheel precompilato) delle grammatiche tree-sitter, base del [[chunking-dispatch|chunking sintattico]]: API **a metodi** (non attributi) avvolta dal wrapper `_Node`, byte-range UTF-8, righe 0→1-based. *(Distillato 2026-06-08, allineato a `code.py`.)*
- **[[wiki-tools]]** — Il nucleo **deterministico** del wiki (`sertor-wiki-tools`): `scan`/`lint`/`validate`/`collect`/`structure`/`index` + `append-log`/`migrate` (rotazione del log a un file/giorno, FEAT-008), contratti JSON versionati, host-agnostico via `wiki.config.toml`, zero LLM/rete (stdlib). È la metà **D** del confine D↔N.
- **[[mcp-server]]** — Il server MCP `sertor-rag` (`sertor_mcp`): la superficie che espone la facade del core come 10 tool (3 ricerca `search_code/docs/combined` + 4 grafo + 3 memoria, gated `SERTOR_MEMORY`), facade memoizzata, formato citabile `path#chunk`, trasporto stdio. Esempio canonico di [[thin-consumer]].
- **[[sertor-rag-cli]]** — La CLI di esecuzione RAG `sertor-rag` (feature 011, PR #21): `index`/`search` dal terminale, exit code per scripting, anteprime troncate (`preview_chars`), osservabilità `-v`/`--log-json`/`--log-config`, validazione statica del backend (`validate_backend`). Secondo esempio realizzato di [[thin-consumer]]; comandi spartiti per DA-8 (`sertor`=installer, core=esecuzione).
- **[[sertor-install-kit]]** — ✨ **Toolkit di installazione estratto** (feature 037, 2026-06-15): motore generico, stdlib-only, zero dipendenza dal core. Riusato da [[sertor-installer]] (wiki+rag) e [[sertor-flow]] (governance). Tassonomia `Artifact`/`ArtifactKind`, merge intelligenti, executor fail-fast, logging strutturato, errors. NFR-2 (DRY).
- **[[sertor-installer]]** — Il pacchetto/comando installer `sertor` (feature 012, PR #22): `sertor install wiki` porta il sistema-wiki su un ospite (skill+agente+hook+rituale a marker+config inferita+struttura), non distruttivo per artefatto, idempotente, install≠run; assets **package-data** come fonte canonica (`.claude/` del repo = derivato + test di guardia). Motore ora in [[sertor-install-kit]]. Aperto: tema lingua degli asset.
- **[[sertor-flow]]** — ✨ **Installer di governance/SDLC** (feature 037, 2026-06-15): pacchetto separato che porta metodo SDLC (SpecKit vendor, costituzione-starter, rituale) su un ospite **senza dipendere dal RAG** (REQ-002). Riusa [[sertor-install-kit]], thin consumer, host-agnostico (Principio X).
- **[[assistant-targeting]]** — ✨ **Targeting per-assistente** (FEAT-007, feature 044, PR #64, 2026-06-15): il seam `AssistantId`/`Surface`/`AssistantProfile` nel [[sertor-install-kit]] che rende l'installer capace di depositare le superfici per **Claude e GitHub Copilot** a parità funzionale. Decisione DA-2 = ibrido (riuso contenuto + traduzione contenitore, fonte unica + guardia anti-drift); plan-builder parametrici, `merge_mcp` con `root_key`. Embodiment del Principio X all'assistente. Riuso da FEAT-009.
- **[[installer-lifecycle]]** — ✨ **Ciclo di vita dell'installer** (FEAT-008, feature 048, PR #71, 2026-06-17): i verbi `upgrade`/`uninstall` per `sertor` e `sertor-flow` oltre al primo install. Verbo ortogonale `LifecycleOp` + funzioni inverse pure **una volta sola nel kit** (no raddoppio tassonomia, divergenza impossibile per costruzione), riuso degli stessi plan-builder col verbo, diff a posteriori `sertor_owned_paths` + test invariante `plan ⊆ owned` (no manifest), `--purge-wiki` opt-in CI-safe, report `install.report/1` esteso additivo, tipi artefatto A/B/C/D. Invariante: `sertor-flow` senza dipendenza da `sertor-core`/`sertor`.
- **[[doctor]]** — **`sertor-rag doctor`** (E12-FEAT-001): la diagnostica di salute **deterministica, zero-LLM, offline-safe** che risponde «ha funzionato il setup?» su 4 aree (config/provider/indice/mcp), verdetto pass/warn/fail + causa/rimedio, schema `doctor.report/1`, exit-code gate, `--online` per il probe provider. Ancorata alla radice (FEAT-038, invariante alla cwd). Verify-step di [[guided-setup]] e misura dell'hook [[auto-heal-staleness|rag-freshness]].
- **[[auto-update-version-check]]** — Il meccanismo **solo-avviso** di consapevolezza-aggiornamenti (E2-FEAT-013/017): SessionEnd `version-check` confronta lo stamp locale col `/VERSION` su master (GET cachata ~24h) → stato `version.check/1`; SessionStart avvisa **solo** se `behind`, **mai** auto-upgrade; parità per-assistente; **dormiente-fino-alla-release** (legato al bump manuale `/VERSION`, policy A-15) + onestà su `unknown`/repo privato (FEAT-017).
- **[[identita-hook-nel-merge]]** — **Identità di un hook nel merge** (E10-FEAT-032, 2026-07-17): qual è l'identità di un hook quando `sertor upgrade` lo ri-cabla? La stringa del comando è un dettaglio di resa mutabile — usarla faceva sembrare nuovo ogni ri-cablaggio, arrivando a duplicare (Claude) o scartare (Copilot) il fix. La soluzione: lo **stem dello script** (`rag-freshness` da `rag-freshness.py` o `.ps1`) è l'identità. I due contratti `install` (non distruttivo) vs `upgrade` (in place) diventati espliciti. Meccanismo: [[sertor-install-kit]]. Lezione compagna: [[esito-sull-host-vs-forma-dell-asset]].
- **[[corpus-index-naming]]** — Schema naming chiarificato (dal 2026-06-04): corpus `sertor` (prodotto, radice) vs `prototype` (prototipo, congelato); indici `.index-sertor` (radice) vs `.index-prototype` (prototipo).
- **[[transcript-capture-adapter-e-storage]]** — 9ª porta `TranscriptCaptureAdapter` (host-agnostico, Principio X) + adapter Claude-Code + store SQLite `MemoryArchive`. Entità di dominio pure (`SessionRef`, `TranscriptTurn`, `TranscriptContent`, `ArchivedSession`). Granularità ibrida (sessione archiviata, turni interrogabili). Servizio orchestrante `MemoryArchiveService`.
- **[[scrub-segreti-in-contenuto]]** — Tecnica di ripulitura dei segreti nel testo libero prima di archiviar (pattern-based, configurabile, ripiego conservativo). Estende la redazione per-campo al contenuto. Mai bypassabile, nessun segreto mai in chiaro.
- **[[ricerca-episodica-fts5]]** — Il motore **FTS5 nativo di SQLite** per la ricerca full-text locale (FEAT-002): indice virtuale `turns_fts` sincronizzato via trigger, BM25 ranking, snippet context extraction, offline + robusto. Schema, logica SQL, osservabilità, limiti (no regex, no stemming, no semantica).
- [[concepts/daily-distill-floor.md]] — Il pavimento giornaliero del distill: un hook PreToolUse blocca il merge finche la giornata non ha una voce distill; distill-audit come contesto advisory. Gemella lato-enforcement di ritual-check (E10-FEAT-026).
- [[concepts/product-plane-vs-fixture-plane.md]] — Principio XIII della costituzione: nel dogfooding piano-prodotto e piano-fixture distinti; un buco-prodotto tappato da un workaround-fixture = OPEN PRODUCT QUESTION. Origine Sinthari.
- [[concepts/fail-loud-fix-cause.md]] — Principio XII della costituzione: rimuovi la causa, non silenziare per schivare l errore; la degradazione e ammessa solo se segnala. Origine episodio OTel.
- [[concepts/ritual-check.md]] — Sotto-comando deterministico di sertor-wiki-tools (FEAT-026): trova candidati distill/drift per uno step (git-diff) + scaffold di dichiarazione forzata. Gemella per-step di daily-distill-floor. Il tool trova, l agente giudica.
