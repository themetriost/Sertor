# Epica — Debito tecnico, igiene e portabilità interna

> Livello: **epica trasversale (interna).** Non aggiunge **capacità di prodotto**: paga il **debito** che
> rallenta o irrigidisce lo sviluppo e mette a rischio la promessa host-agnostica. Raccoglie le voci §7 del
> [backlog audit](../../wiki/syntheses/backlog-audit-2026-06-15.md) finora senza casa durevole. Si decompone
> in `requirements/debito-tecnico/<feature>/requirements.md` (EARS), ma molte voci sono interventi mirati,
> non feature da SpecKit pesante.

## 1. Visione e problema (perché)

Sertor è cresciuto in fretta e ha accumulato debiti che **non sono capacità mancanti** ma **frizioni**:

- **Asset Sertor-coupled:** alcune skill wiki / il playbook / il rituale di step sono ancora **legati a
  Sertor** invece di essere host-agnostici, contraddicendo la mission «framework installabile ovunque»
  (**Principio X**) — pezzi viaggiano sull'ospite ma assumono il contesto di Sertor.
- **Rituale non portabile:** la nota «riesportare il rituale/governance come **plugin portabile**
  repo-agnostico» è solo **parzialmente** assorbita da [[sertor-flow]].
- **Due venv divergenti** (`.venv` / `.venv-core`): footgun operativo, fonte di guasti silenziosi.
- **Igiene del wiki:** mancano hub/overview per-area, una tassonomia più fine, il distill della pagina
  osservabilità; alcune pagine sono gonfie ([[tree-sitter-language-pack]]); manca l'override dei seed
  `[strings]`; il `reconcile` periodico è solo documentato (nessun trigger).
- **Bundle governance rigido:** `sertor-flow` è all-or-nothing (selettività = Could) e senza hook harness (DA-g).
- **CI non Linux:** i test girano su Windows; manca il **test Linux nativo** (debito noto, rag-baseline DA-2).
- **Naming `--assistant` incoerente:** la distribuzione Copilot espone **due** valori (`copilot` = VS Code ·
  `copilot-cli` = Copilot CLI) per quello che l'utente percepisce come «un solo Copilot». Va **allineato a un
  solo `copilot`** (user-flagged 2026-06-16). Apre una decisione di design: come riconciliare i due
  contenitori MCP sotto un nome unico — `.vscode/mcp.json`/`servers` (VS Code) vs `.mcp.json`/`mcpServers`
  (CLI) — es. scriverli **entrambi**, oppure eleggere `.mcp.json` come canonico (GitHub sta convergendo lì).

Il valore: ridurre la frizione e **onorare il Principio X** anche sugli asset interni, così il prodotto
resta davvero portabile e lo sviluppo resta veloce.

> Il *come* (refactor, packaging del plugin, config CI) è materia di design/implementazione.

## 2. Ambito

### In ambito
- **Host-agnosticità degli asset Sertor-authored** residui (skill wiki, playbook, rituale) — chiudere il gap col Principio X.
- **Plugin portabile** del rituale/governance, repo-agnostico (oltre ciò che `sertor-flow` già copre).
- **Unificazione/igiene degli ambienti** (`.venv`/`.venv-core`).
- **Igiene del wiki** (hub per-area, tassonomia, distill mancanti, pagine gonfie, seed override, trigger `reconcile`).
- **Robustezza del bundle governance** (selettività, hook harness).
- **CI multipiattaforma** (Linux nativo).
- **Coerenza del naming dell'installer** (`--assistant`): un solo `copilot`, non `copilot`/`copilot-cli`.

### Fuori ambito
- Qualunque **nuova capacità di prodotto** (retrieval, ingestione, memoria, osservabilità): le rispettive epiche.
- Le **osservabilità minori** (export CSV/MD, bucket «hour», eviction cache): promosse nell'epica `osservabilita`.
- I leak di **enforcement Principio XI** (FR-007 export `__init__`, hook block-mode): vivono in
  `sertor-core/enforcement-principio-xi/` — non qui.

## 3. Criteri di successo
- **CS-1 (host-agnostico):** gli asset interni residui non contengono assunzioni hardcoded su Sertor; un
  ospite li riceve e li usa senza patch manuali (test di guardia, come per `sertor-installer`).
- **CS-2 (plugin):** il rituale/governance è installabile come plugin portabile su un repo terzo senza riferimenti a Sertor.
- **CS-3 (un solo env):** lo sviluppo usa un ambiente coerente; non esistono due venv che divergono silenziosamente.
- **CS-4 (wiki igienico):** il lint organizzativo (livello C) non segnala hub mancanti/pagine fuori posto/seed non-overridabili sui casi noti.
- **CS-5 (CI Linux):** la suite passa in CI su Linux **oltre** che su Windows, in **0** regressioni di piattaforma.

## 4. Stakeholder e attori
- **Owner/maintainer (tu):** paga meno frizione, sviluppa più veloce.
- **Ospite terzo:** riceve asset davvero portabili (Principio X mantenuto).
- **Il sistema-wiki & `sertor-flow`:** oggetti del refactor di igiene/portabilità.

## 5. Vincoli, assunzioni e dipendenze
- **Non-regressione:** ogni intervento mantiene verdi le suite esistenti (root/kit/sertor/sertor-flow).
- **Principio X come bussola:** il refactor host-agnostico riusa la metodologia già applicata (config
  esternalizzata, marker, package-data canonico + derivato + guard test).
- **Calibra al valore:** molte voci sono interventi mirati; non tutte richiedono un flusso SpecKit completo.
- **Coordinamento con `sertor-flow`:** la selettività bundle e gli hook harness toccano quel pacchetto.

## 6. Rischi
- **R-1 — Debito invisibile rimandato all'infinito:** senza casa durevole queste voci si perdono; l'epica
  è proprio la loro casa.
- **R-2 — Refactor host-agnostico che rompe il dogfood:** mitigare con guard test e modifiche incrementali.
- **R-3 — Unificazione venv che rompe ambienti cloud/extra:** procedere con cautela, isolare gli extra pesanti.
- **R-4 — Scope creep dell'igiene wiki:** tenere gli interventi atomici, guidati dal lint C.

## 7. Requisiti trasversali (EARS)
- **REQ-E1 (Ubiquitous):** *The internal Sertor-authored assets shall be host-agnostic: no hardcoded
  assumptions about Sertor, verifiable by guard tests (Principio X).*
- **REQ-E2 (Unwanted):** *If two divergent virtual environments can drift silently, then the development
  setup shall be consolidated to a single coherent environment.*
- **REQ-E3 (Optional):** *Where the governance/ritual is exported as a portable plugin, it shall install on
  a third-party repo without references to Sertor.*
- **REQ-E4 (Ubiquitous):** *The test suite shall pass on Linux in CI in addition to Windows.*

## 8. Backlog di feature

> **Cross-ref `usabilità` (E12):** l'aspetto *user-facing* della freschezza RAG («indice stantio →
> fai X», reconnect MCP) è ownership dell'epica [`usabilità`](../usabilita/epic.md); qui resta il
> **meccanismo** d'enforcement deterministico (hook `SessionEnd`, FEAT-011).

| ID | Feature | Valore / obiettivo | Priorità (MoSCoW) | Stato |
|----|---------|--------------------|-------------------|-------|
| FEAT-001 | **Host-agnosticità degli asset residui** (skill wiki / playbook / rituale ancora Sertor-coupled) | Onora il Principio X anche sugli asset interni | **Should** | ✅ **DONE (2026-06-19)** — feature 056, PR #80 (`97c91f5`): parità wiki Copilot CLI via **agent-skills NATIVE** (`.github/skills/wiki-author/`, SKILL.md dispatcher che assorbe `/wiki` + payload byte-copiato co-locato), body host-agnostici, guardia di parità con closure; verificata LIVE su Copilot CLI reale (Spike). *(Eventuali altri asset host-coupled scoperti in futuro → nuove voci.)* |
| FEAT-002 | **Unificazione degli ambienti** (`.venv` / `.venv-core` → uno coerente) | Elimina un footgun operativo | **Should** | ✅ **DONE (2026-06-18)** — un solo `.venv` (default workspace `uv`); `dev` superset (incl. `mcp`+`graph`), `azure` opt-in; `.mcp.json` ripuntato; `.venv-core` eliminato; guard test CS-3 |
| FEAT-003 | **CI Linux nativo** (suite verde su Linux oltre Windows; debito rag-baseline DA-2) | Portabilità reale verificata in CI | **Should** | ✅ **DONE 2026-06-23** — `.github/workflows/ci.yml` (matrix os=windows-latest+ubuntu-latest, `fail-fast: false`, ruff `extend-exclude=["prototype"]`); **Windows + Linux verdi** su PR #96 (run `28015943729`). Prima CI del progetto. La prima esecuzione ha scoperto un test non-ermetico (`test_engine_selection` dipendeva dalla cache GloVe) → reso ermetico col provider `hash`. |
| FEAT-004 | **Rituale/governance come plugin portabile** repo-agnostico (oltre ciò che `sertor-flow` copre) | Riuso del metodo su repo terzi senza Sertor | **Could** | da decomporre |
| FEAT-005 | **Igiene del wiki** — hub/overview per-area, tassonomia più fine, distill pagina osservabilità, ripasso [[tree-sitter-language-pack]], override seed `[strings]`, trigger periodico `reconcile` | Wiki navigabile e senza deriva organizzativa | **Could** | da decomporre — guidata dal lint C |
| FEAT-006 | **Robustezza del bundle `sertor-flow`** — selettività (vs all-or-nothing) + hook harness governance (DA-g) | Install governance più flessibile | **Could** | da decomporre |
| FEAT-007 | **Allineamento naming `--assistant`** — unificare i due valori Copilot (`copilot` VS Code + `copilot-cli`) in **un solo `copilot`** | Coerenza dell'API installer, meno confusione utente | **Could** | da decomporre — *user-flagged 2026-06-16*; decisione di design aperta: due contenitori MCP (`.vscode/mcp.json`/`servers` vs `.mcp.json`/`mcpServers`) sotto un nome unico (scrivere entrambi? `.mcp.json` canonico?) |
| FEAT-008 | **Visibilità del context-load SessionStart su Copilot CLI** — investigare se si può **ridurre/eliminare la visibilità** del prompt di SessionStart all'avvio | UX d'avvio meno rumorosa senza perdere il context-load del rituale wiki | **Could** | da investigare — *user-flagged 2026-06-19*. Su Copilot CLI l'hook SessionStart è `type:"prompt"` → il client **mostra all'utente** la direttiva «SESSION START - load the project context…» a ogni avvio; su Claude è **silenzioso** (`additionalContext`). Verificare se Copilot CLI offre un canale d'iniezione contesto meno visibile (o, in alternativa, accettare/accorciare il prompt). Sorgente: `install_wiki._copilot_wiki_hook_specs` (SessionStart) |
| FEAT-010 | **Host-agnosticità degli asset governance (asse Sertor↔ospite)** — gli asset distribuiti da `sertor-flow` avevano **project-coupling**: `requirements-analyst` hardcodava i tool `mcp__sertor-rag__*` nel frontmatter (esistono solo se l'ospite installa *anche* il pacchetto RAG — viola l'indipendenza `sertor-flow`↛RAG); `configuration-manager` si descriveva come gestore di un "workspace RAG" e usava le cartelle-prototipo Sertor (`01-baseline`/`03-graphrag`…) come scope-esempi. Neutralizzati → generici/RAG-opzionale-via-discovery; pin di regressione | Asset governance davvero portabili (Principio X sull'asse progetto, non solo assistente) | **Should** | ✅ **DONE (2026-06-19)** — feature 059: la guardia di parità 056 copriva l'asse Claude↔Copilot, non il project-coupling; scoperto chiedendo «distribuiamo configuration-manager?» |
| FEAT-011 | **Enforcement deterministico della freschezza RAG (hook)** — i passi **MECCANICI** del rituale (re-index incrementale + smoke MCP) NON devono dipendere dalla memoria dell'agente: spostarli in un **hook `SessionEnd`** (`.claude/hooks/rag-freshness.*`) che re-indicizza incrementale + esegue lo smoke MCP e **fallisce loud** se stantio o un tool erra. Include la **reclassificazione nel `CLAUDE.md`** degli step 5/8 da «standing» a «enforced via hook» (confine D↔N: meccanico→harness, giudizio→agente). Host-facing → cablato nell'installer. Complementa la rilevazione del drift (`osservabilita` FEAT-012). | Rende deterministici gli step che oggi si **saltano** (lezione 2026-06-20: discrezionale+condizionato+auto-eseguito = skippabile; il commit sopravvive perché cheap+delegato+incondizionato) | **Should** | ✅ **DONE (2026-06-25, merge `29dd30e`)** — branch `076-enforcement-freschezza-rag`: due hook host-facing (`rag-freshness.ps1` SessionEnd: re-index incondizionato via vehicle + `doctor` + persiste `.sertor/.rag-health.json`; `rag-freshness-start.ps1` SessionStart: induce correzione se `degraded`) + reclass `CLAUDE.md` step 5/8 + `RUNTIME_IGNORES` esteso; Constitution 12/12 + missione, sertor 395/kit 131/root 1128 verdi, `sertor-core` invariato; test funzionale dogfooding ✅ (re-index reale + health 4/4). 4 decisioni scope risolte (re-index incondizionato · fail-loud a 2 tempi · smoke = solo `doctor` · hook separato/non-fatale; buco filtro-metadata `where` promosso a E12-FEAT-011). Follow-up: prova LIVE su ospite + **FEAT-014** (stdin guard) — *user-flagged 2026-06-20* |
| FEAT-012 | **Governance interrogabile via RAG** — includere `.specify/memory/constitution.md` (e valutare `plan-template`) nel **corpus indicizzato**: oggi la **costituzione NON è nel corpus** (verificato con `validate-path`, 2026-06-20) → principi e North Star **non recuperabili** via `search_docs`. | I principi/mission sono interrogabili dall'agente via RAG, non solo letti a mano | **Should** | ✅ **DONE (2026-06-23)** — fix dogfood: rimosso `.specify` dagli exclude del `.env` (host-agnostico default già corretto; template installer già ok). Verifica: `validate-path` conferma `.specify/constitution.md` + `plan-template.md` in indice. Nota latente: template esclude `.git/.hg/.svn`. |
| FEAT-009 | **Distribuzione corretta della costituzione neutra + rifinitura principi** — lo starter neutro **non arriva** sull'ospite: `specify init` (launch-installer FEAT-045) scaffolda un `.specify/memory/constitution.md` **placeholder** (`[PROJECT_NAME]`/`[PRINCIPLE_1_NAME]`) e il nostro CONFIG `create-if-absent` fa **skip** → l'ospite riceve il template vuoto di spec-kit, non la nostra costituzione curata. Fix **replace-if-placeholder** (sovrascrivi il placeholder, preserva una costituzione reale) + rifinitura dei principi neutri (XI/II generalizzati, allineamento VII) | L'ospite riceve una costituzione neutra **sensata e completa**, non il placeholder `[PROJECT_NAME]` | **Should** | ✅ **DONE (2026-06-19)** — feature `058-distribuzione-costituzione`, PR #82 (`b16c4a1`, merge `78f51b8`): replace-if-placeholder in `sertor-flow` (placeholder spec-kit sovrascritto con lo starter neutro, costituzione reale preservata) + rifinitura principi (starter v0.1.0→0.2.0) + mock `FakeSpecifyRunner` reso fedele (depositava il placeholder, era il blind-spot). *(bug scoperto 2026-06-19 via verifica empirica su Spike + install pulito)* |
| FEAT-013 | **Allineamento del layout di config dogfood↔ospite** — il dogfood di Sertor legge la config dal **`.env` a ROOT del repo** (non esiste `.sertor/`), mentre l'installer provvede agli ospiti **`.sertor/.env`** (+ `.sertor/.index`). `_resolve_env_path` onora entrambi (cwd `./.env` → poi `<venv-parent>/.env`), quindi funziona, **ma il dogfood non esercita il layout `.sertor/` che spedisce** → blind-spot di fedeltà del dogfooding (caso 2026-06-22: assunto erroneamente `.sertor/.env` per il dogfood). Decisione di plan: **(a)** migrare la config del dogfood sotto `.sertor/` (mangiare il proprio dog-food anche sul layout) **oppure (b)** documentare esplicitamente la convenzione dev(`./.env`)↔ospite(`.sertor/.env`) e renderla verificabile. | Il dogfood riflette il layout di config reale degli ospiti; niente assunzioni divergenti dev↔host | **Should** | ✅ **DONE (2026-06-23)** — scelta (a): migrato dogfood sotto `.sertor/`. (1) Esteso `_resolve_env_path` per cercare `./.sertor/.env` (additivo, host-agnostico). (2) Spostato `.env` dogfood → `.sertor/.env`, rimosso override `SERTOR_INDEX_DIR` → indice ora `.sertor/.index` per convenzione. Test `test_cwd_sertor_dotenv_resolved_when_venv_not_nested` aggiunto (6/6 verdi). Re-index: 935 doc / 10268 chunk. Finding: server MCP serve ancora da vecchia `.index-sertor` (config all'avvio) → richiede reconnect (segnalato smoke-test standing). |
| FEAT-014 | **Robustezza invocazione manuale dell'hook freschezza (stdin non-bloccante)** — lo script `rag-freshness.ps1` (FEAT-011) blocca su `[Console]::In.ReadToEnd()` quando stdin **non** è rediretto: il docstring dichiara «tolerant if absent (manual invocation)» ma è **falso**, e gli esempi del quickstart (`& .\.claude\hooks\rag-freshness.ps1` senza pipe) si **appendono a tempo indefinito**. In **produzione non si manifesta** (il client agente passa il payload JSON e poi chiude stdin → EOF). Fix proposto: guardia `if ([Console]::IsInputRedirected) { … ReadToEnd … }` (salta la lettura in console interattiva), + correzione degli esempi del quickstart. Scoperto in **dogfooding 2026-06-24** testando FEAT-011 (l'hook è rimasto appeso ~1h al primo run manuale). | L'hook è invocabile a mano/in test senza appendersi; il docstring dice il vero | **Could** | da decomporre (fix mirato; tocca asset bundlato + copia dogfood → sync + `test_assets_sync`) — *dogfooding 2026-06-24* |
| FEAT-015 | **Il refresh/upgrade non disinstalla bene gli artefatti obsoleti** — il percorso di **refresh** (re-install via `uvx --refresh` e/o `sertor upgrade`) **non rimuove in modo pulito** gli artefatti già installati che una nuova versione ha cambiato o tolto dal bundle: sull'ospite restano file/voci **obsoleti** (residui non pruned). Da **riprodurre e isolare** quale percorso cede (re-install idempotente — che by-design non *rimuove* mai — vs `upgrade`, che dovrebbe prunare via diff a posteriori `sertor_owned_paths`), poi correggere. | Un host aggiornato non deve accumulare artefatti stantii/duplicati: il refresh deve lasciare lo stato **pulito** (no residui) | **Could** | 📋 da investigare/decomporre — *user-flagged 2026-06-25*. Verificare contro il lifecycle **FEAT-008** ([[installer-lifecycle]]): `upgrade` dovrebbe già fare prune (diff `sertor_owned_paths` + `LifecycleOp`); capire se il refresh osservato non pulisce per **bug** o per **gap di copertura** (es. l'utente fa `uvx --refresh … install` invece di `upgrade` → il re-install non prune by-design). Possibile esito: documentare «usa `upgrade` per pulire» e/o estendere il prune al percorso di refresh |
| FEAT-016 | **rag-freshness: re-index a SessionEnd non-bloccante** — il re-index sincrono di `rag-freshness.ps1` viene ucciso dal timeout 15s su repo grandi → `doctor` non gira, `.rag-health.json` resta stantio: il meccanismo di freschezza si auto-sabota. Spostare l'index in background/async o gate su change-check rapido (mtime/manifest); separare `doctor` dal re-index; timeout realistico | Freschezza affidabile senza stallo a ogni chiusura sessione (**difetto di FEAT-011**) | **Should (P0)** | ✅ **DONE (2026-06-26, PR #120)** — hook fully-non-blocking: il foreground rilancia se stesso `-Worker` detached (`Start-Process`) e ritorna in ~0.3s; il worker fa `doctor` → scrive `.rag-health.json` (atomico) → `index .` in background. Salute al più una sessione indietro (DA-2). `sertor-core` invariato; verificato live <1-2s; requirements [[rag-freshness-nonblocking]]. Da audit ISSUE-01 |
| FEAT-017 | **Invocazione CLI standardizzata negli hook** — `memory-capture` e `rag-freshness` usano `uv run` *bare* (cwd-fragile, contro CLAUDE.md) → risolvono progetto/venv sbagliato; `wiki-pending-check` ha il pattern giusto (`uv run --project .sertor` + fallback al venv) | Hook corretti da una cwd arbitraria | **Should (P0)** | ✅ **DONE (2026-06-26, PR #121)** — `memory-capture.ps1` portato a `uv run --project <root>/.sertor` + fallback al venv (come `wiki-pending-check`); `rag-freshness` già sistemato in FEAT-016. + **guardia anti-regressione** `test_assets_hook_cli_invocation.py` (nessun `uv run` nudo negli hook-asset). `sertor-core` invariato. Da audit ISSUE-03 |
| FEAT-018 | **Portabilità OS degli hook + onestà sui surface Copilot inerti** — tutti gli hook sono `.ps1`, `pwsh` assunto su mac/Linux senza check/fallback (off-Windows falliscono in silenzio, exit 0); su Copilot `memory-capture`/signal SessionStart sono inerti ma «parity» lo nasconde. Guardia `pwsh`/gemello bash o gap dichiarato + marcare i surface inerti nel report d'install | Portabilità reale + claim onesti (Principio XII) | **Should (P1)** | ✅ **IMPLEMENTATA (2026-06-30, branch `078-portabilita-os-hook`)** — SpecKit completo, Constitution 12/12 + missione, sertor 451/kit 139/root 1131 verdi, ruff clean; `sertor-core` invariato; modulo puro `host_env.py` nel kit (stdlib-only, mockabile offline), note ospite via `InstallReport.notes` (primo uso reale), audit drift corretto (memory-capture=gap FEAT-009, SessionStart=funzionale), causa-radice dichiarata (shell `pwsh` vs `powershell`); pagina experiment nuova; prossimo: prova LIVE ospite (giudizio LLM). Audit ISSUE-04 (2026-06-26) |
| FEAT-019 | **Fail-loud breadcrumb negli hook + fallback «asset mancante → STOP» negli agent** — `memory-capture` e il path catastrofico di `rag-freshness` fanno silent-swallow (zero segnale, contro «Fail Loud, Fix the Cause»); 3 agent dicono «leggi la skill/playbook ed esegui» senza fallback. Hook lasciano una traccia (`.sertor/.last-hook-error`/stderr); agent: «asset non risolvibile → STOP e segnala» | Rotture silenziose visibili; agent non procedono a vuoto | **Should (P1)** | ✅ **DONE (2026-06-29, branch `077-fail-loud-hook-agent`)** — SpecKit completo, Constitution 12/12, 4 guardie, zero core, 1131 test root verdi; pagina experiment nuova; prossimo: prova LIVE ospite (giudizio LLM). Audit ISSUE-05 (2026-06-26) |
| FEAT-020 | **Tool grant minimi negli agent + invariante push allineata alla costituzione** — `Bash` granted-but-unused in `requirements-analyst`, contraddittorio in `concierge` (thin dispatcher ma può mutare l'host); `configuration-manager` lista `push` come routine mentre la costituzione vieta push diretti su default branch. Rimuovere `Bash` dove non serve + invariante «mai push diretto su `main`/`master`, branch-first» | Least-privilege + allineamento policy git | **Should (P1)** | ✅ **DONE (2026-06-26, PR #124)** — `requirements-analyst` senza `Bash` (granted-but-unused); `configuration-manager` con invariante «mai push su `main`/`master`, branch-first + PR» (mantiene `Bash` per git); `concierge` framing riconciliato (è l'esecutore di `guided-setup` sotto consenso → `Bash` mantenuto). + normalizzato il **colon-space** nelle `description` (footgun YAML latente, lezione FEAT-049). `sertor-core` invariato. Da audit ISSUE-06 |
| FEAT-021 | **Ridurre l'altitude dei blocchi `CLAUDE.md` + dedup «How to invoke»** — i 3 blocchi pesano ~13 KB/~205 righe always-on; il blocco «How to invoke/Windows note» è triplicato (guided-setup + wiki-playbook + CLAUDE.md). Ridurre ogni blocco a direttiva breve + pointer; estrarre «How to invoke» in un reference unico citato per nome | Meno contesto always-on, una sola fonte | **Should (P1)** | ✅ **IMPLEMENTATA (2026-06-30, branch `079-altitude-claude-md`)** — SpecKit completo spec→plan→tasks→implement; Constitution 12/12 + missione; asset canonico `sertor-cli-reference.md` depositato in `.sertor/` (owned, CREATE_IF_ABSENT); blocchi CLAUDE.md ridotti a direttiva+pointer; riduzione **208→~166 righe** (−20%): RAG-usage 72→49 (−23) · wiki-ritual 71→52 (−19) · SDLC invariato; zero core (1 file `install_rag.py`, zero `ArtifactKind`/Surface/WriteStrategy); guardia fonte unica assert+grep, closure pointer, parità Copilot verificata offline; sertor 456 · kit 139 · root 1131 verdi (3 skip), ruff clean; pagina experiment [[feat-021-altitude-claude-md]] + log entry registrati, index aggiornato; prossimo: prova LIVE ospite (giudizio). |
| FEAT-022 | **Pulizia stile delle skill** — ALL-CAPS pervasivo (il rubric preferisce imperativo + *why*), sezioni «What NOT to do»/«Hard boundary» che ripetono regole inline, `wiki-playbook.md` (295 righe) senza ToC, wikilink orfano `[[assistant-targeting]]` | Asset più leggibili/manutenibili | **Could (P2)** | ✅ **IMPLEMENTATA (2026-06-30, branch `080-pulizia-stile-skill`)** — SpecKit completo spec→plan→tasks→implement; Constitution 12/12 + missione; ALL-CAPS→imperativo+why (5 skill: guided-setup/eval-suite-author/eval-feedback/wiki-playbook/requirements), condensazione What-NOT sezioni, ToC wiki-playbook, pointer centralizzato eval-skills a `sertor-cli-reference.md`, rimozione orfani wikilink (Principio X); zero core, zero cambio semantico; guardie ALL-CAPS=0/orfani=0/pointer-eval presenti/semantica invariata; riduzione skill ~541→~519 righe (−4%, guided-setup −17%, eval-suite −12%, eval-feedback −3%); sertor 473 · kit 139 · root 1131 verdi, ruff clean; pagina experiment [[feat-022-pulizia-stile-skill]] + log entry registrati, index aggiornato; **finding dichiarato:** fork IT dogfood eval-skill non-sincronizzato promosso FEAT-025 backlog; prossimo: merge branch `080` (giudizio LLM). |
| FEAT-025 | **Riconciliazione fork IT dogfood eval-skill + guardia sync rag-skill** — scoperto durante FEAT-022: i dogfood `.claude/skills/eval-suite-author/SKILL.md` e `eval-feedback/SKILL.md` sono un **fork ITALIANO stantio**, divergente dal canon EN su `master` (vedi `packages/sertor/docs/assets/rag/skills/eval-suite-author/SKILL.md`); `test_assets_sync.py` copre solo asset Claude, NON rag-skill (`assets/rag/skills/`) → **drift silenzioso** pre-esistente (scoperto a posteriori durante pulizia stile). Riallineare dogfood al canon EN + estendere guardia sync rag-skill nel test (bundle→dogfood per tutte skill). | Dogfood fedele, sync-drift colto in CI | **Should (riclassificato Finding 2026-06-30)** | ✅ **CHIUSA da E15-FEAT-002 (2026-07-03)** — l'estensione del sync a `rag/skills` ha **riallineato** i dogfood `eval-suite-author`/`eval-feedback` dal fork IT al canon EN, e la guardia esaustiva `test_assets_rag_dogfood_sync` copre ora anche le rag-skill (il buco d'osservabilità locale è chiuso). Test `test_skill_eval_feedback` riconciliato al canon. |
| FEAT-023 | **Stub `assets/copilot/` fuorviante** — `assets/copilot/**` contiene solo `.gitkeep`; i payload Copilot sono generati a runtime da `assets/claude/**` via `surfaces.py`. Rimuovere il tree stub oppure aggiungere un README «generato a runtime» | Nessuna ambiguità sull'origine dei payload Copilot | **Could (P2)** | ✅ **IMPLEMENTATA (2026-06-30, branch `081-stub-copilot`)** — SpecKit completo spec→plan→tasks→implement; Constitution 12/12 + missione; soluzione: `git rm` 4 `.gitkeep`, guardia anti-ricomparsa `test_no_copilot_asset_directory` in `test_assets_copilot_guard.py` (asserisce non-dir); zero core, zero nuova `ArtifactKind`/Surface; sertor **474** · root **1131 passed**, ruff clean; pagina experiment + log entry registrati, index e epic aggiornati; prossimo: merge branch `081` |
| FEAT-024 | **Parity guard esteso a `.ps1`/`.json` + budget di altitude in CI** — il parity guard esclude script/JSON (il wiring Copilot ha già avuto drift silenzioso); nessun freno alla crescita dei blocchi always-on. Assert sullo shape del wiring Copilot reso + test che fallisce se un `claude-md-block*.md` supera N righe | Drift di wiring e bloat colti in CI | **Could (P2)** | ✅ **IMPLEMENTATA (2026-06-30, branch `082-parity-guard-budget`)** — SpecKit completo spec→plan→tasks→implement; Constitution 12/12 + missione; 3 guardie offline: (1) `test_copilot_hook_presence` shape wiring Copilot (JSON nativo, eventi attesi), (2) `test_claude_md_block_budget` budget per-blocco wiki=60 · rag=58 · sdlc=70 righe, (3) `test_hooks_rag_no_stdout_payload` script `.ps1` non emettono payload stdout; zero core, test-only; root **1134 passed**, ruff clean; pagina experiment [[feat-024-parity-guard-budget]] + log entry registrati, index e epic aggiornati; prossimo: merge branch `082`. |
| FEAT-026 | **Rituale wiki resistente allo skip silenzioso di `distill`/`lint`** — i passi di **giudizio** del rituale (`distill`, `lint semantico B`) sono prosa discrezionale nel `claude-md-block` → si saltano **in silenzio** (nessun errore/warning/output mancante), mentre `record` (meccanico/delegabile) si fa. Convergenza **indipendente** di due feedback ospiti (`Noetix` + `hermes-nunzio-ha`, 2026-07-01, stesso giorno). **Danno dimostrato:** una contraddizione è rimasta nel blocco **EXEC di `roadmap.md`** (mostrato all'utente a ogni SessionStart) per un'intera sessione; un pattern notato in prosa mai distillato; i moduli `ops/*.md` **derivati** non ereditano il rituale. Gemella **lato-giudizio** di FEAT-011 (meccanico→hook) + applicazione del Principio XII (Fail Loud) come FEAT-019. Decomposizione D↔N: (1) **scoperta deterministica** candidati in `sertor-wiki-tools` (euristica: N pagine toccate stessa sessione/branch, ≥2 backlink incrociati nuovi, 0 nuove pagine `concepts/`/`tech/`) + segnale drift per il lint; (2) `wiki-curator` restituisce **sempre** candidati distill + contraddizioni potenziali come output esplicito; (3) **dichiarazione forzata** a fine step (`Rituale: record ✅ · distill: <verdetto> · lint: <verdetto>`, anche «non serve» va dichiarato); (4) **propagazione** del rituale ai moduli derivati. | Il wiki resta fonte di verità cumulativa senza drift silenzioso proprio dove l'utente ripone più fiducia (EXEC a ogni avvio); confine D↔N onorato (il tool *trova*, l'agente *giudica*) | **Should (P1)** | ✅ **CONSEGNATA** (MVP parte 1+3: `ritual-check` + dichiarazione forzata; merge `e906e34`/PR #176, 2026-07-12 — vedi EXEC) · decomposta → [`rituale-anti-skip`](rituale-anti-skip/requirements.md); parti 2/4 (wiki-curator, moduli derivati) = follow-up. **🔎 Punto cieco del segnale `stale-updated` (dogfooding 2026-07-16, da valutare):** è **relativo** — confronta una pagina cambiata con *la più fresca fra le pagine cambiate* (`ritual_check.py` §a) — quindi in uno step a **pagina singola** (il caso più comune) `freshest` è la pagina stessa e la condizione `upd < freshest` **non può mai** essere vera ⇒ segnale **strutturalmente muto** anche su drift oggettivo. Caso reale: `roadmap.md` con `updated: 2026-07-10` modificata il 2026-07-16 → `drift=0`. Leva candidata: confrontare `updated:` anche con la **data del commit/step** (assoluto), non solo fra pari. NB: escludere `log/` dalle `pages_in_scope` è invece **corretto by design** (un log non è pagina distillabile) — **feedback ospiti 2026-07-01** (`wiki/sources/usersfeedback/processed/wiki-ritual-distill-lint-discrezionale.md` + `wiki-ritual-distill-ignorato-noetix.md`), convergenza indipendente su due host. Sorella dichiarata di FEAT-011/FEAT-019 |
| FEAT-027 | **Il dogfood di Sertor deve comportarsi come un client Sertor fedele (machinery SpecKit)** — il workspace di Sertor **non è un client fedele di sé** sul fronte SpecKit: (a) gli **mancano** le skill+script+template che ogni ospite riceve da `sertor-flow install`→`specify init` (`.claude/skills/speckit-*/SKILL.md`, `.specify/scripts/`, template completi — verificato: solo `plan-template.md` presente); (b) **porta** 9 agenti hand-authored `.claude/agents/speckit-*.md` che **nessun client riceve** (`test_no_vendored_speckit_agents` li vieta negli asset) e che puntano a quelle skill+script **assenti** → 9 link morti (residuo pre-pivot 045). Conversione fedele: **materializzare** la machinery via il percorso d'install in modo **isolato** (l'upstream deposita una costituzione-*template* che clobbererebbe la nostra v1.4.0 custom, e `plan-template.md` è Sertor-custom → **preservare** entrambi), **gitignorare** la machinery rigenerabile (come `.venv`: setup step documentato, **non** vendorata — stessa ragione del pivot launch-installer 045), **rimuovere/riconciliare** i 9 agenti orfani (End-state fedele = skill native, niente agenti hand-authored), **doc di setup** + **guardia** «no agenti speckit hand-authored». | Il dogfood **esercita davvero** il percorso d'install dell'ospite; nessuno special-case che diverge dal client (Principio X + «dogfood = client fedele, special-case = debito»); i riferimenti SpecKit **risolvono** invece di essere link morti | **Should (P0)** | ✅ **consegnata** — vedi EXEC (E10-FEAT-027, merge `2f2ea70`/PR #146). *Tema poi assorbito/superato da E15 (asset-install): `materialize-speckit.ps1` retrocesso a dev-tool, fonte = vero install.* — implementata su branch `087-a05-dogfood-client-debt`; SpecKit completo requirements→specify→plan→tasks→implement, Constitution 12/12 + missione. Soluzione: script `scripts/dev/materialize-speckit.ps1` (materializza la machinery via `specify init` **isolato** + copia selettiva + overlay UTF-8 + fail-loud se cambierebbero gli artefatti Sertor) · gitignore del rigenerabile · **9 agenti orfani rimossi** · guardia `tests/unit/test_dogfood_speckit_fidelity.py` (3 verdi, offline). Accettazione OK: 9 skill+5 script materializzati/gitignorati, `constitution.md`/`plan-template.md`/`feature.json` **byte-identici**, **1057 unit verdi**, ruff pulito, `sertor-core` invariato. — **audit SWOT A-05 (2026-07-02), diagnosi 2026-07-03.** A-05 nasceva come «9 skill speckit fantasma»: il primo fix (de-reference + guardia) **incistava** lo special case (la guardia *benediva* la divergenza) invece di risolverlo → **ritirato**. Materializzazione **verificata** dal vivo: `specify init --ai claude` v0.8.18 (con overlay UTF-8 `_UTF8_ENV`, senza cui aborta `UnicodeEncodeError` cp1252 — conferma il valore del guard di produzione) produce le 9 skill + `.specify/scripts/powershell/*` che **risolvono** sia i link-skill sia i riferimenti-script degli agenti. **Rischio costituzione confermato.** Nato dal principio utente «il workspace Sertor lavori come un progetto-cliente di Sertor; gli special-case sono debito» (2026-07-03) |

| FEAT-028 | **`sertor-flow install` sovrascrive il `plan-template.md` customizzato via `specify init --force`** — sul percorso d'install `launch_speckit` lancia `specify init --force` (feature 045) che **clobbera** `.specify/templates/plan-template.md` (**verificato empiricamente 2026-07-03**); e `plan-template.md` **non è nel piano Sertor**, quindi nulla lo ripristina → il template col **mission-gate** viene sostituito con l'upstream. **NB (correzione empirica):** la **costituzione NON viene clobberata** — `specify init` è create-if-absent su `constitution.md` (la v1.4.0 curata sopravvive), e `feature.json` non è toccato. Quindi il rischio è **ristretto a `plan-template.md`**, molto più piccolo del temuto. | L'installer preserva il `plan-template.md` customizzato attraverso lo step `specify init` (backup/restore o replace-if-upstream) → il dogfood può materializzare SpecKit via il **vehicle vero** senza perdere il mission-gate (Principio VI idempotenza) | **Should** | ✅ **IMPLEMENTATA (2026-07-03, ≡ E15-FEAT-005)** — backup/restore di `plan-template.md` attorno a `specify init --force` in `execute_governance_plan`; host-agnostico, fail-loud; 2 test, sertor-flow 142 verdi, core invariato. Prerequisito del modello install-based sbloccato — **finding E10-FEAT-027, corretto empiricamente 2026-07-03** (test isolato: `constitution.md` PRESERVATA, `plan-template.md` CLOBBERATO). Fix candidato: l'installer fa backup di `plan-template.md` prima di `specify init` e lo ripristina dopo (o lo aggiunge al piano come replace-if-upstream). Prerequisito per la fedeltà-processo governance (vedi epica **fedeltà-dogfood**). Gemella di FEAT-015. |
| FEAT-029 | **BUG: hook wiki `SessionStart` viola Principio X (path hardcoded) + `structure init` non crea `roadmap.md` + fallback log rotto** — l'hook `wiki-session-start.py` (distribuito da `sertor install wiki`, byte-identico asset↔dogfood) emette la direttiva di caricamento contesto con path **hardcoded** (`wiki/syntheses/roadmap.md`, `wiki/index.md`, `wiki/log/…`), **non** letti da `wiki.config.toml`. Tre difetti confermati (verifica 2026-07-14): (1) path hardcoded → **viola Principio X** (funziona sul dogfood solo perché la config coincide coi letterali; un host con `root`/tassonomia diversi punterebbe a path inesistenti); (2) `structure init` (`structure.py`) seeda solo `index`+`log`, **non `syntheses/roadmap.md`** → un wiki appena inizializzato, alla sessione dopo, apre con **letture fallite silenziose** (l'hook ordina «Read roadmap.md» inesistente — esattamente il sintomo osservato); (3) fallback log: se manca `wiki/log/` compone `wiki/log/log.md` invece di `wiki/log.md` (piatto pre-rotazione). | L'hook rispetta `wiki.config.toml` (host-agnostico); un wiki nuovo apre senza letture fallite (roadmap-seed opzionale o degrado fail-safe su file assenti) | **Should** | 📋 da decomporre — **BUG confermato (verifica agente 2026-07-14), fonte: handoff Nunzio 2026-07-09 (§2).** A-09 (.ps1→.py) ha portato la logica invariata → i 3 difetti sopravvivono al porting. Host-facing (ogni nuova install `wiki`). Da toccare: asset `wiki-session-start.py` (leggere la config via `_hooklib`/profilo + degrado su file assenti) + `structure.py` (seed roadmap opzionale) + guardia. Gemella di FEAT-017/018 (correttezza hook host-facing) |
| FEAT-030 | **Emendamento starter constitution: Principio «Product Plane vs Fixture Plane»** (proposta Sinthari 2026-07-12, **ACCOLTA 2026-07-14**) — aggiungere allo **starter neutro** seminato da `sertor-flow` (`.specify/memory/constitution.md`) un principio sulla trappola del dogfooding: quando un prodotto è esercitato contro fixture nel proprio repo, i due piani (**prodotto** vs **fixture**) restano distinti; le decisioni di prodotto si giustificano solo col caso reale-utente, mai per comodità-fixture; e — clausola-cuore — **un workaround del piano-test che tappa un buco del piano-prodotto va registrato come OPEN PRODUCT QUESTION, mai nascosto** (Principio XI/XII applicato al dogfooding). Cerimonia proporzionale a quanto il prodotto **muta l'asset in-place**. | Lo starter (e il dogfood, da client fedele) codifica la trappola dogfooding che abbiamo già vissuto (speclift/specaudit-come-fixture, corpus dogfood vs reale, E15); i buchi-di-prodotto mascherati da workaround-test **emergono** invece di restare invisibili | **Should** | 📋 **ACCOLTA, da emendare via constitution-flow** (non per drift). Testo host-agnostic di Sinthari (§«Il principio»). **Numerazione:** da noi il XII è «Fail Loud» → slotta come **XIII**. **Ambito:** (1) emendare lo **starter** (`sertor-flow`) + bump MINOR; (2) allineare la **costituzione dogfood** di Sertor (client fedele); (3) **propagazione:** nuovi workspace automatico via starter, esistenti via `sertor upgrade`/auto-updater (E2-FEAT-013, ora vivo post go-public); (4) opzionale: cue `requirements` dogfood-stateful + convenzione roadmap «due corsie». Fonte: `input-other-agents/processed/sinthari-proposta-principio-xii-...`. Gemella tematica di FEAT-009 (distribuzione costituzione neutra) |
| FEAT-031 | **BUG: wiring degli hook non ancorato alla repo root (regressione A-09)** — gli hook installati erano wirati con path **relativo** allo script (`uv run --no-project python .claude/hooks/X.py`): appena la CWD dell'host devia dalla radice (es. l'agente fa `cd` in una sottocartella) il path risolve dalla dir sbagliata e l'hook fallisce **prima** del comando — su `PreToolUse` **blocca** Bash/Write/Edit. Regressione di **A-09** (`69d527c`, 2026-07-09): la migrazione `.ps1`→`.py` ha eliminato l'ancoraggio `Join-Path $env:CLAUDE_PROJECT_DIR` che i `.ps1` avevano. **Latente 6 giorni**, emersa nel dogfooding vivo. | Gli hook installati risolvono da qualunque CWD; fix **nativo** per assistente (no compat-hack) | **Should** | ✅ **CONSEGNATA (merge `e3c2a97`/PR #190, 2026-07-15)** — vedi EXEC. Claude: ancoraggio `"${CLAUDE_PROJECT_DIR}/.claude/hooks/X.py"` nei 7 asset `settings*.json` (quotato per spazi; sostituito prima della shell, cross-platform). Copilot: nuovo campo `HookEntrySpec.cwd` + `render_copilot_hooks` → `cwd="."` sui 6 command entry (Copilot non ha placeholder di project-root); prompt SessionStart invariati. Guardie: `test_claude_hook_wiring_anchored.py` + `test_schema_copilot_hooks.py` esteso. `sertor-core` invariato. **⚠️ Nota di numerazione:** il commit `202948a` e la PR #190 la etichettano «E10-FEAT-029», ma **FEAT-029 è un altro bug ancora aperto** (hook wiki con path hardcoded): è un **mislabel** in storia immutabile, il numero corretto è **FEAT-031**. **⚠️ Consegna incompleta agli ospiti esistenti → FEAT-032.** |
| FEAT-032 | **BUG (CONFERMATO): l'identità di un hook nel merge è la STRINGA del comando → un cambio di wiring non aggiorna, duplica** — `settings_merge.py` deduplica per `command` esatto (`_inner_commands`/`_dedup_hooks`). Conseguenza su un host che **aggiorna**: (a) **Claude** — FEAT-031 cambia la stringa (relativa → ancorata) → la voce vecchia non è riconosciuta come «la stessa» → **resta** e la nuova viene **appesa** ⇒ hook **duplicati**, con la copia vecchia **rotta ancora attiva** (il `PreToolUse` relativo continua a bloccare) ⇒ **il fix FEAT-031 non arriva a chi aggiorna, solo alle install nuove**; (b) **Copilot** — il fix aggiunge il campo `cwd` ma **non tocca** la stringa → la dedup considera la voce «già presente» e **scarta la nuova** ⇒ il fix **non atterra mai**, senza alcun segnale. **Classe di bug, non istanza:** la primitiva di rimedio `remove_hook_entries_by_command_substring` **esiste già** (scritta per A-09 `.ps1`→`.py`) ma è chiamata **solo** in `_apply_rag_upgrade` e copre **solo** i basename `.ps1`; il path di install (`_apply_rag_settings`) fa solo `merge_settings`, e la transizione A-09→FEAT-031 non è coperta da nessuna parte. **Perché i test non l'hanno colto:** le guardie verificano che l'**asset** dichiari il wiring giusto, **non l'esito su un host che aggiorna** — il buco sta fra le due cose. | Un host che aggiorna resta **single-impl** e ancorato su entrambi gli assistenti; l'identità di un hook Sertor non dipende da un dettaglio mutabile come la stringa del comando; una guardia copre l'**esito d'upgrade** (wiring vecchio → nuovo), non solo la forma dell'asset | **Should (P1)** | ✅ **IMPLEMENTATA (2026-07-16)** — **fix: l'identità di un hook è lo SCRIPT (stem), non la stringa del comando.** Un solo cambio nel kit (`settings_merge.py`) chiude **tutte e tre** le transizioni insieme, invece di rincorrerle una per una: `.ps1`→`.py` (caso Noetix), relativo→ancorato (caso nostro), `cwd` aggiunto a command invariato (caso Copilot, quello silenzioso). **I due contratti sono ora espliciti e onorati:** `merge_settings(..., replace_stale=False)` = **install** — non rimuove (contratto) **e non duplica più**: lascia la forma stantia e la **nomina** nel report puntando a `upgrade` (Principio XII: prima taceva); `replace_stale=True` = **upgrade** — sostituisce in place e **collassa** tutte le rendering dello stesso hook in una (un hook = un wiring per evento). *Deliberatamente NON* si è fatto delegare `install`→`upgrade`: romperebbe la non-distruttività che definisce `install` (vedi requisito FEAT-008 §1, che già nel 2026-06-17 elencava questi 3 sintomi). **+10 guardie** (`test_settings_merge_identity.py`) che asseriscono l'**esito su un host che aggiorna**, non la forma dell'asset — il buco da cui è passato tutto. **Trovato sul campo, non dai test:** la prima versione sostituiva solo la **prima** voce corrispondente → su un host già duplicato produceva due voci *identiche*; colto eseguendo l'upgrade vero sul dogfood, poi coperto da guardia. **Dogfood ripulito e verificato:** 15 voci (9 rotte + 6 buone, duplicate) → **9 voci, tutte ancorate, 0 relative**. **Doc corretta:** `docs/install.md` affermava *«the runtime package itself is updated inside `.sertor/` by re-running the install»* — **falso** (`uv add` non muove un lock già soddisfatto): era **l'origine del consiglio sbagliato dato a Noetix**; ora la doc dice che install/upgrade rinfrescano gli **asset** e il runtime vuole `uv sync --project .sertor --upgrade`. Gate: root **1188** · sertor **531** · flow **142** · kit **188**, ruff clean; `sertor-core` **invariato**. — **CONFERMATA (2026-07-16)** da **verifica indipendente del nodo Noetix** (Acta, canale `Feedback Sertor`, `2026-07-16-noetix-verifica-indipendente-refresh-hook-duplicati`; host Windows/PowerShell, **baseline catturato prima** del refresh) **+ test dal vivo sul dogfood**. **Esito in 3 punti:** (1) **la radice è quella diagnosticata** — identità = stringa del comando ⇒ le generazioni si **accumulano**; (2) **l'asse è più largo del previsto** — da Noetix la duplicazione è `.ps1` **+** `.py` (i loro `.ps1` erano **già ancorati** via `$env:CLAUDE_PROJECT_DIR` con fallback `.`, quindi la regressione A-09 **non li aveva mai colpiti**: su host PowerShell il bug FEAT-031 non esisteva); il caso *relativo-vs-ancorato* è **solo il nostro sotto-caso**, la classe è «qualunque cambio di forma del comando»; (3) **`upgrade` NON è il rimedio generale** — Noetix riporta che `upgrade` ripulisce (`16 updated`, spariscono i `.ps1`, restano 9 hook `.py`), ma **ciò vale solo per la generazione `.ps1`**, l'unica coperta da `_LEGACY_PS1_BASENAMES`: **testato dal vivo sul dogfood** (`sertor upgrade rag` reale, 2026-07-16) → **15 entry pre = 15 post, le 9 relative sopravvivono tutte, summary `0 removed`** ⇒ **il fix serve** (la transizione `.py`-relativo→ancorato non è coperta da nessun percorso). **⚠️ Errore nostro corretto da Noetix:** avevamo offerto `install` e `upgrade` come **equivalenti** e chiesto la verifica con `install` — cioè **il percorso che produce il bug**; `install` (`_apply_rag_usage_block`→`write_marker_block`, presenza del marker) **salta il blocco `CLAUDE.md` stantio**, mentre `upgrade` (`update_marker_block`, confronto di **contenuto**) lo rigenera; idem per i file (`CREATE_IF_ABSENT` vs `update_file_if_changed`). **Formulazione di Noetix, adottata:** *«l'installer ragiona per presenza, non per contenuto»* — su un'install nuova non si vede, su un host che aggiorna ogni «già presente» è un pezzo di stantio che sopravvive (vale anche per `.mcp.json` e le chiavi `.env` obsolete). **Osservabilità del guasto → E2-FEAT-018.** — *(storia: osservata sul **dogfood** eseguendo il re-install reale — `uvx --refresh … sertor install rag --assistant claude` → `+1 hook entries` ×6, `.claude/settings.json` con entrambe le forme — ma il dogfood era un **teste contaminato**, bug prodotto sul nodo dove era stato scritto il fix; da qui la richiesta di verifica indipendente a Noetix, e il gate utente «nessun fix prima dell'esito», ora **sciolto dall'esito stesso**.)* **Fix candidato (confermato necessario):** estendere la migrazione alla forma legacy relativa-`.py` (discriminante `python .claude/hooks/` — la forma ancorata contiene `python "${CLAUDE_PROJECT_DIR}/…` → non collide) + riconoscere su Copilot l'entry con `cwd` assente come da aggiornare + chiamare la migrazione anche nel path di **install**, non solo `upgrade`. Gemella di FEAT-031 (di cui è il debito di consegna) e di E2-FEAT-013/016 (lifecycle robusto). |
| FEAT-033 | **BUG (CONFERMATO): `ritual-check` assume `master` come base del diff → esplode su ogni ospite con default `main`** — `src/sertor_core/wiki_tools/ritual_check.py:52` esegue `merge-base HEAD master` col nome **hardcoded**; su un repo il cui default è `main` non c'è merge-base → fail-loud `cannot determine a git diff base (no merge-base with 'master')`. Aggirabile con `--base main`, ma **arriva nel momento peggiore**: `ritual-check` è il primo tool che un ospite tocca quando il rituale (FEAT-026) lo **obbliga** a dichiarare — il primo contatto con la nostra forced declaration è un errore in faccia. **Violazione del Principio X**: un assunto sull'ospite scritto dentro un tool che si vanta di non averne (il resto del modulo legge tutto da `wiki.config.toml`). | Il tool funziona su qualunque ospite senza flag; la forced declaration non esordisce con un errore; Principio X onorato dove è più visibile | **Should (P1)** | ✅ **CONSEGNATA** (merge `546cb22`/PR #200, 2026-07-18 — vedi EXEC). `_resolve_base` non assume più `master`: nuovo `_default_base_candidates` rileva il default a runtime (`git symbolic-ref --short refs/remotes/origin/HEAD` → ref esistenti ordinati `origin/main`/`origin/master`/`main`/`master` → primo con merge-base), `--base` invariato, fail-loud finale invariato (nomina i ref tentati). Dogfood `master` non regredisce (path primario `origin/HEAD`, **verificato LIVE**); 4 test (main/master/`--base`/orphan); SpecKit completo. **Sblocca la forced declaration sugli ospiti `main`.** **Segnalato dal nodo Noetix** (Acta, `Feedback Sertor`, `2026-07-16-noetix-governance-bundle-divisibile-configuration-manager`), **verificato sul codice da noi** (2026-07-17). |
| FEAT-034 | **BUG (CONFERMATO): `rag-freshness` persiste il verdetto PRIMA di riparare → il caso normale genera un falso allarme** — `_worker()` in `.claude/hooks/rag-freshness.py` esegue nell'ordine: (1) `doctor --json`, (2) deriva `verdict`/`reason`, (3) **scrive `.sertor/.rag-health.json`** (riga ~87), (4) **poi** il re-index incondizionato. Il verdetto persistito è **per costruzione una misura pre-riparazione**, e **nulla riscrive lo stato dopo lo step 4** (verificato: `os.replace` compare una sola volta). `rag-freshness-start.py` non misura nulla — ristampa ciò che trova → all'avvio successivo annuncia `degraded` per un problema **che l'hook stesso ha già riparato**. **Il costo è la svalutazione dell'allarme:** un avviso che grida al lupo nel caso *normale* (RAG stantio → riparato → «degraded» comunque) insegna a scartarlo; il giorno del degrado **reale** arriva con la stessa faccia e passa liscio — e la direttiva è forte («Do not proceed on potentially stale context»). **Piegato qui (stesso hook, stesso fix):** il `reason` tiene solo il **primo** warn (`if not reason`), quindi con `index: warn` **e** `mcp: warn` il messaggio ne dichiara uno solo — chi legge non sa quante aree sono degradate. | L'allarme è vero quando compare, quindi merita di essere creduto; lo stato riflette la salute **dopo** la riparazione, non prima; `reason` non perde informazione | **Should (P1)** | ✅ **CONSEGNATA** (merge `5add90d`/PR #205, 2026-07-20 — **fusa con FEAT-035**, vedi EXEC). `_worker()` ora esegue **re-index (ripara) → `doctor` (rimisura) → scrittura atomica del verdetto post-riparazione**; `reason` elenca **tutte** le aree degradate (non più solo la prima); re-index fallito ⇒ `degraded`. A valle di FEAT-038 (doctor ancorato). Asset host-facing: guard sull'**esito d'upgrade** (REQ-011) + byte-parity bundle↔dogfood; doc utente `install.md` §10.1 + CHANGELOG. **Segnalato dal nodo Acta**, verificato sul codice (2026-07-17); SpecKit completo, Constitution 12/12. Gemella di FEAT-035 (stesso hook, consegnata insieme). |
| FEAT-035 | **BUG: un `rag-freshness` interrotto lascia un lock stantio che blocca OGNI re-index successivo** — il worker gira **detached** (`_spawn_detached`, `DETACHED_PROCESS` su Windows / `start_new_session` su POSIX): se l'albero di processi muore mentre il re-index è in corso, il lock `.sertor/.index/.index.lock` resta con dentro un **PID morto**, e **nulla lo ripulisce**. Ogni `sertor-rag index` successivo fallisce con `index is locked: another process is indexing …` finché non si interviene **a mano**. **Osservato dal vivo (2026-07-17):** lock del PID 33516 (inesistente) lasciato dall'hook delle 10:25, che bloccava il re-index post-merge; rimosso manualmente dopo aver verificato l'assenza del processo. Il messaggio d'errore è onesto (nomina il lock e il rimedio) ma scarica su un umano un lavoro che la macchina può fare: **il lock registra già il PID** — basta verificare se è vivo. | Un hook che muore non lascia il RAG bloccato; il lock è **auto-guarente** (PID morto ⇒ lock stantio ⇒ acquisibile), come già facciamo per Chroma/code-graph (auto-heal, PR #89/#90) | **Should** | ✅ **CONSEGNATA** (merge `5add90d`/PR #205, 2026-07-20 — **fusa in FEAT-034**, vedi EXEC). Auto-heal di `_IndexLock` (`src/sertor_core/services/indexing.py`): un lock con PID **confermato morto** viene reclamato (il run procede); PID **vivo** o lockfile **ambiguo** (vuoto/garbage) → `IndexLockedError` (conservativo, chiude la race create→write); reclamo **osservabile** via `log_event` `index.lock.reclaimed` (Principio XII). Nuovo helper `_pid_alive` cross-OS stdlib (`os.kill` su POSIX; `OpenProcess`/`GetExitCodeProcess` via `ctypes` su Windows — **mai** `os.kill` che lì terminerebbe il processo). **Provato LIVE** (PID morto→procede / PID vivo→locked). Decisione utente 2026-07-20: fusa con FEAT-034 (stesso hook). |
| FEAT-036 | **`upgrade` su una capability NON installata la installa (sorpresa, non bug)** — l'installer è additivo by design, ma chi lancia `sertor-flow upgrade` credendo di **rinfrescare** si ritrova una capability **nuova**, con il suo blocco in `CLAUDE.md`. Verificato da Noetix via `--dry-run`: `created` per tutti e tre gli asset governance su un nodo che non li aveva. Per un ospite che sta *evitando* quel blocco (caso Noetix, vedi E2/bundle divisibile) l'effetto è esattamente ciò che voleva scongiurare. | Nessuna sorpresa al setup: `upgrade` che aggiorna, o dichiara di non avere nulla da aggiornare | **Could** | ✅ **CHIUSA — folded in E2-FEAT-018, CONSEGNATA** (merge `0b45c24`/PR #202, 2026-07-18): con l'esito-che-descrive-l'azione (`Outcome.PRESENT_DIVERGENT`), «upgrade ha creato/trovato artefatti divergenti» è ora un **fatto nel report**, non un `if` hard-coded → 036 chiusa **senza codice proprio** dentro E2-018 (P1). Traccia mantenuta qui per rintracciabilità (regola «promuovi, non seppellire»). **Segnalato dal nodo Noetix** (2026-07-16). *(Resta l'intreccio col bundle divisibile → E2-FEAT-019.)* |
| FEAT-037 | **INVESTIGAZIONE: il check dell'area `index` di `doctor` non è funzione pura di `last_index` + un index incrementale a 0 modifiche non aggiorna `last_index`** — **pista da Acta, non diagnosi** (loro stessi la danno come tale). *Osservazione:* `doctor` il 16/07 20:27 → `index: warn`; il 17/07 → `index: pass`, **con lo stesso identico `last_index=2026-07-16T20:23:44Z`**: stesso input, verdetto opposto ⇒ qualcos'altro guida il verdetto, e dall'esterno non è visibile. *Corollario osservato:* un `index` incrementale con `added=0 updated=0 removed=0` **non sposta** `last_index` → se un controllo (ora o in futuro) si appoggiasse a quel timestamp, un corpus **fermo e perfettamente indicizzato** apparirebbe **sempre più stantio** col passare dei giorni. *Ipotesi collaterale di Acta (non verificata):* `mcp: warn` al SessionEnd potrebbe essere fabbricato dal **momento della misura** (la sessione si sta chiudendo) — a sessione aperta è `pass`. | Il verdetto di `doctor` è spiegabile e riproducibile; la freschezza non degrada da sola su un corpus fermo | **Could** | 🔎→✅ **INVESTIGATA (2026-07-18)** — l'analisi [[setup-dichiara-presunzione-non-azione]] ha sciolto la pista: (a) `last_index` **non è letto da nessun check** (`doctor.py:224`, è solo `detail`) ⇒ un corpus fermo **non invecchia da solo** — NON è un bug; (b) lo swing warn/pass è **probabilmente FEAT-034** (verdetto scritto pre-riparazione), non il timestamp; (c) il difetto **azionabile** trovato cercando la loro causa è un secondo, indipendente — `doctor` non ancorato alla cwd — **scorporato in FEAT-038 (P0)**. *Residuo Could:* valutare se aggiornare `last_index` anche a 0 modifiche («ultima verifica» vs «ultima scrittura»). **Segnalato dal nodo Acta** (`Feedback Sertor`, 2026-07-17), investigato da noi (2026-07-18). |
| FEAT-038 | **BUG: `doctor` non è ancorato alla radice del progetto** — `cli/__main__.py:574` usa `root = Path.cwd()`; `composition.py` risolve ogni sorgente come `root/path`, quindi lo **stesso indice** dà `index: pass` dalla root e `index: warn (index_stale)` da una sottocartella (`src/`) — provato dal vivo (stesso istante, subito dopo un re-index); anche `registered` (area `mcp`) oscilla True→False col cwd. È lo **stesso difetto d'ancoraggio di FEAT-031** (corretto per gli hook, **mai** per `doctor`), già sfiorato e archiviato «not-a-bug» come E15-FEAT-009 (2026-07-04: ci fermammo a «artefatto cwd», senza correggere). Emerso investigando FEAT-037. | Ogni verdetto `doctor` è affidabile **indipendentemente dalla cwd**; `rag-freshness` (che sul doctor costruisce l'allarme) non produce falsi `degraded` | **Should (P0)** | ✅ **CONSEGNATA** (merge `7075a0f`/PR #198, 2026-07-18 — vedi EXEC). `Path.cwd()` → `Settings.project_root` self-localizing (parent di `.sertor/` via `sys.prefix`; `CLAUDE_PROJECT_DIR` override; **fail-loud** se irrisolvibile, Principio XII); `_cmd_doctor` la usa per sorgenti+mcp. **Invarianza al cwd provata LIVE** sul runtime installato (stesso verdetto da root e `src/`) + 5 unit test; SpecKit completo; `sertor-core` engine invariato. **Sblocca FEAT-034**. Analisi: [[setup-dichiara-presunzione-non-azione]]. |
| FEAT-039 | **⭐ SOLUZIONE DEFINITIVA del distill — «daily distill floor» (≥1 distill/giorno).** *Direttiva utente 2026-07-21: non basta tracciare/dichiarare — serve una soluzione definitiva con un pavimento di **almeno un distill al giorno**.* Design (4 parti coese): **(1) `distill-audit`** — nuovo sotto-comando deterministico di `sertor-wiki-tools` (zero-LLM, read-only, **tutto il corpus, cross-sessione — NON git-diff**) che trova le entità referenziate da ≥k punti **senza pagina**; segnale MVP concreto = **wikilink penzolanti** (`[[x]]` senza `x.md`) contati per frequenza → lista candidati + **debito N**; **(2) pavimento giornaliero (hook)** — check once-per-day (SessionStart): se la giornata non ha ancora una voce `distill` **e** N>0 → promemoria vincolante «pavimento non raggiunto, debito N, top X/Y/Z; fai un distill o dichiara un no *costato*»; **(3) il «no» costa** — «distill: non necessario» deve nominare i candidati e perché non durevoli; **(4) regola standing** nel rituale «≥1 distill/giornata attiva». **Confine D↔N:** il tool *trova* (deterministico), l'agente *giudica*; l'hook *annuncia+esige*, non può obbligare il giudizio (limite onesto: pavimento + debito visibile + no-costato = skip caro e visibile, non impossibile). Host-facing (ogni ospite col wiki riceve il pavimento). **Origine doppia:** recupero debito distill di 5 settimane (dogfood) **+ needfinding indipendente del nodo Acta** (stesso giorno, stessi 3 punti a/b/c). Assorbe/riscrive la vecchia idea «estendi `wiki-pending-check` a pretendere la dichiarazione a fine step». *(Vecchia formulazione, per storia: `distill` è giudizio+condizionale+auto-eseguito senza rete; `record` sopravvive perché delegato+hook-nudged.)* — `distill` è *giudizio* + *condizionale* («calibra al valore») + *auto-eseguito dal flusso principale*: la stessa firma dei passi che si saltano in silenzio. A differenza di `record` (delegato al `wiki-curator` **e** sollecitato dall'hook `wiki-pending-check` su Stop/SessionEnd), `distill` **non ha alcuna rete** → dipende solo dall'agente. **Danno misurato:** dal **2026-06-15 al 2026-07-21** (~5 settimane) **nessuna** voce di log taggata `distill`, pur avendo consegnato decine di step; il distill-lavoro è proseguito sporadico (5 pagine-entità) ma la **dichiarazione** del verdetto è sparita — emerso **solo** su domanda diretta dell'utente (2026-07-21). La **dichiarazione forzata** di E10-FEAT-026 è un contratto **comportamentale** (un hook non può *giudicare*), quindi non enforced sul dogfood. | L'hook rileva «step chiuso senza *dichiarazione* di distill» e **la pretende** (nudge non bloccante, come già per `record`); `distill` guadagna la stessa rete di `record`. L'hook non giudica — esige il verdetto (anche «non serve»). | **Should** | ✅ **CONSEGNATA (v0.1.3, 2026-07-22 — vedi EXEC).** ⚠️ **FORMA FINALE ≠ descrizione a sinistra:** ricalibrata in sessione (2026-07-22) da «hook non-bloccante che pretende la *dichiarazione* + debito N come segnale» a **merge-block bloccante** — hook `distill-floor` (`PreToolUse`) che NEGA il merge di consegna finché la giornata non ha una voce `distill`; gate = **sola presenza** (non un conteggio); il tool `distill-audit` è **CONTESTO advisory**, non gate (prosa rumore-dominata: 228 vs 9 wikilink). SpecKit `specs/116`; +25 test; prova LIVE del loop. Gemella lato-enforcement di **E10-FEAT-026** (`ritual-check` = tool per-step; questa = merge-gate cross-sessione). Origine: recupero del debito distill 2026-07-21. **⭐ Arricchita dal needfinding del nodo Acta (2026-07-21, convergenza indipendente: stesso problema, stesso giorno):** tre raffinamenti che rendono il distill *meno discrezionale senza toglierlo dal giudizio* — (a) **discovery cross-sessione, non git-diff**: `ritual-check` scopre via diff del singolo step, ma il distill matura per *accumulo* (un'entità diventa referenziata da ≥k punti settimane dopo) → invisibile al diff. Serve un `distill-audit` che scandisce **tutto** il wiki+requirements per entità referenziate da ≥k punti **senza pagina** (la soglia del playbook resa misurabile; il tool trova, l'agente giudica); (b) **il «no» deve costare**: «distill: non necessario» deve **nominare i candidati considerati e perché** non durevoli (come l'assenza dichiarata di Acta dice *cosa e dove* ha cercato) — un no riflesso non è più a costo zero; (c) **debito visibile**: metrica leggera a fine sessione «N entità candidate senza pagina» → un numero che sale si nota. *(Consolidato in backlog da noi, dichiarato ad Acta.)* |
| FEAT-040 | **Wiki freshness guard — pavimento allo Stop (gemello lato-Stop di FEAT-039).** L'hook Stop non-bloccante `wiki-pending-check` (nudge) viene ignorato → il rituale (record + distill + lint semantico) resta saltabile a fine sessione. `wiki-guard` è il gemello **lato-Stop** di `distill-floor` (lato-merge): allo `Stop`/`agentStop`, se la sessione ha fatto lavoro indicizzato non ancora registrato nel wiki — rileva **riusando** `sertor-wiki-tools scan` (contratto `wiki.scan/1`, campo `pending`), niente reinvenzione — **BLOCCA** la chiusura con `{"decision":"block","reason":…}` e un `reason` specifico che esige record + distill + **lint semantico** (risolvibile in un turno). Confine D↔N: l'hook *esige*, il main agent *giudica* (Principio XI). Anti-loop `stop_hook_active`; **fail-open** (no config / scan assente → non intrappola); read-only/domande (`pending` 0) chiudono normali; exit 0 sempre; stdlib-only. **Parità piena Claude (`Stop`) / Copilot (`agentStop`)** — entrambe accettano `{decision:block, reason}` (verificato sui doc ufficiali). Sostituisce il wiring **Stop** di `wiki-pending-check` (che resta per il solo SessionEnd). | Record e lint semantico guadagnano una **rete deterministica** allo Stop, come il distill al merge; nessuna sessione con lavoro indicizzato chiude senza wiki aggiornato | **Should** | ✅ **CONSEGNATA (2026-07-23).** Hook `wiki-guard.py` (portabile, `_hooklib`), wiring Claude `settings.hooks.json` + Copilot `_copilot_wiki_hook_specs` (generato); 8 test; sync bundle. **Design confermato leggendo i doc ufficiali** (Claude `Stop`+`decision:block`; Copilot `agentStop`+`{decision:block,reason}`); **scartato** l'handler `agent`/`prompt` per il *verify semantico* deterministico (Claude-only — Copilot ha `prompt` solo su `sessionStart` — **e** sposterebbe il giudizio dentro l'hook, contro D↔N). **Debito noto:** la migrazione **upgrade** sugli ospiti esistenti (rimuovere il vecchio wiring Stop di `wiki-pending-check`) → follow-up **FEAT-041**, gemello di FEAT-031→032. |
| FEAT-041 | **Upgrade rimuove il wiring Stop superato di `wiki-pending-check`** (completamento di FEAT-040). Su un host già installato, `upgrade` deve **rimuovere** la vecchia entry Stop di `wiki-pending-check` (rimpiazzata da `wiki-guard`), altrimenti **entrambi** gli hook Stop firano finché non si toglie a mano. Fresh install e dogfood (re-install) sono corretti; solo l'`upgrade` in place è affetto. **Gemello di FEAT-031→032** (identità hook per stem; riusa `remove_hook_entries_by_command_substring` del kit). | Consegna pulita di FEAT-040 **agli ospiti che aggiornano**, non solo ai fresh install | **Should** | ✅ **CONSEGNATA (2026-07-23).** `_apply_wiki_upgrade` (ramo SETTINGS_MERGE) rimuove la entry `--mode Stop` di `wiki-pending-check` **prima** del merge additivo, via `remove_hook_entries_by_command_substring` con substring **assistant-specifico** (`wiki-pending-check.py" --mode Stop` su Claude · `wiki-pending-check.py --mode Stop` su Copilot — la resa del path differisce); la SessionEnd resta. Test `test_upgrade_wiki_stop_supersede.py` (host vecchio → upgrade → Stop=solo wiki-guard, SessionEnd preservata). **Confermato LIVE sul dogfood** (l'`upgrade` reale aveva prodotto il doppio-fire; il fix lo azzera). Origine: dogfooding via installer. |
| FEAT-042 | **`upgrade --dry-run` non proietta i `SETTINGS_MERGE`/`CONFIG`** — in `install_wiki.py` (ramo UPGRADE) il dry-run ritorna hardcoded `SKIPPED "idempotent"` per quelle kind (riga ~588) senza simulare il merge: un `upgrade --dry-run` mostra sempre «idempotent» sui settings anche quando il merge reale cambia (`+1`/rimozioni). Colto dal dogfooding di FEAT-040 (il dry-run diceva «idempotent», il run reale faceva `+1`). | Il `--dry-run` dice la verità sui settings (l'utente si fida per decidere se aggiornare) | **Should** | 📋 da fare — la proiezione dry-run deve chiamare il merge in modalità no-write e riportare l'esito reale |
| FEAT-043 | **Il breadcrumb `.last-hook-error` non viene mai riconciliato al ritorno in verde** — l'artefatto fail-loud di E10-FEAT-019 è scritto una volta e **mai ripulito né fatto emergere**: sul nodo *Acta* `{"hook":"rag-freshness","reason":"index area: warn"}` è rimasto lì **sei giorni** mentre l'indice veniva rifatto più volte e `doctor` dava **PASS 4/4**. Chi lo trova mesi dopo non sa se descrive un problema attuale o risolto da tempo — e un breadcrumb che invecchia in silenzio perde il valore diagnostico per cui esiste. Da decidere il comportamento voluto (ripulire al ritorno verde · aggiungere `resolved_at` · farlo emergere finché non è letto) e implementarlo: oggi non è un bug dichiarato ma un **contratto non definito**. Stessa classe di E2-FEAT-021: **stato scritto una volta e mai confrontato con la realtà**. | Un fail-loud che non si spegne diventa rumore, e il rumore è indistinguibile dal segnale | **Should** | 📋 da fare — domanda del nodo *Acta* (2026-07-24, punto 2), da riportare come risposta |
| FEAT-044 | **Lo skew di versione spegne in SILENZIO proprio gli aiuti «il tool trova, tu giudichi»** — su un runtime rimasto indietro, `ritual-check` e `distill-audit` **non esistono**; `distill-floor.py` li invoca come *best-effort deterministic HINT*, quindi **non fallisce**: blocca il merge (bene) ma **senza il suggerimento dei candidati**, e nulla lo segnala. Il blocco `WIKI-RITUAL` del `CLAUDE.md` istruisce l'agente a lanciare un comando che sul suo host **non c'è**. La promessa di design «il tool trova, tu giudichi» degrada a «**tu ricordi**» — cioè esattamente il fallimento che quelle funzioni esistono per prevenire — e degrada **doppiamente invisibile**: il tool tace perché è best-effort, e l'agente che trova un sottocomando mancante conclude ragionevolmente «non in questa versione» e tira dritto. **È successo davvero** sul nodo *Sinthari* (2026-07-24). Rimedio: un best-effort che **dichiara** quando l'aiuto non è disponibile (e perché), invece di degradare muto — Principio XII applicato alla *degradazione*, non solo all'errore. *(La causa a monte — il pin fermo — è chiusa in v0.2.0; questo è il **secondo strato**, indipendente: resta vero ogni volta che un host è indietro.)* | Un gate che resta in piedi mentre l'assistenza sparisce dà falsa sicurezza: sembra che il sistema funzioni, e invece è tornato alla memoria umana | **Should** | 📋 da fare — segnalato da *Sinthari* (2026-07-24, punto 3) |
| FEAT-045 | **🔴 IL GATE `wiki-guard` PUÒ ESSERE INSODDISFACIBILE: una sessione non può chiudersi sul proprio ultimo merge** — `scan` ancora al **mtime** del file di log più recente (`scan.py:41-58`); `gh pr merge` + `git pull` **riscrivono i file appena mergiati**, che diventano più recenti del giornale → allo `Stop` il gate blocca. Per soddisfarlo bisogna **scrivere ancora nel wiki**, il che produce nuovo lavoro da committare e mergiare, che al merge dopo riproduce la condizione. **L'unica uscita è chiudere con l'albero sporco** (una voce di giornale non committata) — che è come ne è uscito il nodo *Acta*, dopo **sette occorrenze in una giornata**. *Verificato sul nostro codice e sul nostro nodo: il nostro `pending=0` è **una corsa vinta**, non correttezza — l'ancora regge solo perché la voce di log è stata scritta dopo l'ultimo pull. Non deterministico, quindi peggio di sempre-rotto: sembra funzionare.* **Rimedio nella forma del Principio XIV** (derivare dove derivabile, dichiarare dove no): dove l'ospite è un repo git → ancorare al **diff** rispetto all'ultimo commit che ha toccato `wiki/log/` (**derivato** dal fatto; sopravvive a pull/merge/rebase/checkout/clone — ed è già ciò che fa `ritual-check` nello stesso pacchetto); dove non lo è → **tenere l'mtime dichiarandolo proxy**, perché `scan` è host-agnostico *by design* (`scan.py:4`, «works on non-git hosts too») e passare al solo git romperebbe il Principio X. **Bivio aperto (decisione utente):** accettare come chiusura anche un **giudizio registrato** («ho guardato questi file, non vanno registrati» — datato e ispezionabile, stessa forma del registro di azioni preapprovate); rende il gate non-binario ma aggiunge una superficie che può degenerare in «dichiaro sempre che non serve». | **Un gate bloccante che non si può soddisfare non è una rete: è un ostacolo, e la via d'uscita che insegna (albero sporco / toccare il log per far avanzare il proxy) è la nostra stessa classe di guasto** | **Must (P0)** | ✅ **CONSEGNATA e RILASCIATA in v0.3.2** — *vedi EXEC* (policy A-12: lo stato «consegnato» lo dichiara l'EXEC, non questa colonna). **📐 Due principi di costruzione delle guardie, prodotti da questa feature e tenuti qui** (decisione utente 2026-07-28: **non** distribuiti agli ospiti — sono disciplina di chi *scrive* la guardia, non pratica di chi la usa): **(a) il perimetro va fatto combaciare nelle DUE direzioni.** Non basta che il consumatore nuovo capisca il produttore nuovo: il consumatore nuovo deve **tollerare il produttore vecchio**, perché durante un rollout convivono. Verificato dal vivo su `wiki.scan/1`, ed è la ragione per cui la guardia sullo schema è stata scritta **prima** dell'implementazione — e infatti mi ha corretto il perimetro, che era troppo largo. **(b) una guardia fail-open che confronta un contratto per uguaglianza non si rompe: SPARISCE.** I due hook consumatori confrontano la stringa di schema per uguaglianza e vanno in fail-open, quindi un bump di `wiki.scan/1` non avrebbe fatto fallire il gate — lo avrebbe fatto **svanire in silenzio** sugli ospiti non aggiornati. È il modo peggiore in cui una protezione può cedere: senza un errore da nessuna parte. Da cui il vincolo standing: **quella stringa non si bumpa.** Ancora derivata dall'ultima consegna che ha toccato il giornale + modifiche non consegnate dell'albero di lavoro; proxy mtime **dichiarato** con causa tipizzata dove l'ospite non è un repo. Contrasto misurato dal vivo a parità di filesystem: vecchio `pending=14`, nuovo `pending=4`. Segnalato da *Kaelen* (2026-07-24, come incoerenza) e **riqualificato da *Acta*** (2026-07-26) come **deadlock**: *«non esiste un ordine di operazioni che chiuda»*. Acta precisa anche che **E10-FEAT-048 (nominare i file) NON copre questo caso** — allevia la diagnosi, ma dopo il giudizio il gate rifiuta comunque |
| FEAT-046 | **`upsert-index`: riga in coda alla sezione sbagliata + wikilink in forma di path** — la riga nuova finisce **in coda all'ultima sezione** del file d'indice (due pagine `tech/` finite sotto `## Syntheses`) e il wikilink è scritto come **path** — `[[tech/acta-federation.md]]` invece di `[[acta-federation]]`, che è la convenzione del resto del wiki. Il `lint` **non lo segnala** (`broken_links=0` in entrambe le forme), quindi la deriva si accumula silenziosa. Rimedio: instradare la riga nella sezione che compete al `type` della pagina + normalizzare il wikilink alla forma canonica (+ regola di lint che pinni la forma). | L'indice è il catalogo che si legge per primo: se colloca male e usa una forma non canonica, degrada l'unica mappa del wiki | **Could** | 📋 da fare — trovato in autonomia il 2026-07-24 e **confermato indipendentemente** da *Kaelen* (punto 7) lo stesso giorno |
| FEAT-047 | **`lint` non rileva una partizione di log non elencata nell'indice** — `append-log` crea la partizione del giorno (`wiki/log/<data>.md`) e aggiorna `wiki/log/index.md`, ma **niente verifica** che le due cose restino allineate: se la riga d'indice non viene committata (o l'indice viene modificato a mano), il file del giorno **esiste ma non è raggiungibile dall'indice**, e il `lint` resta verde — le sue chiavi sono `broken_links`/`orphans`/`missing_frontmatter`/`naming_violations`/`stubs`, nessuna guarda l'indice del log. **Seconda istanza (2026-07-28): [[archivio-processati]]**, il catalogo di `sources/**/processed/`, duplica anch'esso un elenco derivabile dalla cartella — e nasce dalla decisione, corretta, di **non** nascondere l'archivio al lint: *archiviato non vuol dire nascosto*. La sua meta' di valore (l'**esito** di ogni richiesta) non e' derivabile e va scritta a mano; la meta' derivabile (quali file esistono) va **riconciliata**, altrimenti un documento archiviato domani non comparira' nel catalogo e tornera' orfano in silenzio. Stesso rilevatore, due consumatori. *È un'istanza del **Principio XIV** trovata il giorno stesso della ratifica, e in un caso reale: l'omissione si è ripetuta su **due PR consecutive** (#229 e #231). L'elenco delle partizioni è un fatto **derivabile** dal contenuto della cartella, e lo teniamo copiato in un file senza riconciliatore.* Rimedio: un check di `lint` che confronta le partizioni su disco con quelle elencate nell'indice e segnala la differenza (in entrambe le direzioni: partizione non indicizzata · riga d'indice che punta a un file assente). | L'indice del log è la porta d'ingresso alla cronologia: una partizione non elencata è materiale che nessuno ritrova | **Could** | 📋 da fare — istanza del Principio XIV, ricorsa 2 volte in 2 giorni |
| FEAT-048 | **`scan` conta come «lavoro non registrato» anche i file che il VCS IGNORA** — misurato dal nodo *Acta*: una bozza appoggiata in `.acta/draft-….md`, esclusa dal loro `.gitignore` (confermato con `git check-ignore`), produceva `pending=1`; rimosso quel file, `pending=0`, **nessun'altra variabile**. Un artefatto ignorato dal VCS **non può** essere lavoro da registrare nel wiki — nessuno lo consegnerà a nessuno. **Conseguenza pratica: qualunque scratch che un assistente appoggi nella cartella di lavoro blocca la chiusura di sessione** (il `wiki-guard` è un gate *bloccante*), e la diagnosi non è ovvia perché il messaggio dice **quanti** file, non **quali** — Acta ha dovuto ricostruirli con un `find` per mtime. Due rimedi, in ordine di costo: **rispettare il `.gitignore`** nell'attraversamento · **nominare i file** nel messaggio di blocco (e in `--json`). | Un gate bloccante la cui diagnosi non è ricostruibile dall'ospite si trasforma in un ostacolo da aggirare, non in una rete | **Should (P1)** | 🔨 **ASSORBITA sugli host git da E10-FEAT-045** (stesso branch): la derivazione **non fa entrare** i file ignorati dal VCS invece di filtrarli, e restituisce i path, quindi il blocco li **nomina**. **Residuo dichiarato:** sugli ospiti **non-git** l'informazione «ignorato» non esiste — i file si nominano comunque, ma l'elenco può contenere uno scratch; limite documentato in `docs/troubleshooting.md`, non simulato. Misurato e segnalato dal nodo *Acta* (2026-07-26) |
| FEAT-049 | **Il `lint` semantico deve includere i riferimenti ENTRANTI della pagina toccata** — contromisura sul *come* si esegue il lint, girata dal nodo *Acta* dopo averla pagata: un **indice**, un **catalogo**, una **sintesi** sono affermazioni **su** un'altra pagina, e diventano stantie quando quella pagina cresce — **senza che una riga di codice cambi**. Un lint che chiede solo «il repo contraddice questa pagina?» non li vede **mai**, perché il contraddittore non è il repo: è la pagina vicina. Da loro: l'`index.md` descriveva una pagina come «cinque forme osservate» mentre la pagina ne aveva accumulate altre. Rimedio: il livello B del lint estende lo scope ai **backlink entranti** delle pagine toccate. *(Corollario adottato: nelle pagine-elenco citare le voci **per nome**, mai per posizione ordinale — «la sesta» invecchia da sé; è lo stesso motivo per cui un emendamento va referenziato per titolo, vedi E13-FEAT-015.)* | Chiude la classe di deriva che nessun confronto col codice può cogliere | **Should** | 📋 da fare — dal nodo *Acta* (2026-07-26) |
| FEAT-050 | **Le note della v0.2.1 presentano come UNIVERSALE un difetto che è condizionale** — abbiamo pubblicato che l'entry `.mcp.json` con `--directory` fa risolvere l'indice nel runtime e *«answered nothing to every query»*, con «un nodo ha avuto un RAG cieco per un mese». Il danno di *Kaelen* è **reale**, ma dipendeva dalla **loro installazione** (giugno, indice fuori da `.sertor/`): su una versione corrente la self-localization di `Settings` rende la cwd irrilevante. **Verificato due volte in modo indipendente** — dal nodo *Acta* e da noi, eseguendo la stessa query con cwd nella radice e dentro `.sertor/`: **risultati identici**. *«Un nodo che leggesse la v0.2.1 potrebbe concludere di essere stato cieco senza esserlo»* — e magari rifare indici o indagare un guasto che non ha avuto. **Segno invertito rispetto ai nostri soliti difetti:** di solito sotto-dichiariamo (diciamo «updated» quando nulla si è mosso), qui **sovra-dichiariamo un danno**, in un'affermazione allarmante. Da qualificare (non ritrattare) in: CHANGELOG 0.2.1 · sezione di `docs/troubleshooting.md` · messaggio `mcp_invocation_moves_cwd` di `doctor` · pagine wiki che ripetono la frase. **+ regola:** quando pubblichiamo un difetto, dichiarare **sotto quali condizioni morde**, e riportare **anche le verifiche che non hanno trovato nulla** — *«un canale su cui arrivano solo i difetti confermati diventa un canale su cui non si sa più cosa è stato guardato»* (disciplina di *Acta*, che l'ha applicata due volte in due giorni). Il fix del template resta giusto, ma per la ragione corretta: `--directory` non è tossico oggi, è **giusto per accidente** — dipende da un meccanismo che non c'entra. | Un allarme sovra-dichiarato fa agire chi non deve, e svaluta i nostri allarmi veri | **Should (P1)** | 📋 da fare — dal nodo *Acta* (2026-07-26), verificato da noi |
| FEAT-051 | **Chiudere il gate wiki con un «giudizio registrato», non solo scrivendo** — oggi `wiki-guard` è **binario**: l'unico modo di portare `pending` a zero è **scrivere nel wiki**, perché è la scrittura che sposta l'ancora. Ma la regola aurea prevede che *«modifiche puramente meccaniche e di poco conto non richiedono una voce»*, e `scan` non sa distinguerle: una rinomina di variabile locale o una riformattazione bloccano la chiusura di sessione. Le due uscite disponibili sono **entrambe cattive** — una voce di log di comodo (che sporca il giornale e svaluta il registro) o l'albero sporco. **Forma proposta** (dal nodo *Acta*, stessa del registro di azioni preapprovate): una **dichiarazione** che chiude il gate al posto di una voce di wiki, con tre proprietà vincolanti — **nomina i file** (mai un «niente da fare» generico), è **datata** (copre quel lotto, non vale per sempre), è **ispezionabile** (un lettore successivo può giudicare se il giudizio reggeva). Filosofia del Principio XIV: *conservare può essere giusto, conservare **in silenzio** no*. **Rischio dichiarato:** riporta l'enforcement a dipendere dall'onestà dell'agente — cioè esattamente ciò che aveva fallito per settimane, e che aveva motivato un gate bloccante. Una superficie «dichiaro che non serve» è difficile da togliere una volta messa. | Il caso meccanico legittimo oggi si scarica su voci di log di comodo: deriva di un altro tipo, che rende il giornale meno affidabile | **Could — da RIVALUTARE dopo FEAT-045** | 📋 **rinviato per decisione utente (2026-07-27)**, con motivo: era il bivio aperto di FEAT-045 e **si decide con dati veri, non a priori**. Con l'ancora derivata (FEAT-045) i due sintomi peggiori spariscono da soli — il deadlock e i file gitignorati — quindi **non sappiamo ancora quanto pesi il residuo meccanico**, perché il gate corretto non l'abbiamo mai visto in esercizio. Tarare ora una via d'uscita significherebbe tararla sul comportamento rotto. **Riapre quando:** dopo un periodo di esercizio di FEAT-045, il caso meccanico si dimostra ricorrente |
| FEAT-052 | **La cache degli embedding conserva `float64` dove lo store usa `float32`: metà dello spazio è precisione buttata via** — misurato sul dogfood: `.sertor/.index/embed_cache.sqlite` è **417 MB**, il **singolo pezzo più grande** di tutto `.sertor/` (1.5 GB), più grande dell'indice che serve ad accelerare. Il blob di un vettore misura **24.576 byte** = 3072 dim × **8**, cioè `float64`; Chroma però conserva in **float32**, quindi quella precisione viene **scartata appena il vettore arriva allo store**. **407 MB dove ne basterebbero ~204.** Non è un problema del dogfood: lo paga **ogni ospite** che accende `SERTOR_EMBED_CACHE`, e cresce con la dimensione del corpus. *(Contesto della misura: dei 1.5 GB, ~460 MB sono vettori necessari, ~200 MB testo del corpus duplicato fra `chroma.sqlite3` e `index_manifest.sqlite` — quest'ultimo **voluto**, serve al refresh incrementale — 322 MB il runtime Python, e ~200 MB questo spreco.)* **Rimedio:** troncare a `float32` in scrittura sulla cache (la lettura resta compatibile se si dichiara la larghezza), più una **invalidazione dichiarata** della cache esistente — riscriverla in silenzio sarebbe la classe di difetto che il Principio XIV vieta. Da verificare in fase di design se qualche consumatore dipenda davvero dalla doppia precisione (il sospetto è di no: il percorso verso lo store la perde comunque). | Il pezzo più grande dell'installazione è grande il doppio del necessario, su ogni host | **Could (P2)** | 📋 da fare — **priorità bassa per scelta utente (2026-07-27)**: è spazio su disco, non correttezza. Trovato misurando `.sertor/` dopo una domanda dell'utente («perché è così grande?»), non da un allarme |
| FEAT-053 | **Codice morto: 3 shim di retrocompatibilità e i loro 13 test (~170 righe)** — `sertor_installer/{env_merge,gitignore_append,mcp_merge}.py` sono re-export puri verso `sertor_install_kit`, lasciati dalla feature 037 per «preservare il path storico». **Irraggiungibili da tutti gli 8 entry point** (import-graph); nessun riferimento in produzione, doc, asset o skill; **unici importatori i loro tre file di test** (148 righe, 13 test). I test del kit sono un **superset stretto** — stessi nomi più molti altri (env 5/5, gitignore 4/9, mcp 4/28): cancellando non si perde copertura. `sertor_installer` **non è una libreria pubblicata** (è il pacchetto CLI `sertor`), quindi nessun consumatore esterno può dipendere dal path storico. | Codice che nessun percorso raggiunge, tenuto vivo solo dai propri test: fa sembrare coperta una superficie che non esiste | **Could (P2)** | 📋 da fare — audit codice morto 2026-07-27 |
| FEAT-054 | **Codice morto: `specaudit/observability.py` intero (27 righe), e dichiara il contrario** — zero riferimenti nell'**intero repo**: non produzione, non test, non doc, non asset. Entrambe le funzioni (`get_logger`, `log_stage`) mai chiamate. **L'aggravante non è lo spazio ma l'affermazione:** il docstring dichiara di realizzare l'osservabilità richiesta dalla costituzione — quindi SpecAudit di fatto **non ha osservabilità**, e un file sostiene che ce l'abbia. Rimedio a bivio: **cancellare** (e dichiarare il gap di osservabilità) **oppure cablarla** negli stadi della pipeline. Non lasciarla com'è: è un artefatto che dichiara uno stato che la realtà non ha (Principio XIV). | Un file che dichiara una capacità inesistente è peggio della capacità mancante: nessuno la cerca più | **Should (P1)** | 📋 da fare — audit codice morto 2026-07-27 |
| FEAT-055 | **Codice morto: i 3 formattatori «regression» di `cli/output.py` (~110 righe)** — `format_regression_report` (451-499) · `format_fused_regression` (586-592) · `format_graph_regression` (706-727). **Causa:** il verdetto di non-regressione è stato ripiegato **dentro** `format_eval_report`/`format_fused_eval_report`/`format_graph_eval_report` — i tre che la CLI chiama davvero — e i formattatori separati non sono mai stati rimossi. `format_regression_report` non ha **nemmeno un test**: morto assoluto. Gli altri due sono tenuti in vita solo da `test_output_fused_eval.py` / `test_output_graph_eval.py`. *Verificati tutti e 18 i `format_*` del modulo: gli altri 15 sono vivi.* | Tre formattatori superati che nessun comando può raggiungere, di cui due con test che ne certificano il comportamento | **Could (P2)** | 📋 da fare — audit codice morto 2026-07-27 |
| FEAT-056 | **Codice morto: 13 simboli tenuti in vita solo dai propri test** — definiti in produzione, **zero riferimenti in produzione**, nessuno esportato da un `__init__.py` (quindi nessuna API pubblica): `merge_fused` (`services/retrieval.py` — 23 riferimenti, **tutti** in 4 file di test; superato da `search_combined` che restituisce i flussi **etichettati** invece di fonderli) · `covered_targets` (kit `lifecycle.py`) · `report_from_dict` + `adjudication_to_dict` (`specaudit/serialize.py`) · `generate_init_options` + `generate_integration` (`sertor_flow/generate.py`) · `count_tokens` (`chunking/_tokens.py` — il modulo **vive** via `cap_to_tokens`, che però non la chiama) · `is_healthy` (`doctor.py`) · `has_changes` + `schema_version` (`index_manifest.py`) · `latency_report` (`observability_report.py`) · `existing_taxonomy` (`wiki_tools/profile.py`) · `get_field` (`configure_fields.py`). **Da decidere caso per caso:** alcuni sono resti di design superati (`merge_fused`), altri sono API sensate mai cablate — e per queste la domanda giusta è *«perché il consumatore previsto non è mai arrivato?»*, non solo se cancellarle. | Un test che esercita codice irraggiungibile misura sé stesso: dà fiducia senza coprire nulla di ciò che gira | **Could (P2)** | 📋 da fare — audit codice morto 2026-07-27 |
| FEAT-057 | **Superficie obsoleta (non morta): l'operazione `sertor-wiki-tools migrate`** — è la migrazione **una-tantum** dal log monolitico alle partizioni giornaliere (FEAT-008, giugno 2026). È **raggiungibile e documentata** (tabella delle operazioni del playbook, contratto `wiki.migrate/1`), ma **nessuno la invoca**: ogni ospite installato da allora nasce già con la rotazione, e chi doveva migrare ha migrato. Non è codice morto — è una **finestra chiusa**. Decidere: mantenerla dichiarandola storica, oppure ritirarla con la sua riga di playbook e il suo contratto. *(Verificate tutte e 13 le operazioni: le altre 12 sono invocate da doc, skill, agenti o hook — inclusa `validate`, che il primo pattern di ricerca aveva mancato perché scritta nella forma `… validate --json`.)* | Una superficie documentata che nessuno usa invecchia in silenzio e va mantenuta a vuoto | **Could (P2)** | 📋 da fare — audit codice morto 2026-07-27 |
| FEAT-058 | **Il lint applica al contenuto archiviato una regola pensata per quello vivo** — dopo la riparazione dei 40 residui del 2026-07-28 ne restano **18** (orfani + frontmatter), **tutti** in `wiki/sources/**/processed/`: documenti ricevuti da altri nodi e archiviati dal punto 9 del rituale *proprio* perché non vengano più rielaborati. **Essere orfano è la definizione di archivio**, quindi linkarli dall'indice per azzerare il contatore **peggiorerebbe** il wiki. **Il danno non è il 18, è che è *stabile*:** un contatore che non può andare a zero si legge come «normale», e il giorno che diventa 19 per una pagina viva davvero orfana non se ne accorge nessuno. **Vincolo vincolante sulla forma del rimedio:** qualunque esclusione deve **dichiarare quanti file salta**, non nasconderli (Principio XIV) — e *«non risolvere nascondendo»* è già la terza costrizione della regola del boy scout. Confermato dal campo: il nodo *Acta* riporta `broken_links=22 orphans=13 missing_frontmatter=5` con **21+13+5 tutti** in `sources/**/processed/`, cioè **una sola** segnalazione viva. | Un contatore stabilmente diverso da zero insegna a smettere di leggere la guardia — lo stesso difetto che la v0.3.3 ha corretto sui falsi positivi | **Should (P1)** | 📋 da fare — *riga ricostruita il 2026-07-29: era tracciata nel log del 28/07 ma non era mai stata scritta nel backlog (regola 2 — un rinvio reale non deve vivere solo in un record datato)* |
| FEAT-059 | **Regola del boy scout: se il lint trova rotto, si aggiusta nello stesso passaggio** — direttiva utente (2026-07-28): *«lascio le cose migliori di come le ho trovate»*. Il lint strutturale è **advisory** e nessuno possiede le sue segnalazioni: il 28/07 ne sono state trovate **40 accumulate** (14 link rotti · 20 orfani · 6 frontmatter), alcune vecchie di mesi, in un wiki che il rituale tocca ogni giorno. La regola è già **standing** nel `CLAUDE.md` (comportamento dell'agente, in vigore da subito); questa riga è la sua metà **deterministica**, perché una regola che dipende solo dal fatto che qualcuno lanci `lint` e poi decida di agire è esattamente ciò che ha prodotto l'arretrato. **Da decidere in design** (confine D↔N): quali classi sono **auto-riparabili in sicurezza** — probabilmente *nessuna* senza giudizio, perché il 28/07 il rimedio giusto è stato **diverso per ogni classe** (link a memoria privata → de-linkare · principi → puntare a `[[constitution]]` · artefatti fuori wiki → link markdown · e 3 su 14 erano **falsi positivi del lint stesso**, da non «riparare» affatto) — e quale **superficie di enforcement** ha senso: contatore che non deve crescere (gate sul *delta*, non sull'assoluto) · segnalazione dentro il rituale · `--fix` per il solo sottoinsieme meccanico. | L'arretrato non nasce da difetti nuovi ma dall'assenza di un proprietario: senza delta-gate torna a crescere | **Should (P1)** | 📋 da fare — direttiva utente 2026-07-28; metà standing già attiva in `CLAUDE.md` |
| FEAT-060 | **`ritual-check` e `wiki-guard` misurano realtà diverse, e la differenza è invisibile** — `ritual-check` deriva i candidati dal **diff committato** (`master...HEAD`), `wiki-guard`/`scan` dalle **modifiche non consegnate dell'albero di lavoro**. Chi lavora e **non ha ancora committato** riceve `pages=0 distill=0 drift=0` — cioè *«non c'è niente da dichiarare»* — mentre il gate allo `Stop` vede eccome del lavoro e blocca. **Riscontrato dal vivo il 2026-07-28:** dopo aver riscritto ~100 righe di `CLAUDE.md`, `ritual-check --base master` ha risposto **0 candidati su tutto**. Il tool non sbaglia il calcolo: risponde a **un'altra domanda**, e non lo dichiara. Effetto pratico: lo strumento che dovrebbe *preparare* la dichiarazione del rituale è muto proprio nel momento in cui serve — prima del commit. **Nota di numerazione:** questo problema era elencato nell'EXEC sotto «E10-FEAT-045», numero però già assegnato all'àncora derivata di `scan` (consegnata in v0.3.2): riga rinumerata qui il 2026-07-28. **Forma del rimedio da decidere:** includere l'albero di lavoro anche in `ritual-check` (allineandolo a `scan`, che dopo FEAT-045 sa già farlo) **oppure** dichiarare esplicitamente nell'output quale perimetro si sta misurando — la cosa che oggi manca non è la copertura, è **sapere cosa si sta guardando**. | Un tool che risponde a una domanda diversa da quella che credi di aver posto, e tace sulla differenza, è peggio di un tool assente | **Should (P1)** | 📋 da fare — riscontrato dal vivo 2026-07-28 |
| FEAT-061 | **Distribuire il metodo d'audit del codice morto come skill** — oggi vive **solo** come pagina wiki nostra ([[audit-codice-morto]]), validata su ~30.000 righe di questo repo, e **nessun ospite la riceve**. È una **capacità**, non una pratica da paragrafo: quattro lenti indipendenti (raggiungibilità dagli entry point · livello-definizione · superficie dei comandi · copertura) con i rispettivi **punti ciechi**, la tassonomia dei **cinque falsi positivi ricorrenti** (decoratori, callback di framework, dispatch per stringa, riferimenti in prosa distribuita, entry point `python -m`) e la distinzione **morto / obsoleto / mai-cablato**, che cambia il rimedio. Sul nostro repo ha prodotto **5 finding reali e 8 falsi positivi correttamente scartati**. Forma: skill distribuita sullo stampo di `speclift`/`specaudit` (deterministico prepara, agente giudica), casa naturale **`sertor-flow`**. **Da decidere in design:** quanta parte è meccanizzabile (l'import-graph sì, «è un callback di framework?» no) e se convenga un comando che *prepara le quattro lenti* lasciando all'agente il verdetto. | Il metodo esiste ed è validato, ma resta nostro: un ospite con 30.000 righe ha lo stesso problema e nessuno strumento | **Could (P2)** | 📋 da fare — tenuta **fuori dal pacchetto del 2026-07-28** per decisione utente: è una skill da costruire e verificare, non un paragrafo, e merita un suo giro |
| FEAT-062 | **La registrazione copre un *changeset*, non una *data* — il gate `wiki-guard` è cieco** — `scan` azzera `pending` per la **sola presenza** della partizione di oggi fra le modifiche dell'albero (`scan.py:286-292`), quindi **appena scrivi la voce di giornale il gate smette di vedere tutto il lavoro successivo**: chi soddisfa la regola la disattiva, nella finestra esatta in cui lo `Stop` interroga. **Segnalato dal nodo *Acta* (2026-07-29)** — *«un gate che non blocca mai è indistinguibile da un gate disinstallato»*, 8 nodi con la capability sul loro host — e **riprodotto e misurato qui**: matrice di 15 scenari + 3 race → **8 casi di non-rilevazione con una sola causa** (C·D·F·G·H·I·J·R3) + **1 difetto indipendente** (R1: ogni invocazione git che fallisce degrada verso l'insieme vuoto **in silenzio**, continuando a dichiarare `anchor_kind: git` — Principio XII; e la nostra delega git in background rende la race **normale**, non patologica). Il gate non richiede una *voce*: un file **vuoto** o una **riga vuota** appesa lo soddisfano. **Quinta istanza di [[identita-per-presenza-o-per-contenuto]]**, col segno peggiore — un via libera che sembra pulizia — e **certificata da un test** (`test_todays_uncommitted_entry_satisfies_the_gate_without_a_commit`): guardia e test condividono lo stesso errore di identità. **Forma del rimedio:** una voce **aggiunta** (verificata sul changeset del giornale) dichiara l'**insieme di path che copre**, derivato da `append-log`; `pending = lavoro − copertura`. La **data sparisce dalla logica**. Requisiti EARS + analisi completa (scenari, race, costi misurati) in [`feat-062-copertura-changeset-scan/requirements.md`](feat-062-copertura-changeset-scan/requirements.md). **Vincolo critico:** `wiki.scan/1` **non si bumpa** (i consumatori confrontano per uguaglianza e vanno in fail-open → il gate **sparirebbe in silenzio**). Converge con FEAT-060. | Il gate bloccante della freschezza del wiki oggi non blocca quando dovrebbe, su ogni ospite che ha la capability | **Must (P0)** | 📋 requisiti scritti (2026-07-29) — prossimo: `specify`; **DA-1 migrazione delle voci esistenti = decisione utente** |
| FEAT-063 | **Il perimetro di `scan` esclude l'installer e i file di radice** — sul dogfood `source_dirs = ["src", "specs", "requirements", ".claude"]`, quindi le modifiche a **`packages/`** — cioè l'installer e gli **asset host-facing** che ogni ospite riceve — e ai file di radice come **`CLAUDE.md`** non innescano **mai** il gate del wiki (scenario **M** della matrice FEAT-062, misurato). È **configurazione, non codice**, ma con effetti reali: si può riscrivere ciò che viene distribuito agli ospiti e chiudere la sessione senza che nulla lo chieda — ed è esattamente la superficie su cui il 2026-07-28 è passato il difetto «pratica standing ≠ pratica distribuita» (tracciato come **E15-FEAT-011**, riga in arrivo con la PR #250 — non ancora su `master` al momento in cui questa è scritta). **Da decidere:** allargare il perimetro (e a cosa), oppure dichiarare l'esclusione come deliberata — ma dichiararla, non lasciarla implicita. | Il gate non guarda proprio la superficie il cui drift arriva agli ospiti | **Should (P1)** | 📋 da fare — promosso dal fuori-ambito di FEAT-062 (2026-07-29) |

> **Nota:** non c'è un «MVP» nel senso di prodotto: è debito. La priorità reale è **FEAT-001/002/003**
> (Should): host-agnosticità, un solo env, CI Linux — le tre frizioni che incidono di più su qualità e
> portabilità. Il resto (Could) si paga quando tocca quelle aree.
