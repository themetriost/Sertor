# Dipendenze Tappa 1 — baseline RAG (venv uv, Python 3.12)
chromadb>=0.5
langchain-text-splitters>=0.3
httpx>=0.27
python-dotenv>=1.0
numpy>=1.26

# Dipendenze Tappa 2 — hybrid + reranking
rank-bm25>=0.2.2
flashrank>=0.2.9

# Dipendenze Tappa 3A — code graph
networkx>=3.2

# Chunking code-aware (tree-sitter): confini sintattici per il codice
tree-sitter>=0.25
tree-sitter-python>=0.25

# Tappa 4 — Agentic RAG. L'orchestratore vanilla usa solo httpx (già sopra).
# AutoGen è il 1° dei framework a confronto (adattatore 04-agentic-rag/autogen_app.py);
# usa il client OpenAI-compatible anche verso Ollama (/v1) e Azure.
autogen-agentchat>=0.7
autogen-ext[openai]>=0.7

# Dev — test dimostrativi (suite tests/, vedi DEMOS.md)
pytest>=8.0
